void setQuoteForMinute(int minute) {
  long option = 0;
  switch (minute) {

    // Codegen for 00:00 for 30 quotations:
    case 0:
      option = random(0, 30);
      // 30 options for 00:00
      switch (option) {
        case 0:
      quote = F("Cartridges not allowed after * 0000h. *, to encourage sleep.");
      attribution = F("- Infinite Jest");
      return;
        case 1:
      quote = F("Gately can hear the horns and raised voices and u-turn squeals way down below on Wash. That indicate it's around * 0000h *., the switching hour.");
      attribution = F("- Infinite Jest");
      return;
        case 2:
      quote = F("fill the bulky ledger with cryptic remarks like 'Patrolled perimeter * 12.00 pm *, No Incident'");
      attribution = F("- A Squatter's Tale");
      return;
        case 3:
      quote = F("As * midnight * was striking bronze blows upon the dusky air, Dorian Gray, dressed commonly, and with a muffler wrapped round his throat, crept quietly out of his house.");
      attribution = F("- The Picture of Dorian Gray");
      return;
        case 4:
      quote = F("At * midnight * his wife and daughter might still be bustling about, preparing holiday delicacies in the kitchen, straightening up the house...");
      attribution = F("- Beauty and Sadness");
      return;
        case 5:
      quote = F("Big Ben had once again struck * midnight *. The time outside still corresponded to that registered by the stopped gilt clock, inside.");
      attribution = F("- Nights At The Circus");
      return;
        case 6:
      quote = F("But wait till I tell you, he said. \"We had a * midnight * lunch too after all the jollification and when we sallied forth it was blue o'clock the morning after the night before\"");
      attribution = F("- James Joyce, Ulysses");
      return;
        case 7:
      quote = F("Does this mean I am freer now than I was? I do not know. I shall learn. Then I went back into the house and wrote, It is * midnight *. The rain is beating on the windows.");
      attribution = F("- Samuel Beckett, Molloy");
      return;
        case 8:
      quote = F("He is certain he heard footsteps: they come nearer, and then die away. The ray of light beneath his door is extinguished. It is * midnight *; some one has turned out the gas.");
      attribution = F("- Marcel Proust, Swann's Way");
      return;
        case 9:
      quote = F("I am conceived to the chimes of * midnight * on the clock on the mantelpiece in the room across the hall. The clock once belonged to my great-grandmother.");
      attribution = F("- Kate Atkinson");
      return;
        case 10:
      quote = F("It is * midnight *. The rain is beating on the windows. I am calm. All is sleeping. Nevertheless I get up and go to my desk. I can't sleep. ...");
      attribution = F("- Samuel Beckett, Molloy");
      return;
        case 11:
      quote = F("It was nearing * midnight * and the Prime Minister was sitting alone in his office, reading a long memo");
      attribution = F("- JK Rowling, Harry Potter");
      return;
        case 12:
      quote = F("I was born in the city of Bombay ... On the stroke of * midnight *, as a matter of fact.");
      attribution = F("- Midnight's Children");
      return;
        case 13:
      quote = F("...leaning forwards, ruminated until * midnight * upon both sides of the question.");
      attribution = F("- Laurence Sterne");
      return;
        case 14:
      quote = F("* Midnight * had come upon the crowded city. The palace, the night-cellar, the jail, the madhouse; the chambers of birth and death, of health and sickness.");
      attribution = F("- Oliver Twist");
      return;
        case 15:
      quote = F("* Midnight * is approaching, and while the peak of activity has passed, the basal metabolism that maintains life continues undiminished...");
      attribution = F("- Murakami, After Dark");
      return;
        case 16:
      quote = F("'Midnight,' you said. What is * midnight * to the young? And suddenly a festive blaze was flung Across five cedar trunks, snow patches showed...");
      attribution = F("- Pale Fire");
      return;
        case 17:
      quote = F("Once upon a * midnight * dreary, while I pondered weak and weary, Over many a quaint and curious volume of forgotten lore");
      attribution = F("- Edgar Allan Poe, The Raven");
      return;
        case 18:
      quote = F("'Tis the year's * midnight *, and it is the day's, Lucy's, who scarce seven hours herself unmasks; The sun is spent, and now his flasks Send forth light squibs, no constant rays;");
      attribution = F("- Nocturnal on St Lucy's Day");
      return;
        case 19:
      quote = F("We have heard the chimes at * midnight *.");
      attribution = F("- Henry IV");
      return;
        case 20:
      quote = F("A cheap little clock on the wall struck * twelve * hurriedly, and served to begin the conversation.");
      attribution = F("- The Brothers Karamazov");
      return;
        case 21:
      quote = F("Bernardo: 'Tis now struck * twelve *; get thee to bed, Francisco.");
      attribution = F("- Shakespeare, Hamlet");
      return;
        case 22:
      quote = F("Francisco. You come most carefully upon your hour. Bernardo. 'Tis now struck * twelve *. Get thee to bed, Francisco.");
      attribution = F("- William Shakespeare, Hamlet");
      return;
        case 23:
      quote = F("Hamlet: What hour now? Horatio: I think it lacks of * twelve *. Marcellus: No, it is struck.");
      attribution = F("- William Shakespeare, Hamlet");
      return;
        case 24:
      quote = F("'Myself!' she gasped, 'and the clock is striking * twelve *! It's true, then! that's dreadful!'");
      attribution = F("- Wuthering Heights");
      return;
        case 25:
      quote = F("I record that I was born on a Friday, at * twelve o'clock * at night. It was remarked that the clock began to strike, and I began to cry, simultaneously.");
      attribution = F("- David Copperfield");
      return;
        case 26:
      quote = F("It was precisely * twelve o'clock *; twelve by Big Ben; whose stroke was wafted over the northern part of London; blent with that of other clocks...");
      attribution = F("- Mrs Dalloway");
      return;
        case 27:
      quote = F("The clock striketh * twelve * O it strikes, it strikes! Now body, turn to air, Or Lucifer will bear thee quick to hell.");
      attribution = F("- Dr Faustus");
      return;
        case 28:
      quote = F("The Oxen Christmas Eve, and * twelve * of the clock. \"Now they are all on their knees,\"");
      attribution = F("- Thomas Hardy, The Oxen");
      return;
        case 29:
      quote = F("'There's nobody here!' I insisted. 'It was yourself, Mrs. Linton: you knew it a while since.' 'Myself!' she gasped, 'and the clock is striking * twelve *!");
      attribution = F("- Wuthering Heights");
      return;
      } // end switch (option)

    // Codegen for 00:01 for 1 quotations:
    case 1:
      // Only one option for 00:01:"
      quote = F("With the appointed execution time of * one minute past midnight * just seconds away, I knocked on the metal door twice. The lock turned and the door swiftly swung open.");
      attribution = F("- Death at Midnight");
      return;

    // Codegen for 00:02 for 2 quotations:
    case 2:
      option = random(0, 2);
      // 2 options for 00:02
      switch (option) {
        case 0:
      quote = F("It had struck * twelve o'clock two minutes and a quarter *. The Baron's footman hastily seized a large goblet, and gasped with terror");
      attribution = F("- Crundle Castle");
      return;
        case 1:
      quote = F("* Two minutes past midnight *. With me in the lead the fourteen other men of Teams Yellow, White and Red moved out of the clearing...");
      attribution = F("- Night of the Krait");
      return;
      } // end switch (option)

    // Codegen for 00:03 for 3 quotations:
    case 3:
      option = random(0, 3);
      // 3 options for 00:03
      switch (option) {
        case 0:
      quote = F("It was * after twelve o'clock * when Easton came home. Ruth recognised his footsteps before he reached the house...");
      attribution = F("- Robert Tressell");
      return;
        case 1:
      quote = F("...then a snapping of tension. I glanced at my watch. * Three minutes after midnight. * I was breathing normally and my pen moved freely across the page.");
      attribution = F("- The Historian");
      return;
        case 2:
      quote = F("It was just * three minutes past midnight * when I last saw Archer Harrison alive.");
      attribution = F("- Since Ibsen");
      return;
      } // end switch (option)

    // Codegen for 00:04 for 1 quotations:
    case 4:
      // Only one option for 00:04:"
      quote = F("At * four minutes past midnight *, January 22, Admiral Lowry's armada of more than 250 ships reached the transport area off Anzio. The sea was calm, the night was black.");
      attribution = F("- Fred Sheehan, Anzio");
      return;

    // Codegen for 00:05 for 1 quotations:
    case 5:
      // Only one option for 00:05:"
      quote = F("E.M. Security, normally so scrupulous with their fucking trucks at * 0005h *., is nowhere around, lending weight to yet another cliche.");
      attribution = F("- Infinite Jest");
      return;

    // Codegen for 00:06 for 1 quotations:
    case 6:
      // Only one option for 00:06:"
      quote = F("At * six minutes past midnight *, death relieved the sufferer.");
      attribution = F("- West of Hell's Fringe");
      return;

    // Codegen for 00:07 for 1 quotations:
    case 7:
      // Only one option for 00:07:"
      quote = F("It was * seven minutes after midnight *. The dog was lying on the grass in the middle of the lawn in front of Mrs Shears' house. Its eyes were closed.");
      attribution = F("- Dog in the Night-Time");
      return;

    // Codegen for 00:08 for 1 quotations:
    case 8:
      // Only one option for 00:08:"
      quote = F("Hour of the night! exclaimed the priest; \"it is day, not night, and the hour is * eight past midnight *!\"");
      attribution = F("- The Brigantine");
      return;

    // Codegen for 00:09 for 1 quotations:
    case 9:
      // Only one option for 00:09:"
      quote = F("At * 12.09am * on 18 October, the cavalcade had reached the Karsaz Bridge, still ten kilometres from her destination.");
      attribution = F("- The Bhutto Murder Trail");
      return;

    // Codegen for 00:10 for 1 quotations:
    case 10:
      // Only one option for 00:10:"
      quote = F("It was at * ten minutes past midnight *. Three police cars, Alsations and a black maria arrive at the farmhouse. The farmer clad only in a jock-strap, refused them entry.");
      attribution = F("- Jonathan Barrow, The Queue");
      return;

    // Codegen for 00:11 for 1 quotations:
    case 11:
      // Only one option for 00:11:"
      quote = F("The first incendiaries to hit St Thomas's Hospital had splattered Riddell House at * eleven minutes past midnight *...");
      attribution = F("- The Longest Night");
      return;

    // Codegen for 00:12 for 2 quotations:
    case 12:
      option = random(0, 2);
      // 2 options for 00:12
      switch (option) {
        case 0:
      quote = F("Clock time is * 0 Hours, 12 Minutes *, 0 Seconds. Twenty three minutes later, they have their first sight of Venus.");
      attribution = F("- Mason & Dixon");
      return;
        case 1:
      quote = F("It was * twelve minutes past midnight * when mother and daughter saw the first lightning strike. It hit the main barn with such force the ground trembled under their feet.");
      attribution = F("- Kentucky Heat");
      return;
      } // end switch (option)

    // Codegen for 00:13 for 0 quotations:
    case 13:
      // No quotes for 00:13
        quote = "";
        attribution = "";
      return;

    // Codegen for 00:14 for 2 quotations:
    case 14:
      option = random(0, 2);
      // 2 options for 00:14
      switch (option) {
        case 0:
      quote = F("It was exactly * fourteen minutes past midnight * when he completed the final call.");
      attribution = F("- The Matarese Circle");
      return;
        case 1:
      quote = F("She left London on the * twelve-fourteen * from Paddington, arriving at Bristol (where she had to change) at two-fifty.");
      attribution = F("- The Plymouth Express");
      return;
      } // end switch (option)

    // Codegen for 00:15 for 2 quotations:
    case 15:
      option = random(0, 2);
      // 2 options for 00:15
      switch (option) {
        case 0:
      quote = F("I caught the 10.20 to Eastnor, which isn't a bad train, if you ever want to go down there. I arrived at a * quarter past twelve *, and went straight up to the house.");
      attribution = F("- The Little Nugget");
      return;
        case 1:
      quote = F("At * twelve-fifteen * he got out of the van. He tucked the pistol under the waistband of his trousers and crossed the silent, deserted street to the Hudston house.");
      attribution = F("- Dean Koontz, Watchers");
      return;
      } // end switch (option)

    // Codegen for 00:16 for 1 quotations:
    case 16:
      // Only one option for 00:16:"
      quote = F("At * sixteen minutes past midnight *, Block 4 was hit and the roof set alight.");
      attribution = F("- The Longest Night");
      return;

    // Codegen for 00:17 for 2 quotations:
    case 17:
      option = random(0, 2);
      // 2 options for 00:17
      switch (option) {
        case 0:
      quote = F("Kava ordered two glasses of coffee for himself and his beloved and some cake. When the pair left, exactly * seventeen minutes after twelve *, the club began to buzz with excitement.");
      attribution = F("- Vanvild Kava");
      return;
        case 1:
      quote = F("Kava ordered two glasses of coffee for himself and his beloved and some cake. When the pair left, exactly * seventeen minutes after twelve *, the club began to buzz with excitement.");
      attribution = F("- Vanvild Kava");
      return;
      } // end switch (option)

    // Codegen for 00:18 for 1 quotations:
    case 18:
      // Only one option for 00:18:"
      quote = F("21st December 1985, * 12.18am * [In bed] Michael doesn't believe in Heaven or Hell. He's got closer to death than most living people...");
      attribution = F("- The Book of Lies");
      return;

    // Codegen for 00:19 for 0 quotations:
    case 19:
      // No quotes for 00:19
        quote = "";
        attribution = "";
      return;

    // Codegen for 00:20 for 1 quotations:
    case 20:
      // Only one option for 00:20:"
      quote = F("...I could smell the opium. There is no smell like it. Beside the bed the alarm-clock showed * twelve-twenty *, but already my tension was over. Pyle had diminished.");
      attribution = F("- The Quiet American");
      return;

    // Codegen for 00:21 for 2 quotations:
    case 21:
      option = random(0, 2);
      // 2 options for 00:21
      switch (option) {
        case 0:
      quote = F("Nobody had been one of Mycroft Ward's most important operatives and for sixty seconds every day, between * 12.21am * and 12.22am.");
      attribution = F("- The Raw Shark Texts");
      return;
        case 1:
      quote = F("Jake think of something. PLEASE! * Twelve twenty-one *.");
      attribution = F("- Stephen King, 11/22/63");
      return;
      } // end switch (option)

    // Codegen for 00:22 for 2 quotations:
    case 22:
      option = random(0, 2);
      // 2 options for 00:22
      switch (option) {
        case 0:
      quote = F("Nobody had been one of Mycroft Ward's most important operatives and for sixty seconds every day, between 12.21am and * 12.22am. *");
      attribution = F("- The Raw Shark Texts");
      return;
        case 1:
      quote = F("By * twenty-two minutes past twelve * we leave, much too soon for our desires, this delightful spot, where the pilgrims are in the habit of bathing who come to visit the Jordan.");
      attribution = F("- Felicien de Saulcy");
      return;
      } // end switch (option)

    // Codegen for 00:23 for 1 quotations:
    case 23:
      // Only one option for 00:23:"
      quote = F("Oskar weighed the wristwatch in his hand, then gave the rather fine piece with its luminous dial showing * twenty-three minutes past midnight * to little Pinchcoal.");
      attribution = F("- Gunter Grass, The Tin Drum");
      return;

    // Codegen for 00:24 for 2 quotations:
    case 24:
      option = random(0, 2);
      // 2 options for 00:24
      switch (option) {
        case 0:
      quote = F("* 12:24 * My legs are in total agony. I've been kneeling on hard tiles, cleaning the bath, for what seems like hours.");
      attribution = F("- The Undomestic Goddess");
      return;
        case 1:
      quote = F("Sanders with Sutton as his gunner began their patrol at * 12.24am *, turning south towards Beachy Head at 10,000 ft.");
      attribution = F("- The Longest Night");
      return;
      } // end switch (option)

    // Codegen for 00:25 for 3 quotations:
    case 25:
      option = random(0, 3);
      // 3 options for 00:25
      switch (option) {
        case 0:
      quote = F("Boys, do it now. God's time is * 12.25 *.");
      attribution = F("- James Joyce, Ulysses");
      return;
        case 1:
      quote = F("...whence they must have hurried away to Vieux-Bourg station which the last train to Paris quitted at * five-and-twenty minutes past midnight *.");
      attribution = F("- Emile Zola, Fruitfulness");
      return;
        case 2:
      quote = F("I mean, look at the time! * Twenty-five past midnight! * It was a triumph, it really was!");
      attribution = F("- The Soldier's Wife");
      return;
      } // end switch (option)

    // Codegen for 00:26 for 1 quotations:
    case 26:
      // Only one option for 00:26:"
      quote = F("He said you wanted to see the arrival of yesterday's * 12.26am. * It'll take me a few minutes to cue up the footage.");
      attribution = F("- Bryant & May Off the Rails");
      return;

    // Codegen for 00:27 for 0 quotations:
    case 27:
      // No quotes for 00:27
        quote = "";
        attribution = "";
      return;

    // Codegen for 00:28 for 1 quotations:
    case 28:
      // Only one option for 00:28:"
      quote = F("The DRINK CHEER-UP COFFEE wall clock read * 12.28 *.");
      attribution = F("- Stephen King, 11/22/63");
      return;

    // Codegen for 00:29 for 1 quotations:
    case 29:
      // Only one option for 00:29:"
      quote = F("...The station agent hauled out his big silver watch, looked at it critically and announced: * Twenty-nine minutes past twelve *.\" \"Past twelve!\" repeated Teeny-bits.");
      attribution = F("- The Mark of the Knife");
      return;

    // Codegen for 00:30 for 2 quotations:
    case 30:
      option = random(0, 2);
      // 2 options for 00:30
      switch (option) {
        case 0:
      quote = F("It was * half-past twelve * when I returned to the Albany as a last desperate resort. The scene of my disaster was much as I had left it.");
      attribution = F("- The Amateur Cracksman");
      return;
        case 1:
      quote = F("You'll never believe this but (in Spain) they are two hours late for ever meal - two hours Fanny - (can we lunch at * half-past twelve * today?)");
      attribution = F("- Love in a Cold Climate");
      return;
      } // end switch (option)

    // Codegen for 00:31 for 1 quotations:
    case 31:
      // Only one option for 00:31:"
      quote = F("Third individual approaches unnoticed and without caution. Once within reach, individual reaches out toward subjects. Recording terminates: timecode: * 00:31 *:02.");
      attribution = F("- Zombie Apocalypse Fightback");
      return;

    // Codegen for 00:32 for 2 quotations:
    case 32:
      option = random(0, 2);
      // 2 options for 00:32
      switch (option) {
        case 0:
      quote = F("Why won't anything come out? OK, I'm going to squeeze it really, really hard- That nearly got my eye. * 12:32 * FUCK. What has it done to my HAIR?");
      attribution = F("- The Undomestic Goddess");
      return;
        case 1:
      quote = F("* Thirty-two minutes past midnight *; the way things were going I could be at it all night. Before beginning a completely new search of the dial I had a thought...");
      attribution = F("- Everette Hunt, Ixtapa");
      return;
      } // end switch (option)

    // Codegen for 00:33 for 2 quotations:
    case 33:
      option = random(0, 2);
      // 2 options for 00:33
      switch (option) {
        case 0:
      quote = F("It's * 12.33 * now and I could do it, the station is just down that side road there.");
      attribution = F("- Five Red Herrings");
      return;
        case 1:
      quote = F("So that at twelve-thirty-three you bolted the south door? \"Yes,\" replied Stephen Maxie easily. \"At * thirty-three minutes past midnight *.\"");
      attribution = F("- P.D. James, Cover her Face");
      return;
      } // end switch (option)

    // Codegen for 00:34 for 1 quotations:
    case 34:
      // Only one option for 00:34:"
      quote = F("* Thirty-four minutes past midnight *. 'We got ten minutes to be back here.' LT didn't argue. Schoolboy knew his former trade. LT's eyes fretted over the museum.");
      attribution = F("- Killer Tune");
      return;

    // Codegen for 00:35 for 1 quotations:
    case 35:
      // Only one option for 00:35:"
      quote = F("Everyone else had been waiting five minutes and Colour Sergeant Cork called out the marker just as Trimmer appeared. So it was * twelve-thirty-five * when they were dismissed.");
      attribution = F("- Evelyn Waugh, Men At Arms");
      return;

    // Codegen for 00:36 for 0 quotations:
    case 36:
      // No quotes for 00:36
        quote = "";
        attribution = "";
      return;

    // Codegen for 00:37 for 0 quotations:
    case 37:
      // No quotes for 00:37
        quote = "";
        attribution = "";
      return;

    // Codegen for 00:38 for 0 quotations:
    case 38:
      // No quotes for 00:38
        quote = "";
        attribution = "";
      return;

    // Codegen for 00:39 for 1 quotations:
    case 39:
      // Only one option for 00:39:"
      quote = F("...the half-ebb at a quarter past seven, low water at thirty-three minutes past nine, and half flood at * thirty-nine minutes past twelve *.");
      attribution = F("- The Toilers of the Sea");
      return;

    // Codegen for 00:40 for 2 quotations:
    case 40:
      option = random(0, 2);
      // 2 options for 00:40
      switch (option) {
        case 0:
      quote = F("A little ormolu clock in the outer corridor indicated * twenty minutes to one *. The car was due at one-fifteen. Thirty-five minutes: oh, to escape for only that brief period!");
      attribution = F("- Stacy Aumonier");
      return;
        case 1:
      quote = F("We sat in the car park till * twenty to one *. And now I'm engaged to Miss Joan Hunter Dunn.");
      attribution = F("- A Subaltern's Love Song");
      return;
      } // end switch (option)

    // Codegen for 00:41 for 0 quotations:
    case 41:
      // No quotes for 00:41
        quote = "";
        attribution = "";
      return;

    // Codegen for 00:42 for 1 quotations:
    case 42:
      // Only one option for 00:42:"
      quote = F("She crushed the cigarette out and stood, brushing ash from her black skirt. It was * eighteen minutes to one *. She went to the house phone and called his room.");
      attribution = F("- Marjorie Morningstar");
      return;

    // Codegen for 00:43 for 1 quotations:
    case 43:
      // Only one option for 00:43:"
      quote = F("Died five minutes ago, you say? he asked. His eye went to the watch on his wrist. * Twelve-forty-three *, he wrote on the blotter.");
      attribution = F("- A Pocket Full of Rye");
      return;

    // Codegen for 00:44 for 0 quotations:
    case 44:
      // No quotes for 00:44
        quote = "";
        attribution = "";
      return;

    // Codegen for 00:45 for 3 quotations:
    case 45:
      option = random(0, 3);
      // 3 options for 00:45
      switch (option) {
        case 0:
      quote = F("At * 12.45 *, during a lull, Mr Yoshogi told me that owing to the war there were now many more women in England than men.");
      attribution = F("- David Footman");
      return;
        case 1:
      quote = F("Look out for D. He has just now, * 12:45 *, come from Carfax hurriedly and hastened towards the South. He seems to be going the round and may want to see you: Mina");
      attribution = F("- Bram Stoker, Dracula");
      return;
        case 2:
      quote = F("The clock marked the * third quarter after midnight *, and he knew it would make no difference if he went down to the post-box now or early the next morning.");
      attribution = F("- Edith Wharton, The Reef");
      return;
      } // end switch (option)

    // Codegen for 00:46 for 0 quotations:
    case 46:
      // No quotes for 00:46
        quote = "";
        attribution = "";
      return;

    // Codegen for 00:47 for 1 quotations:
    case 47:
      // Only one option for 00:47:"
      quote = F("At * 12:47a.m *, Uncle Ho left us forever.");
      attribution = F("- Andrew X. Pham");
      return;

    // Codegen for 00:48 for 0 quotations:
    case 48:
      // No quotes for 00:48
        quote = "";
        attribution = "";
      return;

    // Codegen for 00:49 for 1 quotations:
    case 49:
      // Only one option for 00:49:"
      quote = F("The first victim of the Krefeld raid died at * 12:49 hours * Double British Summer Time at B Flight, but it wasn't due to carelessness.");
      attribution = F("- Len Deighton, Bomber");
      return;

    // Codegen for 00:50 for 1 quotations:
    case 50:
      // Only one option for 00:50:"
      quote = F("The packing was done at * 12.50 *; and Harris sat on the big hamper, and said he hoped nothing would be found broken. George said that if anything was broken it was broken...");
      attribution = F("- Three Men in a Boat");
      return;

    // Codegen for 00:51 for 0 quotations:
    case 51:
      // No quotes for 00:51
        quote = "";
        attribution = "";
      return;

    // Codegen for 00:52 for 0 quotations:
    case 52:
      // No quotes for 00:52
        quote = "";
        attribution = "";
      return;

    // Codegen for 00:53 for 1 quotations:
    case 53:
      // Only one option for 00:53:"
      quote = F("Aboot twelve miles. We ought tae pass her at Pinmore. She's due there at * 12:53 *.");
      attribution = F("- Five Red Herrings");
      return;

    // Codegen for 00:54 for 1 quotations:
    case 54:
      // Only one option for 00:54:"
      quote = F("...songs, anecdotes, and more drinks followed, and the pregnant minutes flew. At * six minutes to one *, when the jollity was at its highest- BOOM! There was silence instantly.");
      attribution = F("- Mark Twain");
      return;

    // Codegen for 00:55 for 1 quotations:
    case 55:
      // Only one option for 00:55:"
      quote = F("He rolled one way, rolled the other, listened to the loud tick of the clock, and was asleep a minute later. * Five to one * in the morning. Fifty-one hours to go.");
      attribution = F("- Lee Child, 61 Hours");
      return;

    // Codegen for 00:56 for 2 quotations:
    case 56:
      option = random(0, 2);
      // 2 options for 00:56
      switch (option) {
        case 0:
      quote = F("It was * 12:56 A.M. * when Gerald drove up onto the grass and pulled the limousine right next to the cemetery.");
      attribution = F("- Jonathan Safran Foer");
      return;
        case 1:
      quote = F("One night, and this first night is lost in the countless later nights of compounding wonder, he discovered a game. Say the time was * 12:56 *.");
      attribution = F("- Lessons in Essence");
      return;
      } // end switch (option)

    // Codegen for 00:57 for 1 quotations:
    case 57:
      // Only one option for 00:57:"
      quote = F("A minute had passed, and the roller dropped a new leaf. * 12:57 *. 12 + 57 = 69; 6 + 9 = 15; 1 + 5 = 6. 712 + 5 = 717; 71 + 7 = 78; 7 + 8 = 15; 1 + 5 = 6 again.");
      attribution = F("- Lessons in Essence");
      return;

    // Codegen for 00:58 for 1 quotations:
    case 58:
      // Only one option for 00:58:"
      quote = F("It was downright shameless on his part to come visiting them, especially at night, * almost at one in the morning *, after all that had happened.");
      attribution = F("- The Idiot");
      return;

    // Codegen for 00:59 for 1 quotations:
    case 59:
      // Only one option for 00:59:"
      quote = F("'What time is it now?' she said. '* About one o'clock *'. 'In the morning?' Herera's friend leered at her. 'No, there's a total eclipse of the sun'.");
      attribution = F("- Jonathan Frantzen, Freedom");
      return;

    // Codegen for 01:00 for 14 quotations:
    case 60:
      option = random(0, 14);
      // 14 options for 01:00
      switch (option) {
        case 0:
      quote = F("* 1.00 am. * I felt the surrounding quietness suffocating me.");
      attribution = F("- Rosamund Lupton, Sister");
      return;
        case 1:
      quote = F("After * 1 o'clock * checks, Gretta always goes out for a smoke.");
      attribution = F("- Girl, Interrupted");
      return;
        case 2:
      quote = F("Whoever is there, as the * clock strikes one *, will be shadowed by one of my men; of these, one, or perhaps two, or even three, will leave for France to-morrow.");
      attribution = F("- The Scarlet Pimpernel");
      return;
        case 3:
      quote = F("It was then * nearly one o'clock *. All I could discern distinctly by the moonlight was a colourless, youthful face.");
      attribution = F("- The Woman in White");
      return;
        case 4:
      quote = F("It was the thirtieth of May by now. * One am * on the thirtieth of May 1940. Quite a famous date on which to be lying awake and staring at the ceiling.");
      attribution = F("- London Belongs to Me");
      return;
        case 5:
      quote = F("Though it's already that day: the little luminous hands on my alarm clock (which I haven't set) show just gone * one in the morning *.");
      attribution = F("- Graham Swift, Tomorrow");
      return;
        case 6:
      quote = F("Czarina Catherine reported entering Galatz at * one o'clock * today.");
      attribution = F("- Bram Stoker, Dracula");
      return;
        case 7:
      quote = F("I got to Schmidt's early, feeling horribly nervous. At * one o'clock * sharp: Toni. She was looking at the menu she knew well - Schmorbraten? Schnitzel? - when he loomed over her.");
      attribution = F("- Sybille Bedford, Jigsaw");
      return;
        case 8:
      quote = F("he looked up at the clock - * one o'clock in the morning *. What in the name of God was he doing on King's Cross station...");
      attribution = F("- Douglas Adams");
      return;
        case 9:
      quote = F("It was * one o'clock *. I bought some apples and a small pork pie and drove across the bridge to the other side of the riverbank in the direction of Orford Ness.");
      attribution = F("- Roma Tearne, The Swimmer");
      return;
        case 10:
      quote = F("Tear off the feet, rip out its intestines, burst the eyeballs. He woke up, trembling; it was only * one o'clock *.");
      attribution = F("- Atomised");
      return;
        case 11:
      quote = F("The day-room floor gets cleared of tables and at * one o'clock * the doctor comes out of his office down the hall, nods once at the nurse as he goes past...");
      attribution = F("- Ken Kesey");
      return;
        case 12:
      quote = F("When yon same star that's westward from the pole Had made his course t'illume that part of heaven Where now it burns, Marcellus and myself, The bell then beating * one * -");
      attribution = F("- William Shakespeare, Hamlet");
      return;
        case 13:
      quote = F("It was a bright cold day in April, and the clocks were striking * thirteen *.");
      attribution = F("- Nineteen Eighty-Four");
      return;
      } // end switch (option)

    // Codegen for 01:01 for 2 quotations:
    case 61:
      option = random(0, 2);
      // 2 options for 01:01
      switch (option) {
        case 0:
      quote = F("'What time is it now?' she said. '* About one o'clock *'. 'In the morning?' Herera's friend leered at her. 'No, there's a total eclipse of the sun'.");
      attribution = F("- Jonathan Frantzen, Freedom");
      return;
        case 1:
      quote = F("There's five fathoms out there, he said. It'll be swept up that way when the tide comes in * about one *. It's nine days today.");
      attribution = F("- James Joyce, Ulysses");
      return;
      } // end switch (option)

    // Codegen for 01:02 for 1 quotations:
    case 62:
      // Only one option for 01:02:"
      quote = F("At * about one o'clock * the overseer arrived and told them he had no jobs for them");
      attribution = F("- A Clergyman's Daughter");
      return;

    // Codegen for 01:03 for 1 quotations:
    case 63:
      // Only one option for 01:03:"
      quote = F("It was * a little after one o'clock * when I got there, time for lunch, so I had it. The food was awful. But it would go on the expense account...");
      attribution = F("- The Big Clock");
      return;

    // Codegen for 01:04 for 1 quotations:
    case 64:
      // Only one option for 01:04:"
      quote = F("Jesus Christ! he gasped. \"It's * four minutes past one *!\" Linden frantically seized hold of a pair of steps and began wandering about the room with them.\"");
      attribution = F("- Robert Tressell");
      return;

    // Codegen for 01:05 for 2 quotations:
    case 65:
      option = random(0, 2);
      // 2 options for 01:05
      switch (option) {
        case 0:
      quote = F("At * five past one * Alleyn opened the outer door, knocked his pipe out on the edge of the stone step,and remained staring out on to the drive.");
      attribution = F("- Ngaio Marsh, A Man Lay Dead");
      return;
        case 1:
      quote = F("Um ... hold on! I hurry to the door, trying to block her view. \"It's already * five past one *,\" I can hear her saying a little sharply.");
      attribution = F("- The Undomestic Goddess");
      return;
      } // end switch (option)

    // Codegen for 01:06 for 1 quotations:
    case 66:
      // Only one option for 01:06:"
      quote = F("When he woke it was * 1:06 * by the digital clock on the bedside table. He lay there looking at the ceiling, the raw glare of the vaporlamp outside...");
      attribution = F("- No Country for Old Men");
      return;

    // Codegen for 01:07 for 0 quotations:
    case 67:
      // No quotes for 01:07
        quote = "";
        attribution = "";
      return;

    // Codegen for 01:08 for 1 quotations:
    case 68:
      // Only one option for 01:08:"
      quote = F("It was * 1.08a.m. * but he had left the ball at the same time as I did, and had further to travel.");
      attribution = F("- The Rosie Project");
      return;

    // Codegen for 01:09 for 2 quotations:
    case 69:
      option = random(0, 2);
      // 2 options for 01:09
      switch (option) {
        case 0:
      quote = F("At * nine minutes past one *, a pair of horses approached (not from the city, from which direction Krieger had expected her to come, but from the Desert)");
      attribution = F("- Clive Barker");
      return;
        case 1:
      quote = F("...when the express had rolled on into the west, upon a station platform in a foreign city at * nine minutes past one * o'clock in the morning - but at length without their shadow.");
      attribution = F("- The Black Bag");
      return;
      } // end switch (option)

    // Codegen for 01:10 for 2 quotations:
    case 70:
      option = random(0, 2);
      // 2 options for 01:10
      switch (option) {
        case 0:
      quote = F("February 26, Saturday - Richards went out * 1.10am * and found it clearing a bit, so we got under way as soon as possible, which was 2:10a.m.");
      attribution = F("- Ernest Shackleton");
      return;
        case 1:
      quote = F("It was * ten minutes past one *. \"You are sure of that?\"");
      attribution = F("- Death on the Nile");
      return;
      } // end switch (option)

    // Codegen for 01:11 for 2 quotations:
    case 71:
      option = random(0, 2);
      // 2 options for 01:11
      switch (option) {
        case 0:
      quote = F("I pursued my inquiries at the other stations along the line an' I found there was a gentleman wi' a bicycle tuk the * 1.11 * train at Girvan.");
      attribution = F("- Five Red Herrings");
      return;
        case 1:
      quote = F("Declares one of the waiters was the worse for liquor, and that he was giving him a dressing down. Also that it was * nearer to one than half past *.");
      attribution = F("- Agatha Christie");
      return;
      } // end switch (option)

    // Codegen for 01:12 for 1 quotations:
    case 72:
      // Only one option for 01:12:"
      quote = F("It was * 1:12am * when Father arrived at the police station. I did not see him until 1:28am but I knew he was there because I could hear him.");
      attribution = F("- Mark Haddon");
      return;

    // Codegen for 01:13 for 1 quotations:
    case 73:
      // Only one option for 01:13:"
      quote = F("I looked at Hans. \"Forut!\" was his tranquil answer. \"Forward!\" replied my uncle. It was * thirteen minutes past one *.");
      attribution = F("- Jules Verne");
      return;

    // Codegen for 01:14 for 0 quotations:
    case 74:
      // No quotes for 01:14
        quote = "";
        attribution = "";
      return;

    // Codegen for 01:15 for 4 quotations:
    case 75:
      option = random(0, 4);
      // 4 options for 01:15
      switch (option) {
        case 0:
      quote = F("Lily Chen always prepared an 'evening' snack for her husband to consume on his return at * 1.15am. *");
      attribution = F("- Timothy Mo, Sour Sweet");
      return;
        case 1:
      quote = F("Where are the ladies and Gentlemen? asked Aleyn. \"Sir, in the garding,\" said Bunce. \"What time's lunch?\" \"* One-fifteen *\".");
      attribution = F("- Ngaio Marsh, A Man Lay Dead");
      return;
        case 2:
      quote = F("The clock caught Miss LaFosse's eye. 'Good heavens!' she gasped. 'Look at the time. * Quarter-past one *. You must be starved.' She turned impetuously to Miss Pettigrew.");
      attribution = F("- Winifred Watson");
      return;
        case 3:
      quote = F("They began about a * quarter past one * o'clock in the morning, a rhythmic, quick-cadenced walking around the dining-room table.");
      attribution = F("- James Thurber");
      return;
      } // end switch (option)

    // Codegen for 01:16 for 2 quotations:
    case 76:
      option = random(0, 2);
      // 2 options for 01:16
      switch (option) {
        case 0:
      quote = F("From 1am to * 1.16am * vouched for by other two conductors.");
      attribution = F("- Agatha Christie");
      return;
        case 1:
      quote = F("At * sixteen past one *, they walked into the interview room.");
      attribution = F("- Nothing Gold Can Stay");
      return;
      } // end switch (option)

    // Codegen for 01:17 for 3 quotations:
    case 77:
      option = random(0, 3);
      // 3 options for 01:17
      switch (option) {
        case 0:
      quote = F("The clocks stopped at * 1:17 *. A long shear of light and then a series of low concussions. He got up and went to the window. What is it? she said. He didnt answer.");
      attribution = F("- Cormac McCarthy, The Road");
      return;
        case 1:
      quote = F("* One seventeen * and four seconds. That shorter guy's really got it made, and gets on a scooter, and that taller one, he goes in.");
      attribution = F("- The Terrorist, He Watches");
      return;
        case 2:
      quote = F("At that moment (it was * seventeen minutes past one * in the morning) Lieutenant Bronsfield was preparing to leave the watch and return to his cabin...");
      attribution = F("- A Voyage Round the Moon");
      return;
      } // end switch (option)

    // Codegen for 01:18 for 1 quotations:
    case 78:
      // Only one option for 01:18:"
      quote = F("* One eighteen * exactly. Was she stupid enough to head inside? Or wasn't she? We'll know before long, When the dead are carried out.");
      attribution = F("- The Terrorist, He Watches");
      return;

    // Codegen for 01:19 for 0 quotations:
    case 79:
      // No quotes for 01:19
        quote = "";
        attribution = "";
      return;

    // Codegen for 01:20 for 3 quotations:
    case 80:
      option = random(0, 3);
      // 3 options for 01:20
      switch (option) {
        case 0:
      quote = F("Then it was * 1.20am *, but I hadn't heard Father come upstairs to bed. I wondered if he was asleep downstairs or whether he was waiting to come in and kill me.");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("It is now some * twenty minutes past one * o'clock in the morning, and not a spot of action on your part.");
      attribution = F("- P.G. Wodehouse");
      return;
        case 2:
      quote = F("It will take him time to arrive here, see it is * twenty minutes past one *, and there are yet some times before he can hither come, be he never so quick.");
      attribution = F("- Bram Stoker, Dracula");
      return;
      } // end switch (option)

    // Codegen for 01:21 for 0 quotations:
    case 81:
      // No quotes for 01:21
        quote = "";
        attribution = "";
      return;

    // Codegen for 01:22 for 1 quotations:
    case 82:
      // Only one option for 01:22:"
      quote = F("It was * 1:22 * when we found Dad's grave.");
      attribution = F("- Jonathan Safran Foer");
      return;

    // Codegen for 01:23 for 1 quotations:
    case 83:
      // Only one option for 01:23:"
      quote = F("The clock marked * twenty-three minutes past one *. He was suddenly full of agitation, yet hopeful. She had come! Who could tell what she would say?");
      attribution = F("- A Mummer's Tale");
      return;

    // Codegen for 01:24 for 1 quotations:
    case 84:
      // Only one option for 01:24:"
      quote = F("Larkin had died at * 1.24am *, turning to the nurse who was with him, squeezing her hand, and saying faintly, 'I am going to the inevitable.'");
      attribution = F("- Hermione Lee");
      return;

    // Codegen for 01:25 for 2 quotations:
    case 85:
      option = random(0, 2);
      // 2 options for 01:25
      switch (option) {
        case 0:
      quote = F("I'd really have liked to, I told her, if it weren't for the things I had in the drier. I cast an eye at my watch. * One-twenty-five *. The drier had already stopped.");
      attribution = F("- Haruki Murakami");
      return;
        case 1:
      quote = F("He made a last effort; he tried to rise, and sank back. His head fell on the sofa cushions. It was then * twenty-five minutes past one * o'clock.");
      attribution = F("- The Moonstone");
      return;
      } // end switch (option)

    // Codegen for 01:26 for 1 quotations:
    case 86:
      // Only one option for 01:26:"
      quote = F("When I reached the stop and got off, it was already * one twenty-six A.M. * by the bus's own clock. I had been gone over ten hours.");
      attribution = F("- The Silver Metal Lover");
      return;

    // Codegen for 01:27 for 1 quotations:
    case 87:
      // Only one option for 01:27:"
      quote = F("At * twenty-seven minutes past one * she felt as if she was levitating out of her body.");
      attribution = F("- Deon Meyer, Trackers");
      return;

    // Codegen for 01:28 for 1 quotations:
    case 88:
      // Only one option for 01:28:"
      quote = F("It was 1:12 am when Father arrived at the police station. I did not see him until * 1:28 am * but I knew he was there because I could hear him.");
      attribution = F("- Mark Haddon");
      return;

    // Codegen for 01:29 for 1 quotations:
    case 89:
      // Only one option for 01:29:"
      quote = F("He exited the men's room at * one-twenty-nine A.M. *");
      attribution = F("- The Narc");
      return;

    // Codegen for 01:30 for 7 quotations:
    case 90:
      option = random(0, 7);
      // 7 options for 01:30
      switch (option) {
        case 0:
      quote = F("Around * 1:30 A.M. * the door opened and I thought it was Karla, but it was Bug, saying Karla and Laura had gone out for a stag night after they ran out of paint.");
      attribution = F("- Microserfs");
      return;
        case 1:
      quote = F("The radio alarm clock glowed * 1:30 a.m. * Bad karaoke throbbed through walls. I was wide awake, straightjacketed by my sweaty sheets. A headache dug its thumbs into my temples.");
      attribution = F("- Ghostwritten");
      return;
        case 2:
      quote = F("...announced, genially and fraternally, as if it were a pleasure to Messrs Rigby and Lowndes to give the information gratis, that it was * half-past one *.");
      attribution = F("- Mrs. Dalloway");
      return;
        case 3:
      quote = F("* Half-past one *, The street lamp sputtered, The street lamp muttered, The street lamp said, \"Regard that woman ...\"");
      attribution = F("- Rhapsody on a Windy Night");
      return;
        case 4:
      quote = F("Lupin not having come down, I went up again at * half-past one *, and said we dined at two; he said he \"would be there.\"");
      attribution = F("- The Diary of a Nobody");
      return;
        case 5:
      quote = F("She was a sticker. A clock away in the town struck * half past one *.");
      attribution = F("- Brighton Rock");
      return;
        case 6:
      quote = F("Innocent bystanders were mostly home in bed. I walked for half an hour, but nothing happened. Until * one thirty * in the morning. Until I looped around to 22nd and Broadway.");
      attribution = F("- Lee Child, Gone Tomorrow");
      return;
      } // end switch (option)

    // Codegen for 01:31 for 0 quotations:
    case 91:
      // No quotes for 01:31
        quote = "";
        attribution = "";
      return;

    // Codegen for 01:32 for 2 quotations:
    case 92:
      option = random(0, 2);
      // 2 options for 01:32
      switch (option) {
        case 0:
      quote = F("At the third stroke it will be * one ... thirty-two * ... and twenty seconds. 'Beep ... beep ... beep.' Ford Prefect suppressed a little giggle of evil satisfaction...");
      attribution = F("- Douglas Adams");
      return;
        case 1:
      quote = F("She grinned at him with malicious playfulness, showing great square teeth, and then ran for the stairs. * One-thirty-two *. She thought that she heard a whistle blown...");
      attribution = F("- Stamboul Train");
      return;
      } // end switch (option)

    // Codegen for 01:33 for 2 quotations:
    case 93:
      option = random(0, 2);
      // 2 options for 01:33
      switch (option) {
        case 0:
      quote = F("He looked at his watch. * One-thirty-three a.m. * He had been asleep on this bench for over an hour and a half.");
      attribution = F("- Kat Fox, Skeletons");
      return;
        case 1:
      quote = F("He waited for the green light to show and then opened the door again on to the now empty cargo hold.'... * one ... thirty-three * ... and fifty seconds.' Very nice.");
      attribution = F("- Douglas Adams");
      return;
      } // end switch (option)

    // Codegen for 01:34 for 2 quotations:
    case 94:
      option = random(0, 2);
      // 2 options for 01:34
      switch (option) {
        case 0:
      quote = F("He tiptoed out and returned to the control cabin. '... * one ... thirty-four * and twenty seconds.' The voice sounded as clear as if he was hearing it over a phone in London.");
      attribution = F("- Douglas Adams");
      return;
        case 1:
      quote = F("...which was where he particularly wanted it to be heard. 'At the third stroke it will be * one ... thirty ... four * ... precisely.'");
      attribution = F("- Douglas Adams");
      return;
      } // end switch (option)

    // Codegen for 01:35 for 0 quotations:
    case 95:
      // No quotes for 01:35
        quote = "";
        attribution = "";
      return;

    // Codegen for 01:36 for 0 quotations:
    case 96:
      // No quotes for 01:36
        quote = "";
        attribution = "";
      return;

    // Codegen for 01:37 for 0 quotations:
    case 97:
      // No quotes for 01:37
        quote = "";
        attribution = "";
      return;

    // Codegen for 01:38 for 1 quotations:
    case 98:
      // Only one option for 01:38:"
      quote = F("At * one-thirty-eight * am suspect left the Drive-In and drove to seven hundred and twenty three North Walnut, to the rear of the residence, and parked the car.");
      attribution = F("- The Narc");
      return;

    // Codegen for 01:39 for 0 quotations:
    case 99:
      // No quotes for 01:39
        quote = "";
        attribution = "";
      return;

    // Codegen for 01:40 for 1 quotations:
    case 100:
      // Only one option for 01:40:"
      quote = F("March twelfth, * one-forty am *, she leaves a group of drinking buddies to catch a bus home. She never makes it.");
      attribution = F("- Bones to Ashes");
      return;

    // Codegen for 01:41 for 0 quotations:
    case 101:
      // No quotes for 01:41
        quote = "";
        attribution = "";
      return;

    // Codegen for 01:42 for 0 quotations:
    case 102:
      // No quotes for 01:42
        quote = "";
        attribution = "";
      return;

    // Codegen for 01:43 for 0 quotations:
    case 103:
      // No quotes for 01:43
        quote = "";
        attribution = "";
      return;

    // Codegen for 01:44 for 2 quotations:
    case 104:
      option = random(0, 2);
      // 2 options for 01:44
      switch (option) {
        case 0:
      quote = F("By good luck, the next train was due at * forty-four minutes past one *, and arrived at Yateland (the next station) ten minutes afterward.");
      attribution = F("- Mr. Policeman and the Cook");
      return;
        case 1:
      quote = F("She knew it was the stress, two long days of stress, and she looked at her watch, * sixteen minutes to two *, and she almost leaped with fright...");
      attribution = F("- Deon Meyer, Trackers");
      return;
      } // end switch (option)

    // Codegen for 01:45 for 2 quotations:
    case 105:
      option = random(0, 2);
      // 2 options for 01:45
      switch (option) {
        case 0:
      quote = F("The blow fell at precisely * one forty-five * (summer-time). Benson, my Aunt Agatha's butler, was offering me the fried potatoes at the moment, and such was my emotion...");
      attribution = F("- Sir Roderick Comes to Lunch");
      return;
        case 1:
      quote = F("The last man had just gone to the wickets, with the score at a hundred and thirty-one, when a * quarter to two * arrived, and with it the luncheon interval.");
      attribution = F("- PG Wodehouse, Mike");
      return;
      } // end switch (option)

    // Codegen for 01:46 for 1 quotations:
    case 106:
      // Only one option for 01:46:"
      quote = F("That particular phenomenom got Presto up at * one forty-six a.m. *; silently, he painted his face and naked body with camouflage paint.");
      attribution = F("- Fardnock's Revenge");
      return;

    // Codegen for 01:47 for 0 quotations:
    case 107:
      // No quotes for 01:47
        quote = "";
        attribution = "";
      return;

    // Codegen for 01:48 for 0 quotations:
    case 108:
      // No quotes for 01:48
        quote = "";
        attribution = "";
      return;

    // Codegen for 01:49 for 1 quotations:
    case 109:
      // Only one option for 01:49:"
      quote = F("The bookstall clerk had seen the passenger in grey pass the bookstall at * 1.49 *, in the direction of the exit.");
      attribution = F("- Five Red Herrings");
      return;

    // Codegen for 01:50 for 2 quotations:
    case 110:
      option = random(0, 2);
      // 2 options for 01:50
      switch (option) {
        case 0:
      quote = F("The best train of the day was the * one-fifty * from Paddington which reached Polgarwith just after seven o'clock.");
      attribution = F("- The Cornish Mystery");
      return;
        case 1:
      quote = F("...had a cat or two-and she could not tolerate (not even as a lark, not even for a moment at * ten minutes before two AM *), embodying cliche.");
      attribution = F("- Michelle Herman, Dog");
      return;
      } // end switch (option)

    // Codegen for 01:51 for 1 quotations:
    case 111:
      // Only one option for 01:51:"
      quote = F("At * nine minutes to two * the other vehicle arrived. At first Milla didn't believe her eyes: that shape, those markings.");
      attribution = F("- Deon Meyer, Trackers");
      return;

    // Codegen for 01:52 for 0 quotations:
    case 112:
      // No quotes for 01:52
        quote = "";
        attribution = "";
      return;

    // Codegen for 01:53 for 0 quotations:
    case 113:
      // No quotes for 01:53
        quote = "";
        attribution = "";
      return;

    // Codegen for 01:54 for 1 quotations:
    case 114:
      // Only one option for 01:54:"
      quote = F("* Six minutes to two *. Janina Mentz watched the screen, where the small program window flickered with files scrolling too fast to read, searching for the keyword.");
      attribution = F("- Dean Koontz, Trackers");
      return;

    // Codegen for 01:55 for 0 quotations:
    case 115:
      // No quotes for 01:55
        quote = "";
        attribution = "";
      return;

    // Codegen for 01:56 for 0 quotations:
    case 116:
      // No quotes for 01:56
        quote = "";
        attribution = "";
      return;

    // Codegen for 01:57 for 1 quotations:
    case 117:
      // Only one option for 01:57:"
      quote = F("I looked for a clock. It was * three minutes to two *. \"I hope you can catch him, then. Thank you. I really appreciate it.\"");
      attribution = F("- C.E. Murphy, Urban Shaman");
      return;

    // Codegen for 01:58 for 0 quotations:
    case 118:
      // No quotes for 01:58
        quote = "";
        attribution = "";
      return;

    // Codegen for 01:59 for 1 quotations:
    case 119:
      // Only one option for 01:59:"
      quote = F("..and close the loop and keep it there, orbiting in perpetual obscurity. '* One ... fifty-nine ... *'");
      attribution = F("- Douglas Adams");
      return;

    // Codegen for 02:00 for 8 quotations:
    case 120:
      option = random(0, 8);
      // 8 options for 02:00
      switch (option) {
        case 0:
      quote = F("Get on plane at * 2 A.M. *, amid bundles, chickens, gypsies, sit opposite pair of plump fortune tellers who groan and (very discreetly) throw up all the way to Tbilisi.");
      attribution = F("- J. Updike, Bech: A Book");
      return;
        case 1:
      quote = F("The middle of the night? Alec asked sharply. \"Can you be more definite?\" \"* About two. Just past *.\" Daisy noted that he expressed no concern for her safety.");
      attribution = F("- Dead in the water");
      return;
        case 2:
      quote = F("* At two *, the snowplows were in action in Lillestrom.");
      attribution = F("- Jo Nesbo, The Snowman");
      return;
        case 3:
      quote = F("After an unnaturally prolonged wheezing there followed a shrill, nasty, and as it were unexpectedly rapid, chime - as though someone were suddenly jumping forward. * It struck two. *");
      attribution = F("- Notes From the Underground");
      return;
        case 4:
      quote = F("As * two o'clock * pealed from the cathedral bell, Jean Valjean awoke.");
      attribution = F("- Victor Hugo, Les Miserables");
      return;
        case 5:
      quote = F("At * two o'clock * Gatsby put on his bathing suit and left word with the butler that if any one phoned word was to be brought to him at the pool.");
      attribution = F("- The Great Gatsby");
      return;
        case 6:
      quote = F("When all had grown quiet and Fyodor Pavlovich went to bed at around * two o'clock *, Ivan Fyodorovich also went to bed with the firm resolve of falling quickly asleep.");
      attribution = F("- The Brothers Karamazov");
      return;
        case 7:
      quote = F("Out, damned spot! out, I say!-One: * two *: why, then, 'tis time to do't.-Hell is murky!-Fie, my lord, fie! a soldier, and afeard? What need we fear who knows it?");
      attribution = F("- Shakespeare, Macbeth");
      return;
      } // end switch (option)

    // Codegen for 02:01 for 1 quotations:
    case 121:
      // Only one option for 02:01:"
      quote = F("I checked my watch. * 2.01am. * The cheeseburger Happy Meal was now only a distant memory. I cursed myself for not also ordering a breakfast sandwich for the morning.");
      attribution = F("- Reif Larsen");
      return;

    // Codegen for 02:02 for 1 quotations:
    case 122:
      // Only one option for 02:02:"
      quote = F("I should certainly say you were. \"It was awful. It just went on and on.\" \"I shook you and shook you and.\" \"Time is it.\" \"It's nearly - * almost 2:04 *.\"");
      attribution = F("- Oblivion");
      return;

    // Codegen for 02:03 for 0 quotations:
    case 123:
      // No quotes for 02:03
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:04 for 0 quotations:
    case 124:
      // No quotes for 02:04
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:05 for 2 quotations:
    case 125:
      option = random(0, 2);
      // 2 options for 02:05
      switch (option) {
        case 0:
      quote = F("At * 2.05 * the fizzy tights came crackling off.");
      attribution = F("- Martin Amis, London Fields");
      return;
        case 1:
      quote = F("What time is it?' '* Five minutes past two *, sir,' answered the man, looking at the clock and yawning. 'Five minutes past two? How horribly late!");
      attribution = F("- The Picture of Dorian Gray");
      return;
      } // end switch (option)

    // Codegen for 02:06 for 0 quotations:
    case 126:
      // No quotes for 02:06
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:07 for 3 quotations:
    case 127:
      option = random(0, 3);
      // 3 options for 02:07
      switch (option) {
        case 0:
      quote = F("At * 2:07 a.m. * I decided that I wanted a drink of orange squash before I brushed my teeth and got into bed, so I went downstairs to the kitchen.");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("But I couldn't sleep. And I got out of bed at * 2.07 am * and I felt scared of Mr. Shears so I went downstairs and out of the front door into Chapter Road.");
      attribution = F("- Mark Haddon");
      return;
        case 2:
      quote = F("Saturday, 17 November - * 2.07 a.m. * I cannot sleep. Ben is upstairs, back in bed, and I am writing this in the kitchen. He thinks I am drinking a cup of cocoa...");
      attribution = F("- Before I Go to Sleep");
      return;
      } // end switch (option)

    // Codegen for 02:08 for 0 quotations:
    case 128:
      // No quotes for 02:08
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:09 for 0 quotations:
    case 129:
      // No quotes for 02:09
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:10 for 2 quotations:
    case 130:
      option = random(0, 2);
      // 2 options for 02:10
      switch (option) {
        case 0:
      quote = F("February 26, Saturday - Richards went out 1:10am and found it clearing a bit, so we got under way as soon as possible, which was * 2:10am *");
      attribution = F("- Ernest Shackleton");
      return;
        case 1:
      quote = F("* Ten minutes past two *, sir, answered the man, looking at the clock and blinking. \"Ten minutes past two? How horribly late!\"");
      attribution = F("- The Picture of Dorian Gray");
      return;
      } // end switch (option)

    // Codegen for 02:11 for 0 quotations:
    case 131:
      // No quotes for 02:11
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:12 for 1 quotations:
    case 132:
      // Only one option for 02:12:"
      quote = F("Then the lights went out all over the city. It happened at * 2.12am * according to power-house records, but Blake's diary gives no indication of the time.");
      attribution = F("- The Haunter of the Dark");
      return;

    // Codegen for 02:13 for 2 quotations:
    case 133:
      option = random(0, 2);
      // 2 options for 02:13
      switch (option) {
        case 0:
      quote = F("You're going to a dimension that diverged from our own at * 02.13 * on the morning of Wednesday 20 February 1918, over twenty-six years earlier.");
      attribution = F("- Chris James");
      return;
        case 1:
      quote = F("At the third stroke, it will be * two ... thirteen * ... and fifty seconds.'");
      attribution = F("- Douglas Adams");
      return;
      } // end switch (option)

    // Codegen for 02:14 for 0 quotations:
    case 134:
      // No quotes for 02:14
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:15 for 2 quotations:
    case 135:
      option = random(0, 2);
      // 2 options for 02:15
      switch (option) {
        case 0:
      quote = F("At * 2.15am * a policeman observed the place in darkness, but with the stranger's motor still at the curb.");
      attribution = F("- The Shadow Out of Time");
      return;
        case 1:
      quote = F("When the alarm rang at * two fifteen *, Lew shut it off, snapped on the little bedside lamp, then swung his feet to the floor to sit on the edge of the bed, holding his eyes open.");
      attribution = F("- The Night People");
      return;
      } // end switch (option)

    // Codegen for 02:16 for 0 quotations:
    case 136:
      // No quotes for 02:16
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:17 for 2 quotations:
    case 137:
      option = random(0, 2);
      // 2 options for 02:17
      switch (option) {
        case 0:
      quote = F("The flare spluttered and faded, and now the glare of the spotlight blinded me. I sat very still. It was * 2.17 *.");
      attribution = F("- The Ipcress File");
      return;
        case 1:
      quote = F("What time is it now? He turned her very dusty alarm clock to check. \"* Two-seventeen *,\" he marveled. It was the strangest time he'd seen in his entire life.");
      attribution = F("- Jonathan Franzen, Freedom");
      return;
      } // end switch (option)

    // Codegen for 02:18 for 1 quotations:
    case 138:
      // Only one option for 02:18:"
      quote = F("It was * 2:18 in the morning *, and Donna could see no one else in any other office working so late.");
      attribution = F("- Jane Smiley, Moo");
      return;

    // Codegen for 02:19 for 0 quotations:
    case 139:
      // No quotes for 02:19
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:20 for 4 quotations:
    case 140:
      option = random(0, 4);
      // 4 options for 02:20
      switch (option) {
        case 0:
      quote = F("She looked at her watch and it was * twenty minutes past two *. She had no time to lose but must get ready at once.");
      attribution = F("- A Tale of Two Cities");
      return;
        case 1:
      quote = F("Inevitable, implacable, the rainstorm wept itself out. She saw Tom look at his watch. 'What time is it?' '* Twenty past two *. Want to go back to the hotel for a while?' 'All right.'");
      attribution = F("- The Doctor's Wife");
      return;
        case 2:
      quote = F("She turned abruptly to the nurse and asked the time. '* Two-twenty *' 'Ah...Two-twenty!' Genevieve repeated, as though there was something urgent to be done.");
      attribution = F("- Southern Mail");
      return;
        case 3:
      quote = F("The night of his third walk Lew slept in his own apartment. When his eyes opened at * two twenty *, by the green hands of his alarm...");
      attribution = F("- The Night People");
      return;
      } // end switch (option)

    // Codegen for 02:21 for 2 quotations:
    case 141:
      option = random(0, 2);
      // 2 options for 02:21
      switch (option) {
        case 0:
      quote = F("* 2:21 a.m. * Lance-Corporal Hartmann emerged from the house in the Rue de Londres.");
      attribution = F("- The Night of the Generals");
      return;
        case 1:
      quote = F("I lifted my left hand and pressed on the light of my digital wristwatch. * Two-twenty-one *. It was midnight when we headed underground, so only a little over two hours had passed.");
      attribution = F("- Haruki Murakami");
      return;
      } // end switch (option)

    // Codegen for 02:22 for 0 quotations:
    case 142:
      // No quotes for 02:22
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:23 for 0 quotations:
    case 143:
      // No quotes for 02:23
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:24 for 1 quotations:
    case 144:
      // Only one option for 02:24:"
      quote = F("It was * 2.24am. * She stumbled out of bed, tripping on her shoes that she'd kicked off earlier and pulled on a jumper.");
      attribution = F("- After You'd Gone");
      return;

    // Codegen for 02:25 for 1 quotations:
    case 145:
      // Only one option for 02:25:"
      quote = F("You see it is time: * 2.25am. * You get out of bed.");
      attribution = F("- David Peace");
      return;

    // Codegen for 02:26 for 1 quotations:
    case 146:
      // Only one option for 02:26:"
      quote = F("Listened to a voicemail message left at * 2.26am * by Claude.");
      attribution = F("- The Lighted Rooms");
      return;

    // Codegen for 02:27 for 1 quotations:
    case 147:
      // Only one option for 02:27:"
      quote = F("The moon didn't shine again until * 2.27am. * It was enough to show Wallander that he was positioned some distance below the tree.");
      attribution = F("- One Step Behind");
      return;

    // Codegen for 02:28 for 1 quotations:
    case 148:
      // Only one option for 02:28:"
      quote = F("* 2.28am *: Ran out of sheep and began counting other farmyard animals.");
      attribution = F("- Mike Gayle, Mr Commitment");
      return;

    // Codegen for 02:29 for 0 quotations:
    case 149:
      // No quotes for 02:29
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:30 for 6 quotations:
    case 150:
      option = random(0, 6);
      // 6 options for 02:30
      switch (option) {
        case 0:
      quote = F("May 14th 1800. Wm and John set off into Yorkshire after dinner at * 1/2 past 2 o'clock *, cold pork in their pockets. I left them at the turning of the Low-wood bay under the trees.");
      attribution = F("- Dorothy Wordsworth");
      return;
        case 1:
      quote = F("Ach! It's * 2:30 *. Look how the time is flying. And it's still so much to do today.. It's dishes to clean, dinner to defrost, and my pills I haven't yet counted.");
      attribution = F("- Art Spiegelman, Maus");
      return;
        case 2:
      quote = F("It is * 2.30am * and I am tight. As a tick, as a lord, as a newt. Must write this down before the sublime memories fade and blur.");
      attribution = F("- Any Human Heart");
      return;
        case 3:
      quote = F("Listen, we can't tie up the phones like this. \"Why not?\" said Harry. \"Who's going to call at * 2:30 a.m. * with a better idea? Yippee, to quote Lew, we're having a party!\"");
      attribution = F("- The Night People");
      return;
        case 4:
      quote = F("Inc, I tried to pull her off about 0* 230 *, and there was this fucking... sound.");
      attribution = F("- Infinite Jest");
      return;
        case 5:
      quote = F("At about * half past two * she had been woken by the creak of footsteps out on the stairs. At first she had been frightened.");
      attribution = F("- The Little Stranger");
      return;
      } // end switch (option)

    // Codegen for 02:31 for 1 quotations:
    case 151:
      // Only one option for 02:31:"
      quote = F("And then I woke up because there were people shouting in the flat and it was * 2.31am. * And one of the people was Father and I was frightened.");
      attribution = F("- Mark Haddon");
      return;

    // Codegen for 02:32 for 1 quotations:
    case 152:
      // Only one option for 02:32:"
      quote = F("The last guests departed at * 2.32 a.m. *, two hours and two minutes after the scheduled completion time.");
      attribution = F("- The Rosie Project");
      return;

    // Codegen for 02:33 for 1 quotations:
    case 153:
      // Only one option for 02:33:"
      quote = F("But it wasn't going on! It was two-thirty-four, well. * Two-thirty-three * and nothing had happened. Suppose he got a room call, or the elevator night-bell rang, now.");
      attribution = F("- A Swell-looking Babe");
      return;

    // Codegen for 02:34 for 1 quotations:
    case 154:
      // Only one option for 02:34:"
      quote = F("It was * two-thirty-four *, well. Two-thirty-three and nothing had happened. Suppose he got a room call, or the elevator night-bell rang, now.");
      attribution = F("- A Swell-looking Babe");
      return;

    // Codegen for 02:35 for 1 quotations:
    case 155:
      // Only one option for 02:35:"
      quote = F("For what happened at * 2.35 * we have the testimony of the priest, a young, intelligent, and well-educated person; of Patrolman William J. Monohan of the Central Station...");
      attribution = F("- The Haunter of the Dark");
      return;

    // Codegen for 02:36 for 2 quotations:
    case 156:
      option = random(0, 2);
      // 2 options for 02:36
      switch (option) {
        case 0:
      quote = F("It was about * 2.36am * when a provost colonel arrived to arrest me. At 2.36 1/2 I remembered the big insulating gauntlets. But even had I remembered before, what could I have done?");
      attribution = F("- The Ipcress File");
      return;
        case 1:
      quote = F("I look at my watch. * Two thirty-six *. All I've got left today is take in the laundry and fix dinner.");
      attribution = F("- The Elephant Vanishes");
      return;
      } // end switch (option)

    // Codegen for 02:37 for 1 quotations:
    case 157:
      // Only one option for 02:37:"
      quote = F("June 13, 1990. * Thirty-seven minutes past two * in the morning. And sixteen seconds.");
      attribution = F("- Stephen King, The Stand");
      return;

    // Codegen for 02:38 for 0 quotations:
    case 158:
      // No quotes for 02:38
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:39 for 1 quotations:
    case 159:
      // Only one option for 02:39:"
      quote = F("Noo, there's a report come in fra' the station-master at Pinwherry that there was a gentleman tuk the * 2.39 * at Pinwherry.");
      attribution = F("- Five Red Herrings");
      return;

    // Codegen for 02:40 for 1 quotations:
    case 160:
      // Only one option for 02:40:"
      quote = F("Members of Big Side marked Michael and Alan as the two most promising three-quarters for Middle Side next year, and when the bell sounded at * twenty minutes to three *...");
      attribution = F("- Sinister Street");
      return;

    // Codegen for 02:41 for 0 quotations:
    case 161:
      // No quotes for 02:41
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:42 for 0 quotations:
    case 162:
      // No quotes for 02:42
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:43 for 1 quotations:
    case 163:
      // Only one option for 02:43:"
      quote = F("She settled back beside him. 'It's * 2:43 *:12am, Case. Got a readout chipped into my optic nerve.'");
      attribution = F("- William Gibson, Neuromancer");
      return;

    // Codegen for 02:44 for 0 quotations:
    case 164:
      // No quotes for 02:44
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:45 for 3 quotations:
    case 165:
      option = random(0, 3);
      // 3 options for 02:45
      switch (option) {
        case 0:
      quote = F("* 0245h *., Ennet House, the hours that are truly wee.");
      attribution = F("- Infinite Jest");
      return;
        case 1:
      quote = F("I look, and I see the clock has no hands. 'There are no hands,' I say. The old woman looks at the clock face and says to me, 'It's a * quarter to three *'.");
      attribution = F("- The Old Woman");
      return;
        case 2:
      quote = F("Pull the other one, and tell it to the marines, and don't make me laugh, and fuck off out of it, and all that, but the fact remained that it was still only * two forty-five *'.");
      attribution = F("- The Pregnant Window");
      return;
      } // end switch (option)

    // Codegen for 02:46 for 2 quotations:
    case 166:
      option = random(0, 2);
      // 2 options for 02:46
      switch (option) {
        case 0:
      quote = F("* 2.46am. * The chain drive whirred and the paper target slid down the darkened range, ducking in and out of shafts of yellow incandescent light.");
      attribution = F("- Steve Sohmer, Patriots");
      return;
        case 1:
      quote = F("Vicki shoved her glasses at her face and peered at the clock. * Two forty-six *. 'I don't have time for this' she muttered...");
      attribution = F("- Tanya Huff, Blood Lines");
      return;
      } // end switch (option)

    // Codegen for 02:47 for 1 quotations:
    case 167:
      // Only one option for 02:47:"
      quote = F("The glowing numbers read * 2.47am. * Moises sighs and turns back to the bathroom door. Finally, the doorknob turns and Conchita comes back to bed.");
      attribution = F("- The Book of Want");
      return;

    // Codegen for 02:48 for 0 quotations:
    case 168:
      // No quotes for 02:48
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:49 for 0 quotations:
    case 169:
      // No quotes for 02:49
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:50 for 1 quotations:
    case 170:
      // Only one option for 02:50:"
      quote = F("Stands the Church clock at * ten to three *? And is there honey still for tea?");
      attribution = F("- Rupert Brooke");
      return;

    // Codegen for 02:51 for 0 quotations:
    case 171:
      // No quotes for 02:51
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:52 for 0 quotations:
    case 172:
      // No quotes for 02:52
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:53 for 0 quotations:
    case 173:
      // No quotes for 02:53
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:54 for 1 quotations:
    case 174:
      // Only one option for 02:54:"
      quote = F("...and that terrible listlessness that starts to set in * about 2.55 *, when you know you've had all the baths you can usefully have that day...");
      attribution = F("- Douglas Adams");
      return;

    // Codegen for 02:55 for 2 quotations:
    case 175:
      option = random(0, 2);
      // 2 options for 02:55
      switch (option) {
        case 0:
      quote = F("Recorded cocktail music nuclear-powered to play on for centuries after all life has been destroyed. That do you for * 2:55 a.m. *?");
      attribution = F("- The Night People");
      return;
        case 1:
      quote = F("Time to go: * 2.55am. * Two-handed, Cec lifted his peak cap from the chair.");
      attribution = F("- Iain Sinclair, Downriver");
      return;
      } // end switch (option)

    // Codegen for 02:56 for 1 quotations:
    case 176:
      // Only one option for 02:56:"
      quote = F("It was * 2:56 * when the shovel touched the coffin. We all heard the sound and looked at each other.");
      attribution = F("- Jonathan Safran Foer");
      return;

    // Codegen for 02:57 for 0 quotations:
    case 177:
      // No quotes for 02:57
        quote = "";
        attribution = "";
      return;

    // Codegen for 02:58 for 1 quotations:
    case 178:
      // Only one option for 02:58:"
      quote = F("...from twenty-five minutes past eleven until twenty-eight minutes past eleven, from ten minutes to three until * two minutes to three * the heroes of the school met...");
      attribution = F("- Sinister Street");
      return;

    // Codegen for 02:59 for 1 quotations:
    case 179:
      // Only one option for 02:59:"
      quote = F("I remembered arriving in this room at * 2.59 * one night. I remembered the sergeant who called me names: mostly Anglo-Saxon monosyllabic four-letter ones...");
      attribution = F("- The Ipcress File");
      return;

    // Codegen for 03:00 for 24 quotations:
    case 180:
      option = random(0, 24);
      // 24 options for 03:00
      switch (option) {
        case 0:
      quote = F("When Sophie awoke, it was * 3:00 a.m. *");
      attribution = F("- Desperate Characters");
      return;
        case 1:
      quote = F("The Regrouting Committee's been dragging along since may. Suddenly they're pulling secret 0* 300 * milk-switches? It doesn't ring true, Jim.");
      attribution = F("- Infinite Jest");
      return;
        case 2:
      quote = F("I slam the phone down but it misses the base. I hit the clock instead, which flashes * 3 a.m. *");
      attribution = F("- Dan Holloway");
      return;
        case 3:
      quote = F("In a real dark night of the soul it is always * 3 o'clock * in the morning.");
      attribution = F("- The Crack-Up");
      return;
        case 4:
      quote = F("* At three am * I was walking the floor and listening to Katchaturian working in a tractor factory. He called it a violin concerto. I called it a loose fan belt and the hell with it.");
      attribution = F("- The Long Goodbye");
      return;
        case 5:
      quote = F("...that someone stumbled jerkily, as if his ankles were in chains, to the Comanche cabin's bathroom * at three A.M. *");
      attribution = F("- Philip Roth, Nemesis");
      return;
        case 6:
      quote = F("But * at three o'clock in the morning *, a forgotten package has the same tragic importance as a death sentence, and the cure doesn't work");
      attribution = F("- The Crack-Up");
      return;
        case 7:
      quote = F("'Lo,' she mumbled, wondering what time it was. She made out the luminous dial of the clock beside the bed. * Three AM. * Good grief.");
      attribution = F("- The Game Players of Titan");
      return;
        case 8:
      quote = F("It was * three in the morning * when his taxi stopped by giant mounds of snow outside his hotel. He had not eaten in hours.");
      attribution = F("- Ian McEwan, Solar");
      return;
        case 9:
      quote = F("* Three in the morning *, thought Charles Halloway, seated on the edge of his bed. Why did the train come at that hour?");
      attribution = F("- Ray Bradbury");
      return;
        case 10:
      quote = F("Three a.m. That's our reward. * Three in the morn. * The soul's midnight. The tide goes out, the soul ebbs. And a train arrives at an hour of despair. Why?");
      attribution = F("- Ray Bradbury");
      return;
        case 11:
      quote = F("According to her watch it was shortly after * three o'clock *, and according to everything else it was night-time.");
      attribution = F("- Douglas Adams");
      return;
        case 12:
      quote = F("Once I saw a figure I shall never forget. It was * three o'clock at night *, as I was going home from Blacky as usual; it was a short-cut for me...");
      attribution = F("- Max Frisch, I'm Not Stiller");
      return;
        case 13:
      quote = F("At * three o'clock * precisely I was at Baker Street, but Holmes had not yet returned.");
      attribution = F("- A Scandal in Bohemia");
      return;
        case 14:
      quote = F("I had a * three o'clock * class in psychology, the first meeting of the semester, and I suspected I was going to miss it. I was right.");
      attribution = F("- T.C. Boyle, Achates McNeil");
      return;
        case 15:
      quote = F("At * three o' clock in the morning * Eurydice is bound to come into it. After all, why did I sit here like a telegrapher at a lost outpost...");
      attribution = F("- The Medusa Frequency");
      return;
        case 16:
      quote = F("It was * three o'clock * in the beautiful breezy autumn day when Mr. Casaubon drove off to his Rectory at Lowick, only five miles from Tipton.");
      attribution = F("- George Eliot, Middlemarch");
      return;
        case 17:
      quote = F("Remember, they shouted, \"battle at * three o'clock * sharp. There's no time to lose.\"");
      attribution = F("- Swallows and Amazons");
      return;
        case 18:
      quote = F("She died this morning, very early, about * three o'clock *.");
      attribution = F("- The Voyage Out");
      return;
        case 19:
      quote = F("She is an insomniac. Her nights are wide-eyed and excited with worry. Even at * three o'clock * in the morning one might hear her eating a Bain Marie biscuit in the kitchen.");
      attribution = F("- Afternoon Raag");
      return;
        case 20:
      quote = F("* Three o'clock * is always too late or too early for anything you want to do.");
      attribution = F("- Jean-Paul Sartre, Nausea");
      return;
        case 21:
      quote = F("You hearken, Missy. It's * three o'clock * in the morning and I've got all my faculties as well as ever I had in my life. I know all my property and where the money's put out.");
      attribution = F("- George Eliot, Middlemarch");
      return;
        case 22:
      quote = F("... when she heard, distractedly, something fumbling, something scratching at the door. Who at this hour? * Three *, good Heavens! Three already!");
      attribution = F("- Mrs Dalloway");
      return;
        case 23:
      quote = F("Why, if you listen you will hear it strike. George listened, and a neighbouring clock immediately obliged. \"But it's only gone * three *!\"");
      attribution = F("- Three Men in a Boat");
      return;
      } // end switch (option)

    // Codegen for 03:01 for 1 quotations:
    case 181:
      // Only one option for 03:01:"
      quote = F("It was now * about three o'clock * in the morning and Francis Macomber, who had been asleep a little while after he had stopped thinking about the lion...");
      attribution = F("- Ernest Hemingway");
      return;

    // Codegen for 03:02 for 0 quotations:
    case 182:
      // No quotes for 03:02
        quote = "";
        attribution = "";
      return;

    // Codegen for 03:03 for 0 quotations:
    case 183:
      // No quotes for 03:03
        quote = "";
        attribution = "";
      return;

    // Codegen for 03:04 for 1 quotations:
    case 184:
      // Only one option for 03:04:"
      quote = F("...his back-up alarm clock rang. He looked at his front-line clock on the bedside table and noted that it had stopped at * 3.04 *. So, you couldn't even rely on alarm clocks.");
      attribution = F("- Henning Mankell");
      return;

    // Codegen for 03:05 for 1 quotations:
    case 185:
      // Only one option for 03:05:"
      quote = F("On the Sunday before Christmas she awoke at * 3:05 a.m. * and though: Thirty-six hours. Four hours later she got up thinking: Thirty-two hours.");
      attribution = F("- The Corrections");
      return;

    // Codegen for 03:06 for 0 quotations:
    case 186:
      // No quotes for 03:06
        quote = "";
        attribution = "";
      return;

    // Codegen for 03:07 for 2 quotations:
    case 187:
      option = random(0, 2);
      // 2 options for 03:07
      switch (option) {
        case 0:
      quote = F("Wayne late-logged in: * 3.07am * -the late-late show. He parked. He dumped his milk can. He yawned, he stretched. He scratched.");
      attribution = F("- The Cold Six Thousand");
      return;
        case 1:
      quote = F("The next day was grey, threatening rain. He was there at * seven minutes past three *. The clock on the church over the way pointed to it.");
      attribution = F("- Patrick Hamilton");
      return;
      } // end switch (option)

    // Codegen for 03:08 for 0 quotations:
    case 188:
      // No quotes for 03:08
        quote = "";
        attribution = "";
      return;

    // Codegen for 03:09 for 0 quotations:
    case 189:
      // No quotes for 03:09
        quote = "";
        attribution = "";
      return;

    // Codegen for 03:10 for 2 quotations:
    case 190:
      option = random(0, 2);
      // 2 options for 03:10
      switch (option) {
        case 0:
      quote = F("Love again; wanking at * ten past three *");
      attribution = F("- Philip Larkin, Love Again");
      return;
        case 1:
      quote = F("...the loss so you couldn't be charge for anything beyond the time of my calling them up. I looked at the clock. It was * ten-past three *.");
      attribution = F("- Ali Smith");
      return;
      } // end switch (option)

    // Codegen for 03:11 for 0 quotations:
    case 191:
      // No quotes for 03:11
        quote = "";
        attribution = "";
      return;

    // Codegen for 03:12 for 0 quotations:
    case 192:
      // No quotes for 03:12
        quote = "";
        attribution = "";
      return;

    // Codegen for 03:13 for 1 quotations:
    case 193:
      // Only one option for 03:13:"
      quote = F("The lift moved. It was * thirteen minutes past three *. The bell gave out its ping. Two men stepped out of the lift, Alan Norman and another man. Tony Blair walked into the office.");
      attribution = F("- Virtual Assassin");
      return;

    // Codegen for 03:14 for 3 quotations:
    case 194:
      option = random(0, 3);
      // 3 options for 03:14
      switch (option) {
        case 0:
      quote = F("A signal sounded. \"There's the * 3.14 * up,\" said Perks. \"You lie low till she's through, and then we'll go up along to my place, and see if there's any of them strawberries...\"");
      attribution = F("- The Railway Children");
      return;
        case 1:
      quote = F("Since he had told the girl that it had to end, he'd been waking up every morning at * 3.14 *, without fail. Every morning his eyes would flick open, alert...");
      attribution = F("- Christos Tsiolkas, The Slap");
      return;
        case 2:
      quote = F("I shall be back at exactly * THREE fourteen *, for our hour of revery together, real sweet revery darling");
      attribution = F("- Jack Kerouac, On the Road");
      return;
      } // end switch (option)

    // Codegen for 03:15 for 3 quotations:
    case 195:
      option = random(0, 3);
      // 3 options for 03:15
      switch (option) {
        case 0:
      quote = F("Above the door of Room 69 the clock ticked on at * 3:15 *. The motion was accelerating. What had once been the gymnasium was now a small room...");
      attribution = F("- JG Ballard, Manhole 69");
      return;
        case 1:
      quote = F("As I was going out the door, I decided to leave a note. So I wrote: \"Feeling better and going to Birch Creek. Back soon. R. * 3:15 *.\"");
      attribution = F("- Where I'm Calling From");
      return;
        case 2:
      quote = F("He had been on the point of slipping Edgar Wallace into his pocket. The clock over the Prince of Wales struck a * quarter past three *.");
      attribution = F("- Keep the Aspidistra Flying");
      return;
      } // end switch (option)

    // Codegen for 03:16 for 0 quotations:
    case 196:
      // No quotes for 03:16
        quote = "";
        attribution = "";
      return;

    // Codegen for 03:17 for 2 quotations:
    case 197:
      option = random(0, 2);
      // 2 options for 03:17
      switch (option) {
        case 0:
      quote = F("...each one able to display eight different camera mountings, giving Kurt 192 different still lives of Green Oaks at * 3.17 a.m. * this March night.");
      attribution = F("- What Was Lost");
      return;
        case 1:
      quote = F("...and then lay back down. \"Two of them.\" I rolled over, and checked my iPod. It was * 3:17 * in the morning.");
      attribution = F("- Donna Tartt, The Goldfinch");
      return;
      } // end switch (option)

    // Codegen for 03:18 for 0 quotations:
    case 198:
      // No quotes for 03:18
        quote = "";
        attribution = "";
      return;

    // Codegen for 03:19 for 1 quotations:
    case 199:
      // Only one option for 03:19:"
      quote = F("The time stamp on Navidson's camcorder indicates that it is exactly * 3.19 A.M. *");
      attribution = F("- House of Leaves");
      return;

    // Codegen for 03:20 for 1 quotations:
    case 200:
      // Only one option for 03:20:"
      quote = F("Prabath Kumara, 16. 17th November 1989. At * 3.20am * from the home of a friend.");
      attribution = F("- Anil's Ghost");
      return;

    // Codegen for 03:21 for 1 quotations:
    case 201:
      // Only one option for 03:21:"
      quote = F("Next, he remembered that the morrow of Christmas would be the twenty-seventh day of the moon, and that consequently high water would be at * twenty-one minutes past three *");
      attribution = F("- The Toilers of the Sea");
      return;

    // Codegen for 03:22 for 0 quotations:
    case 202:
      // No quotes for 03:22
        quote = "";
        attribution = "";
      return;

    // Codegen for 03:23 for 1 quotations:
    case 203:
      // Only one option for 03:23:"
      quote = F("* Three twenty-three *! Is that all? Doesn't time - no, I've already said that, thought that. I sit and watch the seconds change on the watch.");
      attribution = F("- Iain Banks, Espedair Street");
      return;

    // Codegen for 03:24 for 0 quotations:
    case 204:
      // No quotes for 03:24
        quote = "";
        attribution = "";
      return;

    // Codegen for 03:25 for 1 quotations:
    case 205:
      // Only one option for 03:25:"
      quote = F("It was * 3:25 a.m. * A strange thrill, to think I was the only Mulvaney awake in the house.");
      attribution = F("- We Were the Mulvaneys");
      return;

    // Codegen for 03:26 for 0 quotations:
    case 206:
      // No quotes for 03:26
        quote = "";
        attribution = "";
      return;

    // Codegen for 03:27 for 0 quotations:
    case 207:
      // No quotes for 03:27
        quote = "";
        attribution = "";
      return;

    // Codegen for 03:28 for 1 quotations:
    case 208:
      // Only one option for 03:28:"
      quote = F("A door slammed. That foreign language again. What the devil was going on? he switched on his light and peered at his watch. * 3.28 *. He got out of bed.");
      attribution = F("- Dreams of Leaving");
      return;

    // Codegen for 03:29 for 1 quotations:
    case 209:
      // Only one option for 03:29:"
      quote = F("Good heavens! she said, \"it's * nearly half-past three *. I must fly. Don't forget about the funeral service\"");
      attribution = F("- Nuns at Luncheon");
      return;

    // Codegen for 03:30 for 5 quotations:
    case 210:
      option = random(0, 5);
      // 5 options for 03:30
      switch (option) {
        case 0:
      quote = F("It's * 3:30 A.M. * in Mrs. Ralph's finally quiet house when Garp decides to clean the kitchen, to kill the time until dawn.");
      attribution = F("- The World According to Garp");
      return;
        case 1:
      quote = F("At * half-past three A.M. * he lost one illusion: officers sent to reconnoitre informed him that the enemy was making no movement.");
      attribution = F("- Victor Hugo, Les Miserables");
      return;
        case 2:
      quote = F("At * Half past Three *, a single Bird Unto a silent Sky Propounded but a single term Of cautious melody.");
      attribution = F("- Emily Dickinson");
      return;
        case 3:
      quote = F("Let's go to sleep, I say. \"Look at what time it is.\" The clock radio is right there beside the bed. Anyone can see it says * three-thirty *.\"");
      attribution = F("- Whoever Was Using This Bed");
      return;
        case 4:
      quote = F("Now, look. I am not going to call Dr. McGrath at * three thirty * in the morning to ask if it's all right for my son to eat worms. That's flat.");
      attribution = F("- How to Eat Fried Worms");
      return;
      } // end switch (option)

    // Codegen for 03:31 for 0 quotations:
    case 211:
      // No quotes for 03:31
        quote = "";
        attribution = "";
      return;

    // Codegen for 03:32 for 1 quotations:
    case 212:
      // Only one option for 03:32:"
      quote = F("At * 3:32 * precisely, I noticed Kaitlyn striding confidently past the Wok House. She saw me the moment I raised my hand, flashed her very white and newly straightened teeth at me...");
      attribution = F("- The Fault in Our Stars");
      return;

    // Codegen for 03:33 for 1 quotations:
    case 213:
      // Only one option for 03:33:"
      quote = F("A draft whistled in around the kitchen window frame and I shivered. The digital clock on Perkus's stove read * 3:33 *.");
      attribution = F("- Chronic City");
      return;

    // Codegen for 03:34 for 1 quotations:
    case 214:
      // Only one option for 03:34:"
      quote = F("It was * 3:34 am. * and he was wide-awake. He'd heard the phone ring and the sound of his uncle's voice.");
      attribution = F("- Always Florence");
      return;

    // Codegen for 03:35 for 2 quotations:
    case 215:
      option = random(0, 2);
      // 2 options for 03:35
      switch (option) {
        case 0:
      quote = F("He could just see the hands of the alarm clock in the darkness: * 3.35 a.m. * He adjusted his pillow and shut his eyes.");
      attribution = F("- The Dogs of Riga");
      return;
        case 1:
      quote = F("By * three-thirty-five * business really winds down. I have already sold my ladderback chairs and my Scottish cardigans. I'm not even sure now why I've sold all these...");
      attribution = F("- Lorrie Moore, Anagrams");
      return;
      } // end switch (option)

    // Codegen for 03:36 for 1 quotations:
    case 216:
      // Only one option for 03:36:"
      quote = F("As I near Deadhorse, it's * 3:36 a.m. * and seventeen below. Tall, sodium vapor lights spill on the road and there are no trees, only machines, mechanical shadows.");
      attribution = F("- Zoopraxis");
      return;

    // Codegen for 03:37 for 1 quotations:
    case 217:
      // Only one option for 03:37:"
      quote = F("It was three * thirty-seven A.M. *, and for once Maggie was asleep. She had got to be a pretty good sleeper in the last few months. Clyde was prouder of this fact than anything.");
      attribution = F("- Stephen Bury, The Cobweb");
      return;

    // Codegen for 03:38 for 1 quotations:
    case 218:
      // Only one option for 03:38:"
      quote = F("At * 3.38am *, it began to snow in Bowling Green, Kentucky. The geese circling the city flew back to the park, landed, and hunkered down to sit it out on their island in the lake.");
      attribution = F("- Connie Willis");
      return;

    // Codegen for 03:39 for 1 quotations:
    case 219:
      // Only one option for 03:39:"
      quote = F("23 October 1893 * 3.39am. * Upon further thought, I feel it necessary to explain that exile into the Master's workshop is not an unpleasant fate.");
      attribution = F("- The Clockwork man");
      return;

    // Codegen for 03:40 for 1 quotations:
    case 220:
      // Only one option for 03:40:"
      quote = F("His bedside clock shows * three forty *. He has no idea what he's doing out of bed: he has no need to relieve himself...");
      attribution = F("- Ian McEwan, Saturday");
      return;

    // Codegen for 03:41 for 1 quotations:
    case 221:
      // Only one option for 03:41:"
      quote = F("The alarm clock said * 3.41am. * He sat up. Why was the alarm clock slow? He picked up the alarm clock and adjusted the hands to show the same time as his wristwatch: 3.44am");
      attribution = F("- The Dogs of Riga");
      return;

    // Codegen for 03:42 for 1 quotations:
    case 222:
      // Only one option for 03:42:"
      quote = F("We are due in Yellow Sky at * 3:42 *, he said, looking tenderly into her eyes. \"Oh, are we?\" she said, as if she had not been aware of it.");
      attribution = F("- Bride Comes to Yellow Sky");
      return;

    // Codegen for 03:43 for 1 quotations:
    case 223:
      // Only one option for 03:43:"
      quote = F("The clock says * 3.43am. * The thermometer says it's a chilly fourteen degrees Fahrenheit. The weatherman says the cold spell will last until Thursday...");
      attribution = F("- Ghostwritten");
      return;

    // Codegen for 03:44 for 1 quotations:
    case 224:
      // Only one option for 03:44:"
      quote = F("It was dark. After she had switched the light on and been to the toilet, she checked her watch: * 3.44 a.m. * She undressed, put the cat out the door and returned to the twin bed.");
      attribution = F("- Liver: Leberknodel");
      return;

    // Codegen for 03:45 for 1 quotations:
    case 225:
      // Only one option for 03:45:"
      quote = F("You keep too late hours! I heard of you the other night at Lady Rufford's dancing till four o' clock in the morning! Lord Goring: Only a * quarter to four *, father.");
      attribution = F("- An Ideal Husband");
      return;

    // Codegen for 03:46 for 0 quotations:
    case 226:
      // No quotes for 03:46
        quote = "";
        attribution = "";
      return;

    // Codegen for 03:47 for 1 quotations:
    case 227:
      // Only one option for 03:47:"
      quote = F("I stayed awake until * 3:47 *. That was the last time I looked at my watch before I fell asleep.");
      attribution = F("- Mark Haddon");
      return;

    // Codegen for 03:48 for 0 quotations:
    case 228:
      // No quotes for 03:48
        quote = "";
        attribution = "";
      return;

    // Codegen for 03:49 for 1 quotations:
    case 229:
      // Only one option for 03:49:"
      quote = F("It was * 3.49 * when he hit me because of the two hundred times I had said, \"I don't know.\" He hit me a lot after that.");
      attribution = F("- The Ipcress File");
      return;

    // Codegen for 03:50 for 1 quotations:
    case 230:
      // Only one option for 03:50:"
      quote = F("The first was recorded at * ten or five to four * in the morning.");
      attribution = F("- Peter Robb");
      return;

    // Codegen for 03:51 for 2 quotations:
    case 231:
      option = random(0, 2);
      // 2 options for 03:51
      switch (option) {
        case 0:
      quote = F("Sweat trickled down my ribs. The digital reading on the clock-radio was * 3:51 *. Always odd numbered at times like this. What does it mean? Is death odd-numbered?");
      attribution = F("- Don DeLillo, White Noise");
      return;
        case 1:
      quote = F("The digital reading on the clock-radio was * 3:51 *. Always odd numbers at times like this. What does it mean? Is death odd-numbered?");
      attribution = F("- Don DeLillo, White Noise");
      return;
      } // end switch (option)

    // Codegen for 03:52 for 0 quotations:
    case 232:
      // No quotes for 03:52
        quote = "";
        attribution = "";
      return;

    // Codegen for 03:53 for 0 quotations:
    case 233:
      // No quotes for 03:53
        quote = "";
        attribution = "";
      return;

    // Codegen for 03:54 for 1 quotations:
    case 234:
      // Only one option for 03:54:"
      quote = F("The charter flight from Florida touched down at Aldergrove minutes earlier, at * 3.54 a.m. *");
      attribution = F("- Paul Muldoon");
      return;

    // Codegen for 03:55 for 1 quotations:
    case 235:
      // Only one option for 03:55:"
      quote = F("Here in the cavernous basement at * 3.55 a.m. *, in a single pool of light, is Theo Perowne.");
      attribution = F("- Ian McEwan, Saturday");
      return;

    // Codegen for 03:56 for 1 quotations:
    case 236:
      // Only one option for 03:56:"
      quote = F("* Four minutes to four *. Newman sighed again, lost in thought.");
      attribution = F("- Lee Child, Tripwire");
      return;

    // Codegen for 03:57 for 1 quotations:
    case 237:
      // Only one option for 03:57:"
      quote = F("...I am in my old room, yes, in the dark, certainly, and it is cold, obviously, but what time is it? \"* Nearly four *, son.\" But I mean what time?");
      attribution = F("- Sometimes a Great Notion");
      return;

    // Codegen for 03:58 for 2 quotations:
    case 238:
      option = random(0, 2);
      // 2 options for 03:58
      switch (option) {
        case 0:
      quote = F("The clock atop the clubhouse reads * 3:58 *.");
      attribution = F("- Don Delillo, Underworld");
      return;
        case 1:
      quote = F("By peering through a crack in the bubbling sun on the window - the face of a clock on the opposite wall. The clock had stopped at * two minutes to four * early in the morning");
      attribution = F("- Wilson Harris, Heartland");
      return;
      } // end switch (option)

    // Codegen for 03:59 for 2 quotations:
    case 239:
      option = random(0, 2);
      // 2 options for 03:59
      switch (option) {
        case 0:
      quote = F("He looked at his watch: it was * nearly 4 *. He helped Delphine to her feet and led her down a passage to a rear door that gave on to the hospital garden.");
      attribution = F("- The Blue Afternoon");
      return;
        case 1:
      quote = F("...I am in my old room, yes, in the dark, certainly, and it is cold, obviously, but what time is it? \"* Nearly four *, son.\"");
      attribution = F("- Sometimes a Great Notion");
      return;
      } // end switch (option)

    // Codegen for 04:00 for 19 quotations:
    case 240:
      option = random(0, 19);
      // 19 options for 04:00
      switch (option) {
        case 0:
      quote = F("Suddenly, he started to cry. Curled up on the sofa he sobbed loudly. Michel looked at his watch; it was just after * 4am *. On the screen a wild cat had a rabbit in its mouth.");
      attribution = F("- Atomised");
      return;
        case 1:
      quote = F("It was exactly * 4 o'clock * as I made my way out of the building, and doors opened behind and before me, discharging salvos of vocal babble and the noise of chair-legs scraping...");
      attribution = F("- David Lodge, Deaf Sentence");
      return;
        case 2:
      quote = F("he asked if he could have tea available for them. Tommy agreed, and the chefs, grateful for a hot drink * at four * in the morning, lingered and bought.");
      attribution = F("- The Tea Rose");
      return;
        case 3:
      quote = F("The night before Albert Kessler arrived in Santa Teresa, * at four in the morning *, Sergio Gonzalez Rodriguez got a call from Azucena Esquivel Plata");
      attribution = F("- Roberto Bolano, 2666");
      return;
        case 4:
      quote = F("Djerzinski arrived punctually * at four o'clock *. Desplechin had asked to see him. The case was intriguing.");
      attribution = F("- Atomised");
      return;
        case 5:
      quote = F("Waking * at four * to soundless dark, I stare. In time the curtain-edges will grow light. Till then I see what's really always there: Unresting death");
      attribution = F("- Philip Larkin, Aubade");
      return;
        case 6:
      quote = F("What else can I answer, When the lights come on * at four * At the end of another year");
      attribution = F("- Philip Larkin");
      return;
        case 7:
      quote = F("I looked at the clock and it was (yes, you guessed it) * four am. *");
      attribution = F("- Marian Keyes, Watermelon");
      return;
        case 8:
      quote = F("...and that as you stare at the clock the hands will move relentlessly on to * four o'clock *, and you will enter the long dark teatime of the soul.");
      attribution = F("- Douglas Adams");
      return;
        case 9:
      quote = F("* Four o'clock * has just struck. Good! Arrangement, revision, reading from four to five. Short snooze of restoration for myself, from five to six.");
      attribution = F("- Walter Hartwright VII");
      return;
        case 10:
      quote = F("* Four o'clock *: wedge-shaped gardens lie Under a cavernous, a wind-picked sky.");
      attribution = F("- Philip Larkin, Sad Steps");
      return;
        case 11:
      quote = F("* Four o'clock *: when time in the city quivers on its axis - the day not yet spent, the wheels of evening just beginning to turn.");
      attribution = F("- The Act of Love");
      return;
        case 12:
      quote = F("I doubt whether anyone was commissioned to send the news along the actual telegraph, and yet Mrs. Proudie knew it before * four o'clock *.");
      attribution = F("- Anthony Trollope");
      return;
        case 13:
      quote = F("I only found out much later that those flowers were called * Four O'clock *, and were not magic at all. The magic was in the seed, waiting to be watered and cared for...");
      attribution = F("- Ghost Generations");
      return;
        case 14:
      quote = F("Look to the west. A clack. For me. -What time is that? asked Blazes Boylan. * Four? O'clock *. Lenehan, small eyes ahunger on her humming, bust ahumming...");
      attribution = F("- James Joyce, Ulysses");
      return;
        case 15:
      quote = F("Nothing happened, he said wanly. \"I waited, and about * four o'clock * she came to the window and stood there for a minute and then turned out the light.\"");
      attribution = F("- The Great Gatsby");
      return;
        case 16:
      quote = F("The Birds begun at * Four o'clock *- Their period for Dawn-");
      attribution = F("- Emily Dickinson");
      return;
        case 17:
      quote = F("...when they all sat down to table at * four o'clock *, about three hours after his arrival, he had secured his lady, engaged her mother's consent...");
      attribution = F("- Sense and Sensibility");
      return;
        case 18:
      quote = F("In the four thousand rooms of the Centre the four thousand electric clocks simultaneously * struck four *. Discarnate voices called from the trumpet mouths.");
      attribution = F("- Brave New World");
      return;
      } // end switch (option)

    // Codegen for 04:01 for 1 quotations:
    case 241:
      // Only one option for 04:01:"
      quote = F("Suddenly, he started to cry. Curled up on the sofa he sobbed loudly. Michel looked at his watch; it was * just after 4am *. On the screen a wild cat had a rabbit in its mouth.");
      attribution = F("- Atomised");
      return;

    // Codegen for 04:02 for 2 quotations:
    case 242:
      option = random(0, 2);
      // 2 options for 04:02
      switch (option) {
        case 0:
      quote = F("I walked up and down the row. No one gave me a second look. Finally I sat down next to a man. He paid no attention. My watch said * 4:02 *. Maybe he was late.");
      attribution = F("- The History of Love");
      return;
        case 1:
      quote = F("I'd just looked up at the clock, to make sure time wasn't getting away from me, when I heard the shot. It was * two minutes after four *. I didn't know what to do.");
      attribution = F("- Southern Ghost");
      return;
      } // end switch (option)

    // Codegen for 04:03 for 1 quotations:
    case 243:
      // Only one option for 04:03:"
      quote = F("It's * 4:03 a.m. * on a supremely cold January morning and I'm just getting home. I've been out dancing and I'm only half drunk but utterly exhausted.");
      attribution = F("- The Time Traveler's Wife");
      return;

    // Codegen for 04:04 for 1 quotations:
    case 244:
      // Only one option for 04:04:"
      quote = F("* Four minutes after four! * It's still very early and to get from here to there won't take me more than 15 minutes, even walking slowly.");
      attribution = F("- Angel Hill");
      return;

    // Codegen for 04:05 for 2 quotations:
    case 245:
      option = random(0, 2);
      // 2 options for 04:05
      switch (option) {
        case 0:
      quote = F("Leaves were being blown against my window. It was * 4.05am. * The moon had shifted in the sky, glaring through a clotted mass of clouds like a candled egg.");
      attribution = F("- We Were the Mulvaneys");
      return;
        case 1:
      quote = F("It was exactly * five minutes past four * as Mr. Robert Audley stepped out upon the platform at Shoreditch, and waited placidly...");
      attribution = F("- Lady Audley's Secret");
      return;
      } // end switch (option)

    // Codegen for 04:06 for 2 quotations:
    case 246:
      option = random(0, 2);
      // 2 options for 04:06
      switch (option) {
        case 0:
      quote = F("Dexter looked at Kate's note, then her face, then the clock. It was * 4.06am *, the night before they would go to the restaurant.");
      attribution = F("- Chris Pavone, The Expats");
      return;
        case 1:
      quote = F("At * six minutes after four *, Benny's Cadillac pulled up in front of Mr. Botelia's store, and Benny's mother stepped out of the car with Penelope...");
      attribution = F("- Follow Me: A Novel");
      return;
      } // end switch (option)

    // Codegen for 04:07 for 1 quotations:
    case 247:
      // Only one option for 04:07:"
      quote = F("* 4.07am. * Why am I standing? My shoulders feel cold and I'm shivering. I become aware that I'm standing in the middle of the room. I immediately look at the bedroom door.");
      attribution = F("- Guarding Hanna: A Novel");
      return;

    // Codegen for 04:08 for 2 quotations:
    case 248:
      option = random(0, 2);
      // 2 options for 04:08
      switch (option) {
        case 0:
      quote = F("It was at * 4:08 a.m. * beneath the cool metal of a jungle gym that all Andrew's dreams came true. He kissed his one true love and swore up and down that it would last forever...");
      attribution = F("- Seth O'Connell");
      return;
        case 1:
      quote = F("It was * eight minutes after four *. I still don't have a plan. Maybe the guys in the Nova, maybe they had a plan.");
      attribution = F("- The Monkey's Raincoat");
      return;
      } // end switch (option)

    // Codegen for 04:09 for 1 quotations:
    case 249:
      // Only one option for 04:09:"
      quote = F("I just wanted to know if there was any improvement. \"No, there isn't. It was nice of you to call.\" She hung up. It was * nine minutes after four *.");
      attribution = F("- Ira Levin, Rosemary's Baby");
      return;

    // Codegen for 04:10 for 0 quotations:
    case 250:
      // No quotes for 04:10
        quote = "";
        attribution = "";
      return;

    // Codegen for 04:11 for 1 quotations:
    case 251:
      // Only one option for 04:11:"
      quote = F("The next morning I awaken at exactly * eleven minutes after four *, having slept straight through my normal middle-of-the-night insomniac waking at three.");
      attribution = F("- The Stuff of Life");
      return;

    // Codegen for 04:12 for 3 quotations:
    case 252:
      option = random(0, 3);
      // 3 options for 04:12
      switch (option) {
        case 0:
      quote = F("Karen felt the bed move beneath Harry's weight. Lying on her side she opened her eyes to see digital numbers in the dark, * 4:12 * in pale green.");
      attribution = F("- Elmore Leonard, Get Shorty");
      return;
        case 1:
      quote = F("I signalled back, then shined the light downward to see how far the water had risen. I couldn't make out a thing. My watch read * four-twelve * in the morning. Not yet dawn.");
      attribution = F("- Haruki Murakami");
      return;
        case 2:
      quote = F("At precisely * twelve minutes after four * a body of cavalry rode into the square, four abreast, clearing a way for the funeral cortege.");
      attribution = F("- The Empty Mirror");
      return;
      } // end switch (option)

    // Codegen for 04:13 for 1 quotations:
    case 253:
      // Only one option for 04:13:"
      quote = F("...she opened her eyes to see digital numbers in the dark, 4:12 in pale green. Behind her Harry continued to move, settling in. She watched the numbers change to * 4:13 *.");
      attribution = F("- Elmore Leonard, Get Shorty");
      return;

    // Codegen for 04:14 for 1 quotations:
    case 254:
      // Only one option for 04:14:"
      quote = F("At * 4:14 a.m. *, the two men returned to the Jeep. After the passenger replaced the cans in the back of the Jeep, the driver backed out of the driveway and headed east.");
      attribution = F("- A Real Nightmare");
      return;

    // Codegen for 04:15 for 3 quotations:
    case 255:
      option = random(0, 3);
      // 3 options for 04:15
      switch (option) {
        case 0:
      quote = F("...like flat feet or diabetes: they're all in danger of ending up alone by their own stubborn choice. The ugly kitchen clock says * four-fifteen *.");
      attribution = F("- Pigs in Heaven");
      return;
        case 1:
      quote = F("I remember the dread with which I at * quarter past four */ Let go with a bang behind me our house front door");
      attribution = F("- False Security");
      return;
        case 2:
      quote = F("It is only a * quarter past four *, (shewing his watch) and you are not now in Bath. No theatre, no rooms to prepare for. Half an hour at Northanger must be enough.");
      attribution = F("- Northanger Abbey");
      return;
      } // end switch (option)

    // Codegen for 04:16 for 2 quotations:
    case 256:
      option = random(0, 2);
      // 2 options for 04:16
      switch (option) {
        case 0:
      quote = F("I stooped to pick up my watch from the floor. * Four-sixteen *. Another hour until dawn.");
      attribution = F("- Haruki Murakami");
      return;
        case 1:
      quote = F("They pulled into the visitor's carpark at * four sixteen * am. He knew it was four sixteen because the entrance to the maternity unit sported a digital clock beneath the signage.");
      attribution = F("- Maree Anderson");
      return;
      } // end switch (option)

    // Codegen for 04:17 for 2 quotations:
    case 257:
      option = random(0, 2);
      // 2 options for 04:17
      switch (option) {
        case 0:
      quote = F("He awoke at * 4.17am * in a sweat. He had been dreaming of Africa again, and then the dream had continued in the U.S. when he was a young man. But Inbata had been there, watching him.");
      attribution = F("- Douglas Phinney, The Vile");
      return;
        case 1:
      quote = F("...seeing only the police cars, knowing as she glanced down at her watch and saw that it was * seventeen minutes after four *, that for her time had stopped.");
      attribution = F("- Joy Fielding, Life Penalty");
      return;
      } // end switch (option)

    // Codegen for 04:18 for 1 quotations:
    case 258:
      // Only one option for 04:18:"
      quote = F("The ringing didn't stop. The telephone! The clock read * four-eighteen *. It was dark outside. Four-eighteen a.m. I got out of bed and picked up the receiver. \"Hello?\"");
      attribution = F("- Haruki Murakami");
      return;

    // Codegen for 04:19 for 0 quotations:
    case 259:
      // No quotes for 04:19
        quote = "";
        attribution = "";
      return;

    // Codegen for 04:20 for 1 quotations:
    case 260:
      // Only one option for 04:20:"
      quote = F("At * twenty minutes past four * - or, to put it another, blunter way, an hour and twenty minutes past what seemed to be all reasonable hope...");
      attribution = F("- J. D. Salinger");
      return;

    // Codegen for 04:21 for 0 quotations:
    case 261:
      // No quotes for 04:21
        quote = "";
        attribution = "";
      return;

    // Codegen for 04:22 for 1 quotations:
    case 262:
      // Only one option for 04:22:"
      quote = F("He hurt me to the point where I wanted to tell him something. My watch said * 4.22 * now. It had stopped. It was smashed.");
      attribution = F("- The Ipcress File");
      return;

    // Codegen for 04:23 for 2 quotations:
    case 263:
      option = random(0, 2);
      // 2 options for 04:23
      switch (option) {
        case 0:
      quote = F("Her chip pulsed the time. * 04:23 *:04. It had been a long day.");
      attribution = F("- William Gibson, Neuromancer");
      return;
        case 1:
      quote = F("* 4:23 *, Monday morning, Iceland Square. A number of people in the vicinity of Bjornsongatan are awakened by loud screams.");
      attribution = F("- Let The Right One In");
      return;
      } // end switch (option)

    // Codegen for 04:24 for 1 quotations:
    case 264:
      // Only one option for 04:24:"
      quote = F("Mike winked at Ashley and continued with the remaining greetings and hugs and handshakes. The time was * 4:24 *. Six hours to go. The minutes seemed to just melt away.");
      attribution = F("- Travis Thrasher, Teardrop");
      return;

    // Codegen for 04:25 for 2 quotations:
    case 265:
      option = random(0, 2);
      // 2 options for 04:25
      switch (option) {
        case 0:
      quote = F("As I dressed I glanced at my watch. It was no wonder that no one was stirring. It was * twenty-five minutes past four *.");
      attribution = F("- Sir Arthur Conan Doyle");
      return;
        case 1:
      quote = F("As I dressed I glanced at my watch. It was no wonder that no one was stirring. It was * twenty-five minutes past four *.");
      attribution = F("- Arthur Conan Doyle");
      return;
      } // end switch (option)

    // Codegen for 04:26 for 0 quotations:
    case 266:
      // No quotes for 04:26
        quote = "";
        attribution = "";
      return;

    // Codegen for 04:27 for 0 quotations:
    case 267:
      // No quotes for 04:27
        quote = "";
        attribution = "";
      return;

    // Codegen for 04:28 for 0 quotations:
    case 268:
      // No quotes for 04:28
        quote = "";
        attribution = "";
      return;

    // Codegen for 04:29 for 0 quotations:
    case 269:
      // No quotes for 04:29
        quote = "";
        attribution = "";
      return;

    // Codegen for 04:30 for 3 quotations:
    case 270:
      option = random(0, 3);
      // 3 options for 04:30
      switch (option) {
        case 0:
      quote = F("Hester Thrale undulates in in a false fox jacket at 2330 as usual even though she has to be up at like 0* 430 * for the breakfast shift at the Provident Nursing Home...");
      attribution = F("- Infinite Jest");
      return;
        case 1:
      quote = F("Chloe had not given it a chance, I argued with myself, knowing the hopelessness of these inner courts announcing hollow verdicts at * four thirty * in the morning.");
      attribution = F("- Essays on Love");
      return;
        case 2:
      quote = F("I leave the office at * four thirty *, head up to Xclusive where I work out on free weights for an hour, then taxi across the park to Gio's in the Pierre Room for a facial...");
      attribution = F("- American Psycho");
      return;
      } // end switch (option)

    // Codegen for 04:31 for 1 quotations:
    case 271:
      // Only one option for 04:31:"
      quote = F("An earthquake hit Los Angeles at * 4:31 * this morning and the images began arriving via CNN right away.");
      attribution = F("- Microserfs");
      return;

    // Codegen for 04:32 for 1 quotations:
    case 272:
      // Only one option for 04:32:"
      quote = F("On his first day of kindergarten, Peter Houghton woke up at * 4:32 a.m. * He padded into his parents' room and asked if it was time yet to take the school bus.");
      attribution = F("- Nineteen Minutes");
      return;

    // Codegen for 04:33 for 0 quotations:
    case 273:
      // No quotes for 04:33
        quote = "";
        attribution = "";
      return;

    // Codegen for 04:34 for 0 quotations:
    case 274:
      // No quotes for 04:34
        quote = "";
        attribution = "";
      return;

    // Codegen for 04:35 for 1 quotations:
    case 275:
      // Only one option for 04:35:"
      quote = F("No manner of exhaustion can keep a child asleep much later than six a.m. on Christmas Day. Colby awoke at * 4:35 *.");
      attribution = F("- Dreams and Shadows");
      return;

    // Codegen for 04:36 for 1 quotations:
    case 276:
      // Only one option for 04:36:"
      quote = F("At * 4:36 * that morning, alone in my hotel room, it had been a much better scene. Spencer had blanched, confounded by the inescapable logic of my accusation.");
      attribution = F("- The Brass Go-Between");
      return;

    // Codegen for 04:37 for 0 quotations:
    case 277:
      // No quotes for 04:37
        quote = "";
        attribution = "";
      return;

    // Codegen for 04:38 for 1 quotations:
    case 278:
      // Only one option for 04:38:"
      quote = F("At * 4.38 a.m. * as the sun is coming up over Gorley Woods, I hear a strange rustling in the grass beside me. I peer closely but can see nothing.");
      attribution = F("- Jonathan Barrow, The Queue");
      return;

    // Codegen for 04:39 for 0 quotations:
    case 279:
      // No quotes for 04:39
        quote = "";
        attribution = "";
      return;

    // Codegen for 04:40 for 1 quotations:
    case 280:
      // Only one option for 04:40:"
      quote = F("I settled into a daily routine. Wake up at * 4.40am *, shower, get on the train north by ten after five.");
      attribution = F("- Tina Fey, Bossypants");
      return;

    // Codegen for 04:41 for 1 quotations:
    case 281:
      // Only one option for 04:41:"
      quote = F("At * 4:41 * Crane's voice crackled through the walkie-talkie as if he'd read their thoughts of mutiny. \"Everyone into the elevator. Now!\"");
      attribution = F("- Damaged Goods: A Novel");
      return;

    // Codegen for 04:42 for 0 quotations:
    case 282:
      // No quotes for 04:42
        quote = "";
        attribution = "";
      return;

    // Codegen for 04:43 for 1 quotations:
    case 283:
      // Only one option for 04:43:"
      quote = F("The time is * four forty-three * in the mornin an it's almost light oot there.");
      attribution = F("- Suhayl Saadi, Pyschoraag");
      return;

    // Codegen for 04:44 for 0 quotations:
    case 284:
      // No quotes for 04:44
        quote = "";
        attribution = "";
      return;

    // Codegen for 04:45 for 1 quotations:
    case 285:
      // Only one option for 04:45:"
      quote = F("Or maybe it'll be me. Daybreak will reveal that one of us has been left alone. He checks the clock on the table next to the bed. The hands glow and register * 4:45 a.m. *");
      attribution = F("- Faceless Killers");
      return;

    // Codegen for 04:46 for 2 quotations:
    case 286:
      option = random(0, 2);
      // 2 options for 04:46
      switch (option) {
        case 0:
      quote = F("At * 4:46 * an obese, middle-aged man shuffled in. Wearing a starched guayabera and dark green pants, Urena asked for a book on confectionery...");
      attribution = F("- The Havana World Series");
      return;
        case 1:
      quote = F("The phone rang again at * four-forty-six *. \"Hello,\" I said. \"Hello,\" came a woman's voice. \"Sorry about the time before. There's a disturbance in the sound field.\"");
      attribution = F("- Haruki Murakami");
      return;
      } // end switch (option)

    // Codegen for 04:47 for 1 quotations:
    case 287:
      // Only one option for 04:47:"
      quote = F("But maybe it was more than that, maybe Affenlight had erred badly somehow, because here it was 4:49 by his watch, * 4:47 * by the wall clock, and Owen had not yet come.");
      attribution = F("- The Art of Fielding");
      return;

    // Codegen for 04:48 for 1 quotations:
    case 288:
      // Only one option for 04:48:"
      quote = F("At * 4:48 * the happy hour when clarity visits warm darkness which soaks my eyes I know no sin");
      attribution = F("- Sarah Kane, 4:48 Psychosis");
      return;

    // Codegen for 04:49 for 0 quotations:
    case 289:
      // No quotes for 04:49
        quote = "";
        attribution = "";
      return;

    // Codegen for 04:50 for 2 quotations:
    case 290:
      option = random(0, 2);
      // 2 options for 04:50
      switch (option) {
        case 0:
      quote = F("They had all frozen at the same time, on a snowy night, seven years before, and after that it was always * ten minutes to five * in the castle.");
      attribution = F("- The 13 Clocks");
      return;
        case 1:
      quote = F("When the clock said * ten minutes to five *, she began to listen, and a few moments later, punctually as always, she heard the tires on the gravel outside...");
      attribution = F("- Lamb to the Slaughter");
      return;
      } // end switch (option)

    // Codegen for 04:51 for 1 quotations:
    case 291:
      // Only one option for 04:51:"
      quote = F("* Nine minutes to five. * If this wasn't some new ordeal, intended to fray her nerves to shreds, if this important person really did exist...");
      attribution = F("- Compulsory Happiness");
      return;

    // Codegen for 04:52 for 1 quotations:
    case 292:
      // Only one option for 04:52:"
      quote = F("The five-footed beasts make a point of arriving at the latest at eight or seven minutes to: ritual again. At * eight minutes to five *, there they were.");
      attribution = F("- Chaos and Night");
      return;

    // Codegen for 04:53 for 1 quotations:
    case 293:
      // Only one option for 04:53:"
      quote = F("It was so quiet in the post office that Trinidad could hear the soft tick of the clock's second hand every time it moved. It was now * seven minutes before five *.");
      attribution = F("- Rita Leganski");
      return;

    // Codegen for 04:54 for 2 quotations:
    case 294:
      option = random(0, 2);
      // 2 options for 04:54
      switch (option) {
        case 0:
      quote = F("At * six minutes before five o'clock *, Daisy Robinson, about to reach her own apartment door, paused to look and to listen. Something was out of order.");
      attribution = F("- Charlotte Armstrong");
      return;
        case 1:
      quote = F("* Six minutes to five *. Six minutes to go. Suddenly I felt quite clearheaded. There was an unexpected light in the cell; the boundaries were drawn, the roles well defined.");
      attribution = F("- Elie Wiesel, Dawn: A Novel");
      return;
      } // end switch (option)

    // Codegen for 04:55 for 1 quotations:
    case 295:
      // Only one option for 04:55:"
      quote = F("* 4:55 * - Mank holding phone. Turns to Caddell - 'Who is this?' Caddell: 'Jim.' (shrugs) 'I think he's our man in Cincinnati.'");
      attribution = F("- Hunter S. Thompson");
      return;

    // Codegen for 04:56 for 0 quotations:
    case 296:
      // No quotes for 04:56
        quote = "";
        attribution = "";
      return;

    // Codegen for 04:57 for 2 quotations:
    case 297:
      option = random(0, 2);
      // 2 options for 04:57
      switch (option) {
        case 0:
      quote = F("The second said the same thing a * few minutes before five *, and mentioned eternity... I'm sure I'll meet you in the other world.");
      attribution = F("- Peter Robb");
      return;
        case 1:
      quote = F("Then at * three minutes to five * - Pendel had somehow never doubted that Osnard would be punctual - along comes a brown Ford hatchback with an Avis sticker on the back window...");
      attribution = F("- The Tailor of Panama");
      return;
      } // end switch (option)

    // Codegen for 04:58 for 1 quotations:
    case 298:
      // Only one option for 04:58:"
      quote = F("He wants to look death in the face. * Two minutes to five *. I took a handkerchief out of my pocket, but John Dawson ordered me to put it back.");
      attribution = F("- Elie Wiesel, Dawn: A Novel");
      return;

    // Codegen for 04:59 for 1 quotations:
    case 299:
      // Only one option for 04:59:"
      quote = F("The whole place smells like death no matter what the fuck you do. Gately gets to the shelter at 0* 4:59 * and just shuts his head off as if his head had a control switch.");
      attribution = F("- Infinite Jest");
      return;

    // Codegen for 05:00 for 10 quotations:
    case 300:
      option = random(0, 10);
      // 10 options for 05:00
      switch (option) {
        case 0:
      quote = F("...set against a hillside 4 miles from the village and a mile and a half from any other dwelling, that Wilbur Whately was born at * 5 a.m. * on Sunday, 2 February, 1913.");
      attribution = F("- The Dunwich Horror");
      return;
        case 1:
      quote = F("There are worse things than not being able to sleep for thinking about them. It is * 5 a.m. * All the worse things come stalking in and stand icily about the bed...");
      attribution = F("- Fleur Adcock, Things");
      return;
        case 2:
      quote = F("About * five *, the Abbot, a young Manchester terrier, began chirruping. He stood on the body of his owner, Flora, with his forepaws on the sill of the balcony, stared...");
      attribution = F("- The People With the Dogs");
      return;
        case 3:
      quote = F("At * five o'clock * adieux were waved, and the ponderous liner edged away from the long pier, slowly turned its nose seaward, discarded its tug, and headed for the widening water...");
      attribution = F("- The Horror at Red Hook");
      return;
        case 4:
      quote = F("...but the noble ambition of matrimony? What sends them trooping to watering-places? What keeps them dancing till * five o'clock * in the morning...");
      attribution = F("- Vanity Fair");
      return;
        case 5:
      quote = F("* Five o'clock * had hardly struck on the morning of the 19th of January, when Bessie brought a candle into my closet and found me already up and nearly dressed.");
      attribution = F("- Charlotte Bronte, Jane Eyre");
      return;
        case 6:
      quote = F("From * five o'clock * to eight is on certain occasions a little eternity; but on such an occasion as this the interval could be only an eternity of pleasure.");
      attribution = F("- The Portrait of a Lady");
      return;
        case 7:
      quote = F("The day came slow, till * five o'clock *. Then sprang before the hills. Like hindered rubies, or the light. A sudden musket spills");
      attribution = F("- Emily Dickinson");
      return;
        case 8:
      quote = F("Until * five o'clock * there was no sign of life from the room. Then he rang for his servant and ordered a cold bath.");
      attribution = F("- Sandor Marai, Embers");
      return;
        case 9:
      quote = F("One! said Hark. \"Two!\" cried Zorn of Zorna. \"Three!\" the Duke's voice almost whispered. \"Four!\" sighed Saralinda. \"* Five *!\" the Golux crowed, and pointed at the table.");
      attribution = F("- The 13 Clocks");
      return;
      } // end switch (option)

    // Codegen for 05:01 for 3 quotations:
    case 301:
      option = random(0, 3);
      // 3 options for 05:01
      switch (option) {
        case 0:
      quote = F("Just * after five o'clock * on this chill September morning, the fishmonger's cart, containing Kirsten and Emilia and such possessions as they have been able to assemble...");
      attribution = F("- Music and Silence");
      return;
        case 1:
      quote = F("* One minute after five. * The seated guests were told that the ceremony would begin shortly. A little more patience was required.");
      attribution = F("- Tip 'T.I.' Harris");
      return;
        case 2:
      quote = F("Oh yes. His clocks were set at * one minute past five *, four minutes past five and seven minutes past five. That was the combination number of a safe, 515457.");
      attribution = F("- Agatha Christie, The Clocks");
      return;
      } // end switch (option)

    // Codegen for 05:02 for 1 quotations:
    case 302:
      // Only one option for 05:02:"
      quote = F("It was * 5:02 a.m. *, December 14. In another fifty-eight minutes he would set sail for America. He did not want to leave his bride; he did not want to go.");
      attribution = F("- Brenda Joyce, The Prize");
      return;

    // Codegen for 05:03 for 2 quotations:
    case 303:
      option = random(0, 2);
      // 2 options for 05:03
      switch (option) {
        case 0:
      quote = F("It was * 5:03 a.m. * It didn't matter. She wasn't going to get back to sleep. She threw off her covers and, swearing at herself, Caleb and Mr. Griffin, she headed into the shower.");
      attribution = F("- Unhallowed ground");
      return;
        case 1:
      quote = F("I sure will. Thanks. Bye, he said, waving to Elliot, who returned the goodbye. It was * 5:03 * when Elliot rested the handset in its cradle.");
      attribution = F("- Comrades in Miami: A Novel");
      return;
      } // end switch (option)

    // Codegen for 05:04 for 2 quotations:
    case 304:
      option = random(0, 2);
      // 2 options for 05:04
      switch (option) {
        case 0:
      quote = F("* 5.04 a.m. * on the substandard clock radio. Because why do people always say the day starts now? Really it starts in the middle of the night at a fraction of a second past midnight.");
      attribution = F("- Ali Smith, The Accidental");
      return;
        case 1:
      quote = F("Oh yes. His clocks were set at one minute past five, * four minutes past five * and seven minutes past five. That was the combination number of a safe, 515457.");
      attribution = F("- Agatha Christie, The clocks");
      return;
      } // end switch (option)

    // Codegen for 05:05 for 1 quotations:
    case 305:
      // Only one option for 05:05:"
      quote = F("The baby, a boy, is born at * five past five * in the morning.");
      attribution = F("- Jhumpa Lahiri, The Namesake");
      return;

    // Codegen for 05:06 for 1 quotations:
    case 306:
      // Only one option for 05:06:"
      quote = F("* 5:06 a.m. * I wake up strangely energized, my stomach growling. Upstairs, the overstocked fridge offers me its bounty of sympathy food.");
      attribution = F("- This is Where I Leave you");
      return;

    // Codegen for 05:07 for 1 quotations:
    case 307:
      // Only one option for 05:07:"
      quote = F("Oh yes. His clocks were set at one minute past five, four minutes past five and * seven minutes past five *. That was the combination number of a safe, 515457.");
      attribution = F("- Agatha Christie, The clocks");
      return;

    // Codegen for 05:08 for 1 quotations:
    case 308:
      // Only one option for 05:08:"
      quote = F("Ambrose and I will marry at Fort McHenry at * 5:08 * EDST this coming Saturday, Rosh Hashanah!");
      attribution = F("- John Barth, Letters");
      return;

    // Codegen for 05:09 for 1 quotations:
    case 309:
      // Only one option for 05:09:"
      quote = F("...when inquiry of the station attendants educed the information that the two earliest trains to be obtained were the * 5:09 * to Dunkerque and the 5:37 for Ostend.");
      attribution = F("- The Black Bag");
      return;

    // Codegen for 05:10 for 2 quotations:
    case 310:
      option = random(0, 2);
      // 2 options for 05:10
      switch (option) {
        case 0:
      quote = F("I settled into a daily routine. Wake up at 4:40am, shower, get on the train north by * ten after five *.");
      attribution = F("- Tina Fey, Bossypants");
      return;
        case 1:
      quote = F("If this is not enough to kill me, I have more left in the bottle. * Ten minutes past five *. \"You have just gone, after giving me my composing draught.\"");
      attribution = F("- The Law and the Lady");
      return;
      } // end switch (option)

    // Codegen for 05:11 for 1 quotations:
    case 311:
      // Only one option for 05:11:"
      quote = F("Today was Tuesday, the fifteenth of August; the sun had risen at * eleven minutes past five * this morning and would set at two minutes before seven this evening.");
      attribution = F("- The Hot Rock");
      return;

    // Codegen for 05:12 for 2 quotations:
    case 312:
      option = random(0, 2);
      // 2 options for 05:12
      switch (option) {
        case 0:
      quote = F("At * twelve minutes and six seconds past five o'clock * on the morning of April 18th, 1906, the San francisco peninsula began to shiver in the grip of an earthquake...");
      attribution = F("- Slummer's Paradise");
      return;
        case 1:
      quote = F("Well, here we are, said Colonel Julyan, \"and it's exactly * twelve minutes past five *. We shall catch them in the middle of their tea. Better wait for a bit\"");
      attribution = F("- Daphne du Maurier, Rebecca");
      return;
      } // end switch (option)

    // Codegen for 05:13 for 1 quotations:
    case 313:
      // Only one option for 05:13:"
      quote = F("...like a welcome sign beckoning me into the depths of hell's crimson-colored cavities. * 5:13 am. * To describe this Monday as a blue Monday was an understatement.");
      attribution = F("- Nakia D Johnson, Uptempo");
      return;

    // Codegen for 05:14 for 1 quotations:
    case 314:
      // Only one option for 05:14:"
      quote = F("The time was * 5.14am *, a very strange time indeed for the sheriff to have seen what he claimed he saw as he made his early-morning rounds...");
      attribution = F("- Thomas H Cook, Into the Web");
      return;

    // Codegen for 05:15 for 2 quotations:
    case 315:
      option = random(0, 2);
      // 2 options for 05:15
      switch (option) {
        case 0:
      quote = F("By the first week of May, Ralph was waking up to birdsong at * 5:15 a.m. * He tried earplugs for a few nights, although he doubted from the outset that they would work.");
      attribution = F("- Stephen King, Insomnia");
      return;
        case 1:
      quote = F("Weird conversation with Brown, a tired & confused old man who's been jerked out of bed at * 5:15 *.");
      attribution = F("- Hunter S Thompson");
      return;
      } // end switch (option)

    // Codegen for 05:16 for 2 quotations:
    case 316:
      option = random(0, 2);
      // 2 options for 05:16
      switch (option) {
        case 0:
      quote = F("* 5:16 * - Mank on phone to Secretary of State Brown: 'Mr Brown, we're profoundly disturbed about this situation in the 21st. We can't get a single result out of there.");
      attribution = F("- Hunter S Thompson");
      return;
        case 1:
      quote = F("It is completely light outside now; you can see for miles. Except there is nothing to see here; trees and fields and that kind of thing. * 5:16 a.m * on the substandard clock radio.");
      attribution = F("- Ali Smith, The Accidental");
      return;
      } // end switch (option)

    // Codegen for 05:17 for 0 quotations:
    case 317:
      // No quotes for 05:17
        quote = "";
        attribution = "";
      return;

    // Codegen for 05:18 for 1 quotations:
    case 318:
      // Only one option for 05:18:"
      quote = F("I remembered soon after that, at this moment, I happened to look at the clock. It was * eighteen minutes past five *.");
      attribution = F("- Maurice LeBlanc");
      return;

    // Codegen for 05:19 for 0 quotations:
    case 319:
      // No quotes for 05:19
        quote = "";
        attribution = "";
      return;

    // Codegen for 05:20 for 1 quotations:
    case 320:
      // Only one option for 05:20:"
      quote = F("He saw on the floor his cigarette reduced to a long thin cylinder of ash: it had smoked itself. It was * five twenty *, dawn was breaking behind the shed of empty barrels...");
      attribution = F("- The Periodic Table");
      return;

    // Codegen for 05:21 for 1 quotations:
    case 321:
      // Only one option for 05:21:"
      quote = F("The Meeting was listed as starting at 1730, and it was only * around 1720 *, and Hal thought the voices might signify some sort of pre-Meeting orientation...");
      attribution = F("- Infinite Jest");
      return;

    // Codegen for 05:22 for 0 quotations:
    case 322:
      // No quotes for 05:22
        quote = "";
        attribution = "";
      return;

    // Codegen for 05:23 for 2 quotations:
    case 323:
      option = random(0, 2);
      // 2 options for 05:23
      switch (option) {
        case 0:
      quote = F("If I could count precisely to sixty between two passing orange minutes on her digital clock, starting at * 5.23am * and ending exactly as it melted into 5:24...");
      attribution = F("- The Tragedy of Arthur");
      return;
        case 1:
      quote = F("What for? \"Just for a chat. Do you know the Royal batsman, near Central Station? We could meet tomorrow at five?\" \"* Five twenty-three *,\" I said.");
      attribution = F("- A Fraction of the Whole");
      return;
      } // end switch (option)

    // Codegen for 05:24 for 1 quotations:
    case 324:
      // Only one option for 05:24:"
      quote = F("If I could count precisely to sixty between two passing orange minutes on her digital clock, starting at 523am. and ending exactly as it melted into * 5:24 *...");
      attribution = F("- The Tragedy of Arthur");
      return;

    // Codegen for 05:25 for 2 quotations:
    case 325:
      option = random(0, 2);
      // 2 options for 05:25
      switch (option) {
        case 0:
      quote = F("George's train home from New Street leaves at * 5.25 *. On the return journey, there are rarely schoolboys.");
      attribution = F("- Arthur and George");
      return;
        case 1:
      quote = F("It was * five-twenty-five * when I pulled up in front of the library. Still early for our date, so I got out of the car and took a stroll down the misty streets.");
      attribution = F("- Haruki Murakami");
      return;
      } // end switch (option)

    // Codegen for 05:26 for 1 quotations:
    case 326:
      // Only one option for 05:26:"
      quote = F("There isn't a sound from them. Not a moan. Poor them. Poor me. I look at the clock. * 05:26 *.");
      attribution = F("- 101 Reykjavik");
      return;

    // Codegen for 05:27 for 0 quotations:
    case 327:
      // No quotes for 05:27
        quote = "";
        attribution = "";
      return;

    // Codegen for 05:28 for 1 quotations:
    case 328:
      // Only one option for 05:28:"
      quote = F("I pulled into the Aoyama supermarket parking garage at * five-twenty-eight *. The sky to the east was getting light. I entered the store carrying my bag.");
      attribution = F("- Haruki Murakami");
      return;

    // Codegen for 05:29 for 0 quotations:
    case 329:
      // No quotes for 05:29
        quote = "";
        attribution = "";
      return;

    // Codegen for 05:30 for 4 quotations:
    case 330:
      option = random(0, 4);
      // 4 options for 05:30
      switch (option) {
        case 0:
      quote = F("On the day they were going to kill him, Santiago Nasar got up at * five-thirty * in the morning to wait for the boat the bishop was coming on.");
      attribution = F("- Gabriel Garcia Marquez");
      return;
        case 1:
      quote = F("...he has not had time, or more to the point, inclination, to notice aberrant behaviour. For instance, it is * half-past five * in the summer morning.");
      attribution = F("- An Insular Possession");
      return;
        case 2:
      quote = F("It was by this time * half-past five *, and the sun was on the point of rising; but I found the kitchen still dark and silent.");
      attribution = F("- Charlotte Bronte, Jane Eyre");
      return;
        case 3:
      quote = F("It was only after driving nearly five miles that he had sufficiently recovered himself to look at his watch, and realise that it was * half-past five *, and he was late.");
      attribution = F("- Leo Tolstoy, Anna Karenina");
      return;
      } // end switch (option)

    // Codegen for 05:31 for 1 quotations:
    case 331:
      // Only one option for 05:31:"
      quote = F("* 5:31 * - Mank on phone to lawyer: 'Jesus, I think we gotta go in there and get those ballots! Impound 'em! Every damn one!'");
      attribution = F("- Hunter S Thompson");
      return;

    // Codegen for 05:32 for 0 quotations:
    case 332:
      // No quotes for 05:32
        quote = "";
        attribution = "";
      return;

    // Codegen for 05:33 for 0 quotations:
    case 333:
      // No quotes for 05:33
        quote = "";
        attribution = "";
      return;

    // Codegen for 05:34 for 1 quotations:
    case 334:
      // Only one option for 05:34:"
      quote = F("I asked \"What time is sunrise?\" A second's silence while the crestfallen Bush absorbed his rebuke, and then another voice answered: \"* Five-thirty-four *, sir.\"");
      attribution = F("- The Commodore");
      return;

    // Codegen for 05:35 for 2 quotations:
    case 335:
      option = random(0, 2);
      // 2 options for 05:35
      switch (option) {
        case 0:
      quote = F("* 5:35 * - All phones ringing now, the swing shift has shot the gap - now the others are waking up.");
      attribution = F("- Hunter S Thompson");
      return;
        case 1:
      quote = F("I squinted at the clock. 'It says * twenty-five before six *,' I said and rolled away from him.");
      attribution = F("- the dice man");
      return;
      } // end switch (option)

    // Codegen for 05:36 for 0 quotations:
    case 336:
      // No quotes for 05:36
        quote = "";
        attribution = "";
      return;

    // Codegen for 05:37 for 2 quotations:
    case 337:
      option = random(0, 2);
      // 2 options for 05:37
      switch (option) {
        case 0:
      quote = F("Look, Lucille, said Joe when Lucille strolled into the office at * 5:37 *. \"I don't know what you said to this gal...\"");
      attribution = F("- Lightning Rods");
      return;
        case 1:
      quote = F("Richard glanced at the clock on the microwave - * 5:37 * - almost twelve hours, almost one half-day since he'd dialed 911.");
      attribution = F("- AM Homes");
      return;
      } // end switch (option)

    // Codegen for 05:38 for 1 quotations:
    case 338:
      // Only one option for 05:38:"
      quote = F("It was very rare for the quantum computer and not Sol to wake him up. \"What's going on? What time is it?\" \"Good morning, Johnny,\" said the ship. \"It is * 5.38 a.m. *\"");
      attribution = F("- Keith Mansfield");
      return;

    // Codegen for 05:39 for 0 quotations:
    case 339:
      // No quotes for 05:39
        quote = "";
        attribution = "";
      return;

    // Codegen for 05:40 for 3 quotations:
    case 340:
      option = random(0, 3);
      // 3 options for 05:40
      switch (option) {
        case 0:
      quote = F("Hey, young man, what time is it? 'What?' I said, is it 5:30 yet? 'Er, * 5:40 *.' Heavens, they'll be starving. But then that's a good thing. Let them.'");
      attribution = F("- Zhu Wen, I Love Dollars");
      return;
        case 1:
      quote = F("It's * five-forty * now. The party's at six. By about ten past, the eleventh floor should be clearing. Arnold is a very popular partner...");
      attribution = F("- The Undomestic Goddess");
      return;
        case 2:
      quote = F("* Twenty minutes to six *. 'Rob's boys were already on the platform, barrows ready.");
      attribution = F("- Bruce Robinson");
      return;
      } // end switch (option)

    // Codegen for 05:41 for 0 quotations:
    case 341:
      // No quotes for 05:41
        quote = "";
        attribution = "";
      return;

    // Codegen for 05:42 for 0 quotations:
    case 342:
      // No quotes for 05:42
        quote = "";
        attribution = "";
      return;

    // Codegen for 05:43 for 1 quotations:
    case 343:
      // Only one option for 05:43:"
      quote = F("* 5.43 * - Mank on phone to 'Mary' in Washington; 'It now appears quite clear that we'll lead the state - without the 21st.'");
      attribution = F("- Hunter S. Thompson");
      return;

    // Codegen for 05:44 for 0 quotations:
    case 344:
      // No quotes for 05:44
        quote = "";
        attribution = "";
      return;

    // Codegen for 05:45 for 2 quotations:
    case 345:
      option = random(0, 2);
      // 2 options for 05:45
      switch (option) {
        case 0:
      quote = F("At * 5:45 * a power-transformer on a pole beside the abandoned Tracker Brothers' Truck Depot exploded in a flash of purple light, spraying twisted chunks of metal...");
      attribution = F("- Stephen King, IT");
      return;
        case 1:
      quote = F("Janice is not waiting for him in the lounge or beside the pool when at last * around 5.45 * they come home from playing the par-5 eighteenth.");
      attribution = F("- John Updike, Rabbit Is Rich");
      return;
      } // end switch (option)

    // Codegen for 05:46 for 2 quotations:
    case 346:
      option = random(0, 2);
      // 2 options for 05:46
      switch (option) {
        case 0:
      quote = F("He wrote a legal-sounding phrase to the effect that the sentence had been carried out at * 5.46am *, adding, 'without a snag'.");
      attribution = F("- A Whispered Name");
      return;
        case 1:
      quote = F("...those of the carriage clock on the bookshelf to * fourteen minutes to six *. In other words, it was exactly eight; and Mrs. Hignett acknowledged the fact...");
      attribution = F("- Three Men and a Maid");
      return;
      } // end switch (option)

    // Codegen for 05:47 for 0 quotations:
    case 347:
      // No quotes for 05:47
        quote = "";
        attribution = "";
      return;

    // Codegen for 05:48 for 0 quotations:
    case 348:
      // No quotes for 05:48
        quote = "";
        attribution = "";
      return;

    // Codegen for 05:49 for 0 quotations:
    case 349:
      // No quotes for 05:49
        quote = "";
        attribution = "";
      return;

    // Codegen for 05:50 for 0 quotations:
    case 350:
      // No quotes for 05:50
        quote = "";
        attribution = "";
      return;

    // Codegen for 05:51 for 0 quotations:
    case 351:
      // No quotes for 05:51
        quote = "";
        attribution = "";
      return;

    // Codegen for 05:52 for 1 quotations:
    case 352:
      // Only one option for 05:52:"
      quote = F("At * 5.52am * paramedics from the St. Petersburg Fire Department and SunStar Medic One ambulance service responded to a medical emergency call...");
      attribution = F("- Silent Witness");
      return;

    // Codegen for 05:53 for 0 quotations:
    case 353:
      // No quotes for 05:53
        quote = "";
        attribution = "";
      return;

    // Codegen for 05:54 for 0 quotations:
    case 354:
      // No quotes for 05:54
        quote = "";
        attribution = "";
      return;

    // Codegen for 05:55 for 2 quotations:
    case 355:
      option = random(0, 2);
      // 2 options for 05:55
      switch (option) {
        case 0:
      quote = F("It was * 5.55am * and raining hard when I pedalled up to the bike stand just outside the forecourt of the station and dashed inside.");
      attribution = F("- The Lost Luggage Porter");
      return;
        case 1:
      quote = F("...as he stood outside and put the shutters up with this own cold hands in despair of further trade. It was * five minutes to six *.");
      attribution = F("- The Deferred Appointment");
      return;
      } // end switch (option)

    // Codegen for 05:56 for 0 quotations:
    case 356:
      // No quotes for 05:56
        quote = "";
        attribution = "";
      return;

    // Codegen for 05:57 for 0 quotations:
    case 357:
      // No quotes for 05:57
        quote = "";
        attribution = "";
      return;

    // Codegen for 05:58 for 2 quotations:
    case 358:
      option = random(0, 2);
      // 2 options for 05:58
      switch (option) {
        case 0:
      quote = F("Annika Giannini woke with a start. She saw that it was * 5.58 a.m. *");
      attribution = F("- Stieg Larsson");
      return;
        case 1:
      quote = F("It was * nearly six o'clock * in the evening, and the absurd bell in the six-foot tin steeple of the church went clank-clank, clank- clank! as old Mattu pulled the rope within.'");
      attribution = F("- George Orwell, Burmese Days");
      return;
      } // end switch (option)

    // Codegen for 05:59 for 0 quotations:
    case 359:
      // No quotes for 05:59
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:00 for 12 quotations:
    case 360:
      option = random(0, 12);
      // 12 options for 06:00
      switch (option) {
        case 0:
      quote = F("Rise from bed ............... . * 6.00 A.M. *");
      attribution = F("- The Great Gatsby");
      return;
        case 1:
      quote = F("...whose rays streamed through the narrow window near my crib. I was to leave Gateshead that day by a coach which passed the lodge gates * at six a.m. *");
      attribution = F("- Charlotte Bronte, Jane Eyre");
      return;
        case 2:
      quote = F("But every morning, even if there's been a nighttime session and he has only slept two hours, he gets up * at six * and reads his paper while he drinks a strong cup of coffee.");
      attribution = F("- Muriel Barbery");
      return;
        case 3:
      quote = F("The ball went on for a long time, until * six in the morning *; all were exhausted and wishing they had been in bed for at least three hours");
      attribution = F("- The Leopard");
      return;
        case 4:
      quote = F("Lying awake in my attic room, I hear a clock strike * six * downstairs. It was fairly light and people were beginning to walk up and down the stairs...");
      attribution = F("- Knut Hamsun, Hunger");
      return;
        case 5:
      quote = F("I ask, and telling him so that he knows, \"My mother likes 'peace and quiet' to sleep late on Saturday mornings.\" \"She does, does she? It's * six o'clock *. I couldn't sleep.\"");
      attribution = F("- Patsy Hickman, The Saints");
      return;
        case 6:
      quote = F("Leon waited all day for * six o'clock * to arrive; when he got to the inn, he found no one there but Monsieur Binet, already at the table.");
      attribution = F("- Madame Bovary");
      return;
        case 7:
      quote = F("Oh oh oh. * Six o'clock * and the master not home yet.");
      attribution = F("- The skin of our teeth");
      return;
        case 8:
      quote = F("On the 15th of September 1840, about * six o'clock * in the morning, the Ville-de-Montereau, ready to depart, pouring out great whirls of smoke by the quai Saint-Bernard.");
      attribution = F("- L'Education sentimentale");
      return;
        case 9:
      quote = F("The newspaper snaked through the door and there was suddenly a * six o'clock * feeling in the house");
      attribution = F("- Muriel Spark");
      return;
        case 10:
      quote = F("The winter evening settles down With smell of steaks in passageways. * Six o'clock *.");
      attribution = F("- T S Eliot, Preludes");
      return;
        case 11:
      quote = F("When the bells of Calvary Church struck * six *, she saw Mr and Mrs Biggs hurrying down the front stoop, rushing off to the shops before they closed.");
      attribution = F("- Jed Rubenfeld");
      return;
      } // end switch (option)

    // Codegen for 06:01 for 0 quotations:
    case 361:
      // No quotes for 06:01
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:02 for 1 quotations:
    case 362:
      // Only one option for 06:02:"
      quote = F("This train does not stop at Birchills, for reasons George has never been able to ascertain. Then it is Bloxwich * 6.02 *, Wyrley & Churchbridge 6.09.");
      attribution = F("- Arthur and George");
      return;

    // Codegen for 06:03 for 0 quotations:
    case 363:
      // No quotes for 06:03
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:04 for 1 quotations:
    case 364:
      // Only one option for 06:04:"
      quote = F("...we will celebrate it with thanksgivings, all the years of our life. \"We will, we will, Alonzo!\" \"* Four minutes after six *, in the evening, my Rosannah...\"");
      attribution = F("- Mark Twain");
      return;

    // Codegen for 06:05 for 1 quotations:
    case 365:
      // Only one option for 06:05:"
      quote = F("A second man went in and found the shop empty, as he thought, at * five minutes past six *. That puts the time at between 5:30 and 6:05.");
      attribution = F("- The ABC Murders");
      return;

    // Codegen for 06:06 for 1 quotations:
    case 366:
      // Only one option for 06:06:"
      quote = F("At * 6:06 *, every toilet on Merit Street suddenly exploded in a geyser of shit and raw sewage as some unimaginable reversal took place...");
      attribution = F("- Stephen King, IT");
      return;

    // Codegen for 06:07 for 0 quotations:
    case 367:
      // No quotes for 06:07
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:08 for 1 quotations:
    case 368:
      // Only one option for 06:08:"
      quote = F("At * six oh-eight a.m. * two men wearing ragged trench coats approached the Casino. The shorter of the men burst into flames.");
      attribution = F("- Ilona Andrews, Magic Bleeds");
      return;

    // Codegen for 06:09 for 0 quotations:
    case 369:
      // No quotes for 06:09
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:10 for 2 quotations:
    case 370:
      option = random(0, 2);
      // 2 options for 06:10
      switch (option) {
        case 0:
      quote = F("'Let me see now. You had a drink at the Continental at * six ten *.' 'Yes.' 'And at six forty-five you were talking to another journalist at the door of the Majestic?' 'Yes, Wilkins.'");
      attribution = F("- The Quiet American");
      return;
        case 1:
      quote = F("The bus left the station at * ten past six * - and she sat proud, like an accustomed traveller, apart from her father, John Henry, and Berenice.");
      attribution = F("- The Member of the Wedding");
      return;
      } // end switch (option)

    // Codegen for 06:11 for 0 quotations:
    case 371:
      // No quotes for 06:11
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:12 for 0 quotations:
    case 372:
      // No quotes for 06:12
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:13 for 1 quotations:
    case 373:
      // Only one option for 06:13:"
      quote = F("It's * 06:13 * .........Ma says I ought to be wrapped up in Rug already, Old Nick might possibly come.");
      attribution = F("- Emma Donoghue, Room");
      return;

    // Codegen for 06:14 for 0 quotations:
    case 374:
      // No quotes for 06:14
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:15 for 5 quotations:
    case 375:
      option = random(0, 5);
      // 5 options for 06:15
      switch (option) {
        case 0:
      quote = F("It was * 6.15 am. * Just starting to get light. A small knot of older teenagers were leaning against a nearby wall. They looked as though they had been out all night.");
      attribution = F("- Girl Missing");
      return;
        case 1:
      quote = F("Dumbbell exercise and wall-scaling ..... . * 6.15 *-6.30");
      attribution = F("- The Great Gatsby");
      return;
        case 2:
      quote = F("At a * quarter past six * he was through with them.");
      attribution = F("- The Photograph");
      return;
        case 3:
      quote = F("Father expected his shaving-water to be ready at a * quarter past six *. Just seven minutes late, Dorothy took the can upstairs and knocked at her father's door.");
      attribution = F("- A Clergyman's Daughter");
      return;
        case 4:
      quote = F("'* Quarter past six *,' said Tony. 'He's bound to have told her by now.'");
      attribution = F("- A Handful of Dust");
      return;
      } // end switch (option)

    // Codegen for 06:16 for 0 quotations:
    case 376:
      // No quotes for 06:16
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:17 for 1 quotations:
    case 377:
      // Only one option for 06:17:"
      quote = F("Dizzy, come on.' He turned slowly, coaxing the animal down on to the pillow. The clock read * six-seventeen *. A second cat, Miles, purred on contentedly...");
      attribution = F("- John Harvey, Lonely Hearts");
      return;

    // Codegen for 06:18 for 0 quotations:
    case 378:
      // No quotes for 06:18
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:19 for 1 quotations:
    case 379:
      // Only one option for 06:19:"
      quote = F("* 6.19 am *, 8th June 2004, the jet of your pupil set in the gold of your eye.");
      attribution = F("- Carol Ann Duffy, Venus");
      return;

    // Codegen for 06:20 for 2 quotations:
    case 380:
      option = random(0, 2);
      // 2 options for 06:20
      switch (option) {
        case 0:
      quote = F("It was * 6:20 a.m. *, and my parents and I were standing, stunned and haf-awake, in the parking lot of a Howard Johnson's in Iowa.");
      attribution = F("- Soon I Will Be Invincible");
      return;
        case 1:
      quote = F("By the time Elliot's mother arrived at * twenty past six *, Mrs Sen always made sure all evidence of her chopping was disposed of.");
      attribution = F("- Interpreter of Maladies");
      return;
      } // end switch (option)

    // Codegen for 06:21 for 0 quotations:
    case 381:
      // No quotes for 06:21
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:22 for 1 quotations:
    case 382:
      // Only one option for 06:22:"
      quote = F("Clock overturned when he fell forward. That'll give us the time of the crime. * Twenty-two minutes past six *.");
      attribution = F("- The Murder at the Vicarage");
      return;

    // Codegen for 06:23 for 0 quotations:
    case 383:
      // No quotes for 06:23
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:24 for 0 quotations:
    case 384:
      // No quotes for 06:24
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:25 for 3 quotations:
    case 385:
      option = random(0, 3);
      // 3 options for 06:25
      switch (option) {
        case 0:
      quote = F("Simon is happy to travel scum class when he's on his own and even sometimes deliberately aims for the * 6.25 *. But today the .25 is delayed to 6.44.");
      attribution = F("- Mark Lawson, The Deaths");
      return;
        case 1:
      quote = F("The Professor looked at his watch. I looked at my watch. * Six-twenty-five *. Well past daybreak. Morning papers delivered.");
      attribution = F("- Haruki Murakami");
      return;
        case 2:
      quote = F("At * twenty-five past six * I go into the bathroom and have a wash, then while the Old Lady's busy in the kitchen helping Chris with the washing up...");
      attribution = F("- A Kind of Loving");
      return;
      } // end switch (option)

    // Codegen for 06:26 for 0 quotations:
    case 386:
      // No quotes for 06:26
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:27 for 2 quotations:
    case 387:
      option = random(0, 2);
      // 2 options for 06:27
      switch (option) {
        case 0:
      quote = F("* 06:27 *:52 by the chip in her optic nerve; Case had been following her progress through Villa Straylight for over an hour...");
      attribution = F("- William Gibson, Neuromancer");
      return;
        case 1:
      quote = F("Early in the morning, late in the century, Cricklewood Broadway. At * 0627 hours * on January 1, 1975, Alfred Archibald Jones was dressed in corduroy...");
      attribution = F("- Zadie Smith, White Teeth");
      return;
      } // end switch (option)

    // Codegen for 06:28 for 0 quotations:
    case 388:
      // No quotes for 06:28
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:29 for 1 quotations:
    case 389:
      // Only one option for 06:29:"
      quote = F("I sat up. There was a rug over me. I threw that off and got my feet on the floor. I scowled at a clock. The clock said * a minute short of six-thirty *.");
      attribution = F("- The Big Sleep");
      return;

    // Codegen for 06:30 for 10 quotations:
    case 390:
      option = random(0, 10);
      // 10 options for 06:30
      switch (option) {
        case 0:
      quote = F("She pushed the three of us into the hotel room, thern shut the soor. I glanced at the clock by the bed. * 6.30 am. * Why were they waking Mum and Dad up this early?");
      attribution = F("- Girl Missing");
      return;
        case 1:
      quote = F("As late as 1914 I knew the carrier of Croydon-cum-Clopton, twelve miles from Cambridge; his cart started at * 6.30 in the morning * and got back at about ten at night.");
      attribution = F("- Gwen Raverat, Period Piece");
      return;
        case 2:
      quote = F("At five o'clock the two ladies retired to dress, and at * half past six * Elizabeth was summoned to dinner.");
      attribution = F("- Pride and Prejudice");
      return;
        case 3:
      quote = F("But two of the three gentlemen assembled in the Saloon at * half-past six * on a wintry evening of late February were in no danger of falling into this error.");
      attribution = F("- Georgette Heyer, Cotillion");
      return;
        case 4:
      quote = F("Through her wide-open window the brilliant rays of the morning sun were flooding her room with light. She looked up at the clock; it was * half-past six *...");
      attribution = F("- The Scarlet Pimpernel");
      return;
        case 5:
      quote = F("A civilised * six-thirty * for twelve noon was almost certainly what he had in mind.");
      attribution = F("- Douglas Adams");
      return;
        case 6:
      quote = F("At * six-thirty * I left the bar and walked outside. It was getting dark and the big Avenida looked cool and graceful. On the other side were homes that once looked out on the beach.");
      attribution = F("- The Rum Diary");
      return;
        case 7:
      quote = F("He was ready to close the door the second he was told to. \"What time is it?\" said his father in a drugged voice. \"Oh my God, it's * six-thirty *,\" his mother said.");
      attribution = F("- The Book of Daniel");
      return;
        case 8:
      quote = F("It was * six-thirty *. When the baby's cry came, they could not pick it out, and Sam, eagerly thrusting his face amongst their ears...");
      attribution = F("- The Man Who Loved Children");
      return;
        case 9:
      quote = F("Then he got back in the truck. The bar clock said * six-thirty *...By eight-thirty he'd decided that it that was it would take to make the cab arrive...");
      attribution = F("- Cities of the Plain");
      return;
      } // end switch (option)

    // Codegen for 06:31 for 1 quotations:
    case 391:
      // Only one option for 06:31:"
      quote = F("I had been delayed at a case and it was * a little after half past six * when I found myself at Baker Street once more");
      attribution = F("- Arthur Conan Doyle");
      return;

    // Codegen for 06:32 for 1 quotations:
    case 392:
      // Only one option for 06:32:"
      quote = F("The familiar radium numerals on my left wrist confirmed the clock tower. It was * twenty-eight minutes to seven *...");
      attribution = F("- Too Like the Lightning");
      return;

    // Codegen for 06:33 for 1 quotations:
    case 393:
      // Only one option for 06:33:"
      quote = F("Woke * 6.33 a.m. * Last session with Anderson. He made it plain he's seen enough of me, and from now on I'm better alone. To sleep 8:00? (These count-downs terrify me.)");
      attribution = F("- The Voices of Time");
      return;

    // Codegen for 06:34 for 0 quotations:
    case 394:
      // No quotes for 06:34
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:35 for 1 quotations:
    case 395:
      // Only one option for 06:35:"
      quote = F("My watch lay on the dressing-table close by; glancing at it, I saw that the time was * twenty-five minutes to seven *.");
      attribution = F("- Ravensdene Court");
      return;

    // Codegen for 06:36 for 1 quotations:
    case 396:
      // Only one option for 06:36:"
      quote = F("Kaldren pursues me like luminescent shadow. He has chalked up on the gateway '96,688,365,498,702'. Should confuse the mail man. Woke 9:05. To sleep * 6:36 *.");
      attribution = F("- The Voices of Time");
      return;

    // Codegen for 06:37 for 1 quotations:
    case 397:
      // Only one option for 06:37:"
      quote = F("The dashboard clock said * 6.37am * Town frowned, and checked his wristwatch, which blinked that it was 1.58pm.");
      attribution = F("- Neil Gaiman, American Gods");
      return;

    // Codegen for 06:38 for 1 quotations:
    case 398:
      // Only one option for 06:38:"
      quote = F("The clock on the dashboard said it was * 6.38am. * He left the keys in the car, and walked toward the tree.");
      attribution = F("- Neil Gaiman, American Gods");
      return;

    // Codegen for 06:39 for 0 quotations:
    case 399:
      // No quotes for 06:39
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:40 for 2 quotations:
    case 400:
      option = random(0, 2);
      // 2 options for 06:40
      switch (option) {
        case 0:
      quote = F("I allowed plenty of time - in fact, too much; for we arrived at * twenty minutes to seven *, and Franching, so the servant said, had only just gone up to dress.");
      attribution = F("- Diary of a Nobody");
      return;
        case 1:
      quote = F("...any more than it has responded when the phone had rung at twenty-five to seven in the morning, and again at * twenty to seven *.");
      attribution = F("- Douglas Adams");
      return;
      } // end switch (option)

    // Codegen for 06:41 for 1 quotations:
    case 401:
      // Only one option for 06:41:"
      quote = F("Stillman's train was not due to arrive until * six forty-one *, but Quinn wanted time to study the geography of the place, to make sure that Stillman would not be able to slip away...");
      attribution = F("- The New York Trilogy");
      return;

    // Codegen for 06:42 for 0 quotations:
    case 402:
      // No quotes for 06:42
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:43 for 1 quotations:
    case 403:
      // Only one option for 06:43:"
      quote = F("To London on the * 6.43am. * Jessica is back from her holiday. Things are looking up, she called me Chris, instead of Minister, when we talked on the phone this afternoon.");
      attribution = F("- A View From the Foothills");
      return;

    // Codegen for 06:44 for 1 quotations:
    case 404:
      // Only one option for 06:44:"
      quote = F("Simon is happy to travel scum class when he's on his own and even sometimes deliberately aims for the 6.25. But today the .25 is delayed to * 6.44 *.");
      attribution = F("- Mark Lawson, The Deaths");
      return;

    // Codegen for 06:45 for 5 quotations:
    case 405:
      option = random(0, 5);
      // 5 options for 06:45
      switch (option) {
        case 0:
      quote = F("As the clock pointed to a * quarter to seven *, the dog woke and shook himself. After waiting in vain for the footman, who was accustomed to let him out...");
      attribution = F("- Wilkie Collins, No Name");
      return;
        case 1:
      quote = F("He was still hurriedly thinking all this through, unable to decide to get out of the bed, when the clock struck * quarter to seven *.");
      attribution = F("- Franz Kafka, Metamorphosis");
      return;
        case 2:
      quote = F("It was a * quarter to seven * when I let myself into the office and clicked the light on and picked a piece of paper off the floor.");
      attribution = F("- The High Window");
      return;
        case 3:
      quote = F("'Let me see now. You had a drink at the Continental at six ten.' 'Yes.' 'And at * six forty-five * you were talking to another journalist at the door of the Majestic?'");
      attribution = F("- The Quiet American");
      return;
        case 4:
      quote = F("* Six forty-five *, called Louie. \"Did you hear, Ming,\" he asked, \"did you hear?\" \"Yes, Taddy, I heard.\" \"What is it?\" asked Tommy. \"The new baby, listen, the new baby.\"");
      attribution = F("- The Man Who Loved Children");
      return;
      } // end switch (option)

    // Codegen for 06:46 for 2 quotations:
    case 406:
      option = random(0, 2);
      // 2 options for 06:46
      switch (option) {
        case 0:
      quote = F("At * one minute after the quarter to seven * I heard the rattle of the cans outside. I opened the front door, and there was my man...");
      attribution = F("- The Thirty-Nine Steps");
      return;
        case 1:
      quote = F("...but still he did not come. The fool had chosen this day of all days to be late. At * one minute after the quarter to seven * I heard the rattle of the cans outside.");
      attribution = F("- The Thirty-Nine Steps");
      return;
      } // end switch (option)

    // Codegen for 06:47 for 0 quotations:
    case 407:
      // No quotes for 06:47
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:48 for 0 quotations:
    case 408:
      // No quotes for 06:48
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:49 for 1 quotations:
    case 409:
      // Only one option for 06:49:"
      quote = F("Night ends, * 6:49 *. Meet in the coffee shop at 7:30; press conference at 10:00.");
      attribution = F("- Hunter S. Thompson");
      return;

    // Codegen for 06:50 for 2 quotations:
    case 410:
      option = random(0, 2);
      // 2 options for 06:50
      switch (option) {
        case 0:
      quote = F("Will, my fiance, was coming from Boston on the * six-fifty * train - the dawn train, the only train that still stopped in the small Ohio city where I lived.");
      attribution = F("- Mary Robison, Pretty Ice");
      return;
        case 1:
      quote = F("At * ten minutes to seven * Dulcie was ready. She looked at herself in the wrinkly mirror. The reflection was satisfactory.");
      attribution = F("- O. Henry, The Four Million");
      return;
      } // end switch (option)

    // Codegen for 06:51 for 1 quotations:
    case 411:
      // Only one option for 06:51:"
      quote = F("The square of light in the kitchen doorway had faded to thin purple; his watch said * 6:51 *.");
      attribution = F("- Stephen King, Salem's Lot");
      return;

    // Codegen for 06:52 for 0 quotations:
    case 412:
      // No quotes for 06:52
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:53 for 1 quotations:
    case 413:
      // Only one option for 06:53:"
      quote = F("It was * near on seven o'clock * when I got to Mr. and Mrs. Fleming's house on 6th Street, where I was renting a room.");
      attribution = F("- All Aunt Hagar's Children");
      return;

    // Codegen for 06:54 for 0 quotations:
    case 414:
      // No quotes for 06:54
        quote = "";
        attribution = "";
      return;

    // Codegen for 06:55 for 2 quotations:
    case 415:
      option = random(0, 2);
      // 2 options for 06:55
      switch (option) {
        case 0:
      quote = F("At * 6:55 am * Lisa parked and took the lift from the frozen underground car park up to level 1 of Green Oaks Shopping Centre.");
      attribution = F("- What was Lost");
      return;
        case 1:
      quote = F("Indeed how suspicious it would be if you had been completely accurate. Haven't I been? Not quite. It was * five to seven * that you talked to Wilkins. \"Another ten minutes.\"");
      attribution = F("- The Quiet American");
      return;
      } // end switch (option)

    // Codegen for 06:56 for 1 quotations:
    case 416:
      // Only one option for 06:56:"
      quote = F("Then it was * 6.56 *. A black Rover - a Rover 90, registration PYX 520 - turned into the street that ran down the left-hand side of The Bunker. It parked.");
      attribution = F("- Dreams of leaving");
      return;

    // Codegen for 06:57 for 1 quotations:
    case 417:
      // Only one option for 06:57:"
      quote = F("...while on what seemed to be a wall, was hung a great clock face; the hands pointing to * three minutes to the hour; which was seven *.");
      attribution = F("- Between the Acts");
      return;

    // Codegen for 06:58 for 1 quotations:
    case 418:
      // Only one option for 06:58:"
      quote = F("Walk fast, says Perry, \"it's * two minutes to seven *, and I got to be home by-\" \"Oh, shut up,\" says I.\"");
      attribution = F("- O. Henry, Roads of Destiny");
      return;

    // Codegen for 06:59 for 2 quotations:
    case 419:
      option = random(0, 2);
      // 2 options for 06:59
      switch (option) {
        case 0:
      quote = F("It was * 6.59 a.m. * on Maundy Thursday as Blomkvist and Berger let themselves into the \"Millennium\" offices.");
      attribution = F("- Stieg Larsson");
      return;
        case 1:
      quote = F("* About seven o'clock * in the evening she had died, and her frantic husband had made a frightful scene in his efforts to kill West...");
      attribution = F("- Herbert West - Reanimator");
      return;
      } // end switch (option)

    // Codegen for 07:00 for 7 quotations:
    case 420:
      option = random(0, 7);
      // 7 options for 07:00
      switch (option) {
        case 0:
      quote = F("I had left directions that I was to be called * at seven *; for it was plain that I must see Wemmick before seeing any one else");
      attribution = F("- Great Expectations");
      return;
        case 1:
      quote = F("At * seven o'clock * in the morning, Rubashov was awakened by a bugle, but he did not get up. Soon he heard sounds in the corridor. He imagined that someone was to be tortured...");
      attribution = F("- Darkness at Noon");
      return;
        case 2:
      quote = F("By * seven o'clock * the orchestra has arrived - no thin five-piece affair but a whole pitful of oboes and trombones and saxophones ...");
      attribution = F("- The Great Gatsby");
      return;
        case 3:
      quote = F("It was * seven o'clock * when we got into the coupe with him and started for Long Island. [...] So we drove on toward death through the cooling twilight.");
      attribution = F("- The Great Gatsby");
      return;
        case 4:
      quote = F("Seven o'clock, already, he said to himself when the clock struck again, \"* seven o'clock *, and there's still a fog like this.\"");
      attribution = F("- Franz Kafka, Metamorphosis");
      return;
        case 5:
      quote = F("She locked herself in, made no reply to my bonjour through the door; she was up at * seven o'clock *, the samovar was taken in to her from the kitchen.");
      attribution = F("- Crime and Punishment");
      return;
        case 6:
      quote = F("The town clock struck * seven *. The echoes of the great chime wandered in the unlit halls of the library. An autumn leaf, very crisp, fell somewhere in the dark.");
      attribution = F("- Ray Bradbury");
      return;
      } // end switch (option)

    // Codegen for 07:01 for 1 quotations:
    case 421:
      // Only one option for 07:01:"
      quote = F("He waited until nearly eight, because * around seven * there were always more people coming in and out of the house than at other times.");
      attribution = F("- The Talented Mr Ripley");
      return;

    // Codegen for 07:02 for 1 quotations:
    case 422:
      // Only one option for 07:02:"
      quote = F("* 07:02 *:18 One and a half hours. 'Case,' she said, 'I wanna favour.'");
      attribution = F("- William Gibson, Neuromancer");
      return;

    // Codegen for 07:03 for 1 quotations:
    case 423:
      // Only one option for 07:03:"
      quote = F("* 7:03am * General Tanz woke up as though aroused by a mental alarm-clock.");
      attribution = F("- The Night of the Generals");
      return;

    // Codegen for 07:04 for 1 quotations:
    case 424:
      // Only one option for 07:04:"
      quote = F("Sunday evening at almost the same hour (to be precise, at about * 7:04 p.m. *) she rings the front door bell at the home of Walter Moeding, Crime Commissioner...");
      attribution = F("- Heinrich Boll");
      return;

    // Codegen for 07:05 for 3 quotations:
    case 425:
      option = random(0, 3);
      // 3 options for 07:05
      switch (option) {
        case 0:
      quote = F("Ryan missed the dawn. He boarded a TWA 747 that left Dulles on time, at * 7:05 A.M. * The sky was overcast, and when the aircraft burst through the cloud layer into sunlight...");
      attribution = F("- The Hunt for Red October");
      return;
        case 1:
      quote = F("Outside my window the sky hung low and gray. It looked like snow, which added to my malaise. The clock read * five after seven *. I punched the remote control...");
      attribution = F("- Dance Dance Dance");
      return;
        case 2:
      quote = F("He decided he'd wait until * five minutes after seven o'clock *, and then he'd call in, take the day off, and make every effort in the book to locate someone reliable.");
      attribution = F("- Where I'm Calling From");
      return;
      } // end switch (option)

    // Codegen for 07:06 for 1 quotations:
    case 426:
      // Only one option for 07:06:"
      quote = F("...you should get right back on the horse that had thrown you, because here he came through the door at * six minutes past seven *, resplendent in his blue uniform with his sidearm");
      attribution = F("- The Green Mile");
      return;

    // Codegen for 07:07 for 0 quotations:
    case 427:
      // No quotes for 07:07
        quote = "";
        attribution = "";
      return;

    // Codegen for 07:08 for 2 quotations:
    case 428:
      option = random(0, 2);
      // 2 options for 07:08
      switch (option) {
        case 0:
      quote = F("Reacher had no watch but he figured when he saw Gregory it must have been * between eight and nine minutes after seven o'clock *.");
      attribution = F("- Lee Child, The Hard Way");
      return;
        case 1:
      quote = F("It was * eight minutes past seven * and still no girl. I waited impatiently. I watched another crowd surge through the barriers and move quickly down the steps.");
      attribution = F("- The Girl from East Berlin");
      return;
      } // end switch (option)

    // Codegen for 07:09 for 1 quotations:
    case 429:
      // Only one option for 07:09:"
      quote = F("The morning house lay empty. The clock ticked on, repeating and repeating its sounds into the emptiness. Seven-nine, breakfast time, * seven-nine *!");
      attribution = F("- There Will Come Soft Rains");
      return;

    // Codegen for 07:10 for 3 quotations:
    case 430:
      option = random(0, 3);
      // 3 options for 07:10
      switch (option) {
        case 0:
      quote = F("A search in Bradshaw informed me that a train left St Pancras at * 7.10 *, which would land me at any Galloway station in the late afternoon.");
      attribution = F("- The Thirty-Nine Steps");
      return;
        case 1:
      quote = F("The time was * 7:10 * in the morning. The manoeuvre would start at 7:12. She looked apprehensively at the giant clock at the railway station.");
      attribution = F("- The Fourth Passenger");
      return;
        case 2:
      quote = F("...he maintained his equilibrium with difficulty, and very soon he would finally have to make a final decision, for * in five minutes it would be a quarter past seven *.");
      attribution = F("- Franz Kafka, Metamorphosis");
      return;
      } // end switch (option)

    // Codegen for 07:11 for 0 quotations:
    case 431:
      // No quotes for 07:11
        quote = "";
        attribution = "";
      return;

    // Codegen for 07:12 for 2 quotations:
    case 432:
      option = random(0, 2);
      // 2 options for 07:12
      switch (option) {
        case 0:
      quote = F("...arbitrarily choosing a time based on fifteen-minute intervals. \"Never meet people at 7:45 or 6:30, Jasper, but pick times like * 7:12 * and 8:03!\"");
      attribution = F("- A Fraction of the Whole");
      return;
        case 1:
      quote = F("...the 'stupid human habit' of arbitrarily choosing a time based on fifteen-minute intervals. 'Never meet people at 7:45 or 6:30, Jasper, but pick times like * 7:12 * and 8:03!'");
      attribution = F("- A Fraction of the Whole");
      return;
      } // end switch (option)

    // Codegen for 07:13 for 1 quotations:
    case 433:
      // Only one option for 07:13:"
      quote = F("...when I looked out of the corridor window of my carriage just before the train left at * seven-thirteen *, to find it dawning upon me with perfect certainty.");
      attribution = F("- WG Sebald, Austerlitz");
      return;

    // Codegen for 07:14 for 1 quotations:
    case 434:
      // Only one option for 07:14:"
      quote = F("At * 7.14 * Harry knew he was alive. He knew that because the pain could be felt in every nerve fibre.");
      attribution = F("- Jo Nesbo, The Redeemer");
      return;

    // Codegen for 07:15 for 5 quotations:
    case 435:
      option = random(0, 5);
      // 5 options for 07:15
      switch (option) {
        case 0:
      quote = F("At * 7:15 A.M. *, January 25th, we started flying northwestward under McTighe's pilotage with ten men, seven dogs, a sledge, a fuel and food supply, and other items...");
      attribution = F("- At the Mountains of Madness");
      return;
        case 1:
      quote = F("Mary Kent had got out of bed and put on her dressing gown, having just checked her husband's watch: it was * 7.15 *. A confused conversation ensued...");
      attribution = F("- Kate Summerscale");
      return;
        case 2:
      quote = F("He was a late riser, as a rule, and as the clock on the mantelpiece showed me that it was only a * quarter-past seven *, I blinked up at him in some surprise...");
      attribution = F("- Arthur Conan Doyle");
      return;
        case 3:
      quote = F("... but for now he let Leo take charge: they were going to go back to Notting Hill and catch the * seven fifteen * screening of Scarface at the Gate.");
      attribution = F("- The Line of Beauty");
      return;
        case 4:
      quote = F("... everyone would come a little late, so as not to be the first to arrive. .. By * seven-fifteen * not another soul could squeeze into the house.");
      attribution = F("- The elderly lady");
      return;
      } // end switch (option)

    // Codegen for 07:16 for 0 quotations:
    case 436:
      // No quotes for 07:16
        quote = "";
        attribution = "";
      return;

    // Codegen for 07:17 for 1 quotations:
    case 437:
      // Only one option for 07:17:"
      quote = F("As of * 7.17am * local time on 30 June 1908, Padzhitnoff had been working for nearly a year as a contract employee of the Okhrana, receiving five hundred rubles a month...");
      attribution = F("- Against the Day");
      return;

    // Codegen for 07:18 for 0 quotations:
    case 438:
      // No quotes for 07:18
        quote = "";
        attribution = "";
      return;

    // Codegen for 07:19 for 2 quotations:
    case 439:
      option = random(0, 2);
      // 2 options for 07:19
      switch (option) {
        case 0:
      quote = F("I opened the sunroof and turned up the CD player volume to combat fatigue, and at * 7.19am * on Saturday, with the caffeine still running all around my brain...");
      attribution = F("- The Rosie Project");
      return;
        case 1:
      quote = F("In the end we won the point- but I can't feel my usual triumph. All I know is, it's * seven-nineteen *, and in eleven minutes I'm supposed to be halfway across town...");
      attribution = F("- The Undomestic Goddess");
      return;
      } // end switch (option)

    // Codegen for 07:20 for 4 quotations:
    case 440:
      option = random(0, 4);
      // 4 options for 07:20
      switch (option) {
        case 0:
      quote = F("...I thought that Mother was dead from a heart attack (this was the timetable for a Monday and also it is an approximation). * 7.20 a.m. * Wake up");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("The pause, we finally concluded, was to allow the other important people to catch up, those who had arrived at 7:10 waiting for those who had arrived at * 7:20 *.");
      attribution = F("- C Northcote Parkinson");
      return;
        case 2:
      quote = F("The clock read * seven-twenty *, but I felt no hunger. You'd think I might have wanted to eat something after the day I'd had, but I cringed at the very thought of food.");
      attribution = F("- Haruki Murakami");
      return;
        case 3:
      quote = F("...was no longer greatly interested in the possible and improbable adventures of each new day. He escaped from reality till the alarm-clock rang, at * seven-twenty *.");
      attribution = F("- Sinclair Lewis, Babbitt");
      return;
      } // end switch (option)

    // Codegen for 07:21 for 0 quotations:
    case 441:
      // No quotes for 07:21
        quote = "";
        attribution = "";
      return;

    // Codegen for 07:22 for 0 quotations:
    case 442:
      // No quotes for 07:22
        quote = "";
        attribution = "";
      return;

    // Codegen for 07:23 for 0 quotations:
    case 443:
      // No quotes for 07:23
        quote = "";
        attribution = "";
      return;

    // Codegen for 07:24 for 0 quotations:
    case 444:
      // No quotes for 07:24
        quote = "";
        attribution = "";
      return;

    // Codegen for 07:25 for 1 quotations:
    case 445:
      // Only one option for 07:25:"
      quote = F("* 7.25 a.m. * clean teeth and wash face");
      attribution = F("- Mark Haddon");
      return;

    // Codegen for 07:26 for 0 quotations:
    case 446:
      // No quotes for 07:26
        quote = "";
        attribution = "";
      return;

    // Codegen for 07:27 for 1 quotations:
    case 447:
      // Only one option for 07:27:"
      quote = F("His appointment with the doctor was for 8.45. It was * 7.27 *.");
      attribution = F("- Henning Mankell");
      return;

    // Codegen for 07:28 for 0 quotations:
    case 448:
      // No quotes for 07:28
        quote = "";
        attribution = "";
      return;

    // Codegen for 07:29 for 1 quotations:
    case 449:
      // Only one option for 07:29:"
      quote = F("At * 7.29 * in the morning of 1 July, the cinematographer finds himself filming silence itself.");
      attribution = F("- At Break of Day");
      return;

    // Codegen for 07:30 for 4 quotations:
    case 450:
      option = random(0, 4);
      // 4 options for 07:30
      switch (option) {
        case 0:
      quote = F("At * half-past seven * the next morning he rang the bell of 21 Blenheim Avenue.");
      attribution = F("- William Trevor, After Rain");
      return;
        case 1:
      quote = F("But now he was close - here was the house, here were the gates. Somewhere a clock beat a single chime. 'What, is it really * half-past seven *? That's impossible, it must be fast!'");
      attribution = F("- Crime and Punishment");
      return;
        case 2:
      quote = F("Precisely at * half past seven * the station-master came into the traffic office. He weighed almost sixteen stone...");
      attribution = F("- Closely Observed Trains");
      return;
        case 3:
      quote = F("The clock showed * half-past seven *. This was the twilight time. He would be there now. I pictured him in his old navy-blue sweater and peaked cap...");
      attribution = F("- Roald Dahl");
      return;
      } // end switch (option)

    // Codegen for 07:31 for 0 quotations:
    case 451:
      // No quotes for 07:31
        quote = "";
        attribution = "";
      return;

    // Codegen for 07:32 for 1 quotations:
    case 452:
      // Only one option for 07:32:"
      quote = F("At * 7:32 *, he suffered a fatal stroke.");
      attribution = F("- Stephen King, IT");
      return;

    // Codegen for 07:33 for 0 quotations:
    case 453:
      // No quotes for 07:33
        quote = "";
        attribution = "";
      return;

    // Codegen for 07:34 for 1 quotations:
    case 454:
      // Only one option for 07:34:"
      quote = F("* 7:34. * Monday morning, Blackeberg. The burglar alarm at the ICA grocery store on Arvid Morne's way is set off.");
      attribution = F("- Let The Right One In");
      return;

    // Codegen for 07:35 for 3 quotations:
    case 455:
      option = random(0, 3);
      // 3 options for 07:35
      switch (option) {
        case 0:
      quote = F("* 7.35 *-40. Yseut arrives at 'M. and S.', puts through phone call.");
      attribution = F("- The Case of the Gilded Fly");
      return;
        case 1:
      quote = F("At * 7:35am * Ishigami left his apartment as he did every weekday morning.");
      attribution = F("- The Devotion of Duspect X");
      return;
        case 2:
      quote = F("I looked at my watch. * Seven thirty-five *.");
      attribution = F("- Kathy Reichs, Bare Bones");
      return;
      } // end switch (option)

    // Codegen for 07:36 for 1 quotations:
    case 456:
      // Only one option for 07:36:"
      quote = F("* 7:36 *, sunrise. The hospital blinds were much better, darker than her own.");
      attribution = F("- Let The Right One In");
      return;

    // Codegen for 07:37 for 0 quotations:
    case 457:
      // No quotes for 07:37
        quote = "";
        attribution = "";
      return;

    // Codegen for 07:38 for 0 quotations:
    case 458:
      // No quotes for 07:38
        quote = "";
        attribution = "";
      return;

    // Codegen for 07:39 for 1 quotations:
    case 459:
      // Only one option for 07:39:"
      quote = F("Now, at the station, do you recall speaking to Mr Joseph Markew?' 'Yes, indeed. I was standing on the platform waiting for my usual train - the * 7.39 * - when he accosted me.'");
      attribution = F("- Arthur & George");
      return;

    // Codegen for 07:40 for 2 quotations:
    case 460:
      option = random(0, 2);
      // 2 options for 07:40
      switch (option) {
        case 0:
      quote = F("* 7.40 a.m. * Have breakfast.");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("She arrives at * 7.40 *, ten minutes late, but the children, Jimmy and Bitsy, are still eating supper and their parents are not ready to go yet.");
      attribution = F("- The Babysitter");
      return;
      } // end switch (option)

    // Codegen for 07:41 for 0 quotations:
    case 461:
      // No quotes for 07:41
        quote = "";
        attribution = "";
      return;

    // Codegen for 07:42 for 1 quotations:
    case 462:
      // Only one option for 07:42:"
      quote = F("* Seven forty-two * am., Mr Gasparian: I curse you. I curse your arms so they will wither and die and fall off your body...");
      attribution = F("- Ilona Andrews, Magic Bleeds");
      return;

    // Codegen for 07:43 for 0 quotations:
    case 463:
      // No quotes for 07:43
        quote = "";
        attribution = "";
      return;

    // Codegen for 07:44 for 1 quotations:
    case 464:
      // Only one option for 07:44:"
      quote = F("It's not exactly how you'd choose to go, is it? You'd rather die flying a kite with your grandchildren, or at a great party or something. Not on the * seven forty-four *.");
      attribution = F("- One Moment, One Morning");
      return;

    // Codegen for 07:45 for 2 quotations:
    case 465:
      option = random(0, 2);
      // 2 options for 07:45
      switch (option) {
        case 0:
      quote = F("...the 'stupid human habit' of arbitrarily choosing a time based on fifteen-minute intervals. \"Never meet people at * 7:45 * or 6:30, Jasper, but pick times like 7:12 and 8:03!\"");
      attribution = F("- A Fraction of the Whole");
      return;
        case 1:
      quote = F("Mr. Green left for work at a * quarter to eight *, as he did every morning. He walked down his front steps carrying his empty-looking leatherette briefcase...");
      attribution = F("- A Crime in The Neighborhood");
      return;
      } // end switch (option)

    // Codegen for 07:46 for 1 quotations:
    case 466:
      // Only one option for 07:46:"
      quote = F("The clock on his bedside table said * 7.46 a.m. * He cursed, jumped out of bed and dressed. He stuffed his toothbrush and toothpaste in his jacket pocket...");
      attribution = F("- The Dogs of Riga");
      return;

    // Codegen for 07:47 for 0 quotations:
    case 467:
      // No quotes for 07:47
        quote = "";
        attribution = "";
      return;

    // Codegen for 07:48 for 0 quotations:
    case 468:
      // No quotes for 07:48
        quote = "";
        attribution = "";
      return;

    // Codegen for 07:49 for 1 quotations:
    case 469:
      // Only one option for 07:49:"
      quote = F("I noticed that our clock was a couple of minutes slow, and I put it right. \"When did you next see Dr. Wellesley?\" \"At just * eleven minutes to eight *.\"");
      attribution = F("- In the Mayor's Parlour");
      return;

    // Codegen for 07:50 for 2 quotations:
    case 470:
      option = random(0, 2);
      // 2 options for 07:50
      switch (option) {
        case 0:
      quote = F("At about * ten minutes to eight *, Jim had squared the part of the work he had been doing - the window - so he decided not to start on the door or the skirting until after breakfast.");
      attribution = F("- Robert Tressell");
      return;
        case 1:
      quote = F("At * ten to eight *, he strolled downstairs, to make sure that Signora Buffi was not pottering around in the hall and that her door was not open...");
      attribution = F("- The Talented Mr Ripley");
      return;
      } // end switch (option)

    // Codegen for 07:51 for 1 quotations:
    case 471:
      // Only one option for 07:51:"
      quote = F("Good morning! he said brightly to the surprised imp. \"What is the time, please?\" \"Er...* nine minutes to eight *, Insert Name Here,\" said the imp.");
      attribution = F("- Terry Pratchett, Thud!");
      return;

    // Codegen for 07:52 for 1 quotations:
    case 472:
      // Only one option for 07:52:"
      quote = F("He waited until * nearly eight *, because around seven there were always more people coming in and out of the house than at other times.");
      attribution = F("- The Talented Mr Ripley");
      return;

    // Codegen for 07:53 for 1 quotations:
    case 473:
      // Only one option for 07:53:"
      quote = F("What time is it? \"* Seven to eight *. Won't be long now ...\"");
      attribution = F("- Never go back");
      return;

    // Codegen for 07:54 for 1 quotations:
    case 474:
      // Only one option for 07:54:"
      quote = F("The body was found at * six minutes to eight *. Doctor Young arrived some thirty minutes later. Just let me get that clear - I've a filthy memory.");
      attribution = F("- Ngaio Marsh, A Man Lay Dead");
      return;

    // Codegen for 07:55 for 2 quotations:
    case 475:
      option = random(0, 2);
      // 2 options for 07:55
      switch (option) {
        case 0:
      quote = F("at * 7.55 * this morning the circus ran away to join me.");
      attribution = F("- Roger McGough");
      return;
        case 1:
      quote = F("Flora drew her coat round her, and looked up into the darkening vault of the sky. Then she glanced at her watch. It was * five to eight *.");
      attribution = F("- Cold Comfort Farm");
      return;
      } // end switch (option)

    // Codegen for 07:56 for 3 quotations:
    case 476:
      option = random(0, 3);
      // 3 options for 07:56
      switch (option) {
        case 0:
      quote = F("The Castle Gate - only the Castle Gate - and it was * four minutes to eight *.");
      attribution = F("- Thomas Mann, Buddenbrooks");
      return;
        case 1:
      quote = F("'What time is it, Ireno?' Without consulting the sky, without stopping, he replied: 'It's * four minutes to eight *, young Bernardo Juan Franciso.'");
      attribution = F("- Jorge Luis Borges");
      return;
        case 2:
      quote = F("...two cups of coffee, and all the Saturday sections, I stretch my arms in a big yawn and glance at the clock. I don't believe it. It's only * seven fifty-six *.");
      attribution = F("- The Undomestic Goddess");
      return;
      } // end switch (option)

    // Codegen for 07:57 for 0 quotations:
    case 477:
      // No quotes for 07:57
        quote = "";
        attribution = "";
      return;

    // Codegen for 07:58 for 0 quotations:
    case 478:
      // No quotes for 07:58
        quote = "";
        attribution = "";
      return;

    // Codegen for 07:59 for 2 quotations:
    case 479:
      option = random(0, 2);
      // 2 options for 07:59
      switch (option) {
        case 0:
      quote = F("I'd spent fifty two days in 1958, but here it was * 7.59 * in the morning.");
      attribution = F("- Stephen King, 11/22/63");
      return;
        case 1:
      quote = F("Kuniang made her appearance in my study * just before eight o' clock *, arrayed in what had once been a \"party frock\".");
      attribution = F("- Daniel Vare");
      return;
      } // end switch (option)

    // Codegen for 08:00 for 24 quotations:
    case 480:
      option = random(0, 24);
      // 24 options for 08:00
      switch (option) {
        case 0:
      quote = F("* 8.00 a.m. * Put school clothes on");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("I'm not crying, Maria said when Carter called from the desert at * 8 a.m. * \"I'm perfectly alright\". \"You don't sound perfectly alright\"");
      attribution = F("- Play it as is Lays");
      return;
        case 2:
      quote = F("At * 8 o'clock * on Thursday morning Arthur didn't feel very good.");
      attribution = F("- Douglas Adams");
      return;
        case 3:
      quote = F("He was woken by reception * at eight * the following morning to be told that everyone was waiting for him downstairs, the wrapper of a Mars bar was still folded in his fist.");
      attribution = F("- Ian McEwan, Solar");
      return;
        case 4:
      quote = F("I hear noise at the ward door, off up the hall out of my sight. That ward door starts opening * at eight * and opens and closes a thousand times a day, kashash, click.");
      attribution = F("- Ken Kesey");
      return;
        case 5:
      quote = F("I trace the words, I'll arrive to collect you for drinks * at eight * on Saturday.");
      attribution = F("- Chuck Palahniuk, Tell-All");
      return;
        case 6:
      quote = F("* At eight o'clock *, a shaft of daylight came to wake us. The thousand facets of the lava on the rock face picked it up as it passed, scattering like a shower of sparks.");
      attribution = F("- Jules Verne");
      return;
        case 7:
      quote = F("That day he forgot to go to dinner; he noticed the fact * at eight * in the evening, and as it was too late to go to the Rue St Jaques, he ate a lump of bread.");
      attribution = F("- Victor Hugo, Les Miserables");
      return;
        case 8:
      quote = F("Too late. He must go on wanting what he had wanted yesterday. He dressed and rode down to the ground floor * at eight * for breakfast.");
      attribution = F("- Death in Venice");
      return;
        case 9:
      quote = F("Freud had me knock on Jung's door, to no avail. They waited until * eight *, then set off for Brill's without him.");
      attribution = F("- Jed Rubenfeld");
      return;
        case 10:
      quote = F("She did not exist: she would not be born till to-morrow, some time after * eight o'clock a.m. *; and I would wait to be assured she had come into the world alive...");
      attribution = F("- Charlotte Bronte, Jane Eyre");
      return;
        case 11:
      quote = F("Arthur thought he could even bear to listen to the album of bagpipe music he had won. It was * eight o'clock * and he decided he would make himself, force himself, to listen...");
      attribution = F("- Douglas Adams");
      return;
        case 12:
      quote = F("At * eight o'clock * on Thursday morning Arthur didn't feel very good. He woke up blearily, got up, wandered blearily round his room, opened a window, saw a bulldozer...");
      attribution = F("- Douglas Adams");
      return;
        case 13:
      quote = F("But for now it was still * eight o'clock *, and as I walked along the avenue under that brilliant blue sky, I was happy, my friends, as happy as any man who had ever lived.");
      attribution = F("- Brooklyn Follies");
      return;
        case 14:
      quote = F("By * eight o'clock * Stillman would come out, always in his long brown overcoat, carrying a large, old-fashioned carpet bag. For two weeks this routine did not vary.");
      attribution = F("- Paul Auster, City of Glass");
      return;
        case 15:
      quote = F("It's the twenty-third of June nineteen seventy-five, and it is * eight o'clock * in the evening, seated at his jigsaw puzzle, Bartlebooth has just died.");
      attribution = F("- Life: A User's Manual");
      return;
        case 16:
      quote = F("It was dated from Rosings, at * eight o'clock * in the morning, and was as follows: \"Be not alarmed, madam, on receiving this letter...\"");
      attribution = F("- Pride and Prejudice");
      return;
        case 17:
      quote = F("Mr. Pumblechook and I breakfasted at * eight o'clock * in the parlour behind the shop, while the shopman took his mug of tea and hunch of bread-and-butter...");
      attribution = F("- Great Expectations");
      return;
        case 18:
      quote = F("Prince Stepan Arkadyevitch Oblonsky--Stiva, as he was called in the fashionable world-- woke up at his usual hour, that is, at * eight o'clock * in the morning...");
      attribution = F("- Leo Tolstoy, Anna Karenina");
      return;
        case 19:
      quote = F("She looked at her watch- it was * eight o'clock *");
      attribution = F("- The Beautiful and Damned");
      return;
        case 20:
      quote = F("...without having done anything wrong he was arrested one fine morning. His landlady's cook, who always brought him breakfast at * eight o'clock *, failed to appear on this occasion.");
      attribution = F("- Franz Kafka, The Trial");
      return;
        case 21:
      quote = F("So here I'll watch the night and wait To see the morning shine, When he will hear the stroke of * eight * And not the stroke of nine;");
      attribution = F("- A shropshire Lad");
      return;
        case 22:
      quote = F("The clock struck * eight *. Had it been ten, Elinor would have been convinced that at that moment she heard a carriage driving up to the house.");
      attribution = F("- Sense and Sensibility");
      return;
        case 23:
      quote = F("The next morning I woke up at * oh eight oh oh hours *, my brothers, and as I still felt shagged and fagged and fashed and bashed...");
      attribution = F("- A Clockwork Orange");
      return;
      } // end switch (option)

    // Codegen for 08:01 for 2 quotations:
    case 481:
      option = random(0, 2);
      // 2 options for 08:01
      switch (option) {
        case 0:
      quote = F("It was only * a little after eight o'clock *, so all the shows were about silliness or murder.");
      attribution = F("- Slaughterhouse 5");
      return;
        case 1:
      quote = F("Eight-one, tick-tock, * eight-one * o'clock, off to school, off to work, run, run, eight-one!");
      attribution = F("- There Will Come Soft Rains");
      return;
      } // end switch (option)

    // Codegen for 08:02 for 2 quotations:
    case 482:
      option = random(0, 2);
      // 2 options for 08:02
      switch (option) {
        case 0:
      quote = F("... bingeley ... * Eight oh two * eh em, Death of Corporal Littlebottombottom ... Eight oh three eh em ... Death of Sergeant Detritus ...");
      attribution = F("- Terry Pratchett, Jingo");
      return;
        case 1:
      quote = F("Anna looked at the railway timetable in the newspapers. An evening train went at * two minutes past eight *. \"Yes, I shall be in time.\"");
      attribution = F("- Leo Tolstoy, Anna Karenina");
      return;
      } // end switch (option)

    // Codegen for 08:03 for 2 quotations:
    case 483:
      option = random(0, 2);
      // 2 options for 08:03
      switch (option) {
        case 0:
      quote = F("...the 'stupid human habit' of arbitrarily choosing a time based on fifteen-minute intervals. 'Never meet people at 7:45 or 6:30, Jasper, but pick times like 7:12 and * 8:03 *!'");
      attribution = F("- A Fraction of the Whole");
      return;
        case 1:
      quote = F("... bingeley ... Eight oh two eh em, Death of Corporal Littlebottombottom ... * Eight oh three * eh em ... Death of Sergeant Detritus ...");
      attribution = F("- Terry Pratchett, Jingo");
      return;
      } // end switch (option)

    // Codegen for 08:04 for 1 quotations:
    case 484:
      // Only one option for 08:04:"
      quote = F("...which coincided with a single pair of tram runs coming from the city: A had to come in at 8, B at * 8:04 *, C at 8:08 and so on, and the same for quitting times.");
      attribution = F("- The Periodic Table");
      return;

    // Codegen for 08:05 for 2 quotations:
    case 485:
      option = random(0, 2);
      // 2 options for 08:05
      switch (option) {
        case 0:
      quote = F("* 8.05 a.m. * Pack school bag");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("... Boston time, and saw that the minutes had sped with increasing velocity during this interview, and that it now marked * five minutes past eight *.");
      attribution = F("- Henry James, The Bostonians");
      return;
      } // end switch (option)

    // Codegen for 08:06 for 0 quotations:
    case 486:
      // No quotes for 08:06
        quote = "";
        attribution = "";
      return;

    // Codegen for 08:07 for 0 quotations:
    case 487:
      // No quotes for 08:07
        quote = "";
        attribution = "";
      return;

    // Codegen for 08:08 for 1 quotations:
    case 488:
      // Only one option for 08:08:"
      quote = F("...which coincided with a single pair of tram runs coming from the city: A had to come in at 8, B at 8:04, C at * 8:08 * and so on, and the same for quitting times.");
      attribution = F("- The Periodic Table");
      return;

    // Codegen for 08:09 for 1 quotations:
    case 489:
      // Only one option for 08:09:"
      quote = F("He followed the squeals down a hallway. A wall clock read * 8:09 * - 10:09 Dallas time.");
      attribution = F("- American Tabloid");
      return;

    // Codegen for 08:10 for 3 quotations:
    case 490:
      option = random(0, 3);
      // 3 options for 08:10
      switch (option) {
        case 0:
      quote = F("* 8.10 a.m. * Read book or watch video");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("Cell count down to 400,000. Woke * 8:10 *. To sleep 7:15. (Appear to have lost my watch without realising it, had to drive into town to buy another.)");
      attribution = F("- The Voices of Time");
      return;
        case 2:
      quote = F("Sloane, with his shoulder punctured, was on another lounge. He was half delirious, and kept calling something about a chemistry lecture at * 8:10 *.");
      attribution = F("- This Side of Paradise");
      return;
      } // end switch (option)

    // Codegen for 08:11 for 1 quotations:
    case 491:
      // Only one option for 08:11:"
      quote = F("For some reason they both wore white rosettes pinned to their dresses. I looked again at the clock: * eight-eleven *.");
      attribution = F("- The Blackpool Highflyer");
      return;

    // Codegen for 08:12 for 1 quotations:
    case 492:
      // Only one option for 08:12:"
      quote = F("At * 8:12 a.m. *, just before the moment of pff, all the business of the cellars was being transacted - garbage transferred from small cans into large ones...");
      attribution = F("- Grace Paley");
      return;

    // Codegen for 08:13 for 1 quotations:
    case 493:
      // Only one option for 08:13:"
      quote = F("At * 8:13 a.m. * the alarm clock in the laboratory gave the ringing word. Eddie touched a button in the substructure of an ordinary glass coffeepot...");
      attribution = F("- Grace Paley");
      return;

    // Codegen for 08:14 for 0 quotations:
    case 494:
      // No quotes for 08:14
        quote = "";
        attribution = "";
      return;

    // Codegen for 08:15 for 3 quotations:
    case 495:
      option = random(0, 3);
      // 3 options for 08:15
      switch (option) {
        case 0:
      quote = F("You scrutinized your wrist: \"It's * eight fifteen *. (And here time forked.) I'll turn it on.\" The screen In its blank broth evolved a lifelike blur, And music welled.");
      attribution = F("- Pale Fire");
      return;
        case 1:
      quote = F("[Puts down her candle] What's the time? Andrey: [Looks at his watch] A * quarter past eight *. Natasha: And Olga and Irina aren't in yet. The poor things are still at work.");
      attribution = F("- The Three Sisters");
      return;
        case 2:
      quote = F("... the fact that it was still very dark when George woke in the morning was no guide to him as to the time. He reached up, and hauled down his watch. It was a * quarter-past eight *.");
      attribution = F("- Three Men in a Boat");
      return;
      } // end switch (option)

    // Codegen for 08:16 for 2 quotations:
    case 496:
      option = random(0, 2);
      // 2 options for 08:16
      switch (option) {
        case 0:
      quote = F("I feel I should have something to read. Or someone to call. My fingers are itching to move. I look at my watch. Still only * eight sixteen *. Oh God.");
      attribution = F("- The Undomestic Goddess");
      return;
        case 1:
      quote = F("He kissed her hand and after a while went to get two more drinks. When he got back, it was * sixteen minutes past eight *, and Lois was humming softly along with the jukebox");
      attribution = F("- The Rotters' Club");
      return;
      } // end switch (option)

    // Codegen for 08:17 for 2 quotations:
    case 497:
      option = random(0, 2);
      // 2 options for 08:17
      switch (option) {
        case 0:
      quote = F("He consulted his instruments, and recorded: \"Monday, July 1. Chronometer, * 8.17 a.m. *; barometer, 297 in.; thermometer, 6* (43* F.). Direction, E.S.E.\"");
      attribution = F("- Jules Verne");
      return;
        case 1:
      quote = F("I lean back and watch a little speckled bird pecking the ground nearby for a while. Then I look at my watch again: * eight seventeen *. I can't do this.");
      attribution = F("- The Undomestic Goddess");
      return;
      } // end switch (option)

    // Codegen for 08:18 for 0 quotations:
    case 498:
      // No quotes for 08:18
        quote = "";
        attribution = "";
      return;

    // Codegen for 08:19 for 1 quotations:
    case 499:
      // Only one option for 08:19:"
      quote = F("I had arranged to meet the Occupational Health Officer at 10:30. I took the train from Watford Junction at * 8.19 * and arrived at London Euston seven minutes late, at 8.49.");
      attribution = F("- Jonathan Coe");
      return;

    // Codegen for 08:20 for 1 quotations:
    case 500:
      // Only one option for 08:20:"
      quote = F("When the typewriters happen to pause (* 8:20 * and other mythical hours), and there are no flights of American bombers in the sky...");
      attribution = F("- Gravity's Rainbow");
      return;

    // Codegen for 08:21 for 0 quotations:
    case 501:
      // No quotes for 08:21
        quote = "";
        attribution = "";
      return;

    // Codegen for 08:22 for 0 quotations:
    case 502:
      // No quotes for 08:22
        quote = "";
        attribution = "";
      return;

    // Codegen for 08:23 for 2 quotations:
    case 503:
      option = random(0, 2);
      // 2 options for 08:23
      switch (option) {
        case 0:
      quote = F("At * 8:23 * there seemed every chance of a lasting alliance starting between Florin and Guilder. At 8:24 the two nations were very close to war.");
      attribution = F("- The Princess Bride");
      return;
        case 1:
      quote = F("And then Wedderburn looked at his watch. \"* Twenty-three minutes past eight *. I am going up by the quarter to twelve train...\"");
      attribution = F("- HG Wells");
      return;
      } // end switch (option)

    // Codegen for 08:24 for 2 quotations:
    case 504:
      option = random(0, 2);
      // 2 options for 08:24
      switch (option) {
        case 0:
      quote = F("At 8:23 there seemed every chance of a lasting alliance starting between Florin and Guilder. At * 8:24 * the two nations were very close to war.");
      attribution = F("- The Princess Bride");
      return;
        case 1:
      quote = F("Peach checked his watch. * 8.24. * If he wasn't in a taxi in twenty minutes he'd be done for.");
      attribution = F("- Dreams of Leaving");
      return;
      } // end switch (option)

    // Codegen for 08:25 for 1 quotations:
    case 505:
      // Only one option for 08:25:"
      quote = F("She sat down in her usual seat and smiled at her husband as he sank into his own chair opposite her. She was saved. It was only * five and twenty past eight *.");
      attribution = F("- The Listerdale Mystery");
      return;

    // Codegen for 08:26 for 1 quotations:
    case 506:
      // Only one option for 08:26:"
      quote = F("It exploded much later than intended, probably a good twelve hours later, at * twenty-six minutes past eight * on Monday morning.");
      attribution = F("- The Little Drummer Girl");
      return;

    // Codegen for 08:27 for 2 quotations:
    case 507:
      option = random(0, 2);
      // 2 options for 08:27
      switch (option) {
        case 0:
      quote = F("The lecture was to be given tomorrow, and it was now * almost eight-thirty *.");
      attribution = F("- A Confederacy of Dunces");
      return;
        case 1:
      quote = F("At * seven-and-twenty minutes past eight * Mrs Lofthouse was seated at Aurora's piano, in the first agonies of a prelude in six flats...");
      attribution = F("- Aurora Floyd");
      return;
      } // end switch (option)

    // Codegen for 08:28 for 1 quotations:
    case 508:
      // Only one option for 08:28:"
      quote = F("And at * 8.28 * on the following morning, with a novel chilliness about the upper lip, and a vast excess of strength and spirits...");
      attribution = F("- The Riddle of the Sands");
      return;

    // Codegen for 08:29 for 2 quotations:
    case 509:
      option = random(0, 2);
      // 2 options for 08:29
      switch (option) {
        case 0:
      quote = F("At * 8.29 * I punched the front doorbell in Elgin Crescent. It was opened by a small oriental woman in a white apron.");
      attribution = F("- Sebastian Faulks, Engleby");
      return;
        case 1:
      quote = F("* Twenty-nine and a half minutes past eight *, sir. And then, from habit, he glanced at the clock in the tower, and made further oration.");
      attribution = F("- O. Henry, The Four Million");
      return;
      } // end switch (option)

    // Codegen for 08:30 for 6 quotations:
    case 510:
      option = random(0, 6);
      // 6 options for 08:30
      switch (option) {
        case 0:
      quote = F("On July 25th, * 8:30 a.m. * the bitch Novaya dies whelping. At 10 o'clock she is lowered into her cool grave, at 7:30 that same evening we see our first floes...");
      attribution = F("- Christoph Ransmayr");
      return;
        case 1:
      quote = F("It is around * 8:30 *. Sunshine comes through the windows at right. As the curtain rises, the family has just finished breakfast.");
      attribution = F("- Eugene O'Neill");
      return;
        case 2:
      quote = F("The lecture was to be given tomorrow, and it was now almost * eight-thirty *.");
      attribution = F("- A Confederacy of Dunces");
      return;
        case 3:
      quote = F("When he woke, at * eight-thirty *, he was alone in the bedroom. He put on his dressing gown and put in his hearing aid and went into the living room.");
      attribution = F("- David Lodge, Deaf Sentence");
      return;
        case 4:
      quote = F("At * half past eight *, Mr. Dursley picked up his briefcase, pecked Mrs. Dursley on the cheek, and tried to kiss Dudley good-bye but missed...");
      attribution = F("- JK Rowling");
      return;
        case 5:
      quote = F("Gerald read a few pages of his book. Then he glanced up at the clock and tossed the book away. \"* Half-past eight *. Time to go down to the cellar and start work.\"");
      attribution = F("- The Listerdale Mystery");
      return;
      } // end switch (option)

    // Codegen for 08:31 for 0 quotations:
    case 511:
      // No quotes for 08:31
        quote = "";
        attribution = "";
      return;

    // Codegen for 08:32 for 2 quotations:
    case 512:
      option = random(0, 2);
      // 2 options for 08:32
      switch (option) {
        case 0:
      quote = F("'anybody know the time a little more exactly is what I'm wondering, Don, since Day doesn't.' Gately checks his cheap digital, head still hung over the sofa's arm. 'I got * 0832 *:14'");
      attribution = F("- Infinite Jest");
      return;
        case 1:
      quote = F("* 8.32 a.m. * Catch bus to school");
      attribution = F("- Mark Haddon");
      return;
      } // end switch (option)

    // Codegen for 08:33 for 0 quotations:
    case 513:
      // No quotes for 08:33
        quote = "";
        attribution = "";
      return;

    // Codegen for 08:34 for 0 quotations:
    case 514:
      // No quotes for 08:34
        quote = "";
        attribution = "";
      return;

    // Codegen for 08:35 for 2 quotations:
    case 515:
      option = random(0, 2);
      // 2 options for 08:35
      switch (option) {
        case 0:
      quote = F("Old gummy granny (thrusts a dagger towards Stephen's hand) Remove him, acushla. At * 8.35 a.m. * you will be in heaven and Ireland will be free (she prays) O good God take him!");
      attribution = F("- James Joyce, Ulysses");
      return;
        case 1:
      quote = F("It was * thirty-five minutes past eight * by the big clock of the central building when Mathieu crossed the yard towards the office which he occupied as chief designer.");
      attribution = F("- Emile Zola, Fruitfulness");
      return;
      } // end switch (option)

    // Codegen for 08:36 for 0 quotations:
    case 516:
      // No quotes for 08:36
        quote = "";
        attribution = "";
      return;

    // Codegen for 08:37 for 1 quotations:
    case 517:
      // Only one option for 08:37:"
      quote = F("* Eight thirty-seven * am., Patrice Lane, Biohazard: The dog's clean. The Good Samaritan was a woman with an accent of some sort. Why haven't you called me?");
      attribution = F("- Ilona Andrews, Magic Bleeds");
      return;

    // Codegen for 08:38 for 0 quotations:
    case 518:
      // No quotes for 08:38
        quote = "";
        attribution = "";
      return;

    // Codegen for 08:39 for 1 quotations:
    case 519:
      // Only one option for 08:39:"
      quote = F("Doug McGuire noticed the early hour, * 8:39 A.M. * on the one wall clock that gave Daylight Savings Time for the East Coast.");
      attribution = F("- Terminal Compromise");
      return;

    // Codegen for 08:40 for 4 quotations:
    case 520:
      option = random(0, 4);
      // 4 options for 08:40
      switch (option) {
        case 0:
      quote = F("At this moment the clock indicated * 8.40 *. 'Five minutes more,' said Andrew Stuart. The five friends looked at each other.");
      attribution = F("- Jules Verne");
      return;
        case 1:
      quote = F("I took note of the surrounding objects in detail, and saw that her watch had stopped at * twenty minutes to nine *, and that a clock in the room had stopped at twenty minutes to nine.");
      attribution = F("- Great Expectations");
      return;
        case 2:
      quote = F("It was when I stood before her, avoiding her eyes, that I took note of the surrounding objects in detail, and saw that her watch had stopped at * twenty minutes to nine *...");
      attribution = F("- Great Expectations");
      return;
        case 3:
      quote = F("The letter had been brought in at * twenty minutes to nine *.");
      attribution = F("- The Murder of Roger Ackroyd");
      return;
      } // end switch (option)

    // Codegen for 08:41 for 1 quotations:
    case 521:
      // Only one option for 08:41:"
      quote = F("By * forty-one minutes past eight * we are five hundred yards from the water's edge, and between our road and the foot of the mountain we descry...");
      attribution = F("- Felicien de Saulcy");
      return;

    // Codegen for 08:42 for 1 quotations:
    case 522:
      // Only one option for 08:42:"
      quote = F("The hand at this moment pointed to * 8.42 *. The players took up their cards, but their eyes were constantly on the clock.");
      attribution = F("- Jules Verne");
      return;

    // Codegen for 08:43 for 2 quotations:
    case 523:
      option = random(0, 2);
      // 2 options for 08:43
      switch (option) {
        case 0:
      quote = F("* 8.43 a.m. * Go past tropical fish shop");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("You understand this tape recorder is on? \"Uh huh\" \"And it's Wednesday, May 15, at * eight forty-three * in the mornin'.\" \"If you say so\"");
      attribution = F("- A Time to Kill");
      return;
      } // end switch (option)

    // Codegen for 08:44 for 1 quotations:
    case 524:
      // Only one option for 08:44:"
      quote = F("Several soldiers - some with their uniforms unbuttoned - were looking over a motorcycle, arguing about it. The sergeant looked at his watch; it was * eight forty-four *.");
      attribution = F("- The Secret Miracle");
      return;

    // Codegen for 08:45 for 3 quotations:
    case 525:
      option = random(0, 3);
      // 3 options for 08:45
      switch (option) {
        case 0:
      quote = F("He paid the waitress and left the cafe. It was * 8:45 *. The sun pressed against the inside of a thin layer of cloud. He unbuttoned his jacket as he hurried down Queensway.");
      attribution = F("- Dreams of leaving");
      return;
        case 1:
      quote = F("It's not impossible, Phileas said quietly. \"I bet you 20,000 pounds I could do it. If I leave this evening on the * 8.45 * train to Dover, I can be back here at the Reform Club...\"");
      attribution = F("- Jules Verne");
      return;
        case 2:
      quote = F("Beaver arrived at * quarter to nine * in a state of high self-approval; he had refused two invitations for dinner while dressing that evening...");
      attribution = F("- A Handful of Dust");
      return;
      } // end switch (option)

    // Codegen for 08:46 for 1 quotations:
    case 526:
      // Only one option for 08:46:"
      quote = F("At the tone, the time will be * eight forty six *, exactly. One cubic mile of seawater contains about 50 pounds of gold.");
      attribution = F("- Tom Lichtenberg, Macedonia");
      return;

    // Codegen for 08:47 for 2 quotations:
    case 527:
      option = random(0, 2);
      // 2 options for 08:47
      switch (option) {
        case 0:
      quote = F("* 8.47 *. Bit misty on the roads");
      attribution = F("- Douglas Adams");
      return;
        case 1:
      quote = F("Just on my way to the cottage. It's, er, ..* 8.47 *. Bit misty on the roads.....");
      attribution = F("- Douglas Adams");
      return;
      } // end switch (option)

    // Codegen for 08:48 for 0 quotations:
    case 528:
      // No quotes for 08:48
        quote = "";
        attribution = "";
      return;

    // Codegen for 08:49 for 2 quotations:
    case 529:
      option = random(0, 2);
      // 2 options for 08:49
      switch (option) {
        case 0:
      quote = F("* 8.49 *. I took the phone, cleared my throat, and dialled the keep, the packs stronghold on the outskirts of Atlanta. Just keep it professional. Less pathetic that way.");
      attribution = F("- Ilona Andrews, Magic Bleeds");
      return;
        case 1:
      quote = F("I had arranged to meet the Occupational Health Officer at 10:30. I took the train from Watford Junction at 8.19 and arrived at London Euston seven minutes late, at * 8.49 *.");
      attribution = F("- Jonathan Coe");
      return;
      } // end switch (option)

    // Codegen for 08:50 for 6 quotations:
    case 530:
      option = random(0, 6);
      // 6 options for 08:50
      switch (option) {
        case 0:
      quote = F("It was * 8:50 * in the morning and Bernie and I were alone on an Astoria side street, not far from a sandwich shop that sold a sopressatta sub called \"The Bypass\".");
      attribution = F("- Sam Lipsyte, The Ask");
      return;
        case 1:
      quote = F("all the clocks in London were striking * ten minutes before nine *.");
      attribution = F("- Jules Verne");
      return;
        case 2:
      quote = F("He glanced at the bracket-clock on the mantelpiece, but as this had stopped, drew out his watch. \"It is already too late,\" he said. \"It wants only * ten minutes to nine *.\"");
      attribution = F("- The Reluctant Widow");
      return;
        case 3:
      quote = F("Punctually at * ten minutes to nine *, a quarter hour after early mass, the boy stood in his Sunday uniform outside his father's door.");
      attribution = F("- The Radetzky March");
      return;
        case 4:
      quote = F("What did it mean by beginning to tick so loudly all of a sudden? Its face indicated * ten minutes to nine *. Mrs Verloc cared nothing for time, and the ticking went on.");
      attribution = F("- The Secret Agent");
      return;
        case 5:
      quote = F("At * ten to nine * the clerks began to arrive. When they had hung up their coats and hates they came to the fireplace and stood warming themselves.");
      attribution = F("- The Chestnut Tree");
      return;
      } // end switch (option)

    // Codegen for 08:51 for 1 quotations:
    case 531:
      // Only one option for 08:51:"
      quote = F("* 8.51 a.m. * Arrive at school");
      attribution = F("- Mark Haddon");
      return;

    // Codegen for 08:52 for 1 quotations:
    case 532:
      // Only one option for 08:52:"
      quote = F("Message one. Tuesday, * 8.52am. * Is anybody there? Hello?");
      attribution = F("- Jonathan Safran Foer");
      return;

    // Codegen for 08:53 for 1 quotations:
    case 533:
      // Only one option for 08:53:"
      quote = F("Only * eight fifty-three *. The partners' decision meeting starts in seven minutes. I'm not sure I can bear this.");
      attribution = F("- The Undomestic Goddess");
      return;

    // Codegen for 08:54 for 1 quotations:
    case 534:
      // Only one option for 08:54:"
      quote = F("It was Mrs. Poppets that woke me up next morning. She said: \"Do you know that it's * nearly nine o'clock *, sir?\" \"Nine o' what?\" I cried, starting up.");
      attribution = F("- Three Men in a Boat");
      return;

    // Codegen for 08:55 for 2 quotations:
    case 535:
      option = random(0, 2);
      // 2 options for 08:55
      switch (option) {
        case 0:
      quote = F("At * five minutes to nine *, Jacques, in his gray butler's livery, came down the stairs and said, \"Young master, your Herr Papa is coming.\"");
      attribution = F("- The Radetzky March");
      return;
        case 1:
      quote = F("George pulled out his watch and looked at it: it was * five minutes to nine *!");
      attribution = F("- Three Men in a Boat");
      return;
      } // end switch (option)

    // Codegen for 08:56 for 2 quotations:
    case 536:
      option = random(0, 2);
      // 2 options for 08:56
      switch (option) {
        case 0:
      quote = F("No. 7 berth-a second-class. The gentleman has not yet come, and it is * four minutes to nine *.");
      attribution = F("- Agatha Christie");
      return;
        case 1:
      quote = F("It was * nearly nine o'clock * and the sun was fiercer every minute.'");
      attribution = F("- George Orwell, Burmese Days");
      return;
      } // end switch (option)

    // Codegen for 08:57 for 2 quotations:
    case 537:
      option = random(0, 2);
      // 2 options for 08:57
      switch (option) {
        case 0:
      quote = F("... they should cause to be rung a bell from its smaller bell-tower for * three minutes before nine * o'clock every morning, all the year round.");
      attribution = F("- The Paradise Mystery");
      return;
        case 1:
      quote = F("they're a nuisance, anyway--always ticking and striking and getting you late for dinner. He glanced again at the clock in the tower. The hands stood at * three minutes to nine *.");
      attribution = F("- O. Henry, The Four Million");
      return;
      } // end switch (option)

    // Codegen for 08:58 for 2 quotations:
    case 538:
      option = random(0, 2);
      // 2 options for 08:58
      switch (option) {
        case 0:
      quote = F("It was * two minutes of nine * now - two minutes before the bombs were set to explode - and three or four people were gathered in front of the bank waiting for it to open.");
      attribution = F("- Jim Thompson, The Getaway");
      return;
        case 1:
      quote = F("What time is it? she asked, quiet, definite, hopeless. \"* Two minutes to nine *,\" he replied, telling the truth with a struggle.");
      attribution = F("- Sons and Lovers");
      return;
      } // end switch (option)

    // Codegen for 08:59 for 1 quotations:
    case 539:
      // Only one option for 08:59:"
      quote = F("The reading lamp by the bed had been on all night. The green glowing digits on her desk alarm clock showed * 8:59 *.");
      attribution = F("- Sophie's World");
      return;

    // Codegen for 09:00 for 18 quotations:
    case 540:
      option = random(0, 18);
      // 18 options for 09:00
      switch (option) {
        case 0:
      quote = F("14 June * 9:00 am * woke up");
      attribution = F("- A Fraction of the Whole");
      return;
        case 1:
      quote = F("* 9.00 a.m. * School assembly");
      attribution = F("- Mark Haddon");
      return;
        case 2:
      quote = F("Sometimes what I wouldn't give to have us sitting in a bar again at * 9.00am * telling lies to one another, far from God.");
      attribution = F("- Denis Johnson, Jesus' Son");
      return;
        case 3:
      quote = F("He was at breakfast * at nine *, and for the twentieth time consulted his \"Bradshaw,\" to see at what earliest hour Dr. Grantly could arrive from Barchester.");
      attribution = F("- The Warden");
      return;
        case 4:
      quote = F("To be on the safe side, he did not unpack everything. * At nine * he went to breakfast in the specially designated buffet between the lobby and the dining room.");
      attribution = F("- Death in Venice");
      return;
        case 5:
      quote = F("A fly buzzed, the wall clock began to strike. After the * nine * golden strokes faded, the district captain began. \"How is Herr Colonel Marek?\" \"Thank you, Papa, he's fine.\"");
      attribution = F("- The Radetzky March");
      return;
        case 6:
      quote = F("Flowed up the hill and down King William Street, To where Saint Mary Woolnoth kept the hours With a dead sound on the final stroke of * nine *.");
      attribution = F("- T S Eliot, The Waste Land");
      return;
        case 7:
      quote = F("As * nine o' clock * was left behind, the preposterousness of the delay overwhelmed me, and I went in a kind of temper to the owner and said...");
      attribution = F("- A Single Pebble");
      return;
        case 8:
      quote = F("At * nine o'clock *, one morning late in July, Gatsby's gorgeous car lurched up the rocky drive to my door and gave out a burst of melody from its three-noted horn");
      attribution = F("- The Great Gatsby");
      return;
        case 9:
      quote = F("...he'd do almost anything you liked with the clock. For instance, suppose it were * nine o'clock * in the morning, just time to begin lessons");
      attribution = F("- Lewis Carroll");
      return;
        case 10:
      quote = F("'I could never get all the way down there before * nine o'clock *.'");
      attribution = F("- A Confederacy of Dunces");
      return;
        case 11:
      quote = F("It was around * nine o'clock * that I crossed the border into Cornwall. This was at least three hours before the rain began and the clouds were still all of a brilliant white.");
      attribution = F("- The Remains of the Day");
      return;
        case 12:
      quote = F("'Look. Ignatius. I'm beat. I've been on the road since * nine o'clock * yesterday morning.'");
      attribution = F("- A Confederacy of Dunces");
      return;
        case 13:
      quote = F("On the stroke of * nine o'clock * Mr. and Mrs. De Voted took their places on either side of the drawing-room fire, in attitudes of gracefully combined hospitality and unconcern...");
      attribution = F("- A Marriage of Passion");
      return;
        case 14:
      quote = F("Shortly after * nine o'clock * that evening, Weyrother drove with his plans to Kutuzov's quarters where the council of war was to be held.");
      attribution = F("- Leo Tolstoy, War and Peace");
      return;
        case 15:
      quote = F("On the third morning after their arrival, just as all the clocks in the city were striking * nine * individually, and somewhere about nine hundred and ninety-nine collectively...");
      attribution = F("- The Pickwick Papers");
      return;
        case 16:
      quote = F("The clock struck * nine * when I did send the nurse; In half an hour she promised to return. Perchance she cannot meet him: that's not so.");
      attribution = F("- Romeo and Juliet");
      return;
        case 17:
      quote = F("To where Saint Mary Woolnoth kept the hours With a dead sound on the final stroke of * nine *.");
      attribution = F("- T S Eliot, The Waste Land");
      return;
      } // end switch (option)

    // Codegen for 09:01 for 1 quotations:
    case 541:
      // Only one option for 09:01:"
      quote = F("* 9:01am * lay in bed, staring at ceiling.");
      attribution = F("- A Fraction of the Whole");
      return;

    // Codegen for 09:02 for 1 quotations:
    case 542:
      // Only one option for 09:02:"
      quote = F("* 9:02am * lay in bed, staring at ceiling.");
      attribution = F("- A Fraction of the Whole");
      return;

    // Codegen for 09:03 for 3 quotations:
    case 543:
      option = random(0, 3);
      // 3 options for 09:03
      switch (option) {
        case 0:
      quote = F("* 9:03am * lay in bed, staring at ceiling.");
      attribution = F("- A Fraction of the Whole");
      return;
        case 1:
      quote = F("...with a change at Swindon on the way, and by the time he got to Bath it was * about nine o'clock * in the evening and the moon was coming up...");
      attribution = F("- Roald Dahl, The Landlady");
      return;
        case 2:
      quote = F("...bell had rung as I was crossing the playground, but you always get five minutes to get to your classroom. \"It's * three minutes past nine *,\" Miss Beckworth announced.");
      attribution = F("- The Lottie Project");
      return;
      } // end switch (option)

    // Codegen for 09:04 for 2 quotations:
    case 544:
      option = random(0, 2);
      // 2 options for 09:04
      switch (option) {
        case 0:
      quote = F("* 9:04am * lay in bed, staring at ceiling");
      attribution = F("- A Fraction of the Whole");
      return;
        case 1:
      quote = F("In the light of a narrow-beam lantern, Pierce checked his watch. It was * 9.04 *.");
      attribution = F("- The Great Train Robbery");
      return;
      } // end switch (option)

    // Codegen for 09:05 for 4 quotations:
    case 545:
      option = random(0, 4);
      // 4 options for 09:05
      switch (option) {
        case 0:
      quote = F("* 9:05am * lay in bed, staring at ceiling");
      attribution = F("- A Fraction of the Whole");
      return;
        case 1:
      quote = F("The tour of the office doesn't take that long. In fact, we're pretty much done by * 9:05 a.m. *, and even though it's just a room with a window...");
      attribution = F("- Twenties Girl");
      return;
        case 2:
      quote = F("Kaldren pursues me like luminescent shadow. He has chalked up on the gateway '96,688,365,498,702'. Should confuse the mail man. Woke * 9:05 *. To sleep 6:36.");
      attribution = F("- The Voices of Time");
      return;
        case 3:
      quote = F("* Nine-five *. A voice spoke from the study ceiling: \"Mrs. McClellan, which poem would you like this evening?\". The house was silent.");
      attribution = F("- There will come soft rains");
      return;
      } // end switch (option)

    // Codegen for 09:06 for 1 quotations:
    case 546:
      // Only one option for 09:06:"
      quote = F("* 9:06am * lay in bed, staring at ceiling");
      attribution = F("- A Fraction of the Whole");
      return;

    // Codegen for 09:07 for 2 quotations:
    case 547:
      option = random(0, 2);
      // 2 options for 09:07
      switch (option) {
        case 0:
      quote = F("* 9:07am * lay in bed, staring at ceiling");
      attribution = F("- A Fraction of the Whole");
      return;
        case 1:
      quote = F("It was a sparkling morning, * 9:07 * by the clock when Mrs. Flett stepped aboard the Imperial Limited at the Tyndall station, certain that her life was ruined...");
      attribution = F("- The Stone Diaries");
      return;
      } // end switch (option)

    // Codegen for 09:08 for 1 quotations:
    case 548:
      // Only one option for 09:08:"
      quote = F("* 9.08am * rolled over onto left side.");
      attribution = F("- A Fraction of the Whole");
      return;

    // Codegen for 09:09 for 2 quotations:
    case 549:
      option = random(0, 2);
      // 2 options for 09:09
      switch (option) {
        case 0:
      quote = F("* 9.09 *. Too late to turn around and go back. Too late, too dangerous.");
      attribution = F("- Dreams of Leaving");
      return;
        case 1:
      quote = F("* 9.09am * lay in bed, staring at wall.");
      attribution = F("- A Fraction of the Whole");
      return;
      } // end switch (option)

    // Codegen for 09:10 for 1 quotations:
    case 550:
      // Only one option for 09:10:"
      quote = F("* 9.10am * lay in bed, staring at wall.");
      attribution = F("- A Fraction of the Whole");
      return;

    // Codegen for 09:11 for 1 quotations:
    case 551:
      // Only one option for 09:11:"
      quote = F("* 9:11am * lay in bed, staring at wall");
      attribution = F("- A Fraction of the Whole");
      return;

    // Codegen for 09:12 for 1 quotations:
    case 552:
      // Only one option for 09:12:"
      quote = F("* 9.12am * lay in bed, staring at wall.");
      attribution = F("- A Fraction of the Whole");
      return;

    // Codegen for 09:13 for 2 quotations:
    case 553:
      option = random(0, 2);
      // 2 options for 09:13
      switch (option) {
        case 0:
      quote = F("* 9:13am * lay in bed, staring at wall");
      attribution = F("- A Fraction of the Whole");
      return;
        case 1:
      quote = F("She tucked the phone in the crook of her neck and thumbed hurriedly through her pink messages. Dr. Provetto, at * 9:13 A.M. *");
      attribution = F("- Mistaken Identity");
      return;
      } // end switch (option)

    // Codegen for 09:14 for 1 quotations:
    case 554:
      // Only one option for 09:14:"
      quote = F("* 9.14am * lay in bed, staring at wall.");
      attribution = F("- A Fraction of the Whole");
      return;

    // Codegen for 09:15 for 5 quotations:
    case 555:
      option = random(0, 5);
      // 5 options for 09:15
      switch (option) {
        case 0:
      quote = F("* 9.15 *. Did Roberts pay you yet?");
      attribution = F("- James Joyce, Ulysses");
      return;
        case 1:
      quote = F("* 9:15am * doubled over pillow, sat up to see out window");
      attribution = F("- A Fraction of the Whole");
      return;
        case 2:
      quote = F("* 9.15 a.m. * First morning class");
      attribution = F("- Mark Haddon");
      return;
        case 3:
      quote = F("That's all right. Okay. Jones pulled a handful of pencils from his back pocket. \"Now, I got the contact first at 0* 915 * or so, and the bearing was about two-six-nine.\"");
      attribution = F("- The Hunt for Red October");
      return;
        case 4:
      quote = F("Miss Pettigrew pushed open the door of the employment agency and went in as the clock struck a * quarter past nine *.");
      attribution = F("- Winifred Watson");
      return;
      } // end switch (option)

    // Codegen for 09:16 for 1 quotations:
    case 556:
      // Only one option for 09:16:"
      quote = F("* 9.16am * sat in bed, staring out window.");
      attribution = F("- A Fraction of the Whole");
      return;

    // Codegen for 09:17 for 1 quotations:
    case 557:
      // Only one option for 09:17:"
      quote = F("* 9.17am * sat in bed, staring out window.");
      attribution = F("- A Fraction of the Whole");
      return;

    // Codegen for 09:18 for 1 quotations:
    case 558:
      // Only one option for 09:18:"
      quote = F("* 9.18am * sat in bed, staring out window.");
      attribution = F("- A Fraction of the Whole");
      return;

    // Codegen for 09:19 for 1 quotations:
    case 559:
      // Only one option for 09:19:"
      quote = F("* 9.19am * sat in bed, staring out window.");
      attribution = F("- A Fraction of the Whole");
      return;

    // Codegen for 09:20 for 3 quotations:
    case 560:
      option = random(0, 3);
      // 3 options for 09:20
      switch (option) {
        case 0:
      quote = F("The following morning at * 9.20 * Mr Cribbage straightened his greasy old tie, combed his Hitler moustache and arranged the few strands of his hair across his bald patch.");
      attribution = F("- Louis de Bernieres, Red Dog");
      return;
        case 1:
      quote = F("I'll compromise by saying that I left home at eight and spent an hour travelling to a nine o'clock appointment. Twenty minutes later is * nine-twenty *.");
      attribution = F("- Girl, Interrupted");
      return;
        case 2:
      quote = F("At * twenty minutes past nine *, the Duke of Dunstable, who had dined off a tray in his room, was still there, waiting for his coffee and liqueur.");
      attribution = F("- P.G. Wodehouse");
      return;
      } // end switch (option)

    // Codegen for 09:21 for 1 quotations:
    case 561:
      // Only one option for 09:21:"
      quote = F("It was * nine twenty-one *. With one minute to go, there was no sign of Herbert's mother.");
      attribution = F("- Dan Rhodes, This is Life");
      return;

    // Codegen for 09:22 for 1 quotations:
    case 562:
      // Only one option for 09:22:"
      quote = F("No more throwing stones at him, and I'll see you back here exactly one week from now. She looked at her watch. 'At * nine twenty-two * next Wednesday.'");
      attribution = F("- Dan Rhodes, This is Life");
      return;

    // Codegen for 09:23 for 1 quotations:
    case 563:
      // Only one option for 09:23:"
      quote = F("* 9.23 *. What possessed me to buy this comb?");
      attribution = F("- James Joyce, Ulysses");
      return;

    // Codegen for 09:24 for 1 quotations:
    case 564:
      // Only one option for 09:24:"
      quote = F("* 9.24 * I'm swelled after that cabbage. A speck of dust on the patent leather of her boot.");
      attribution = F("- James Joyce, Ulysses");
      return;

    // Codegen for 09:25 for 1 quotations:
    case 565:
      // Only one option for 09:25:"
      quote = F("A man I would cross the street to avoid at nine o'clock - by * nine twenty-five * I wanted to fuck him until he wept. My legs trembled with it.");
      attribution = F("- The Forgotten Waltz");
      return;

    // Codegen for 09:26 for 0 quotations:
    case 566:
      // No quotes for 09:26
        quote = "";
        attribution = "";
      return;

    // Codegen for 09:27 for 1 quotations:
    case 567:
      // Only one option for 09:27:"
      quote = F("From twenty minutes past nine until * twenty-seven minutes past nine *, the heroes of the school met in a large familiarity");
      attribution = F("- Sinister Street");
      return;

    // Codegen for 09:28 for 2 quotations:
    case 568:
      option = random(0, 2);
      // 2 options for 09:28
      switch (option) {
        case 0:
      quote = F("From that moment on--* 9:28 * in the evening, June 18, 1941--everything was different.");
      attribution = F("- Everything is Illuminated");
      return;
        case 1:
      quote = F("This clock right? he asked the butler in the hall. \"Yes, sir.\" The clock showed * twenty-eight minutes past nine *. \"The clocks here have to be right, sir.\"");
      attribution = F("- Arnold Bennett, Lord Raingo");
      return;
      } // end switch (option)

    // Codegen for 09:29 for 0 quotations:
    case 569:
      // No quotes for 09:29
        quote = "";
        attribution = "";
      return;

    // Codegen for 09:30 for 4 quotations:
    case 570:
      option = random(0, 4);
      // 4 options for 09:30
      switch (option) {
        case 0:
      quote = F("Up the welcomingly warm morning hill we trudge, side by each, bound finally for the Hall of Fame. It's * 9.30 *, and time is in fact a-wastin'.");
      attribution = F("- Independence Day");
      return;
        case 1:
      quote = F("he looked at his watch; it was * half-past nine *");
      attribution = F("- A watcher by the dead");
      return;
        case 2:
      quote = F("It was * nine-thirty *. In another ten minutes she would turn off the heat; then it would take a while for the water to cool. In the meantime there was nothing to do but wait.");
      attribution = F("- Revolutionary Road");
      return;
        case 3:
      quote = F("The body came in at * nine-thirty * this morning. One of Holding's men went to the house and collected it. There was nothing particularly unusual about the death.");
      attribution = F("- Jackie Kay, Trumpet");
      return;
      } // end switch (option)

    // Codegen for 09:31 for 1 quotations:
    case 571:
      // Only one option for 09:31:"
      quote = F("On the table was a note from my girlfriend: \"Gone out to eat. Back by 9:30.\" The digital clock on the table read 9:30. I watched it flip over to * 9:31 *, then to 9:32.\"");
      attribution = F("- A Wild Sheep Chase");
      return;

    // Codegen for 09:32 for 3 quotations:
    case 572:
      option = random(0, 3);
      // 3 options for 09:32
      switch (option) {
        case 0:
      quote = F("Anyhow, if he wasn't the 11.5 for Kingston, he said he was pretty confident he was the * 9.32 * for Virginia Water, or the 10 a.m. express for the Isle of Wight...");
      attribution = F("- Three Men in a Boat");
      return;
        case 1:
      quote = F("On the table was a note from my girlfriend: \"Gone out to eat. Back by 9:30.\" The digital clock on the table read 9:30. I watched it flip over to 9:31, then to * 9:32 *.\"");
      attribution = F("- A Wild Sheep Chase");
      return;
        case 2:
      quote = F("Sandy barely made the * nine-thirty-two * and found a seat in no-smoking. She'd been looking forward to this visit with Lisbeth.");
      attribution = F("- Judy Blume, Wifey");
      return;
      } // end switch (option)

    // Codegen for 09:33 for 1 quotations:
    case 573:
      // Only one option for 09:33:"
      quote = F("...the half-ebb at a quarter past seven, low water at * thirty-three minutes past nine *, and half flood at thirty-nine minutes past twelve.");
      attribution = F("- The Toilers of the Sea");
      return;

    // Codegen for 09:34 for 0 quotations:
    case 574:
      // No quotes for 09:34
        quote = "";
        attribution = "";
      return;

    // Codegen for 09:35 for 1 quotations:
    case 575:
      // Only one option for 09:35:"
      quote = F("* Nine-thirty-five *. He really must be gone. The bird is no longer feeding but sitting at the apex of a curl of razor wire.");
      attribution = F("- The Memory of Love");
      return;

    // Codegen for 09:36 for 2 quotations:
    case 576:
      option = random(0, 2);
      // 2 options for 09:36
      switch (option) {
        case 0:
      quote = F("9:42-10:00 Clean bathrooms I get to the end and read it over with a fresh surge of optimism. At this rate I should be done easily by lunchtime. * 9:36 * Fuck. I cannot make this bed.");
      attribution = F("- The Undomestic Goddess");
      return;
        case 1:
      quote = F("Monday February 6th. '* 9.36am. * Oh god, Oh god. Maybe he's fallen in love in New York and stayed there'.");
      attribution = F("- Bridget Jones Diary");
      return;
      } // end switch (option)

    // Codegen for 09:37 for 1 quotations:
    case 577:
      // Only one option for 09:37:"
      quote = F("...all the details of service, the tea and toast at twenty-three minutes past eight, the shaving-water at * thirty-seven minutes past nine *...");
      attribution = F("- Around the World in 80 days");
      return;

    // Codegen for 09:38 for 0 quotations:
    case 578:
      // No quotes for 09:38
        quote = "";
        attribution = "";
      return;

    // Codegen for 09:39 for 0 quotations:
    case 579:
      // No quotes for 09:39
        quote = "";
        attribution = "";
      return;

    // Codegen for 09:40 for 2 quotations:
    case 580:
      option = random(0, 2);
      // 2 options for 09:40
      switch (option) {
        case 0:
      quote = F("Just the sort of irritating thing you don't expect to hang over your final exit. (Moral: don't try too hard to forget Eniwetok.) Woke * 9:40 *. To sleep 4:15.");
      attribution = F("- The Voices of Time");
      return;
        case 1:
      quote = F("... the tea and toast at twenty-three minutes past eight, the shaving-water at thirty-seven minutes past nine, and the toilet at * twenty minutes before ten *.");
      attribution = F("- Around the World in 80 days");
      return;
      } // end switch (option)

    // Codegen for 09:41 for 0 quotations:
    case 581:
      // No quotes for 09:41
        quote = "";
        attribution = "";
      return;

    // Codegen for 09:42 for 1 quotations:
    case 582:
      // Only one option for 09:42:"
      quote = F("9:30-9:36 Make Geigers' bed 9:36-* 9:42 * Take laundry out of machine and put in dryer 9:42-10:00 Clean bathrooms I get to the end and read it over with a fresh surge of optimism.");
      attribution = F("- The Undomestic Goddess");
      return;

    // Codegen for 09:43 for 0 quotations:
    case 583:
      // No quotes for 09:43
        quote = "";
        attribution = "";
      return;

    // Codegen for 09:44 for 0 quotations:
    case 584:
      // No quotes for 09:44
        quote = "";
        attribution = "";
      return;

    // Codegen for 09:45 for 1 quotations:
    case 585:
      // Only one option for 09:45:"
      quote = F("9.15, 9.30, * 9.45 *, 10! Bond felt the excitement ball up inside him like cat's fur.");
      attribution = F("- Ian Fleming");
      return;

    // Codegen for 09:46 for 0 quotations:
    case 586:
      // No quotes for 09:46
        quote = "";
        attribution = "";
      return;

    // Codegen for 09:47 for 2 quotations:
    case 587:
      option = random(0, 2);
      // 2 options for 09:47
      switch (option) {
        case 0:
      quote = F("Monday February 6th. '* 9.47am. * Or gone to Las Vegas and got married'.");
      attribution = F("- Bridget Jones Diary");
      return;
        case 1:
      quote = F("For Hunter, who was trained to note times exactly, the final emergency started at * thirteen minutes to ten *.");
      attribution = F("- Elegy for a Revolutionary");
      return;
      } // end switch (option)

    // Codegen for 09:48 for 0 quotations:
    case 588:
      // No quotes for 09:48
        quote = "";
        attribution = "";
      return;

    // Codegen for 09:49 for 0 quotations:
    case 589:
      // No quotes for 09:49
        quote = "";
        attribution = "";
      return;

    // Codegen for 09:50 for 2 quotations:
    case 590:
      option = random(0, 2);
      // 2 options for 09:50
      switch (option) {
        case 0:
      quote = F("* 9.50am. * Hmmm. Think will go inspect make-up in case he does come in");
      attribution = F("- Bridget Jones Diary");
      return;
        case 1:
      quote = F("* Ten minutes to ten. * I had just time to hide the bottle (after the nurse had left me) when you came into my room.");
      attribution = F("- The Law and the Lady");
      return;
      } // end switch (option)

    // Codegen for 09:51 for 0 quotations:
    case 591:
      // No quotes for 09:51
        quote = "";
        attribution = "";
      return;

    // Codegen for 09:52 for 1 quotations:
    case 592:
      // Only one option for 09:52:"
      quote = F("She caught the * 9:52 * to Victoria. I kept well clear of her on the train and picked her up as she went through the barrier. Then she took a taxi to Hammersmith.");
      attribution = F("- Call for the Dead");
      return;

    // Codegen for 09:53 for 1 quotations:
    case 593:
      // Only one option for 09:53:"
      quote = F("The bus deposited her about five minutes' walk from Onslow Mansions, an at * seven minutes to ten * precisely she was outside her destination.");
      attribution = F("- Winifred Watson");
      return;

    // Codegen for 09:54 for 1 quotations:
    case 594:
      // Only one option for 09:54:"
      quote = F("* 9:54 * This is sheer torture. My arms have never ached so much in my entire life.");
      attribution = F("- The Undomestic Goddess");
      return;

    // Codegen for 09:55 for 2 quotations:
    case 595:
      option = random(0, 2);
      // 2 options for 09:55
      switch (option) {
        case 0:
      quote = F("your punctuality really alarms me. What do I say? punctuality! You, whom I expected last, you arrive at * five minutes to ten *, when the time fixed was half-past!");
      attribution = F("- The Count of Monte Cristo");
      return;
        case 1:
      quote = F("At * five to ten * I'm ready in the hall. Nathaniel's mother's house is nearby but apparently tricky to find, so the plan is to meet here and he'll walk me over.");
      attribution = F("- The Undomestic Goddess");
      return;
      } // end switch (option)

    // Codegen for 09:56 for 0 quotations:
    case 596:
      // No quotes for 09:56
        quote = "";
        attribution = "";
      return;

    // Codegen for 09:57 for 0 quotations:
    case 597:
      // No quotes for 09:57
        quote = "";
        attribution = "";
      return;

    // Codegen for 09:58 for 1 quotations:
    case 598:
      // Only one option for 09:58:"
      quote = F("I didn't sleep too long, because I think it was only * around ten o'clock * when I woke up.");
      attribution = F("- Catcher in the Rye");
      return;

    // Codegen for 09:59 for 1 quotations:
    case 599:
      // Only one option for 09:59:"
      quote = F("* One minute to ten. * With a heavy heart Bert watched the clock. His legs were still aching very badly. He could not see the hands of the clock moving...");
      attribution = F("- Robert Tressell");
      return;

    // Codegen for 10:00 for 18 quotations:
    case 600:
      option = random(0, 18);
      // 18 options for 10:00
      switch (option) {
        case 0:
      quote = F("The written verdict was handed down at * 10:00 * on Friday morning, and all that remained was a summing up from the reporters waiting in the corridor outside the district court.");
      attribution = F("- Stieg Larsson");
      return;
        case 1:
      quote = F("According to military records no US bombers or any other kind of aircraft were flying over that region at the time, that is around * 10 am * on November 7,1944.");
      attribution = F("- Kafka on the shore");
      return;
        case 2:
      quote = F("Monday 30 March 1668 Up betimes, and so to the office, there to do business till about * 10 o'clock *");
      attribution = F("- The Diary of Samuel Pepys");
      return;
        case 3:
      quote = F("On July 25th, 8:30 a.m. the bitch Novaya dies whelping. At * 10 o'clock * she is lowered into her cool grave, at 7:30 that same evening we see our first floes...");
      attribution = F("- Christoph Ransmayr");
      return;
        case 4:
      quote = F("'Tis but * an hour ago since it was nine *, And after one hour more 'twill be eleven.");
      attribution = F("- As You Like It");
      return;
        case 5:
      quote = F("Each of us saved up sixpence. We were to meet * at ten * in the morning on the Canal Bridge.");
      attribution = F("- James Joyce, Dubliners");
      return;
        case 6:
      quote = F("By * ten *, Quoyle was drunk. The crowd was enormous, crushed together so densely that Nutbeem could not force his way down the hall...");
      attribution = F("- The Shipping News");
      return;
        case 7:
      quote = F("King Richard: Well, but what's o'clock? Buckingham: Upon the stroke of * ten *.");
      attribution = F("- Richard III");
      return;
        case 8:
      quote = F("At about * ten o'clock * in the morning the sun threw a bright dust-laden bar through one of the side windows, and in and out of the beam flies shot like rushing stars.");
      attribution = F("- Of Mice And Men");
      return;
        case 9:
      quote = F("By * ten o'clock *, the chaise and four conveyed the two from the abbey; and, after an agreeable drive of almost twenty miles, they entered Woodston...");
      attribution = F("- Northanger Abbey");
      return;
        case 10:
      quote = F("Florence set down her knife and reached for Edward's hand and squeezed. From downstairs they heard the wireless, the chimes of Big Ben at the start of the * ten o'clock * news.");
      attribution = F("- Ian McEwan, On Chesil Beach");
      return;
        case 11:
      quote = F("Her body asserted itself with a restless movement of the knee, and she stood up. \"* Ten o'clock *,\" she remarked, apparently finding the time on the ceiling.");
      attribution = F("- The Great Gatsby");
      return;
        case 12:
      quote = F("Orlando leapt as if she had been violently struck on the head. Ten times she was struck. In fact it was * ten o'clock in the morning *. It was the eleventh of October. It was 1928.");
      attribution = F("- Virginia Woolf, Orlando");
      return;
        case 13:
      quote = F("I went to bed and the next thing I knew I was awake again and it was getting on for * ten o' clock * in the morning. Ring, ring, said the telephone, ring, ring.");
      attribution = F("- The Medusa Frequency");
      return;
        case 14:
      quote = F("The Saturday immediately preceding the examinations was a very busy day for Kennedy. At * ten o' clock * he was entering Willey's room; the latter had given him a key...");
      attribution = F("- Barry Unsworth");
      return;
        case 15:
      quote = F("...who appeared to have nothing to do on this particular evening; we simply could not get rid of him without being rude; he hung about until * ten o'clock *.");
      attribution = F("- Anne Frank");
      return;
        case 16:
      quote = F("The grandfather clock in the State Room strikes * ten * times.");
      attribution = F("- David Mitchell");
      return;
        case 17:
      quote = F("...as always falls out when a man is in the most haste, -* 'twas ten o'clock *, which was half an hour later than his usual time before my uncle Toby sallied out.");
      attribution = F("- Tristram Shandy");
      return;
      } // end switch (option)

    // Codegen for 10:01 for 1 quotations:
    case 601:
      // Only one option for 10:01:"
      quote = F("At * about ten o'clock * in the morning the sun threw a bright dust-laden bar through one of the side windows, and in and out of the beam flies shot like rushing stars.'");
      attribution = F("- Of Mice And Men");
      return;

    // Codegen for 10:02 for 1 quotations:
    case 602:
      // Only one option for 10:02:"
      quote = F("It was * two minutes after ten *; she was not satisfied with her clothes, her face, her apartment. She heated the coffee again and sat down in the chair by the window.");
      attribution = F("- The Daemon Lover");
      return;

    // Codegen for 10:03 for 2 quotations:
    case 603:
      option = random(0, 2);
      // 2 options for 10:03
      switch (option) {
        case 0:
      quote = F("It's * 10.03 * according to his watch, and he is travelling down through the Scottish highlands to Inverness, tired and ever-so-slightly anxious in case he falls asleep...");
      attribution = F("- The Farenheit Twins");
      return;
        case 1:
      quote = F("...the time was twenty three minutes past ten, so he tapped in the numbers 10.23. At least, that's what he meant to do. In fact he typed in the numbers * 10.03 *.");
      attribution = F("- Andrew Norriss, Ctrl-Z");
      return;
      } // end switch (option)

    // Codegen for 10:04 for 0 quotations:
    case 604:
      // No quotes for 10:04
        quote = "";
        attribution = "";
      return;

    // Codegen for 10:05 for 1 quotations:
    case 605:
      // Only one option for 10:05:"
      quote = F("Then I glance at my watch. It's already * five past ten *. \"We should get going,\" I say with a little start. \"Your mother will be waiting.\"");
      attribution = F("- The Undomestic Goddess");
      return;

    // Codegen for 10:06 for 0 quotations:
    case 606:
      // No quotes for 10:06
        quote = "";
        attribution = "";
      return;

    // Codegen for 10:07 for 1 quotations:
    case 607:
      // Only one option for 10:07:"
      quote = F("* 10.07 am *: In a meeting with Rod, Momo and Guy. We are rehearsing the final for the third time, with Rod and Guy taking the parts of the clients...");
      attribution = F("- Allison Pearson");
      return;

    // Codegen for 10:08 for 1 quotations:
    case 608:
      // Only one option for 10:08:"
      quote = F("My watch is always a little fast, I said. \"What time do you make it now?\" \"* Ten eight *.\" \"Ten eighteen by mine. You see.\"");
      attribution = F("- The Quiet American");
      return;

    // Codegen for 10:09 for 1 quotations:
    case 609:
      // Only one option for 10:09:"
      quote = F("He followed the squeals down a hallway. A wall clock read 8:09-* 10:09 * Dallas time.");
      attribution = F("- American Tabloid");
      return;

    // Codegen for 10:10 for 2 quotations:
    case 610:
      option = random(0, 2);
      // 2 options for 10:10
      switch (option) {
        case 0:
      quote = F("* 10:10 * Shot is fired.");
      attribution = F("- The Hollow Man");
      return;
        case 1:
      quote = F("Saturday morning was bright and sunny, and at * ten minutes past 10 * Donald arrived at the Embankment entrance of Charing Cross Underground Station...");
      attribution = F("- England, Their England");
      return;
      } // end switch (option)

    // Codegen for 10:11 for 2 quotations:
    case 611:
      option = random(0, 2);
      // 2 options for 10:11
      switch (option) {
        case 0:
      quote = F("Therefore a sergeant called Trifonov had been on post all day or all week and then he had left at * eleven minutes past ten * in the evening.");
      attribution = F("- Lee Child, The Enemy");
      return;
        case 1:
      quote = F("...those of the ormolu clock in the sitting-room to * eleven minutes past ten *; those of the carriage clock on the bookshelf to fourteen minutes to six.");
      attribution = F("- Three Men and a Maid");
      return;
      } // end switch (option)

    // Codegen for 10:12 for 2 quotations:
    case 612:
      option = random(0, 2);
      // 2 options for 10:12
      switch (option) {
        case 0:
      quote = F("He stood up once, early on, to lock his office door, and then he was reading the last page, and it was exactly * 10:12 a.m. *, and the sun beating...");
      attribution = F("- Jonathan Franzen, Freedom");
      return;
        case 1:
      quote = F("I'll take the coffee tray out, I suggest humbly. As I pick it up I glance again at my watch. * Ten twelve *. I wonder if they've started the meeting.");
      attribution = F("- The Undomestic Goddess");
      return;
      } // end switch (option)

    // Codegen for 10:13 for 1 quotations:
    case 613:
      // Only one option for 10:13:"
      quote = F("And I sent it to the Daily Telegraph at * thirteen minutes past ten *. \"Bravo, Mr. Blount!\" \"Very good, M. Jolivet.\"");
      attribution = F("- Michel Strogoff");
      return;

    // Codegen for 10:14 for 1 quotations:
    case 614:
      // Only one option for 10:14:"
      quote = F("Okay. * Ten fourteen *: Mrs. Narada reports that her cat has been attacked by a large dog. Now I send all the boys out looking, but they don't find anything until eleven.");
      attribution = F("- Coyote Blue");
      return;

    // Codegen for 10:15 for 1 quotations:
    case 615:
      // Only one option for 10:15:"
      quote = F("At * 10.15 * Arlena departed from her rondezvous, a minute or two later Patrick Redfern came down and registered surprise, annoyance, etc.");
      attribution = F("- Evil Under the Sun");
      return;

    // Codegen for 10:16 for 1 quotations:
    case 616:
      // Only one option for 10:16:"
      quote = F("* 10:16 * At last. Forty minutes of hard work and I have made precisely one bed. I'm way behind. But never mind. Just keep moving. Laundry next.");
      attribution = F("- The Undomestic Goddess");
      return;

    // Codegen for 10:17 for 2 quotations:
    case 617:
      option = random(0, 2);
      // 2 options for 10:17
      switch (option) {
        case 0:
      quote = F("* 10:17 p. m. * Casette will not go in");
      attribution = F("- Bridget Jones's Diary");
      return;
        case 1:
      quote = F("By the bye, said the first, \"I was able this morning to telegraph the very words of the order to my cousin at * seventeen minutes past ten *.\"");
      attribution = F("- Michel Strogoff");
      return;
      } // end switch (option)

    // Codegen for 10:18 for 2 quotations:
    case 618:
      option = random(0, 2);
      // 2 options for 10:18
      switch (option) {
        case 0:
      quote = F("I know that it was * 10:18 * when I got home because I look at my watch a lot.");
      attribution = F("- Jonathan Safran Foer");
      return;
        case 1:
      quote = F("My watch is always a little fast, I said. \"What time do you make it now?\" \"Ten eight.\" \"* Ten eighteen * by mine. You see.\"");
      attribution = F("- The Quiet American");
      return;
      } // end switch (option)

    // Codegen for 10:19 for 0 quotations:
    case 619:
      // No quotes for 10:19
        quote = "";
        attribution = "";
      return;

    // Codegen for 10:20 for 2 quotations:
    case 620:
      option = random(0, 2);
      // 2 options for 10:20
      switch (option) {
        case 0:
      quote = F("At * 10:20 * she returned with a shopping bag from the supermarket. In the bag were three scrub brushes, one box of paperclips and a well-chilled six-pack of canned beer.");
      attribution = F("- A Wild Sheep Chase");
      return;
        case 1:
      quote = F("An hour and twenty-five minutes, she said. The right time now is * twenty past ten *.");
      attribution = F("- James Joyce");
      return;
      } // end switch (option)

    // Codegen for 10:21 for 1 quotations:
    case 621:
      // Only one option for 10:21:"
      quote = F("Liz Headleand stares into the mirror, as though entranced. She does not see herself or the objects on her dressing-table. The clock abruptly jerks to * 10.21 *.");
      attribution = F("- The Radiant Way");
      return;

    // Codegen for 10:22 for 1 quotations:
    case 622:
      // Only one option for 10:22:"
      quote = F("...then before I had time to figure out what to do, or even what to think or feel, the phone started ringing. It was * 10:22:27 *. I looked at the caller ID and saw that it was him.");
      attribution = F("- Jonathan Safran Foer");
      return;

    // Codegen for 10:23 for 1 quotations:
    case 623:
      // Only one option for 10:23:"
      quote = F("The date was the 14th of May and the clock on his desk said the time was * twenty three minutes past ten *, so he tapped in the numbers 10.23.");
      attribution = F("- Andrew Norriss, Ctrl-Z");
      return;

    // Codegen for 10:24 for 0 quotations:
    case 624:
      // No quotes for 10:24
        quote = "";
        attribution = "";
      return;

    // Codegen for 10:25 for 2 quotations:
    case 625:
      option = random(0, 2);
      // 2 options for 10:25
      switch (option) {
        case 0:
      quote = F("* 10:25 *: Phone call from Luding, very worked up, urging me to return at once and get in touch with Alois, who was equally worked up.");
      attribution = F("- Heinrich Boll");
      return;
        case 1:
      quote = F("Last night I took a 16 mm. film of the first three hours, screened it this morning at the lab. The first true-horror movie. I looked like a half-animated corpse. Woke * 10:25 *.");
      attribution = F("- The Voices of Time");
      return;
      } // end switch (option)

    // Codegen for 10:26 for 2 quotations:
    case 626:
      option = random(0, 2);
      // 2 options for 10:26
      switch (option) {
        case 0:
      quote = F("* 10:26 * No. Please, no. I can hardly bear to look. It's a total disaster. Everything in the washing machine has gone pink. Every single thing. What happened?");
      attribution = F("- The Undomestic Goddess");
      return;
        case 1:
      quote = F("In the exact centre of my visual field was the alarm clock, hands pointing to * ten-twenty-six *. An alarm clock I received as a memento of somebody's wedding.");
      attribution = F("- Haruki Murakami");
      return;
      } // end switch (option)

    // Codegen for 10:27 for 2 quotations:
    case 627:
      option = random(0, 2);
      // 2 options for 10:27
      switch (option) {
        case 0:
      quote = F("The substandard clock radio says * 10:27 a.m. * The noise is Katrina the Cleaner thumping the hoover against the skirting boards and the bedroom doors. Her hand is asleep.");
      attribution = F("- Ali Smith, The Accidental");
      return;
        case 1:
      quote = F("Mr. Harcourt woke up with mysterious suddenness at * twenty-seven minutes past 10 *, and, by a curious coincidence, it was at that very instant...");
      attribution = F("- England, Their England");
      return;
      } // end switch (option)

    // Codegen for 10:28 for 0 quotations:
    case 628:
      // No quotes for 10:28
        quote = "";
        attribution = "";
      return;

    // Codegen for 10:29 for 0 quotations:
    case 629:
      // No quotes for 10:29
        quote = "";
        attribution = "";
      return;

    // Codegen for 10:30 for 5 quotations:
    case 630:
      option = random(0, 5);
      // 5 options for 10:30
      switch (option) {
        case 0:
      quote = F("* 10.30 a.m. * Break");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("according to the clock on the wall, it is barely * ten thirty *.");
      attribution = F("- The Hours");
      return;
        case 2:
      quote = F("At * ten-thirty * I'm cleaned up, shaved and dressed in my Easter best - a two-piece seersucker Palm Beach I've had since college.");
      attribution = F("- The Sportswriter");
      return;
        case 3:
      quote = F("Just his head was sticking out of the partially opened door. The money from blessing the ice-cream factory must have dulled his desire for work, I thought. 'Ten.' '* Ten-thirty *.'");
      attribution = F("- An Obedient Father");
      return;
        case 4:
      quote = F("She looked at the clock; it was * ten thirty *. If she could get there quickly on the subway, then she could be at his house in less than an hour, maybe a bit longer...");
      attribution = F("- Colm Toibin, Brooklyn");
      return;
      } // end switch (option)

    // Codegen for 10:31 for 0 quotations:
    case 631:
      // No quotes for 10:31
        quote = "";
        attribution = "";
      return;

    // Codegen for 10:32 for 1 quotations:
    case 632:
      // Only one option for 10:32:"
      quote = F("If you please. You went to bed at what time, Madame? \"* Just after half past ten. *\"");
      attribution = F("- Death on the Nile");
      return;

    // Codegen for 10:33 for 0 quotations:
    case 633:
      // No quotes for 10:33
        quote = "";
        attribution = "";
      return;

    // Codegen for 10:34 for 0 quotations:
    case 634:
      // No quotes for 10:34
        quote = "";
        attribution = "";
      return;

    // Codegen for 10:35 for 1 quotations:
    case 635:
      // Only one option for 10:35:"
      quote = F("* Five-and-twenty to eleven *. A horrible hour - a macabre hour, for it is not only the hour of pleasure ended, it is the hour when pleasure itself has been found wanting.");
      attribution = F("- Patrick Hamilton, Rope");
      return;

    // Codegen for 10:36 for 1 quotations:
    case 636:
      // Only one option for 10:36:"
      quote = F("Strand post mark and dispatched * ten thirty-six * said Holmes reading it over and over.");
      attribution = F("- Arthur Conan Doyle");
      return;

    // Codegen for 10:37 for 1 quotations:
    case 637:
      // Only one option for 10:37:"
      quote = F("I quite agree with you,' said Mr Murbles. 'It is a most awkward situation. Lady Dormer died at precisely * 10.37 a.m. * on November 11th.'");
      attribution = F("- Dorothy L. Sayers");
      return;

    // Codegen for 10:38 for 1 quotations:
    case 638:
      // Only one option for 10:38:"
      quote = F("Frantically I scan the cans of products stacked on the shelves. Stain Away. Vanish. There has to be a remedy. ... I just need to think. ... * 10:38 * OK, I have the answer.");
      attribution = F("- The Undomestic Goddess");
      return;

    // Codegen for 10:39 for 0 quotations:
    case 639:
      // No quotes for 10:39
        quote = "";
        attribution = "";
      return;

    // Codegen for 10:40 for 2 quotations:
    case 640:
      option = random(0, 2);
      // 2 options for 10:40
      switch (option) {
        case 0:
      quote = F("* 10:40 *: Call from Katharina asking me whether I had really said what was in the News.");
      attribution = F("- Heinrich Boll");
      return;
        case 1:
      quote = F("The station clock told him the time: * twenty to eleven *. He went to the booking office and asked the clerk in a polite tone when was the next train to Paris. 'In twelve minutes.'");
      attribution = F("- Georges Simenon");
      return;
      } // end switch (option)

    // Codegen for 10:41 for 1 quotations:
    case 641:
      // Only one option for 10:41:"
      quote = F("There, the bastards, said Julian, and smashed the clock with the bottom of the bottle, to give them an approximate time. It was * 10:41 *");
      attribution = F("- Appointment in Samarra");
      return;

    // Codegen for 10:42 for 0 quotations:
    case 642:
      // No quotes for 10:42
        quote = "";
        attribution = "";
      return;

    // Codegen for 10:43 for 1 quotations:
    case 643:
      // Only one option for 10:43:"
      quote = F("24 January, * 10.43 a.m *: one month and two days later I wonder if I should worry about the fact that my darling boyfriend bought me a birthday present...");
      attribution = F("- The Wish List");
      return;

    // Codegen for 10:44 for 1 quotations:
    case 644:
      // Only one option for 10:44:"
      quote = F("Alec pricked up his ears. \"When was that?\" \"Oh, yesterday evening.\" \"What time?\" \"* About a quarter to eleven. * I was playing bridge.\"");
      attribution = F("- Dead in the Water");
      return;

    // Codegen for 10:45 for 2 quotations:
    case 645:
      option = random(0, 2);
      // 2 options for 10:45
      switch (option) {
        case 0:
      quote = F("If this is so, we have now to determine what Barker and Mrs. Douglas, presuming they are not the actual murderers, would have been doing from * quarter to eleven *, when...");
      attribution = F("- The Valley of Fear");
      return;
        case 1:
      quote = F("They reached King's Cross at a * quarter to eleven *. Mr Weasley dashed across the road to get trolleys for their trunks and they all hurried into the station.");
      attribution = F("- J.K.Rowling");
      return;
      } // end switch (option)

    // Codegen for 10:46 for 0 quotations:
    case 646:
      // No quotes for 10:46
        quote = "";
        attribution = "";
      return;

    // Codegen for 10:47 for 1 quotations:
    case 647:
      // Only one option for 10:47:"
      quote = F("He whistles in the shower. It is * 10.47 * and he is ready for the off.");
      attribution = F("- Jackie Kay, Trumpet");
      return;

    // Codegen for 10:48 for 2 quotations:
    case 648:
      option = random(0, 2);
      // 2 options for 10:48
      switch (option) {
        case 0:
      quote = F("At * 10.48am *, I closed my folder but didn't bother putting it back in my bag, so you knew I was on my way to a committee or meeting room nearby.");
      attribution = F("- Apple Tree Yard");
      return;
        case 1:
      quote = F("I was down there for a bit last summer. I found it quite convenient for town. Of course the trains only go every hour. 48 minutes past the hour from Waterloo-up to * 10.48 *.");
      attribution = F("- The Sign in the Sky");
      return;
      } // end switch (option)

    // Codegen for 10:49 for 1 quotations:
    case 649:
      // Only one option for 10:49:"
      quote = F("By * forty-nine minutes past ten *, we fall in again with a fine portion of the ancient road, which the modern track constantly follows, and descend by some steep windings");
      attribution = F("- Felicien de Saulcy");
      return;

    // Codegen for 10:50 for 3 quotations:
    case 650:
      option = random(0, 3);
      // 3 options for 10:50
      switch (option) {
        case 0:
      quote = F("* 10.50 a.m. * Art class with Mrs Peters");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("...airmen were forming a line to await the arrival of the NAAFI van with morning tea and cakes. Lambert looked at his watch; it was * ten to eleven *.");
      attribution = F("- Len Deighton, Bomber");
      return;
        case 2:
      quote = F("It's a hundred this year, ain't anybody noticed? Jack said, \"What's a hundred?\" I said, \"Pub is. Coach is. Look at the clock.\" Jack said, \"It's * ten to eleven *.\"");
      attribution = F("- Graham Swift, Last Orders");
      return;
      } // end switch (option)

    // Codegen for 10:51 for 0 quotations:
    case 651:
      // No quotes for 10:51
        quote = "";
        attribution = "";
      return;

    // Codegen for 10:52 for 0 quotations:
    case 652:
      // No quotes for 10:52
        quote = "";
        attribution = "";
      return;

    // Codegen for 10:53 for 2 quotations:
    case 653:
      option = random(0, 2);
      // 2 options for 10:53
      switch (option) {
        case 0:
      quote = F("He begins to make a record of our observations.'* 10.53 hrs *,' he writes, as we crouch at the top of the stairs, listening to his mother in the hall below.");
      attribution = F("- Michael Frayn, Spies");
      return;
        case 1:
      quote = F("... left-hand corner of the screen, where the figures covering part of her forehead show the minutes and seconds, from * 10:53 * to 10:57, while the hundredths of a second flash by...");
      attribution = F("- W.G. Sebald, Austerlitz");
      return;
      } // end switch (option)

    // Codegen for 10:54 for 0 quotations:
    case 654:
      // No quotes for 10:54
        quote = "";
        attribution = "";
      return;

    // Codegen for 10:55 for 2 quotations:
    case 655:
      option = random(0, 2);
      // 2 options for 10:55
      switch (option) {
        case 0:
      quote = F("It is eleven o'clock! * Eleven o'clock, all but five minutes! * \"But which eleven o'clock?\" \"The eleven o'clock that is to decide life or death!\"");
      attribution = F("- The Phantom of the Opera");
      return;
        case 1:
      quote = F("The clock was still saying * five minutes to eleven * when Pooh and Piglet set out on their way half an hour later.");
      attribution = F("- The House at Pooh Corner");
      return;
      } // end switch (option)

    // Codegen for 10:56 for 0 quotations:
    case 656:
      // No quotes for 10:56
        quote = "";
        attribution = "";
      return;

    // Codegen for 10:57 for 1 quotations:
    case 657:
      // Only one option for 10:57:"
      quote = F("...looking at the time indicator in the top left-hand corner of the screen, where the figures covering part of her forehead show the minutes and seconds, from 10.53 to * 10.57 *.");
      attribution = F("- W. G. Sebald, Austerlitz");
      return;

    // Codegen for 10:58 for 1 quotations:
    case 658:
      // Only one option for 10:58:"
      quote = F("One day Joe was sitting in the office waiting for his 11 o'clock appointment, and at * 10:58 * this black gal came in.");
      attribution = F("- Lightning Rods");
      return;

    // Codegen for 10:59 for 2 quotations:
    case 659:
      option = random(0, 2);
      // 2 options for 10:59
      switch (option) {
        case 0:
      quote = F("...but he did not wake up. An alarm clock, repaired by Harry several years ago, ticked loudly on the sill, showing * one minute to eleven *.");
      attribution = F("- J. K. Rowling");
      return;
        case 1:
      quote = F("They parked the car outside Lowther's at precisely * one minute to eleven *. People were leaving, not all of them happy at having their evening curtailed.");
      attribution = F("- Ian Rankin, The Complaints");
      return;
      } // end switch (option)

    // Codegen for 11:00 for 17 quotations:
    case 660:
      option = random(0, 17);
      // 17 options for 11:00
      switch (option) {
        case 0:
      quote = F("As her husband had told him, she was still in bed although it was past * 11 o'clock *. Her normally mobile face was encased in clay, rigid and menacing as an Aztec mask.");
      attribution = F("- Evelyn Waugh, Scoop");
      return;
        case 1:
      quote = F("By * 11 o'clock * I have finished the first chapter of Mr Y. The winter sun is peeping meekly through the thin curtains and I decide to get up.");
      attribution = F("- The End of Mr Y");
      return;
        case 2:
      quote = F("He will be here at * eleven exactly *, sir. At the bar, naked couples had begun dancing.");
      attribution = F("- Smiley's People");
      return;
        case 3:
      quote = F("I dare say it was champagne * at eleven in the morning *.");
      attribution = F("- The Weather in the Streets");
      return;
        case 4:
      quote = F("We got to Waterloo * at eleven *, and asked where the eleven-five started from. Of course nobody knew.");
      attribution = F("- Three Men in a Boat");
      return;
        case 5:
      quote = F("... and in this extraordinary silence and peace, in this pallor, in this purity, bells struck * eleven * times the sound fading up there among the gulls.");
      attribution = F("- Mrs. Dalloway");
      return;
        case 6:
      quote = F("At * eleven o'clock * in the morning, large flakes had appeared from a colourless sky and invaded the fields, gardens and lawns of Romerike...");
      attribution = F("- Jo Nesbo, The Snowman");
      return;
        case 7:
      quote = F("At * eleven o'clock * the phone rang, and still the figure did not respond, any more than it had responded when the phone had rung at twenty-five to seven...");
      attribution = F("- Douglas Adams");
      return;
        case 8:
      quote = F("Big Ben was striking as she stepped out into the street. It was * eleven o'clock * and the unused hour was fresh as if issued to children on a beach.");
      attribution = F("- Mrs Dalloway");
      return;
        case 9:
      quote = F("He says, \"They've killed Jan. Clear out.\" \"The suitcase?\" I ask. \"Take it away again. We want nothing to do with it now. Catch the * eleven o'clock * express.\"");
      attribution = F("- Italo Calvino");
      return;
        case 10:
      quote = F("I keep telling myself that once in the car I will be fine, I can listen to Radio Four Woman's Hour and that will take me till * eleven o'clock * when the class starts.");
      attribution = F("- Patsy Hickman, The Saints");
      return;
        case 11:
      quote = F("Quiet as I am, I become at * Eleven o'Clock in the Morning * on every day of the week save Sunday");
      attribution = F("- Twice Around the Clock");
      return;
        case 12:
      quote = F("It was about * eleven o'clock *; and the Enchantress was attired in a delicious deshabille. With her little feet upon an ottoman near the fender...");
      attribution = F("- The Mysteries of London");
      return;
        case 13:
      quote = F("It was about * eleven o'clock * in the morning, mid October, with the sun not shining and a look of hard wet rain in the clearness of the foothills.");
      attribution = F("- The big sleep");
      return;
        case 14:
      quote = F("We passed a few sad hours until * eleven o'clock *, when the trial was to commence. My father and the rest of the family being obliged to attend as witnesses...");
      attribution = F("- Mary Shelley, Frankenstein");
      return;
        case 15:
      quote = F("Who can - what can - asked Mrs Dalloway (thinking it was outrageous to be interrupted at * eleven o'clock * on the morning of the day she was giving a party)...");
      attribution = F("- Mrs Dalloway");
      return;
        case 16:
      quote = F("Though perhaps' - but here the bracket clock whirred and then hectically struck * eleven *, its weights spooling downwards at the sudden expense of energy.");
      attribution = F("- The Stranger's Child");
      return;
      } // end switch (option)

    // Codegen for 11:01 for 1 quotations:
    case 661:
      // Only one option for 11:01:"
      quote = F("The clock above the stove says that it is * just past eleven *, and guests will be arriving at the house now.");
      attribution = F("- Mary and O'Neil");
      return;

    // Codegen for 11:02 for 1 quotations:
    case 662:
      // Only one option for 11:02:"
      quote = F("On August 9th, three days later, at * 11.02am *, another B-29 dropped the second bomb on the industrial section of the city of Nagasaki, totally destroying 1 1/2 square miles...");
      attribution = F("- Manhattan Engineer District");
      return;

    // Codegen for 11:03 for 3 quotations:
    case 663:
      option = random(0, 3);
      // 3 options for 11:03
      switch (option) {
        case 0:
      quote = F("On the fourth, at * 11.03am *, the editor of the Yidische Zaitung put in a call to him; Doctor Yarmolinsky did not answer. He was found in his room, his face already a little dark...");
      attribution = F("- Death and the Compass");
      return;
        case 1:
      quote = F("Good question. Guess I'll find out. She went back into the kitchen. \"What time are you meeting him?\" she called out. \"* Eleven oh-three *,\" he said.");
      attribution = F("- Little Green Men");
      return;
        case 2:
      quote = F("What time are you meeting him? she called out. \"* Eleven oh-three *,\" he said. \"That made me think he's for real. Military and intelligence types set precise appointment times...\"");
      attribution = F("- Little Green Men");
      return;
      } // end switch (option)

    // Codegen for 11:04 for 1 quotations:
    case 664:
      // Only one option for 11:04:"
      quote = F("As her husband had told him, she was still in bed although it was * past 11 o'clock *. Her normally mobile face was encased in clay, rigid and menacing as an Aztec mask.");
      attribution = F("- Evelyn Waugh, Scoop");
      return;

    // Codegen for 11:05 for 2 quotations:
    case 665:
      option = random(0, 2);
      // 2 options for 11:05
      switch (option) {
        case 0:
      quote = F("...operating manuals Whitby left for the last time, decided on a delivery rate of 40 rontgens/min., target distance of 530 cm. Everything is ready now. Woke * 11:05 *. To sleep 3:15.");
      attribution = F("- The Voices of Time");
      return;
        case 1:
      quote = F("Sansom arrived in a Town Car at * five past eleven *. Local plates, which meant he had ridden up most of the way on the train.");
      attribution = F("- Lee Child, Gone Tomorrow");
      return;
      } // end switch (option)

    // Codegen for 11:06 for 2 quotations:
    case 666:
      option = random(0, 2);
      // 2 options for 11:06
      switch (option) {
        case 0:
      quote = F("* 11:06 * And ... oh. The ironing. What am I going to do about that?");
      attribution = F("- The Undomestic Goddess");
      return;
        case 1:
      quote = F("A fantastic notion, until you consider that up until * 11.06am * on 12 April 1948, everyone else had thought the same.");
      attribution = F("- Jasper Fforde");
      return;
      } // end switch (option)

    // Codegen for 11:07 for 1 quotations:
    case 667:
      // Only one option for 11:07:"
      quote = F("At exactly * seven minutes past eleven * by the ship's clock the Adventurer gave a prolonged screech and, moorings cast off...");
      attribution = F("- The Adventure Club Afloat");
      return;

    // Codegen for 11:08 for 1 quotations:
    case 668:
      // Only one option for 11:08:"
      quote = F("...with his arms folded across his chest and when he caught sight of the fat young man he looked significantly at the clock. It was * eight minutes past eleven *.");
      attribution = F("- James Joyce, Stephen Hero");
      return;

    // Codegen for 11:09 for 1 quotations:
    case 669:
      // Only one option for 11:09:"
      quote = F("The first time I saw them it was * around eleven *, eleven-fifteen, a Saturday morning, I was about two thirds through my route when I turned onto their block and noticed...");
      attribution = F("- Where I'm Calling From");
      return;

    // Codegen for 11:10 for 2 quotations:
    case 670:
      option = random(0, 2);
      // 2 options for 11:10
      switch (option) {
        case 0:
      quote = F("* Ten minutes after eleven * in Archie McCue's room on the third floor of the extension to the Robert Matthews' soaring sixties' tower...");
      attribution = F("- Emotionally Weird");
      return;
        case 1:
      quote = F("He had not the strength to help himself, and at * ten minutes past eleven * no one could have helped him, no one in the world");
      attribution = F("- Appointment in Samarra");
      return;
      } // end switch (option)

    // Codegen for 11:11 for 0 quotations:
    case 671:
      // No quotes for 11:11
        quote = "";
        attribution = "";
      return;

    // Codegen for 11:12 for 2 quotations:
    case 672:
      option = random(0, 2);
      // 2 options for 11:12
      switch (option) {
        case 0:
      quote = F("* 11:12 * I have a solution, via the local paper. A girl from the village will collect it, iron it all overnight at L3 a shirt, and sew on Eddie's button.");
      attribution = F("- The Undomestic Goddess");
      return;
        case 1:
      quote = F("I squinted down the street at the bank clock: * 11:12 *, 87 degrees. \"It's only a block and a half and it's not that hot, Daddy. The walk will do you good.\"");
      attribution = F("- A Thousand Acres");
      return;
      } // end switch (option)

    // Codegen for 11:13 for 0 quotations:
    case 673:
      // No quotes for 11:13
        quote = "";
        attribution = "";
      return;

    // Codegen for 11:14 for 1 quotations:
    case 674:
      // Only one option for 11:14:"
      quote = F("The report was dated Sunday, 25 September, 1966, at * 11.14am. * The text was laconic. Call from Hrk Vanger.");
      attribution = F("- Stieg Larsson");
      return;

    // Codegen for 11:15 for 3 quotations:
    case 675:
      option = random(0, 3);
      // 3 options for 11:15
      switch (option) {
        case 0:
      quote = F("Shall be glad if you will come with me. Air and scenery perfect. Leave Paddington by the * 11:15 *.");
      attribution = F("- Sir Arthur Conan Doyle");
      return;
        case 1:
      quote = F("The first time I saw them it was around eleven, * eleven-fifteen *, a Saturday morning, I was about two thirds through my route when I turned onto their block and noticed...");
      attribution = F("- Where I'm Calling From");
      return;
        case 2:
      quote = F("On arriving home at a * quarter-past eleven *, we found a hansom cab, which had been waiting for me for two hours with a letter.");
      attribution = F("- Dairy of a Nobody");
      return;
      } // end switch (option)

    // Codegen for 11:16 for 0 quotations:
    case 676:
      // No quotes for 11:16
        quote = "";
        attribution = "";
      return;

    // Codegen for 11:17 for 1 quotations:
    case 677:
      // Only one option for 11:17:"
      quote = F("It was * seventeen minutes past eleven *: she would have lots of time to have the matter out with Mr. Doran...");
      attribution = F("- James Joyce, Dubliners");
      return;

    // Codegen for 11:18 for 1 quotations:
    case 678:
      // Only one option for 11:18:"
      quote = F("It is * 11.18 *. A row of bungalows in a round with a clump of larch tree in the middle.");
      attribution = F("- Jackie Kay, Trumpet");
      return;

    // Codegen for 11:19 for 1 quotations:
    case 679:
      // Only one option for 11:19:"
      quote = F("Peter got onto his knees to look out the window, and Miss Fuller glared at him. Polly looked down at her watch: * 11:19 *. The train.");
      attribution = F("- Connie Willis, Blackout");
      return;

    // Codegen for 11:20 for 3 quotations:
    case 680:
      option = random(0, 3);
      // 3 options for 11:20
      switch (option) {
        case 0:
      quote = F("Sweeney pointed to the clock above the bar, held in the massive and indifferent jaws of a stuffed alligator head. The time was * 11.20 *.");
      attribution = F("- Neil Gaiman, American Gods");
      return;
        case 1:
      quote = F("OFFICER'S NOTES Disruption alert logged * 11h20 * from Stones' Pool Hall (Premises ID 33CBD-Long181). Officer and Aito 379 responded. On arrival found subject shouting threats...");
      attribution = F("- Lauren Beukes, Moxyland");
      return;
        case 2:
      quote = F("From Balboa Island, he drove south to Laguna Beach. At * eleven-twenty *, he parked his van across the street from the Hudston house.");
      attribution = F("- Dean Koontz, Watchers");
      return;
      } // end switch (option)

    // Codegen for 11:21 for 0 quotations:
    case 681:
      // No quotes for 11:21
        quote = "";
        attribution = "";
      return;

    // Codegen for 11:22 for 1 quotations:
    case 682:
      // Only one option for 11:22:"
      quote = F("At * 11.22 * he handed his ticket to a yawning guard and walked down a long flight of wooden steps to the car-park. A breeze lifted and dropped the leaves of a tree...");
      attribution = F("- Dreams of leaving");
      return;

    // Codegen for 11:23 for 0 quotations:
    case 683:
      // No quotes for 11:23
        quote = "";
        attribution = "";
      return;

    // Codegen for 11:24 for 0 quotations:
    case 684:
      // No quotes for 11:24
        quote = "";
        attribution = "";
      return;

    // Codegen for 11:25 for 3 quotations:
    case 685:
      option = random(0, 3);
      // 3 options for 11:25
      switch (option) {
        case 0:
      quote = F("When, at about * 11.25am *, Katharina Blum was finally taken from her apartment for questioning, it was decided not to handcuff her at all.");
      attribution = F("- Heinrich Boll");
      return;
        case 1:
      quote = F("...see if he could make out the figures on the dial. They were plainly visible, and the hands indicated the hour of * eleven o'clock and twenty-five minutes *.");
      attribution = F("- A Wireless Message");
      return;
        case 2:
      quote = F("Keeping her own watch concealed she asked Linda at * twenty-five past eleven * what time it was. Linda looked at her watch and replied that it was a quarter to twelve.");
      attribution = F("- Evil under the Sun");
      return;
      } // end switch (option)

    // Codegen for 11:26 for 0 quotations:
    case 686:
      // No quotes for 11:26
        quote = "";
        attribution = "";
      return;

    // Codegen for 11:27 for 1 quotations:
    case 687:
      // Only one option for 11:27:"
      quote = F("It's from one of the more recent plates the tree has scanned: * 11.27 * in the morning of 4 April 1175");
      attribution = F("- Chris James");
      return;

    // Codegen for 11:28 for 1 quotations:
    case 688:
      // Only one option for 11:28:"
      quote = F("...from twenty-five minutes past eleven until * twenty-eight minutes past eleven *, from ten minutes to three until two minutes to three...");
      attribution = F("- Sinister Street");
      return;

    // Codegen for 11:29 for 1 quotations:
    case 689:
      // Only one option for 11:29:"
      quote = F("No matter; it's enough to mention the error. Now from this moment, * twenty-nine minutes after eleven, a.m. *...");
      attribution = F("- Jules Verne");
      return;

    // Codegen for 11:30 for 5 quotations:
    case 690:
      option = random(0, 5);
      // 5 options for 11:30
      switch (option) {
        case 0:
      quote = F("'It is now * 11.30 *. The door to this room is shut, and will remain shut, barring emergencies, until 12.00. I am authorised to inform you that we are now under battle orders.");
      attribution = F("- Singularity Sky");
      return;
        case 1:
      quote = F("This time it was Kumiko. The wall clock said * 11.30 *.");
      attribution = F("- The Wind-up Bird Chronicle");
      return;
        case 2:
      quote = F("Beddows had abjured Satan at a little after * half-past eleven *. By about eighteen minutes the Canon had beaten them to it again.");
      attribution = F("- To the Devil a Daughter");
      return;
        case 3:
      quote = F("O, Frank - I made a mistake! - I thought that church with the spire was All Saints', and I was at the door at * half-past eleven * to a minute as you said...");
      attribution = F("- Far from the madding crowd");
      return;
        case 4:
      quote = F("The ship's clock in the bar says * half past eleven *. Half past eleven is opening time. The hands of the clock have stayed still at half past eleven for fifty years.");
      attribution = F("- Under Milk Wood");
      return;
      } // end switch (option)

    // Codegen for 11:31 for 1 quotations:
    case 691:
      // Only one option for 11:31:"
      quote = F("Albatross 8 passed over Pamlico Sound at * 1131 * local time. Its on-board programming was designed to trace thermal receptors over the entire visible horizon...");
      attribution = F("- The Hunt for Red October");
      return;

    // Codegen for 11:32 for 1 quotations:
    case 692:
      // Only one option for 11:32:"
      quote = F("...there and then, on a lovely morning, after the universal flood, at about * eleven thirty two * was it? Off the coast of Cominghome...");
      attribution = F("- James Joyce, Finnegans Wake");
      return;

    // Codegen for 11:33 for 0 quotations:
    case 693:
      // No quotes for 11:33
        quote = "";
        attribution = "";
      return;

    // Codegen for 11:34 for 3 quotations:
    case 694:
      option = random(0, 3);
      // 3 options for 11:34
      switch (option) {
        case 0:
      quote = F("Christmas Eve 1995. * 11.34am *. The first time, Almasa says it slowly and softly, as if she is really looking for an answer.");
      attribution = F("- Adnan Mahmutovic");
      return;
        case 1:
      quote = F("* Eleven thirty-four *. We stand on the sidewalk in front of Jean's apartment on the Upper East Side.");
      attribution = F("- American Psycho");
      return;
        case 2:
      quote = F("Reacher retrieved his G36 from under the saloon bar window at * eleven thirty-four * precisely and set out to walk back on the road...");
      attribution = F("- Lee Child, The Hard Way");
      return;
      } // end switch (option)

    // Codegen for 11:35 for 2 quotations:
    case 695:
      option = random(0, 2);
      // 2 options for 11:35
      switch (option) {
        case 0:
      quote = F("At * 11.35 * the Colonel came out; he looked hot and angry as he strode towards the lift. There goes a hanging judge, thought Wormold.");
      attribution = F("- Our Man in Havana");
      return;
        case 1:
      quote = F("Then at * eleven thirty-five * the door at the rear of the hall opened and a police sergeant and three constables entered, ushered by Bagot.");
      attribution = F("- Hamlet, Revenge!");
      return;
      } // end switch (option)

    // Codegen for 11:36 for 1 quotations:
    case 696:
      // Only one option for 11:36:"
      quote = F("I ran up the stairs, away from the heat and the noise, the mess and the confusion. I saw the clock radio by my bed. * Eleven thirty-six *.");
      attribution = F("- Nicci French, Losing You");
      return;

    // Codegen for 11:37 for 0 quotations:
    case 697:
      // No quotes for 11:37
        quote = "";
        attribution = "";
      return;

    // Codegen for 11:38 for 1 quotations:
    case 698:
      // Only one option for 11:38:"
      quote = F("At * 11:38 *, she left her desk and walked to the side door of the auditorium, arriving ten minutes before noon.");
      attribution = F("- Dave Eggers, The Circle");
      return;

    // Codegen for 11:39 for 0 quotations:
    case 699:
      // No quotes for 11:39
        quote = "";
        attribution = "";
      return;

    // Codegen for 11:40 for 2 quotations:
    case 700:
      option = random(0, 2);
      // 2 options for 11:40
      switch (option) {
        case 0:
      quote = F("On the line where these words were written, \"21st December, Saturday, Liverpool,\" he added, \"80th day, * 11.40am *,\" and waited.");
      attribution = F("- Jules Verne");
      return;
        case 1:
      quote = F("... he now seemed to be bringing himself to order through silent meditation. Then, quickly, there came a rap of stone on board. It was * twenty minutes before noon *.");
      attribution = F("- The Master of Go");
      return;
      } // end switch (option)

    // Codegen for 11:41 for 2 quotations:
    case 701:
      option = random(0, 2);
      // 2 options for 11:41
      switch (option) {
        case 0:
      quote = F("The phosphorescent figures on his watch attracted his attention. He peered at them. The time was * 11:41 *. I remember...what do I remember?");
      attribution = F("- James Clavell, Noble House");
      return;
        case 1:
      quote = F("Spagnola took a deep breath and started into the log again. \"* Eleven forty-one *: large dog craps in Dr. Yamata's Aston Martin.\"");
      attribution = F("- Coyote Blue");
      return;
      } // end switch (option)

    // Codegen for 11:42 for 1 quotations:
    case 702:
      // Only one option for 11:42:"
      quote = F("* 11:42 * I'm doing fine. I'm doing well. I've got the Hoover on, I'm cruising along nicely- What was that? What just went up the Hoover?");
      attribution = F("- The Undomestic Goddess");
      return;

    // Codegen for 11:43 for 1 quotations:
    case 703:
      // Only one option for 11:43:"
      quote = F("The clock told him it was * eleven forty-three * and in that moment, in a flash of illumination, Mungo understood what the numbers at the end of Moscow Centre's messages were");
      attribution = F("- Talking to Strange Men");
      return;

    // Codegen for 11:44 for 0 quotations:
    case 704:
      // No quotes for 11:44
        quote = "";
        attribution = "";
      return;

    // Codegen for 11:45 for 8 quotations:
    case 705:
      option = random(0, 8);
      // 8 options for 11:45
      switch (option) {
        case 0:
      quote = F("I arrived at St. Gatien from Nice on Tuesday, the 14th of August. I was arrested at * 11.45am * on Thursday, the 16th by an agent de police and an inspector in plain clothes.");
      attribution = F("- Epitaph for a Spy");
      return;
        case 1:
      quote = F("She tucked the phone in the crook of her neck and thumbed hurriedly through her pink messages. .... Dr. Provetto, at * 11:45 A.M. *");
      attribution = F("- Mistaken Identity");
      return;
        case 2:
      quote = F("As he sat smiling, the quarter struck - the * quarter to twelve *.");
      attribution = F("- Mrs Dalloway");
      return;
        case 3:
      quote = F("...I waited till a * quarter to twelve *, and found then that I was in All Souls'. But I wasn't much frightened, for I thought it could be tomorrow as well.");
      attribution = F("- Far from the madding crowd");
      return;
        case 4:
      quote = F("Keeping her own watch concealed she asked Linda at twenty-five past eleven what time it was. Linda looked at her watch and replied that it was a * quarter to twelve *.");
      attribution = F("- Evil under the Sun");
      return;
        case 5:
      quote = F("Said Septimus, very slowly, very drowsily, smiling mysteriously. As he sat smiling at the dead man in the grey suit the quarter struck, the * quarter to twelve *.");
      attribution = F("- Mrs. Dalloway");
      return;
        case 6:
      quote = F("We passed Shiplake as the clock was striking the * quarter to twelve * and then George said thoughtfully: 'You don't happen to remember which of the islands it was, do you?'");
      attribution = F("- Three Men in a Boat");
      return;
        case 7:
      quote = F("The church clocks chimed * three quarters past eleven *, as two figures emerged on London Bridge.");
      attribution = F("- Oliver Twist");
      return;
      } // end switch (option)

    // Codegen for 11:46 for 0 quotations:
    case 706:
      // No quotes for 11:46
        quote = "";
        attribution = "";
      return;

    // Codegen for 11:47 for 1 quotations:
    case 707:
      // Only one option for 11:47:"
      quote = F("...which didn't let me judge the time (my watch, set like all military watches to Berlin time, hadn't stood up to the swim and showed an eternal * thirteen minutes to noon *).");
      attribution = F("- The Kindly Ones");
      return;

    // Codegen for 11:48 for 1 quotations:
    case 708:
      // Only one option for 11:48:"
      quote = F("At 11:38, she left her desk and walked to the side door of the auditorium, arriving * ten minutes before noon *.");
      attribution = F("- Dave Eggers, The Circle");
      return;

    // Codegen for 11:49 for 0 quotations:
    case 709:
      // No quotes for 11:49
        quote = "";
        attribution = "";
      return;

    // Codegen for 11:50 for 1 quotations:
    case 710:
      // Only one option for 11:50:"
      quote = F("The man who gave them to him handed him a ten-shilling note and promised him another if it were delivered at exactly * ten minutes to twelve *.");
      attribution = F("- Agatha Christie");
      return;

    // Codegen for 11:51 for 2 quotations:
    case 711:
      option = random(0, 2);
      // 2 options for 11:51
      switch (option) {
        case 0:
      quote = F("Due at Waterloo at * eleven-fifty-one *, panted Smith. \"That gives us thirty-nine minutes to get to the other side of the river and reach his hotel.\"");
      attribution = F("- The Mystery of Dr Fu Manchu");
      return;
        case 1:
      quote = F("The next day, at * nine minutes to twelve * o'clock noon, the last clock ran down and stopped. It was then placed in the town museum...");
      attribution = F("- Lanterns & Lances");
      return;
      } // end switch (option)

    // Codegen for 11:52 for 1 quotations:
    case 712:
      // Only one option for 11:52:"
      quote = F("At any rate, we whirled into the station with many more, just as the great clock pointed to * eight minutes to twelve * o'clock. \"Thank God! We are in time,\"");
      attribution = F("- Anna Sewell, Black Beauty");
      return;

    // Codegen for 11:53 for 0 quotations:
    case 713:
      // No quotes for 11:53
        quote = "";
        attribution = "";
      return;

    // Codegen for 11:54 for 1 quotations:
    case 714:
      // Only one option for 11:54:"
      quote = F("He swilled off the remains of [his beer] and looked at the clock. It was * six minutes to twelve *.");
      attribution = F("- Hangover Square");
      return;

    // Codegen for 11:55 for 3 quotations:
    case 715:
      option = random(0, 3);
      // 3 options for 11:55
      switch (option) {
        case 0:
      quote = F("It was * 11:55 a.m. * on April 30.");
      attribution = F("- All the President's Men");
      return;
        case 1:
      quote = F("What time did you arrive at the site? It was * 11:55 *. I remember since I happened to glance at my watch when we got there. We rode our bicycles to the bottom of the hill...");
      attribution = F("- Kafka on the Shore");
      return;
        case 2:
      quote = F("He was tearing off on his bicycle to one of the jobs about * five minutes to twelve * to see if he could catch anyone leaving off for dinner before the proper time.");
      attribution = F("- Robert Tressell");
      return;
      } // end switch (option)

    // Codegen for 11:56 for 2 quotations:
    case 716:
      option = random(0, 2);
      // 2 options for 11:56
      switch (option) {
        case 0:
      quote = F("A few minutes' light * around noon * is all that you need to discover the error, and re-set the clock - provide that you bother to go up and make the observation.");
      attribution = F("- Neal Stephenson");
      return;
        case 1:
      quote = F("I wondered what the time is?' said the latter after a pause'. 'I don't know exactly', replied Easton, 'but it * can't be far-off twelve *.'");
      attribution = F("- Robert Tressell");
      return;
      } // end switch (option)

    // Codegen for 11:57 for 2 quotations:
    case 717:
      option = random(0, 2);
      // 2 options for 11:57
      switch (option) {
        case 0:
      quote = F("I wondered what the time is?' said the latter after a pause'. 'I don't know exactly', replied Easton, 'but it * can't be far-off twelve *.'");
      attribution = F("- Robert Tressell");
      return;
        case 1:
      quote = F("Wells looked out at the street. \"What time is it?\" he said. Chigurh raised his wrist and looked at his watch. \"* Eleven fifty-seven *\" he said. Wells nodded.");
      attribution = F("- No Country for Old Men");
      return;
      } // end switch (option)

    // Codegen for 11:58 for 2 quotations:
    case 718:
      option = random(0, 2);
      // 2 options for 11:58
      switch (option) {
        case 0:
      quote = F("And when you go down the steps, it's always * 11.58 * on the morning of September ninth, 1958.");
      attribution = F("- Stephen King, 11/22/63");
      return;
        case 1:
      quote = F("* Two minutes before the clock struck noon *, the savage baron was on the platform to inspect the preparation for the frightful ceremony of mid-day.");
      attribution = F("- Burlesques");
      return;
      } // end switch (option)

    // Codegen for 11:59 for 1 quotations:
    case 719:
      // Only one option for 11:59:"
      quote = F("There is a big grandfather clock there, and as the hands drew * near to twelve * I don't mind confessing I was as nervous as a cat.");
      attribution = F("- Agatha Christie");
      return;

    // Codegen for 12:00 for 13 quotations:
    case 720:
      option = random(0, 13);
      // 13 options for 12:00
      switch (option) {
        case 0:
      quote = F("'It will soon be an hour and a half,' said the girl who kept the records. The * noon *day siren blew. 'Exactly a minute,' she said, looking at the stopwatch of which she was so proud.");
      attribution = F("- The Master of Go");
      return;
        case 1:
      quote = F("* Noon * found him momentarily alone, while the family prepared lunch in the kitchen. The cracks in the ceiling widened into gaps.");
      attribution = F("- Paul Harding, Tinkers");
      return;
        case 2:
      quote = F("On Friday * noon *, July the twentieth, 1714, the finest bridge in all Peru broke and precipitated five travellers into the gulf below.");
      attribution = F("- The Bridge of San Luis Rey");
      return;
        case 3:
      quote = F("Roaring * noon *. In a well-fanned Forty-second Street cellar I met Gatsby for lunch.");
      attribution = F("- The Great Gatsby");
      return;
        case 4:
      quote = F("The Birds begun at Four o'clock - Their period for Dawn - A Music numerous as space - But neighboring as * Noon *");
      attribution = F("- Emily Dickinson");
      return;
        case 5:
      quote = F("Then came the stroke of * noon *, and all these working and professional people dispersed like a trampled anthill into all the streets and directions.");
      attribution = F("- Robert Walser, The Tanners");
      return;
        case 6:
      quote = F("... the sound of a canal in the morning, the look of some roofs in the * noon * sun, and the fragrance of a certain evening flower.");
      attribution = F("- Nick Joaquin");
      return;
        case 7:
      quote = F("A cheap little clock on the wall struck * twelve * hurriedly, and served to begin the conversation.");
      attribution = F("- The Brothers Karamazov");
      return;
        case 8:
      quote = F("'Myself!' she gasped, 'and the clock is striking * twelve *! It's true, then! that's dreadful!'");
      attribution = F("- Wuthering Heights");
      return;
        case 9:
      quote = F("It was precisely * twelve o'clock *; twelve by Big Ben; whose stroke was wafted over the northern part of London; blent with that of other clocks...");
      attribution = F("- Mrs Dalloway");
      return;
        case 10:
      quote = F("The clock striketh * twelve * O it strikes, it strikes! Now body, turn to air, Or Lucifer will bear thee quick to hell.");
      attribution = F("- Dr Faustus");
      return;
        case 11:
      quote = F("The Oxen Christmas Eve, and * twelve * of the clock. \"Now they are all on their knees...\"");
      attribution = F("- Thomas Hardy, The Oxen");
      return;
        case 12:
      quote = F("'There's nobody here!' I insisted. 'It was yourself, Mrs. Linton: you knew it a while since.' 'Myself!' she gasped, 'and the clock is striking * twelve *!");
      attribution = F("- Wuthering Heights");
      return;
      } // end switch (option)

    // Codegen for 12:01 for 2 quotations:
    case 721:
      option = random(0, 2);
      // 2 options for 12:01
      switch (option) {
        case 0:
      quote = F("It was the twelfth of December, the twelfth month. A was twelve. The electric clock/radio by his bedside table said * 12:01 *. A was waiting for it to read 12:12, he hoped...");
      attribution = F("- Jonathan Trigell, Boy A");
      return;
        case 1:
      quote = F("Most of them were too high off the ground to be reached by anything less than a fireman's ladder and still retained their hands. All registered the same time: * 12:01 *.");
      attribution = F("- J.G. Ballard, Chronopolis");
      return;
      } // end switch (option)

    // Codegen for 12:02 for 1 quotations:
    case 722:
      // Only one option for 12:02:"
      quote = F("It had struck * twelve o'clock two minutes and a quarter *. The Baron's footman hastily seized a large goblet, and gasped with terror");
      attribution = F("- Crundle Castle");
      return;

    // Codegen for 12:03 for 1 quotations:
    case 723:
      // Only one option for 12:03:"
      quote = F("At * 12.03 * the sun has already punched its ticket. Sinking, it stains the cobbles and stucco of the platz in a violin-coloured throb of light...");
      attribution = F("- Michael Chabon");
      return;

    // Codegen for 12:04 for 1 quotations:
    case 724:
      // Only one option for 12:04:"
      quote = F("Though by then it was by Tina's own desk clock * 12.04pm * I was always touched when, out of a morning's worth of repetition...");
      attribution = F("- The Mezzanine");
      return;

    // Codegen for 12:05 for 0 quotations:
    case 725:
      // No quotes for 12:05
        quote = "";
        attribution = "";
      return;

    // Codegen for 12:06 for 1 quotations:
    case 726:
      // Only one option for 12:06:"
      quote = F("A few minutes' light * around noon * is all that you need to discover the error, and re-set the clock - provide that you bother to go up and make the observation.");
      attribution = F("- Neal Stephenson");
      return;

    // Codegen for 12:07 for 1 quotations:
    case 727:
      // Only one option for 12:07:"
      quote = F("On a Monday Simon Hirsch was going to break his leg at * seven minutes after 12 *, noon, and as soon as Satan told us the day before...");
      attribution = F("- Mark Twain");
      return;

    // Codegen for 12:08 for 1 quotations:
    case 728:
      // Only one option for 12:08:"
      quote = F("When a clock struck noon in Washington, D.C., the time was * 12:08 * in Philadephia, 12:12 in new York, and 12:24 in Boston.");
      attribution = F("- Eighty Days");
      return;

    // Codegen for 12:09 for 0 quotations:
    case 729:
      // No quotes for 12:09
        quote = "";
        attribution = "";
      return;

    // Codegen for 12:10 for 2 quotations:
    case 730:
      option = random(0, 2);
      // 2 options for 12:10
      switch (option) {
        case 0:
      quote = F("Madame Dumas arrived at * noon, and ten minutes later * Trause handed her his ATM card and instructed her to go to the neighborhood Citibank near Sheridan Square...");
      attribution = F("- Paul Auster, Oracle Night");
      return;
        case 1:
      quote = F("He dreamed of things with yellow eyes, misshapen heads, and crocodile mouths full of sharks' teeth. He woke five hours later, at * twelve-ten * Thursday afternoon.");
      attribution = F("- Dean Koontz, Watchers");
      return;
      } // end switch (option)

    // Codegen for 12:11 for 1 quotations:
    case 731:
      // Only one option for 12:11:"
      quote = F("At * 12:11 * there was a knock on the door. It was Terry, A could tell. He hadn't known Terry long, but there was something calmer, more patient...");
      attribution = F("- Jonathan Trigell, Boy A");
      return;

    // Codegen for 12:12 for 1 quotations:
    case 732:
      // Only one option for 12:12:"
      quote = F("A was twelve. The electric clock/radio by his bedside table said 12:01. A was waiting for it to read * 12:12 *, he hoped there would be some sense of cosmic rightness when it did.");
      attribution = F("- Jonathan Trigell, Boy A");
      return;

    // Codegen for 12:13 for 0 quotations:
    case 733:
      // No quotes for 12:13
        quote = "";
        attribution = "";
      return;

    // Codegen for 12:14 for 1 quotations:
    case 734:
      // Only one option for 12:14:"
      quote = F("She left London on the * twelve-fourteen * from Paddington, arriving at Bristol (where she had to change) at two-fifty.");
      attribution = F("- The Plymouth Express");
      return;

    // Codegen for 12:15 for 3 quotations:
    case 735:
      option = random(0, 3);
      // 3 options for 12:15
      switch (option) {
        case 0:
      quote = F("I'm in the mood when I open my window at night and look at the stars. Unfortunately it's * 12.15 * on a grey dull day, the aeroplanes are active");
      attribution = F("- Virginia Woolf");
      return;
        case 1:
      quote = F("I caught the 10.20 to Eastnor, which isn't a bad train, if you ever want to go down there. I arrived at a * quarter past twelve *, and went straight up to the house.");
      attribution = F("- The Little Nugget");
      return;
        case 2:
      quote = F("At * twelve-fifteen * he got out of the van. He tucked the pistol under the waistband of his trousers and crossed the silent, deserted street to the Hudston house.");
      attribution = F("- Dean Koontz, Watchers");
      return;
      } // end switch (option)

    // Codegen for 12:16 for 0 quotations:
    case 736:
      // No quotes for 12:16
        quote = "";
        attribution = "";
      return;

    // Codegen for 12:17 for 2 quotations:
    case 737:
      option = random(0, 2);
      // 2 options for 12:17
      switch (option) {
        case 0:
      quote = F("Kava ordered two glasses of coffee for himself and his beloved and some cake. When the pair left, exactly * seventeen minutes after twelve *, the club began to buzz with excitement.");
      attribution = F("- Vanvild Kava");
      return;
        case 1:
      quote = F("Kava ordered two glasses of coffee for himself and his beloved and some cake. When the pair left, exactly * seventeen minutes after twelve *, the club began to buzz with excitement.");
      attribution = F("- Vanvild Kava");
      return;
      } // end switch (option)

    // Codegen for 12:18 for 0 quotations:
    case 738:
      // No quotes for 12:18
        quote = "";
        attribution = "";
      return;

    // Codegen for 12:19 for 0 quotations:
    case 739:
      // No quotes for 12:19
        quote = "";
        attribution = "";
      return;

    // Codegen for 12:20 for 2 quotations:
    case 740:
      option = random(0, 2);
      // 2 options for 12:20
      switch (option) {
        case 0:
      quote = F("It is * 12:20 * in New York a Friday three days after Bastille day, yes it is 1959 and I go get a shoeshine because I will get off the 4:19 in Easthampton...");
      attribution = F("- The Day Lady Died");
      return;
        case 1:
      quote = F("By * twelve-twenty in the afternoon *, Vince was seated in a rattan chair with comfortable yellow and green cushions at a table...");
      attribution = F("- Dean Koontz, Watchers");
      return;
      } // end switch (option)

    // Codegen for 12:21 for 1 quotations:
    case 741:
      // Only one option for 12:21:"
      quote = F("Jake think of something. PLEASE! * Twelve twenty-one *.");
      attribution = F("- Stephen King, 11/22/63");
      return;

    // Codegen for 12:22 for 1 quotations:
    case 742:
      // Only one option for 12:22:"
      quote = F("By * twenty-two minutes past twelve * we leave, much too soon for our desires, this delightful spot, where the pilgrims are in the habit of bathing who come to visit the Jordan.");
      attribution = F("- Felicien de Saulcy");
      return;

    // Codegen for 12:23 for 0 quotations:
    case 743:
      // No quotes for 12:23
        quote = "";
        attribution = "";
      return;

    // Codegen for 12:24 for 1 quotations:
    case 744:
      // Only one option for 12:24:"
      quote = F("* 12:24 * My legs are in total agony. I've been kneeling on hard tiles, cleaning the bath, for what seems like hours.");
      attribution = F("- The Undomestic Goddess");
      return;

    // Codegen for 12:25 for 1 quotations:
    case 745:
      // Only one option for 12:25:"
      quote = F("Boys, do it now. God's time is * 12.25 *.");
      attribution = F("- James Joyce, Ulysses");
      return;

    // Codegen for 12:26 for 1 quotations:
    case 746:
      // Only one option for 12:26:"
      quote = F("12.25pm. * 26. * 27. Every time Billy saved a shot he looked heartbroken");
      attribution = F("- A Kestrel For a Knave");
      return;

    // Codegen for 12:27 for 0 quotations:
    case 747:
      // No quotes for 12:27
        quote = "";
        attribution = "";
      return;

    // Codegen for 12:28 for 1 quotations:
    case 748:
      // Only one option for 12:28:"
      quote = F("The DRINK CHEER-UP COFFEE wall clock read * 12.28 *.");
      attribution = F("- Stephen King, 11/22/63");
      return;

    // Codegen for 12:29 for 1 quotations:
    case 749:
      // Only one option for 12:29:"
      quote = F("...The station agent hauled out his big silver watch, looked at it critically and announced: \"* Twenty-nine minutes past twelve *.\" \"Past twelve!\" repeated Teeny-bits.");
      attribution = F("- The Mark of the Knife");
      return;

    // Codegen for 12:30 for 5 quotations:
    case 750:
      option = random(0, 5);
      // 5 options for 12:30
      switch (option) {
        case 0:
      quote = F("* 12.30 p.m. * Lunch");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("Tuesday, * 12.30pm *... Baker, California... Into the Ballantine Ale now, zombie drunk and nervous. I recognize this feeling: three or four days of booze, drugs, sun, no sleep...");
      attribution = F("- Hunter S. Thompson");
      return;
        case 2:
      quote = F("At * half past twelve *, when Catherine's anxious attention to the weather was over and she could no longer claim any merit from its amendment, the sky began voluntarily to clear.");
      attribution = F("- Northanger Abbey");
      return;
        case 3:
      quote = F("It was * half-past twelve * when I returned to the Albany as a last desperate resort. The scene of my disaster was much as I had left it.");
      attribution = F("- The Amateur Cracksman");
      return;
        case 4:
      quote = F("You'll never believe this but (in Spain) they are two hours late for ever meal - two hours Fanny - (can we lunch at * half-past twelve * today?)");
      attribution = F("- Love in a Cold Climate");
      return;
      } // end switch (option)

    // Codegen for 12:31 for 0 quotations:
    case 751:
      // No quotes for 12:31
        quote = "";
        attribution = "";
      return;

    // Codegen for 12:32 for 2 quotations:
    case 752:
      option = random(0, 2);
      // 2 options for 12:32
      switch (option) {
        case 0:
      quote = F("Why won't anything come out? OK, I'm going to squeeze it really, really hard- That nearly got my eye. * 12:32 * FUCK. What has it done to my HAIR?");
      attribution = F("- The Undomestic Goddess");
      return;
        case 1:
      quote = F("A chutney-biting brigadier named Boyd-Boyd fixed an appointment on the 'phone with Oxted, at Hornborough Station, for the * twelve thirty-two *. He was to deliver the goods.");
      attribution = F("- Stacy Aumonier");
      return;
      } // end switch (option)

    // Codegen for 12:33 for 1 quotations:
    case 753:
      // Only one option for 12:33:"
      quote = F("It's * 12.33 * now and I could do it, the station is just down that side road there.");
      attribution = F("- Five Red Herrings");
      return;

    // Codegen for 12:34 for 0 quotations:
    case 754:
      // No quotes for 12:34
        quote = "";
        attribution = "";
      return;

    // Codegen for 12:35 for 1 quotations:
    case 755:
      // Only one option for 12:35:"
      quote = F("Everyone else had been waiting five minutes and Colour Sergeant Cork called out the marker just as Trimmer appeared. So it was * twelve-thirty-five * when they were dismissed.");
      attribution = F("- Evelyn Waugh, Men At Arms");
      return;

    // Codegen for 12:36 for 0 quotations:
    case 756:
      // No quotes for 12:36
        quote = "";
        attribution = "";
      return;

    // Codegen for 12:37 for 0 quotations:
    case 757:
      // No quotes for 12:37
        quote = "";
        attribution = "";
      return;

    // Codegen for 12:38 for 0 quotations:
    case 758:
      // No quotes for 12:38
        quote = "";
        attribution = "";
      return;

    // Codegen for 12:39 for 1 quotations:
    case 759:
      // Only one option for 12:39:"
      quote = F("...the half-ebb at a quarter past seven, low water at thirty-three minutes past nine, and half flood at * thirty-nine minutes past twelve *.");
      attribution = F("- The Toilers of the Sea");
      return;

    // Codegen for 12:40 for 2 quotations:
    case 760:
      option = random(0, 2);
      // 2 options for 12:40
      switch (option) {
        case 0:
      quote = F("A little ormolu clock in the outer corridor indicated * twenty minutes to one *. The car was due at one-fifteen. Thirty-five minutes: oh, to escape for only that brief period!");
      attribution = F("- Stacy Aumonier");
      return;
        case 1:
      quote = F("We sat in the car park till * twenty to one *. And now I'm engaged to Miss Joan Hunter Dunn.");
      attribution = F("- A Subaltern's Love Song");
      return;
      } // end switch (option)

    // Codegen for 12:41 for 0 quotations:
    case 761:
      // No quotes for 12:41
        quote = "";
        attribution = "";
      return;

    // Codegen for 12:42 for 1 quotations:
    case 762:
      // Only one option for 12:42:"
      quote = F("She crushed the cigarette out and stood, brushing ash from her black skirt. It was * eighteen minutes to one *. She went to the house phone and called his room.");
      attribution = F("- Marjorie Morningstar");
      return;

    // Codegen for 12:43 for 1 quotations:
    case 763:
      // Only one option for 12:43:"
      quote = F("Died five minutes ago, you say? he asked. His eye went to the watch on his wrist. * Twelve-forty-three *, he wrote on the blotter.");
      attribution = F("- A Pocket Full of Rye");
      return;

    // Codegen for 12:44 for 1 quotations:
    case 764:
      // Only one option for 12:44:"
      quote = F("It is * around quarter to one *. No sunlight comes into the room now through the windows at right. Outside the day is fine but increasingly sultry...");
      attribution = F("- Eugene O'Neil");
      return;

    // Codegen for 12:45 for 2 quotations:
    case 765:
      option = random(0, 2);
      // 2 options for 12:45
      switch (option) {
        case 0:
      quote = F("At * 12.45 *, during a lull, Mr Yoshogi told me that owing to the war there were now many more women in England than men.");
      attribution = F("- David Footman");
      return;
        case 1:
      quote = F("Look out for D. He has just now, * 12:45 *, come from Carfax hurriedly and hastened towards the South. He seems to be going the round and may want to see you: Mina");
      attribution = F("- Bram Stoker, Dracula");
      return;
      } // end switch (option)

    // Codegen for 12:46 for 1 quotations:
    case 766:
      // Only one option for 12:46:"
      quote = F("It is * around quarter to one *. No sunlight comes into the room now through the windows at right. Outside the day is fine but increasingly sultry...");
      attribution = F("- Eugene O'Neil");
      return;

    // Codegen for 12:47 for 0 quotations:
    case 767:
      // No quotes for 12:47
        quote = "";
        attribution = "";
      return;

    // Codegen for 12:48 for 0 quotations:
    case 768:
      // No quotes for 12:48
        quote = "";
        attribution = "";
      return;

    // Codegen for 12:49 for 1 quotations:
    case 769:
      // Only one option for 12:49:"
      quote = F("The first victim of the Krefeld raid died at * 12:49 hours * Double British Summer Time at B Flight, but it wasn't due to carelessness.");
      attribution = F("- Len Deighton, Bomber");
      return;

    // Codegen for 12:50 for 1 quotations:
    case 770:
      // Only one option for 12:50:"
      quote = F("So presently Bert was sent up to the top of the house to look at a church clock which was visible therefrom, and when he came down he reported that it was * ten minutes to one *.");
      attribution = F("- Robert Tressell");
      return;

    // Codegen for 12:51 for 0 quotations:
    case 771:
      // No quotes for 12:51
        quote = "";
        attribution = "";
      return;

    // Codegen for 12:52 for 1 quotations:
    case 772:
      // Only one option for 12:52:"
      quote = F("The nightclub stood on the junction, flamboyant, still. It was * 12.52 *.");
      attribution = F("- Dreams of Leaving");
      return;

    // Codegen for 12:53 for 1 quotations:
    case 773:
      // Only one option for 12:53:"
      quote = F("Aboot twelve miles. We ought tae pass her at Pinmore. She's due there at * 12:53 *.");
      attribution = F("- Five Red Herrings");
      return;

    // Codegen for 12:54 for 2 quotations:
    case 774:
      option = random(0, 2);
      // 2 options for 12:54
      switch (option) {
        case 0:
      quote = F("At 5:33 pm there is a blast of two deep, resonant notes a major third apart. On another day there is the same blast at * 12:54 pm. * On another, exactly 8:00 am.");
      attribution = F("- Varieties of Disturbance");
      return;
        case 1:
      quote = F("...songs, anecdotes, and more drinks followed, and the pregnant minutes flew. At * six minutes to one *, when the jollity was at its highest- BOOM! There was silence instantly.");
      attribution = F("- Mark Twain");
      return;
      } // end switch (option)

    // Codegen for 12:55 for 1 quotations:
    case 775:
      // Only one option for 12:55:"
      quote = F("The inspector glanced at the clock. * Five to one *. A busy morning.");
      attribution = F("- Ngaio Marsh, A Man Lay Dead");
      return;

    // Codegen for 12:56 for 0 quotations:
    case 776:
      // No quotes for 12:56
        quote = "";
        attribution = "";
      return;

    // Codegen for 12:57 for 0 quotations:
    case 777:
      // No quotes for 12:57
        quote = "";
        attribution = "";
      return;

    // Codegen for 12:58 for 1 quotations:
    case 778:
      // Only one option for 12:58:"
      quote = F("The watch on my wrist showed * 12.58pm * I'd have time to hit the morgue.");
      attribution = F("- Ilona Andrews, Magic Bites");
      return;

    // Codegen for 12:59 for 1 quotations:
    case 779:
      // Only one option for 12:59:"
      quote = F("And I had been looking at my watch since the train had started at * 12.59pm *");
      attribution = F("- Mark Haddon");
      return;

    // Codegen for 13:00 for 13 quotations:
    case 780:
      option = random(0, 13);
      // 13 options for 13:00
      switch (option) {
        case 0:
      quote = F("* 1.00 p.m. * First afternoon class");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("After * 1 o'clock * checks, Gretta always goes out for a smoke.");
      attribution = F("- Girl, Interrupted");
      return;
        case 2:
      quote = F("Gottfried Rembke arrived at * 1pm * precisely. The moment he walked into the restaurant, handed his coat to the waiter, they knew it was him.");
      attribution = F("- Platform");
      return;
        case 3:
      quote = F("Many moons passed by. Did Baboon ever fly? Did he ever get to the sun? I've just heard today That he's well on his way! He'll be passing through Acton * at one *.");
      attribution = F("- Silly Old Baboon");
      return;
        case 4:
      quote = F("Whoever is there, as the * clock strikes one *, will be shadowed by one of my men; of these, one, or perhaps two, or even three, will leave for France to-morrow.");
      attribution = F("- The Scarlet Pimpernel");
      return;
        case 5:
      quote = F("It was then * nearly one o'clock *. All I could discern distinctly by the moonlight was a colourless, youthful face.");
      attribution = F("- The Woman in White");
      return;
        case 6:
      quote = F("Czarina Catherine reported entering Galatz at * one o'clock * today.");
      attribution = F("- Bram Stoker, Dracula");
      return;
        case 7:
      quote = F("I got to Schmidt's early, feeling horribly nervous. At * one o'clock * sharp: Toni. She was looking at the menu she knew well - Schmorbraten? Schnitzel? - when he loomed over her.");
      attribution = F("- Sybille Bedford, Jigsaw");
      return;
        case 8:
      quote = F("It was * one o'clock *. I bought some apples and a small pork pie and drove across the bridge to the other side of the riverbank in the direction of Orford Ness.");
      attribution = F("- Roma Tearne, The Swimmer");
      return;
        case 9:
      quote = F("* One o'clock * pee em! Hello, Insert Name Here! Said by the Disorganizer");
      attribution = F("- Terry Pratchett, Jingo");
      return;
        case 10:
      quote = F("That day it was * one o'clock * before John and Roger rowed across and went up to Dixon's farm for the milk and a new supply of eggs and butter.");
      attribution = F("- Swallows and Amazons");
      return;
        case 11:
      quote = F("The day-room floor gets cleared of tables and at * one o'clock * the doctor comes out of his office down the hall, nods once at the nurse as he goes past...");
      attribution = F("- Ken Kesey");
      return;
        case 12:
      quote = F("It was a bright cold day in April, and the clocks were striking * thirteen *.");
      attribution = F("- Nineteen Eighty-Four");
      return;
      } // end switch (option)

    // Codegen for 13:01 for 1 quotations:
    case 781:
      // Only one option for 13:01:"
      quote = F("There's five fathoms out there, he said. It'll be swept up that way when the tide comes in * about one *. It's nine days today.");
      attribution = F("- James Joyce, Ulysses");
      return;

    // Codegen for 13:02 for 1 quotations:
    case 782:
      // Only one option for 13:02:"
      quote = F("At * about one o'clock * the overseer arrived and told them he had no jobs for them");
      attribution = F("- A Clergyman's Daughter");
      return;

    // Codegen for 13:03 for 1 quotations:
    case 783:
      // Only one option for 13:03:"
      quote = F("It was * a little after one o'clock * when I got there, time for lunch, so I had it. The food was awful. But it would go on the expense account...");
      attribution = F("- The Big Clock");
      return;

    // Codegen for 13:04 for 1 quotations:
    case 784:
      // Only one option for 13:04:"
      quote = F("Jesus Christ! he gasped. \"It's * four minutes past one *!\" Linden frantically seized hold of a pair of steps and began wandering about the room with them.");
      attribution = F("- Robert Tressell");
      return;

    // Codegen for 13:05 for 2 quotations:
    case 785:
      option = random(0, 2);
      // 2 options for 13:05
      switch (option) {
        case 0:
      quote = F("At * five past one * Alleyn opened the outer door, knocked his pipe out on the edge of the stone step,and remained staring out on to the drive.");
      attribution = F("- Ngaio Marsh, A Man Lay Dead");
      return;
        case 1:
      quote = F("Um ... hold on! I hurry to the door, trying to block her view. \"It's already * five past one *,\" I can hear her saying a little sharply.");
      attribution = F("- The Undomestic Goddess");
      return;
      } // end switch (option)

    // Codegen for 13:06 for 1 quotations:
    case 786:
      // Only one option for 13:06:"
      quote = F("And then at precisely * 13 hours and 6 minutes * - confusion broke out in the rectangle.");
      attribution = F("- Yevgeny Zamyatin, We");
      return;

    // Codegen for 13:07 for 0 quotations:
    case 787:
      // No quotes for 13:07
        quote = "";
        attribution = "";
      return;

    // Codegen for 13:08 for 0 quotations:
    case 788:
      // No quotes for 13:08
        quote = "";
        attribution = "";
      return;

    // Codegen for 13:09 for 1 quotations:
    case 789:
      // Only one option for 13:09:"
      quote = F("At * nine minutes past one *, a pair of horses approached (not from the city, from which direction Krieger had expected her to come, but from the Desert)");
      attribution = F("- Clive Barker");
      return;

    // Codegen for 13:10 for 1 quotations:
    case 790:
      // Only one option for 13:10:"
      quote = F("It was * ten minutes past one *. \"You are sure of that?\"");
      attribution = F("- Death on the Nile");
      return;

    // Codegen for 13:11 for 2 quotations:
    case 791:
      option = random(0, 2);
      // 2 options for 13:11
      switch (option) {
        case 0:
      quote = F("I pursued my inquiries at the other stations along the line an' I found there was a gentleman wi' a bicycle tuk the * 1.11 * train at Girvan.");
      attribution = F("- Five Red Herrings");
      return;
        case 1:
      quote = F("Declares one of the waiters was the worse for liquor, and that he was giving him a dressing down. Also that it was * nearer to one than half past *.");
      attribution = F("- Agatha Christie");
      return;
      } // end switch (option)

    // Codegen for 13:12 for 0 quotations:
    case 792:
      // No quotes for 13:12
        quote = "";
        attribution = "";
      return;

    // Codegen for 13:13 for 1 quotations:
    case 793:
      // Only one option for 13:13:"
      quote = F("I looked at Hans. \"Forut!\" was his tranquil answer. \"Forward!\" replied my uncle. It was * thirteen minutes past one *.");
      attribution = F("- Jules Verne");
      return;

    // Codegen for 13:14 for 0 quotations:
    case 794:
      // No quotes for 13:14
        quote = "";
        attribution = "";
      return;

    // Codegen for 13:15 for 3 quotations:
    case 795:
      option = random(0, 3);
      // 3 options for 13:15
      switch (option) {
        case 0:
      quote = F("Where are the ladies and Gentlemen? asked Aleyn. \"Sir, in the garding,\" said Bunce. \"What time's lunch?\" \"* One-fifteen *\".");
      attribution = F("- Ngaio Marsh, A Man Lay Dead");
      return;
        case 1:
      quote = F("Monsieur has well slept this morning, he said, smiling. \"What o'clock is it, Victor?\" asked Dorian Gray, sleepily. \"* One hour and a quarter *, monsieur.\"");
      attribution = F("- The Picture of Dorian Gray");
      return;
        case 2:
      quote = F("The clock caught Miss LaFosse's eye. 'Good heavens!' she gasped. 'Look at the time. * Quarter-past one *. You must be starved.' She turned impetuously to Miss Pettigrew.");
      attribution = F("- Winifred Watson");
      return;
      } // end switch (option)

    // Codegen for 13:16 for 2 quotations:
    case 796:
      option = random(0, 2);
      // 2 options for 13:16
      switch (option) {
        case 0:
      quote = F("And the first stop had been at * 1.16pm * which was 17 minutes later.");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("At * sixteen past one *, they walked into the interview room.");
      attribution = F("- Nothing Gold Can Stay");
      return;
      } // end switch (option)

    // Codegen for 13:17 for 2 quotations:
    case 797:
      option = random(0, 2);
      // 2 options for 13:17
      switch (option) {
        case 0:
      quote = F("The clocks stopped at * 1:17 *. A long shear of light and then a series of low concussions. He got up and went to the window. What is it? she said. He didnt answer.");
      attribution = F("- Cormac McCarthy, The Road");
      return;
        case 1:
      quote = F("* One seventeen * and four seconds. That shorter guy's really got it made, and gets on a scooter, and that taller one, he goes in.");
      attribution = F("- The Terrorist, He Watches");
      return;
      } // end switch (option)

    // Codegen for 13:18 for 1 quotations:
    case 798:
      // Only one option for 13:18:"
      quote = F("* One eighteen * exactly. Was she stupid enough to head inside? Or wasn't she? We'll know before long, When the dead are carried out.");
      attribution = F("- The Terrorist, He Watches");
      return;

    // Codegen for 13:19 for 0 quotations:
    case 799:
      // No quotes for 13:19
        quote = "";
        attribution = "";
      return;

    // Codegen for 13:20 for 2 quotations:
    case 800:
      option = random(0, 2);
      // 2 options for 13:20
      switch (option) {
        case 0:
      quote = F("Kamarov, signal to Purga: 'Diving at-,' he checked his watch, '-* 1320 hours *. Exercise OCTOBER FROST begins as scheduled.");
      attribution = F("- The Hunt for Red October");
      return;
        case 1:
      quote = F("It will take him time to arrive here, see it is * twenty minutes past one *, and there are yet some times before he can hither come, be he never so quick.");
      attribution = F("- Bram Stoker, Dracula");
      return;
      } // end switch (option)

    // Codegen for 13:21 for 0 quotations:
    case 801:
      // No quotes for 13:21
        quote = "";
        attribution = "";
      return;

    // Codegen for 13:22 for 1 quotations:
    case 802:
      // Only one option for 13:22:"
      quote = F("It was * 1:22 * when we found Dad's grave.");
      attribution = F("- Jonathan Safran Foer");
      return;

    // Codegen for 13:23 for 2 quotations:
    case 803:
      option = random(0, 2);
      // 2 options for 13:23
      switch (option) {
        case 0:
      quote = F("And when we got to Swindon Mother had keys to the house and we went in and she said, \"Hello?\" but there was no one there because it was * 1.23pm *.\"");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("The clock marked * twenty-three minutes past one *. He was suddenly full of agitation, yet hopeful. She had come! Who could tell what she would say?");
      attribution = F("- A Mummer's Tale");
      return;
      } // end switch (option)

    // Codegen for 13:24 for 1 quotations:
    case 804:
      // Only one option for 13:24:"
      quote = F("Littell checked his watch - * 1:24 p.m * - Littell grabbed the phone by the bed.");
      attribution = F("- The Cold Six Thousand");
      return;

    // Codegen for 13:25 for 1 quotations:
    case 805:
      // Only one option for 13:25:"
      quote = F("I'd really have liked to, I told her, if it weren't for the things I had in the drier. I cast an eye at my watch. * One-twenty-five *. The drier had already stopped.");
      attribution = F("- Haruki Murakami");
      return;

    // Codegen for 13:26 for 1 quotations:
    case 806:
      // Only one option for 13:26:"
      quote = F("Raymond came back with Masson * around one-thirty *. His arm was bandaged up and he had an adhesive plaster on the corner of his mouth.");
      attribution = F("- Albert Camus, The Stranger");
      return;

    // Codegen for 13:27 for 1 quotations:
    case 807:
      // Only one option for 13:27:"
      quote = F("At * twenty-seven minutes past one * she felt as if she was levitating out of her body.");
      attribution = F("- Deon Meyer, Trackers");
      return;

    // Codegen for 13:28 for 0 quotations:
    case 808:
      // No quotes for 13:28
        quote = "";
        attribution = "";
      return;

    // Codegen for 13:29 for 0 quotations:
    case 809:
      // No quotes for 13:29
        quote = "";
        attribution = "";
      return;

    // Codegen for 13:30 for 3 quotations:
    case 810:
      option = random(0, 3);
      // 3 options for 13:30
      switch (option) {
        case 0:
      quote = F("...announced, genially and fraternally, as if it were a pleasure to Messrs Rigby and Lowndes to give the information gratis, that it was * half-past one *.");
      attribution = F("- Mrs. Dalloway");
      return;
        case 1:
      quote = F("Lupin not having come down, I went up again at * half-past one *, and said we dined at two; he said he \"would be there.\"");
      attribution = F("- The Diary of a Nobody");
      return;
        case 2:
      quote = F("She was a sticker. A clock away in the town struck * half past one *.");
      attribution = F("- Brighton Rock");
      return;
      } // end switch (option)

    // Codegen for 13:31 for 0 quotations:
    case 811:
      // No quotes for 13:31
        quote = "";
        attribution = "";
      return;

    // Codegen for 13:32 for 2 quotations:
    case 812:
      option = random(0, 2);
      // 2 options for 13:32
      switch (option) {
        case 0:
      quote = F("At the third stroke it will be * one ... thirty-two * ... and twenty seconds. 'Beep ... beep ... beep.' Ford Prefect suppressed a little giggle of evil satisfaction...");
      attribution = F("- Douglas Adams");
      return;
        case 1:
      quote = F("She grinned at him with malicious playfulness, showing great square teeth, and then ran for the stairs. * One-thirty-two *. She thought that she heard a whistle blown...");
      attribution = F("- Stamboul Train");
      return;
      } // end switch (option)

    // Codegen for 13:33 for 1 quotations:
    case 813:
      // Only one option for 13:33:"
      quote = F("He waited for the green light to show and then opened the door again on to the now empty cargo hold.'... * one ... thirty-three * ... and fifty seconds.' Very nice.");
      attribution = F("- Douglas Adams");
      return;

    // Codegen for 13:34 for 2 quotations:
    case 814:
      option = random(0, 2);
      // 2 options for 13:34
      switch (option) {
        case 0:
      quote = F("He tiptoed out and returned to the control cabin. '... * one ... thirty-four * and twenty seconds.' The voice sounded as clear as if he was hearing it over a phone in London.");
      attribution = F("- Douglas Adams");
      return;
        case 1:
      quote = F("...which was where he particularly wanted it to be heard. 'At the third stroke it will be * one ... thirty ... four * ... precisely.'");
      attribution = F("- Douglas Adams");
      return;
      } // end switch (option)

    // Codegen for 13:35 for 0 quotations:
    case 815:
      // No quotes for 13:35
        quote = "";
        attribution = "";
      return;

    // Codegen for 13:36 for 0 quotations:
    case 816:
      // No quotes for 13:36
        quote = "";
        attribution = "";
      return;

    // Codegen for 13:37 for 1 quotations:
    case 817:
      // Only one option for 13:37:"
      quote = F("He was on the job, and he scribbled in his notebook: \"* 1.37pm * Subject appears to be getting laid.\"");
      attribution = F("- Light House");
      return;

    // Codegen for 13:38 for 0 quotations:
    case 818:
      // No quotes for 13:38
        quote = "";
        attribution = "";
      return;

    // Codegen for 13:39 for 1 quotations:
    case 819:
      // Only one option for 13:39:"
      quote = F("And it was now * 1.39pm * which was 23 minutes after the stop, which mean that we would be at the sea if the train didn't go in a big curve.");
      attribution = F("- Mark Haddon");
      return;

    // Codegen for 13:40 for 0 quotations:
    case 820:
      // No quotes for 13:40
        quote = "";
        attribution = "";
      return;

    // Codegen for 13:41 for 0 quotations:
    case 821:
      // No quotes for 13:41
        quote = "";
        attribution = "";
      return;

    // Codegen for 13:42 for 1 quotations:
    case 822:
      // Only one option for 13:42:"
      quote = F("The last note was recorded at * 1.42pm *: G.M. on site at H-by; will take over the matter.");
      attribution = F("- Stieg Larsson");
      return;

    // Codegen for 13:43 for 0 quotations:
    case 823:
      // No quotes for 13:43
        quote = "";
        attribution = "";
      return;

    // Codegen for 13:44 for 1 quotations:
    case 824:
      // Only one option for 13:44:"
      quote = F("By good luck, the next train was due at * forty-four minutes past one *, and arrived at Yateland (the next station) ten minutes afterward.");
      attribution = F("- Mr. Policeman and the Cook");
      return;

    // Codegen for 13:45 for 2 quotations:
    case 825:
      option = random(0, 2);
      // 2 options for 13:45
      switch (option) {
        case 0:
      quote = F("The blow fell at precisely * one forty-five * (summer-time). Benson, my Aunt Agatha's butler, was offering me the fried potatoes at the moment, and such was my emotion...");
      attribution = F("- Sir Roderick Comes to Lunch");
      return;
        case 1:
      quote = F("The last man had just gone to the wickets, with the score at a hundred and thirty-one, when a * quarter to two * arrived, and with it the luncheon interval.");
      attribution = F("- PG Wodehouse, Mike");
      return;
      } // end switch (option)

    // Codegen for 13:46 for 0 quotations:
    case 826:
      // No quotes for 13:46
        quote = "";
        attribution = "";
      return;

    // Codegen for 13:47 for 1 quotations:
    case 827:
      // Only one option for 13:47:"
      quote = F("Brianne was sitting at her desk, trying to complete an essay before the 2pm deadline. It was * 1.47pm. *");
      attribution = F("- Sue Townsend");
      return;

    // Codegen for 13:48 for 1 quotations:
    case 828:
      // Only one option for 13:48:"
      quote = F("It was * twelve minutes to two * in the afternoon when Claude Moreau and his most-trusted field officer, Jacques Bergeron, arrived at the Georges Cinq station...");
      attribution = F("- The Apocalypse Watch");
      return;

    // Codegen for 13:49 for 1 quotations:
    case 829:
      // Only one option for 13:49:"
      quote = F("The bookstall clerk had seen the passenger in grey pass the bookstall at * 1.49 *, in the direction of the exit.");
      attribution = F("- Five Red Herrings");
      return;

    // Codegen for 13:50 for 2 quotations:
    case 830:
      option = random(0, 2);
      // 2 options for 13:50
      switch (option) {
        case 0:
      quote = F("The best train of the day was the * one-fifty * from Paddington which reached Polgarwith just after seven o'clock.");
      attribution = F("- The Cornish Mystery");
      return;
        case 1:
      quote = F("Rahel's toy wristwatch had the time painted on it. * Ten to two *. One of her ambitions was to own a watch on which she could change the time whenever she wanted to...");
      attribution = F("- The God of Small Things");
      return;
      } // end switch (option)

    // Codegen for 13:51 for 1 quotations:
    case 831:
      // Only one option for 13:51:"
      quote = F("At * nine minutes to two * the other vehicle arrived. At first Milla didn't believe her eyes: that shape, those markings.");
      attribution = F("- Deon Meyer, Trackers");
      return;

    // Codegen for 13:52 for 0 quotations:
    case 832:
      // No quotes for 13:52
        quote = "";
        attribution = "";
      return;

    // Codegen for 13:53 for 0 quotations:
    case 833:
      // No quotes for 13:53
        quote = "";
        attribution = "";
      return;

    // Codegen for 13:54 for 0 quotations:
    case 834:
      // No quotes for 13:54
        quote = "";
        attribution = "";
      return;

    // Codegen for 13:55 for 1 quotations:
    case 835:
      // Only one option for 13:55:"
      quote = F("I came the next day at * five minutes before two *, and on reaching the schoolroom door, before I opened it, I heard a rapid, gabbling sound");
      attribution = F("- The Professor");
      return;

    // Codegen for 13:56 for 0 quotations:
    case 836:
      // No quotes for 13:56
        quote = "";
        attribution = "";
      return;

    // Codegen for 13:57 for 1 quotations:
    case 837:
      // Only one option for 13:57:"
      quote = F("I looked for a clock. It was * three minutes to two *. \"I hope you can catch him, then. Thank you. I really appreciate it.\"");
      attribution = F("- C.E. Murphy, Urban Shaman");
      return;

    // Codegen for 13:58 for 1 quotations:
    case 838:
      // Only one option for 13:58:"
      quote = F("It was * almost two o'clock *, but nothing moved, Stari Teocak was silent and so empty it seemed abandoned, and yet Tijmen constantly felt he was being observed by invisible eyes.");
      attribution = F("- King of Tuzla");
      return;

    // Codegen for 13:59 for 1 quotations:
    case 839:
      // Only one option for 13:59:"
      quote = F("..and close the loop and keep it there, orbiting in perpetual obscurity. '* One ... fifty-nine ... *'");
      attribution = F("- Douglas Adams");
      return;

    // Codegen for 14:00 for 9 quotations:
    case 840:
      option = random(0, 9);
      // 9 options for 14:00
      switch (option) {
        case 0:
      quote = F("At approximately * 1400 hours * a pair of enemy Skyhawks came flying in at deck level out of nowhere.");
      attribution = F("- Black Swan Green");
      return;
        case 1:
      quote = F("When Salander woke up it was * 2.00 * on Saturday afternoon and a doctor was poking at her.");
      attribution = F("- Stieg Larsson");
      return;
        case 2:
      quote = F("* At two *, the snowplows were in action in Lillestrom.");
      attribution = F("- Jo Nesbo, The Snowman");
      return;
        case 3:
      quote = F("After an unnaturally prolonged wheezing there followed a shrill, nasty, and as it were unexpectedly rapid, chime - as though someone were suddenly jumping forward. * It struck two. *");
      attribution = F("- Notes From the Underground");
      return;
        case 4:
      quote = F("At * two o'clock * Gatsby put on his bathing suit and left word with the butler that if any one phoned word was to be brought to him at the pool.");
      attribution = F("- The Great Gatsby");
      return;
        case 5:
      quote = F("I caught the * two o'clock * bus. It was very hot. I ate at Celeste's restaurant as usual. They all felt very sorry for me and Celeste told me, 'There's no one like a mother'.");
      attribution = F("- Albert Camus, The Outsider");
      return;
        case 6:
      quote = F("'She could have fired the jig, and he could have kept on picking up his packages at the old time, * two o'clock *. As it was, he had almost been arrested.'");
      attribution = F("- A Confederacy of Dunces");
      return;
        case 7:
      quote = F("The Home for Aged Persons is at Marengo, some fifty miles from Algiers. With the * two o'clock * bus I should get there well before nightfall. Then I can spend the night there.");
      attribution = F("- Albert Camus, The Outsider");
      return;
        case 8:
      quote = F("The old people's home is at Marengo, fifty miles from Algiers. I'll catch the * two o'clock * bus and get there in the afternoon.");
      attribution = F("- Albert Camus, The Outsider");
      return;
      } // end switch (option)

    // Codegen for 14:01 for 2 quotations:
    case 841:
      option = random(0, 2);
      // 2 options for 14:01
      switch (option) {
        case 0:
      quote = F("At * about two o' clock * the owners young wife came, carrying a handleless cup and a pot with a quilted cover, to where I was still lying disconsolate");
      attribution = F("- A Single Pebble");
      return;
        case 1:
      quote = F("So I sent word that I shouldn't be able to umpire for the team at Steeple Sinderby and, after working through the morning, came down * about two o'clock *.");
      attribution = F("- A Month in the Country");
      return;
      } // end switch (option)

    // Codegen for 14:02 for 1 quotations:
    case 842:
      // Only one option for 14:02:"
      quote = F("I'm not dead. How did that happen? He was right. It was * 14.02 * and twenty-six seconds. Destiny had not been fulfilled.");
      attribution = F("- The Woman Who Died A Lot");
      return;

    // Codegen for 14:03 for 0 quotations:
    case 843:
      // No quotes for 14:03
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:04 for 1 quotations:
    case 844:
      // Only one option for 14:04:"
      quote = F("* 2.04pm. * Once again, the Quartermaster-General's office came on the line asking for Colonel Finckh, and once again Finckh heard the quiet, unemotional, unfamiliar voice");
      attribution = F("- The Night of the Generals");
      return;

    // Codegen for 14:05 for 2 quotations:
    case 845:
      option = random(0, 2);
      // 2 options for 14:05
      switch (option) {
        case 0:
      quote = F("What time is it?' '* Five minutes past two *, sir,' answered the man, looking at the clock and yawning. 'Five minutes past two? How horribly late!");
      attribution = F("- The Picture of Dorian Gray");
      return;
        case 1:
      quote = F("...and at * five past two * on 17 September of that same unforgettable year 1916, I was in the Muryovo hospital yard, standing on trampled withered grass...");
      attribution = F("- A Country Doctor's Notebook");
      return;
      } // end switch (option)

    // Codegen for 14:06 for 1 quotations:
    case 846:
      // Only one option for 14:06:"
      quote = F("A man driving a tractor saw her, four hundred yards from her house, * six minutes past two * in the afternoon.");
      attribution = F("- A Change of Climate");
      return;

    // Codegen for 14:07 for 0 quotations:
    case 847:
      // No quotes for 14:07
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:08 for 0 quotations:
    case 848:
      // No quotes for 14:08
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:09 for 0 quotations:
    case 849:
      // No quotes for 14:09
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:10 for 1 quotations:
    case 850:
      // Only one option for 14:10:"
      quote = F("* Ten minutes past two *, sir, answered the man, looking at the clock and blinking. \"Ten minutes past two? How horribly late!\"");
      attribution = F("- The Picture of Dorian Gray");
      return;

    // Codegen for 14:11 for 0 quotations:
    case 851:
      // No quotes for 14:11
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:12 for 0 quotations:
    case 852:
      // No quotes for 14:12
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:13 for 1 quotations:
    case 853:
      // Only one option for 14:13:"
      quote = F("At the third stroke, it will be * two ... thirteen * ... and fifty seconds.'");
      attribution = F("- Douglas Adams");
      return;

    // Codegen for 14:14 for 0 quotations:
    case 854:
      // No quotes for 14:14
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:15 for 3 quotations:
    case 855:
      option = random(0, 3);
      // 3 options for 14:15
      switch (option) {
        case 0:
      quote = F("* 2.15 p.m. * Second afternoon class");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("I had a date with her next day at * 2.15pm * in my own rooms, but it was less successful, she seemed to have grown less juvenile, more of a woman overnight.");
      attribution = F("- Vladimir Nabokov, Lolita");
      return;
        case 2:
      quote = F("I had a date with her next day at * 2.15 P.M. * In my own rooms, but it was less successful, she seemed to have grown less juvenile, more of a woman overnight.");
      attribution = F("- Vladimir Nabokov, Lolita");
      return;
      } // end switch (option)

    // Codegen for 14:16 for 1 quotations:
    case 856:
      // Only one option for 14:16:"
      quote = F("Oh, good evening. I think you were on the barrier when I came in at * 2.16 * this afternoon. Now, do you know that you let me get past without giving up my ticket? Yes, yes he-he!");
      attribution = F("- Five Red Herrings");
      return;

    // Codegen for 14:17 for 1 quotations:
    case 857:
      // Only one option for 14:17:"
      quote = F("The flare spluttered and faded, and now the glare of the spotlight blinded me. I sat very still. It was * 2.17 *.");
      attribution = F("- The Ipcress File");
      return;

    // Codegen for 14:18 for 0 quotations:
    case 858:
      // No quotes for 14:18
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:19 for 1 quotations:
    case 859:
      // Only one option for 14:19:"
      quote = F("* 2:19 *: Duane Hinton walks out. He walks through the backyard. He lugs some clothes. He wore said clothes last night. He walks to the fence. He feeds the incinerator.");
      attribution = F("- The Cold Six Thousand");
      return;

    // Codegen for 14:20 for 4 quotations:
    case 860:
      option = random(0, 4);
      // 4 options for 14:20
      switch (option) {
        case 0:
      quote = F("She looked at her watch and it was * twenty minutes past two *. She had no time to lose but must get ready at once.");
      attribution = F("- A Tale of Two Cities");
      return;
        case 1:
      quote = F("The watch found at the Weir was challenged by the jeweller as one he had wound and set for Edwin Drood, at * twenty minutes past two * on that same afternoon; and it had run down...");
      attribution = F("- The Mystery of Edwin Drood");
      return;
        case 2:
      quote = F("Inevitable, implacable, the rainstorm wept itself out. She saw Tom look at his watch. 'What time is it?' '* Twenty past two *. Want to go back to the hotel for a while?' 'All right.'");
      attribution = F("- The Doctor's Wife");
      return;
        case 3:
      quote = F("She turned abruptly to the nurse and asked the time. '* Two-twenty *' 'Ah...Two-twenty!' Genevieve repeated, as though there was something urgent to be done.");
      attribution = F("- Southern Mail");
      return;
      } // end switch (option)

    // Codegen for 14:21 for 0 quotations:
    case 861:
      // No quotes for 14:21
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:22 for 1 quotations:
    case 862:
      // Only one option for 14:22:"
      quote = F("Garth here. Sunday afternoon. Sorry to miss you, but I'll leave a brief message on your tape. * Two-twenty-two * or there-aboutish. Great party.");
      attribution = F("- Larry's Party");
      return;

    // Codegen for 14:23 for 0 quotations:
    case 863:
      // No quotes for 14:23
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:24 for 0 quotations:
    case 864:
      // No quotes for 14:24
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:25 for 1 quotations:
    case 865:
      // Only one option for 14:25:"
      quote = F("Gary shut himself inside his office and flipped through the messages. Caroline had called at 1:35, 1:40, 1:50, 1:55, and 2:10; it was now * 2:25 *. He pumped his fist in triumph.");
      attribution = F("- The Corrections");
      return;

    // Codegen for 14:26 for 0 quotations:
    case 866:
      // No quotes for 14:26
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:27 for 0 quotations:
    case 867:
      // No quotes for 14:27
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:28 for 1 quotations:
    case 868:
      // Only one option for 14:28:"
      quote = F("Apparently they wanted us to know that that something might happen at * 28 minutes and 57 seconds after 2pm * on a day yet to be specified.");
      attribution = F("- Don DeLillo, Ratner's Star");
      return;

    // Codegen for 14:29 for 0 quotations:
    case 869:
      // No quotes for 14:29
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:30 for 6 quotations:
    case 870:
      option = random(0, 6);
      // 6 options for 14:30
      switch (option) {
        case 0:
      quote = F("May 14th 1800. Wm and John set off into Yorkshire after dinner at * 1/2 past 2 o'clock *, cold pork in their pockets. I left them at the turning of the Low-wood bay under the trees.");
      attribution = F("- Dorothy Wordsworth");
      return;
        case 1:
      quote = F("Ach! It's * 2:30 *. Look how the time is flying. And it's still so much to do today.. It's dishes to clean, dinner to defrost, and my pills I haven't yet counted.");
      attribution = F("- Art Spiegelman, Maus");
      return;
        case 2:
      quote = F("At * 2.30pm * on the 13th inst. began to shadow Sir Bobadil the Ostrich, whom I suspect of being the criminal. Shadowing successful. Didn't lose sight of him once.");
      attribution = F("- The Wind on the Moon");
      return;
        case 3:
      quote = F("At * half past two * the same afternoon the boy and the elderly man are standing in the room directly above the Inner Office and Waiting-Room.");
      attribution = F("- Corker's Freedom");
      return;
        case 4:
      quote = F("It was * half-past two * in the afternoon. The sun hung in the faded blue sky like a burning mirror, and away beyond the paddocks the blue mountains quivered and leapt like sea.");
      attribution = F("- Katherine Mansfield, Millie");
      return;
        case 5:
      quote = F("It was * half-past two * o'clock when the knock came. I took my courage a deux mains and waited. In a few minutes Mary opened the door, and announced \"Dr. Van Helsing\"");
      attribution = F("- Bram Stoker, Dracula");
      return;
      } // end switch (option)

    // Codegen for 14:31 for 0 quotations:
    case 871:
      // No quotes for 14:31
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:32 for 1 quotations:
    case 872:
      // Only one option for 14:32:"
      quote = F("Like * 2.32 p.m. *, Beecher and Avalon, L3 R2 (which meant left three blocks, right two) 2:35 p.m., and you wondered how you could pick up one box...");
      attribution = F("- Post Office");
      return;

    // Codegen for 14:33 for 0 quotations:
    case 873:
      // No quotes for 14:33
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:34 for 0 quotations:
    case 874:
      // No quotes for 14:34
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:35 for 0 quotations:
    case 875:
      // No quotes for 14:35
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:36 for 1 quotations:
    case 876:
      // Only one option for 14:36:"
      quote = F("I look at my watch. * Two thirty-six *. All I've got left today is take in the laundry and fix dinner.");
      attribution = F("- The Elephant Vanishes");
      return;

    // Codegen for 14:37 for 0 quotations:
    case 877:
      // No quotes for 14:37
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:38 for 0 quotations:
    case 878:
      // No quotes for 14:38
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:39 for 1 quotations:
    case 879:
      // Only one option for 14:39:"
      quote = F("Noo, there's a report come in fra' the station-master at Pinwherry that there was a gentleman tuk the * 2.39 * at Pinwherry.");
      attribution = F("- Five Red Herrings");
      return;

    // Codegen for 14:40 for 2 quotations:
    case 880:
      option = random(0, 2);
      // 2 options for 14:40
      switch (option) {
        case 0:
      quote = F("Members of Big Side marked Michael and Alan as the two most promising three-quarters for Middle Side next year, and when the bell sounded at * twenty minutes to three *...");
      attribution = F("- Sinister Street");
      return;
        case 1:
      quote = F("If a girl looks swell when she meets you, who gives a damn when she's late? 'We better hurry', I said. 'The show starts at * two-forty *.'");
      attribution = F("- The Catcher in the Rye");
      return;
      } // end switch (option)

    // Codegen for 14:41 for 1 quotations:
    case 881:
      // Only one option for 14:41:"
      quote = F("At * 2.41 *, when the afternoon fast train to London was pulling out of Larborough prompt to the minute, Miss Pym sat under the cedar on the lawn wondering whether she was a fool...");
      attribution = F("- Miss Pym Disposes");
      return;

    // Codegen for 14:42 for 0 quotations:
    case 882:
      // No quotes for 14:42
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:43 for 1 quotations:
    case 883:
      // Only one option for 14:43:"
      quote = F("Jacobson died at * 2.43pm * the next day after slashing his wrists with a razor blade in the second cubicle from the left in the men's washroom on the third floor.");
      attribution = F("- JG Ballard, Now: Zero");
      return;

    // Codegen for 14:44 for 0 quotations:
    case 884:
      // No quotes for 14:44
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:45 for 3 quotations:
    case 885:
      option = random(0, 3);
      // 3 options for 14:45
      switch (option) {
        case 0:
      quote = F("He never came down till a * quarter to three *.");
      attribution = F("- The Diary of a Nobody");
      return;
        case 1:
      quote = F("I look, and I see the clock has no hands. 'There are no hands,' I say. The old woman looks at the clock face and says to me, 'It's a * quarter to three *'.");
      attribution = F("- The Old Woman");
      return;
        case 2:
      quote = F("Pull the other one, and tell it to the marines, and don't make me laugh, and fuck off out of it, and all that, but the fact remained that it was still only * two forty-five *'.");
      attribution = F("- The Pregnant Window");
      return;
      } // end switch (option)

    // Codegen for 14:46 for 1 quotations:
    case 886:
      // Only one option for 14:46:"
      quote = F("Vicki shoved her glasses at her face and peered at the clock. * Two forty-six *. 'I don't have time for this' she muttered...");
      attribution = F("- Tanya Huff, Blood Lines");
      return;

    // Codegen for 14:47 for 0 quotations:
    case 887:
      // No quotes for 14:47
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:48 for 0 quotations:
    case 888:
      // No quotes for 14:48
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:49 for 0 quotations:
    case 889:
      // No quotes for 14:49
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:50 for 1 quotations:
    case 890:
      // Only one option for 14:50:"
      quote = F("Stands the Church clock at * ten to three *? And is there honey still for tea?");
      attribution = F("- Rupert Brooke");
      return;

    // Codegen for 14:51 for 0 quotations:
    case 891:
      // No quotes for 14:51
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:52 for 0 quotations:
    case 892:
      // No quotes for 14:52
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:53 for 0 quotations:
    case 893:
      // No quotes for 14:53
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:54 for 1 quotations:
    case 894:
      // Only one option for 14:54:"
      quote = F("...and that terrible listlessness that starts to set in * about 2.55 *, when you know you've had all the baths you can usefully have that day...");
      attribution = F("- Douglas Adams");
      return;

    // Codegen for 14:55 for 1 quotations:
    case 895:
      // Only one option for 14:55:"
      quote = F("The superior, the very reverend John Conmee SJ reset his smooth watch in his interior pocket as he came down the presbytery steps. * Five to three *. Just nice time to walk to Artane.");
      attribution = F("- James Joyce, Ulysses");
      return;

    // Codegen for 14:56 for 2 quotations:
    case 896:
      option = random(0, 2);
      // 2 options for 14:56
      switch (option) {
        case 0:
      quote = F("It was * 2:56 * when the shovel touched the coffin. We all heard the sound and looked at each other.");
      attribution = F("- Jonathan Safran Foer");
      return;
        case 1:
      quote = F("* 2.56 P.M. * Helen is alone now. Her face is out of frame, and through the viewfinder I see only a segment of the pillow...");
      attribution = F("- The 60 Minute Zoom");
      return;
      } // end switch (option)

    // Codegen for 14:57 for 0 quotations:
    case 897:
      // No quotes for 14:57
        quote = "";
        attribution = "";
      return;

    // Codegen for 14:58 for 2 quotations:
    case 898:
      option = random(0, 2);
      // 2 options for 14:58
      switch (option) {
        case 0:
      quote = F("...from twenty-five minutes past eleven until twenty-eight minutes past eleven, from ten minutes to three until * two minutes to three * the heroes of the school met...");
      attribution = F("- Sinister Street");
      return;
        case 1:
      quote = F("We betted that it would happen in the afternoon; we got odds of four to one on that; we betted that it would happen at * two minutes to three *...");
      attribution = F("- Mark Twain");
      return;
      } // end switch (option)

    // Codegen for 14:59 for 0 quotations:
    case 899:
      // No quotes for 14:59
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:00 for 18 quotations:
    case 900:
      option = random(0, 18);
      // 18 options for 15:00
      switch (option) {
        case 0:
      quote = F("The Regrouting Committee's been dragging along since may. Suddenly they're pulling secret 0* 300 * milk-switches? It doesn't ring true, Jim.");
      attribution = F("- Infinite Jest");
      return;
        case 1:
      quote = F("* At three * on the Wednesday afternoon, that bit of the painting was completed.");
      attribution = F("- The Moonstone");
      return;
        case 2:
      quote = F("I exalted my suffering into a sign of greatness (lying collapsed on a bed * at three * in the afternoon), and hence protected myself from experiencing my grief...");
      attribution = F("- Essays in Love");
      return;
        case 3:
      quote = F("At * three o'clock * on the afternoon of that same day, he called on her. She held out her two hands, smiling in her usual charming, friendly way...");
      attribution = F("- Guy de Maupassant, Bel-Ami");
      return;
        case 4:
      quote = F("At * three o'clock * precisely I was at Baker Street, but Holmes had not yet returned.");
      attribution = F("- A Scandal in Bohemia");
      return;
        case 5:
      quote = F("He walks into the Hospital for Broken Things at * three o'clock * on Monday afternoon. That was the arrangement.");
      attribution = F("- Paul Auster, Sunset Park");
      return;
        case 6:
      quote = F("'I gotta get uptown by * three o'clock *.'");
      attribution = F("- A Confederacy of Dunces");
      return;
        case 7:
      quote = F("I had a * three o'clock * class in psychology, the first meeting of the semester, and I suspected I was going to miss it. I was right.");
      attribution = F("- T.C. Boyle, Achates McNeil");
      return;
        case 8:
      quote = F("It was * three o'clock * in the beautiful breezy autumn day when Mr. Casaubon drove off to his Rectory at Lowick, only five miles from Tipton.");
      attribution = F("- George Eliot, Middlemarch");
      return;
        case 9:
      quote = F("Ladies bathed before noon, after their * three-o'clock * naps, and by nightfall were like soft teacakes with frostings of sweat and sweet talcum.");
      attribution = F("- To kill a mockingbird");
      return;
        case 10:
      quote = F("M. Madeleine usually came at * three o'clock *, and as punctuality was kindness, he was punctual.");
      attribution = F("- Victor Hugo, Les Miserables");
      return;
        case 11:
      quote = F("On Wednesday at * three o'clock *, Monsieur and Madame Bovary, seated in their dog-cart, set out for Vaubyessard...");
      attribution = F("- Madame Bovary");
      return;
        case 12:
      quote = F("Remember, they shouted, \"battle at * three o'clock * sharp. There's no time to lose.\"");
      attribution = F("- Swallows and Amazons");
      return;
        case 13:
      quote = F("* Three o'clock * is always too late or too early for anything you want to do.");
      attribution = F("- Jean-Paul Sartre, Nausea");
      return;
        case 14:
      quote = F("* Three o'clock * is the perfect time in Cham, because anything is possible. You can still ski, but also respectably start drinking...");
      attribution = F("- Jonathan Trigell, Cham");
      return;
        case 15:
      quote = F("Today was the day Alex had appointed for her 'punishment'. I became increasingly nervous as the hour of * three o'clock * approached. I was alone in the house...");
      attribution = F("- David Lodge, Deaf Sentence");
      return;
        case 16:
      quote = F("... when she heard, distractedly, something fumbling, something scratching at the door. Who at this hour? * Three *, good Heavens! Three already!");
      attribution = F("- Mrs Dalloway");
      return;
        case 17:
      quote = F("Why, if you listen you will hear it strike. George listened, and a neighbouring clock immediately obliged. \"But it's only gone * three *!\"");
      attribution = F("- Three Men in a Boat");
      return;
      } // end switch (option)

    // Codegen for 15:01 for 1 quotations:
    case 901:
      // Only one option for 15:01:"
      quote = F("The sun was now setting. It was * about three in the afternoon * when Alisande had begun to tell me who the cowboys were...");
      attribution = F("- Mark Twain");
      return;

    // Codegen for 15:02 for 0 quotations:
    case 902:
      // No quotes for 15:02
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:03 for 1 quotations:
    case 903:
      // Only one option for 15:03:"
      quote = F("I check Shingi's mobile phone - it says it's * 3.03pm. * I get out of bed, open my suitcase to take clean socks out and the smell of Mother hit my nose and make me feel dizzy.");
      attribution = F("- Harare North");
      return;

    // Codegen for 15:04 for 1 quotations:
    case 904:
      // Only one option for 15:04:"
      quote = F("Woken at * 1504 * by Michelangelo hammering away with his chisel.");
      attribution = F("- 101 Reykjavik");
      return;

    // Codegen for 15:05 for 1 quotations:
    case 905:
      // Only one option for 15:05:"
      quote = F("Ultimately, at * five minutes past three * that afternoon, Smith admitted the falsity of the Fort Scott tale. \"That was only something Dick told his family.\"");
      attribution = F("- In Cold Blood");
      return;

    // Codegen for 15:06 for 0 quotations:
    case 906:
      // No quotes for 15:06
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:07 for 1 quotations:
    case 907:
      // Only one option for 15:07:"
      quote = F("The next day was grey, threatening rain. He was there at * seven minutes past three *. The clock on the church over the way pointed to it.");
      attribution = F("- Patrick Hamilton");
      return;

    // Codegen for 15:08 for 1 quotations:
    case 908:
      // Only one option for 15:08:"
      quote = F("...1 or more miles and furlongs won by an outsider at odds of 50:1 at * 3hr 8m p.m. * at Ascot (Greenwich time), the message being received...");
      attribution = F("- James Joyce, Ulysses");
      return;

    // Codegen for 15:09 for 1 quotations:
    case 909:
      // Only one option for 15:09:"
      quote = F("On the next day he boarded the London train which reaches Hull at * 3.09 *. At Paragon Station he soon singled out Beamish from Merriman's description.");
      attribution = F("- The Pit-Prop Syndicate");
      return;

    // Codegen for 15:10 for 3 quotations:
    case 910:
      option = random(0, 3);
      // 3 options for 15:10
      switch (option) {
        case 0:
      quote = F("This time it was only the simple fact that the hands chanced to point to * 3.10pm *, the precise moment at which all the clocks of London had stopped.");
      attribution = F("- The Purple Cloud");
      return;
        case 1:
      quote = F("Love again; wanking at * ten past three *");
      attribution = F("- Philip Larkin, Love Again");
      return;
        case 2:
      quote = F("...the loss so you couldn't be charge for anything beyond the time of my calling them up. I looked at the clock. It was * ten-past three *.");
      attribution = F("- Ali Smith");
      return;
      } // end switch (option)

    // Codegen for 15:11 for 0 quotations:
    case 911:
      // No quotes for 15:11
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:12 for 0 quotations:
    case 912:
      // No quotes for 15:12
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:13 for 1 quotations:
    case 913:
      // Only one option for 15:13:"
      quote = F("The lift moved. It was * thirteen minutes past three *. The bell gave out its ping. Two men stepped out of the lift, Alan Norman and another man. Tony Blair walked into the office.");
      attribution = F("- Virtual Assassin");
      return;

    // Codegen for 15:14 for 2 quotations:
    case 914:
      option = random(0, 2);
      // 2 options for 15:14
      switch (option) {
        case 0:
      quote = F("A signal sounded. \"There's the * 3.14 * up,\" said Perks. \"You lie low till she's through, and then we'll go up along to my place, and see if there's any of them strawberries...\"");
      attribution = F("- The Railway Children");
      return;
        case 1:
      quote = F("I shall be back at exactly * THREE fourteen *, for our hour of revery together, real sweet revery darling");
      attribution = F("- Jack Kerouac, On the Road");
      return;
      } // end switch (option)

    // Codegen for 15:15 for 4 quotations:
    case 915:
      option = random(0, 4);
      // 4 options for 15:15
      switch (option) {
        case 0:
      quote = F("Above the door of Room 69 the clock ticked on at * 3:15 *. The motion was accelerating. What had once been the gymnasium was now a small room...");
      attribution = F("- JG Ballard, Manhole 69");
      return;
        case 1:
      quote = F("As I was going out the door, I decided to leave a note. So I wrote: \"Feeling better and going to Birch Creek. Back soon. R. * 3:15 *.\"");
      attribution = F("- Where I'm Calling From");
      return;
        case 2:
      quote = F("...decided on a delivery rate of 40 rontgens/min., target distance of 530 cm. Everything is ready now. Woke 11:05. To sleep * 3:15 *.");
      attribution = F("- The Voices of Time");
      return;
        case 3:
      quote = F("He had been on the point of slipping Edgar Wallace into his pocket. The clock over the Prince of Wales struck a * quarter past three *.");
      attribution = F("- Keep the Aspidistra Flying");
      return;
      } // end switch (option)

    // Codegen for 15:16 for 1 quotations:
    case 916:
      // Only one option for 15:16:"
      quote = F("The Nimrod rendezvoused with the light aircraft at * 1516 * GMT.");
      attribution = F("- Iain Banks, The Crow Road");
      return;

    // Codegen for 15:17 for 0 quotations:
    case 917:
      // No quotes for 15:17
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:18 for 0 quotations:
    case 918:
      // No quotes for 15:18
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:19 for 0 quotations:
    case 919:
      // No quotes for 15:19
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:20 for 1 quotations:
    case 920:
      // Only one option for 15:20:"
      quote = F("At * twenty minutes past three * on Monday, 26 January 1948, in Tokyo, and I am drinking and I am drinking and I am drinking and I am drinking...");
      attribution = F("- David Peace, Occupied City");
      return;

    // Codegen for 15:21 for 1 quotations:
    case 921:
      // Only one option for 15:21:"
      quote = F("Next, he remembered that the morrow of Christmas would be the twenty-seventh day of the moon, and that consequently high water would be at * twenty-one minutes past three *");
      attribution = F("- The Toilers of the Sea");
      return;

    // Codegen for 15:22 for 0 quotations:
    case 922:
      // No quotes for 15:22
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:23 for 1 quotations:
    case 923:
      // Only one option for 15:23:"
      quote = F("* Three twenty-three *! Is that all? Doesn't time - no, I've already said that, thought that. I sit and watch the seconds change on the watch.");
      attribution = F("- Iain Banks, Espedair Street");
      return;

    // Codegen for 15:24 for 0 quotations:
    case 924:
      // No quotes for 15:24
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:25 for 1 quotations:
    case 925:
      // Only one option for 15:25:"
      quote = F("Got it: 'Darling Hepzibah'-Hepzibah? What kind of name is that?-'Will meet you Reading Sunday * 15.25 * train Didcot-Reading.' Reading you all right, you idiots.");
      attribution = F("- Tom McCarthy, C");
      return;

    // Codegen for 15:26 for 0 quotations:
    case 926:
      // No quotes for 15:26
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:27 for 1 quotations:
    case 927:
      // Only one option for 15:27:"
      quote = F("And she rang the Reverend Peters and he came into school at * 3.27pm * and he said, 'So, young man, are we ready to roll?'");
      attribution = F("- Mark Haddon");
      return;

    // Codegen for 15:28 for 0 quotations:
    case 928:
      // No quotes for 15:28
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:29 for 1 quotations:
    case 929:
      // Only one option for 15:29:"
      quote = F("Good heavens! she said, it's * nearly half-past three *. I must fly. Don't forget about the funeral service.\"");
      attribution = F("- Nuns at Luncheon");
      return;

    // Codegen for 15:30 for 4 quotations:
    case 930:
      option = random(0, 4);
      // 4 options for 15:30
      switch (option) {
        case 0:
      quote = F("* 3.30 p.m. * Catch school bus home");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("At * Half past Three *, a single Bird Unto a silent Sky Propounded but a single term Of cautious melody.");
      attribution = F("- Emily Dickinson");
      return;
        case 2:
      quote = F("I must have completed my packing with time to spare, for when the knock came on my door at * half past three * precisely, I had been sitting in my chair waiting for a good while.");
      attribution = F("- When We Were Orphans");
      return;
        case 3:
      quote = F("Before I am rrroasting the alarm-clock, I am setting it to go off, not at nine o'clock the next morning, but at * half-past thrrree * the next afternoon.");
      attribution = F("- Roald Dahl, The Witches");
      return;
      } // end switch (option)

    // Codegen for 15:31 for 0 quotations:
    case 931:
      // No quotes for 15:31
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:32 for 1 quotations:
    case 932:
      // Only one option for 15:32:"
      quote = F("At * 3:32 * precisely, I noticed Kaitlyn striding confidently past the Wok House. She saw me the moment I raised my hand, flashed her very white and newly straightened teeth at me...");
      attribution = F("- The Fault in Our Stars");
      return;

    // Codegen for 15:33 for 1 quotations:
    case 933:
      // Only one option for 15:33:"
      quote = F("I picked up my briefcase, glancing at my watch again as I did so. * Three thirty-three *.");
      attribution = F("- Stephen King, 11/22/63");
      return;

    // Codegen for 15:34 for 0 quotations:
    case 934:
      // No quotes for 15:34
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:35 for 2 quotations:
    case 935:
      option = random(0, 2);
      // 2 options for 15:35
      switch (option) {
        case 0:
      quote = F("... it didn't inevitably mean she'd still be in such a state when the schoolbus deposited Wolfie back home at * 3:35 P.M. *");
      attribution = F("- Joyce Carol Oates");
      return;
        case 1:
      quote = F("By * three-thirty-five * business really winds down. I have already sold my ladderback chairs and my Scottish cardigans. I'm not even sure now why I've sold all these...");
      attribution = F("- Lorrie Moore, Anagrams");
      return;
      } // end switch (option)

    // Codegen for 15:36 for 0 quotations:
    case 936:
      // No quotes for 15:36
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:37 for 1 quotations:
    case 937:
      // Only one option for 15:37:"
      quote = F("But, thought Dirk, what god? And why? What god would be hanging around Terminal Two of Heathrow Airport trying to catch the * 15.37 * flight to Oslo?");
      attribution = F("- Douglas Adams");
      return;

    // Codegen for 15:38 for 0 quotations:
    case 938:
      // No quotes for 15:38
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:39 for 1 quotations:
    case 939:
      // Only one option for 15:39:"
      quote = F("I lived two lives in late 1965 and early 1963, one in Dallas and one in Jodie. They came together at * three thirty-nine * in the afternoon of April 10.");
      attribution = F("- Stephen King, 11/22/63");
      return;

    // Codegen for 15:40 for 1 quotations:
    case 940:
      // Only one option for 15:40:"
      quote = F("At * three-forty *, Cliff called to report that Dilworth and his lady friend were sitting on the deck of the Amazing Grace, eating fruit and sipping wine...");
      attribution = F("- Dean Koontz, Watchers");
      return;

    // Codegen for 15:41 for 1 quotations:
    case 941:
      // Only one option for 15:41:"
      quote = F("At * 15:41 * GMT, the Cessna's engine began to cut out and the plane - presumably out of fuel - began to lose altitude");
      attribution = F("- Iain Banks, The Crow Road");
      return;

    // Codegen for 15:42 for 1 quotations:
    case 942:
      // Only one option for 15:42:"
      quote = F("We are due in Yellow Sky at * 3:42 *, he said, looking tenderly into her eyes. \"Oh, are we?\" she said, as if she had not been aware of it.");
      attribution = F("- Bride Comes to Yellow Sky");
      return;

    // Codegen for 15:43 for 0 quotations:
    case 943:
      // No quotes for 15:43
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:44 for 1 quotations:
    case 944:
      // Only one option for 15:44:"
      quote = F("The armed response team hastily assembled from Strangnas arrived at Bjurman's summer cabin at * 3.44 p.m. *");
      attribution = F("- Stieg Larsson");
      return;

    // Codegen for 15:45 for 2 quotations:
    case 945:
      option = random(0, 2);
      // 2 options for 15:45
      switch (option) {
        case 0:
      quote = F("... film of the first three hours, screened it this morning at the lab. The first true-horror movie. I looked like a half-animated corpse. Woke 10:25. To sleep * 3:45 *.");
      attribution = F("- The Voices of Time");
      return;
        case 1:
      quote = F("I opened my notebook, flipped almost to the end before I found a blank page, and wrote \"October 5th, * 3.45pm *, Dunning to Longview Cem, puts flowers on parents' (?) graves. Rain.\"");
      attribution = F("- Stephen King, 11/22/63");
      return;
      } // end switch (option)

    // Codegen for 15:46 for 0 quotations:
    case 946:
      // No quotes for 15:46
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:47 for 0 quotations:
    case 947:
      // No quotes for 15:47
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:48 for 0 quotations:
    case 948:
      // No quotes for 15:48
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:49 for 3 quotations:
    case 949:
      option = random(0, 3);
      // 3 options for 15:49
      switch (option) {
        case 0:
      quote = F("It was * 3.49 * when he hit me because of the two hundred times I had said, \"I don't know.\" He hit me a lot after that.");
      attribution = F("- The Ipcress File");
      return;
        case 1:
      quote = F("* 3.49 p.m. * Get off school bus at home");
      attribution = F("- Mark Haddon");
      return;
        case 2:
      quote = F("And one of them was that Mother didn't get back from work til 5.30 pm so I had to go to Father's house between * 3.49 pm * and 5.30 pm because I wasn't allowed to be on my own...");
      attribution = F("- Mark Haddon");
      return;
      } // end switch (option)

    // Codegen for 15:50 for 1 quotations:
    case 950:
      // Only one option for 15:50:"
      quote = F("* 3.50 p.m. * Have juice and snack");
      attribution = F("- Mark Haddon");
      return;

    // Codegen for 15:51 for 3 quotations:
    case 951:
      option = random(0, 3);
      // 3 options for 15:51
      switch (option) {
        case 0:
      quote = F("Sweat trickled down my ribs. The digital reading on the clock-radio was * 3:51 *. Always odd numbered at times like this. What does it mean? Is death odd-numbered?");
      attribution = F("- Don DeLillo, White Noise");
      return;
        case 1:
      quote = F("The digital reading on the clock-radio was * 3:51 *. Always odd numbers at times like this. What does it mean? Is death odd-numbered?");
      attribution = F("- Don DeLillo, White Noise");
      return;
        case 2:
      quote = F("The telegram seems to say, \"The Sovereigns and the Royal Children expect themselves at Rome tomorrow at * fifty-one minutes after fifteen o'clock *.\"");
      attribution = F("- Italian Without a Master");
      return;
      } // end switch (option)

    // Codegen for 15:52 for 0 quotations:
    case 952:
      // No quotes for 15:52
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:53 for 1 quotations:
    case 953:
      // Only one option for 15:53:"
      quote = F("It was like the clouds lifting away from the sun. Jodie glanced at Reacher. He glanced at the clock. * Seven minutes to four *. Less than three hours to go.");
      attribution = F("- Lee Child, Tripwire");
      return;

    // Codegen for 15:54 for 0 quotations:
    case 954:
      // No quotes for 15:54
        quote = "";
        attribution = "";
      return;

    // Codegen for 15:55 for 1 quotations:
    case 955:
      // Only one option for 15:55:"
      quote = F("* 3.55 p.m. * Give Toby food and water");
      attribution = F("- Mark Haddon");
      return;

    // Codegen for 15:56 for 1 quotations:
    case 956:
      // Only one option for 15:56:"
      quote = F("* Four minutes to four *. Newman sighed again, lost in thought.");
      attribution = F("- Lee Child, Tripwire");
      return;

    // Codegen for 15:57 for 1 quotations:
    case 957:
      // Only one option for 15:57:"
      quote = F("It was * close upon four * before the door opened, and a drunken-looking groom, ill-kempt and side-whiskered with an inflamed face and disreputable clothes, walked into the room.");
      attribution = F("- Arthur Conan Doyle");
      return;

    // Codegen for 15:58 for 2 quotations:
    case 958:
      option = random(0, 2);
      // 2 options for 15:58
      switch (option) {
        case 0:
      quote = F("The clock atop the clubhouse reads * 3:58 *.");
      attribution = F("- Don Delillo, Underworld");
      return;
        case 1:
      quote = F("* Towards four o'clock * the condition of the English army was serious. The Prince of Orange was in command of the centre, Hill of the right wing, Picton of the left wing.");
      attribution = F("- Victor Hugo, Les Miserables");
      return;
      } // end switch (option)

    // Codegen for 15:59 for 1 quotations:
    case 959:
      // Only one option for 15:59:"
      quote = F("He looked at his watch: it was * nearly 4 *. He helped Delphine to her feet and led her down a passage to a rear door that gave on to the hospital garden.");
      attribution = F("- The Blue Afternoon");
      return;

    // Codegen for 16:00 for 18 quotations:
    case 960:
      option = random(0, 18);
      // 18 options for 16:00
      switch (option) {
        case 0:
      quote = F("...face off as best she can with her hand and says she'll try to fit him in for a sponge bath before she goes off shift at * 1600h. *, at which Gately goes rigid with dread.");
      attribution = F("- Infinite Jest");
      return;
        case 1:
      quote = F("* 4.00 p.m. * Take Toby out of his cage");
      attribution = F("- Mark Haddon");
      return;
        case 2:
      quote = F("It was exactly * 4 o'clock * as I made my way out of the building, and doors opened behind and before me, discharging salvos of vocal babble and the noise of chair-legs scraping...");
      attribution = F("- David Lodge, Deaf Sentence");
      return;
        case 3:
      quote = F("Charmian woke * at four * and sensed the emptiness of the house.");
      attribution = F("- Muriel Spark, Memento Mori");
      return;
        case 4:
      quote = F("Djerzinski arrived punctually * at four o'clock *. Desplechin had asked to see him. The case was intriguing.");
      attribution = F("- Atomised");
      return;
        case 5:
      quote = F("Waking * at four * to soundless dark, I stare. In time the curtain-edges will grow light. Till then I see what's really always there: Unresting death");
      attribution = F("- Philip Larkin, Aubade");
      return;
        case 6:
      quote = F("What else can I answer, When the lights come on * at four *. At the end of another year.");
      attribution = F("- Philip Larkin");
      return;
        case 7:
      quote = F("...and that as you stare at the clock the hands will move relentlessly on to * four o'clock *, and you will enter the long dark teatime of the soul.");
      attribution = F("- Douglas Adams");
      return;
        case 8:
      quote = F("As he turned off towards the fishing village of Cellardyke, the familiar pips announced the * four o'clock * news. The comforting voice of the newsreader began the bulletin.");
      attribution = F("- The Last Temptation");
      return;
        case 9:
      quote = F("* Four o'clock * has just struck. Good! Arrangement, revision, reading from four to five. Short snooze of restoration for myself, from five to six.");
      attribution = F("- Walter Hartwright VII");
      return;
        case 10:
      quote = F("* Four o'clock *: wedge-shaped gardens lie Under a cavernous, a wind-picked sky.");
      attribution = F("- Philip Larkin, Sad Steps");
      return;
        case 11:
      quote = F("* Four o'clock *: when time in the city quivers on its axis - the day not yet spent, the wheels of evening just beginning to turn.");
      attribution = F("- The Act of Love");
      return;
        case 12:
      quote = F("He played for twenty-two days, just as he said he would. Every day at * four o'clock * in the afternoon, regardless of how much fighting was going on around him.");
      attribution = F("- The Cellist Of Sarajevo");
      return;
        case 13:
      quote = F("I doubt whether anyone was commissioned to send the news along the actual telegraph, and yet Mrs. Proudie knew it before * four o'clock *.");
      attribution = F("- Anthony Trollope");
      return;
        case 14:
      quote = F("I only found out much later that those flowers were called * Four O'clock *, and were not magic at all. The magic was in the seed, waiting to be watered and cared for...");
      attribution = F("- Ghost Generations");
      return;
        case 15:
      quote = F("Look to the west. A clack. For me. -What time is that? asked Blazes Boylan. * Four? O'clock *. Lenehan, small eyes ahunger on her humming, bust ahumming...");
      attribution = F("- James Joyce, Ulysses");
      return;
        case 16:
      quote = F("...when they all sat down to table at * four o'clock *, about three hours after his arrival, he had secured his lady, engaged her mother's consent...");
      attribution = F("- Sense and Sensibility");
      return;
        case 17:
      quote = F("In the four thousand rooms of the Centre the four thousand electric clocks simultaneously * struck four *. Discarnate voices called from the trumpet mouths.");
      attribution = F("- Brave New World");
      return;
      } // end switch (option)

    // Codegen for 16:01 for 1 quotations:
    case 961:
      // Only one option for 16:01:"
      quote = F("Light is coming in through the curtains. Suddenly the digits on the clock radio look like a year. * 1601 *. I woke up a bit early, don't have to be born for another 400 years.");
      attribution = F("- 101 Reykjavik");
      return;

    // Codegen for 16:02 for 1 quotations:
    case 962:
      // Only one option for 16:02:"
      quote = F("I'd just looked up at the clock, to make sure time wasn't getting away from me, when I heard the shot. It was * two minutes after four *. I didn't know what to do.");
      attribution = F("- Southern Ghost");
      return;

    // Codegen for 16:03 for 1 quotations:
    case 963:
      // Only one option for 16:03:"
      quote = F("She read the page carefully and then said, '* 16.03 * - cat goes to the toilet in front garden.'");
      attribution = F("- What was Lost");
      return;

    // Codegen for 16:04 for 2 quotations:
    case 964:
      option = random(0, 2);
      // 2 options for 16:04
      switch (option) {
        case 0:
      quote = F("* A little after four o'clock *, Pippa meandered over to Dot's house carrying a bottle of wine she had been keeping in reserve...");
      attribution = F("- Rebecca Miller");
      return;
        case 1:
      quote = F("* Four minutes after four! * It's still very early and to get from here to there won't take me more than 15 minutes, even walking slowly.");
      attribution = F("- Angel Hill");
      return;
      } // end switch (option)

    // Codegen for 16:05 for 2 quotations:
    case 965:
      option = random(0, 2);
      // 2 options for 16:05
      switch (option) {
        case 0:
      quote = F("It was exactly * five minutes past four * as Mr. Robert Audley stepped out upon the platform at Shoreditch, and waited placidly...");
      attribution = F("- Lady Audley's Secret");
      return;
        case 1:
      quote = F("...when a tall young man with a rather ugly and bespectacled, but intelligent face, came up and said, 'Could you please tell me the time?' I glanced at my watch. '* Five past four *.'");
      attribution = F("- Sylvia Plath, The Bell Jar");
      return;
      } // end switch (option)

    // Codegen for 16:06 for 1 quotations:
    case 966:
      // Only one option for 16:06:"
      quote = F("At * six minutes after four *, Benny's Cadillac pulled up in front of Mr. Botelia's store, and Benny's mother stepped out of the car with Penelope...");
      attribution = F("- Follow Me: A Novel");
      return;

    // Codegen for 16:07 for 1 quotations:
    case 967:
      // Only one option for 16:07:"
      quote = F("...without time to repent of anything or to say goodbye to anyone, at * seven minutes after four * on Pentecost Sunday.");
      attribution = F("- Love in the Time of Cholera");
      return;

    // Codegen for 16:08 for 1 quotations:
    case 968:
      // Only one option for 16:08:"
      quote = F("It was * eight minutes after four *. I still don't have a plan. Maybe the guys in the Nova, maybe they had a plan.");
      attribution = F("- The Monkey's Raincoat");
      return;

    // Codegen for 16:09 for 1 quotations:
    case 969:
      // Only one option for 16:09:"
      quote = F("I just wanted to know if there was any improvement. \"No, there isn't. It was nice of you to call.\" She hung up. It was * nine minutes after four *.");
      attribution = F("- Ira Levin, Rosemary's Baby");
      return;

    // Codegen for 16:10 for 2 quotations:
    case 970:
      option = random(0, 2);
      // 2 options for 16:10
      switch (option) {
        case 0:
      quote = F("* 1610h. * E.T.A Weight room. Freestyle circuits. The clank and click of various resistance systems.");
      attribution = F("- Infinite Jest");
      return;
        case 1:
      quote = F("She looks at the clock. She's in the kitchen. A minute left. She waits. It's * ten-past four *. She picks up the eclair. She licks the cream out of it.");
      attribution = F("- Roddy Doyle, Paula Spencer");
      return;
      } // end switch (option)

    // Codegen for 16:11 for 1 quotations:
    case 971:
      // Only one option for 16:11:"
      quote = F("* 4:11 P.M. * Thurs. A Huey helicopter flies east overhead as the last of the U.S. Marines make ready to leave the beach; a buzzard dangles in the thermals closer over the town.");
      attribution = F("- Denis Johnson, Seek");
      return;

    // Codegen for 16:12 for 1 quotations:
    case 972:
      // Only one option for 16:12:"
      quote = F("At precisely * twelve minutes after four * a body of cavalry rode into the square, four abreast, clearing a way for the funeral cortege.");
      attribution = F("- The Empty Mirror");
      return;

    // Codegen for 16:13 for 2 quotations:
    case 973:
      option = random(0, 2);
      // 2 options for 16:13
      switch (option) {
        case 0:
      quote = F("But at precisely * 4.13pm *, the fifty thousand spectators saw the totally unexpected happen, before their very eyes.");
      attribution = F("- Mario Vargas Llosa");
      return;
        case 1:
      quote = F("...she opened her eyes to see digital numbers in the dark, 4:12 in pale green. Behind her Harry continued to move, settling in. She watched the numbers change to * 4:13 *.");
      attribution = F("- Elmore Leonard, Get Shorty");
      return;
      } // end switch (option)

    // Codegen for 16:14 for 1 quotations:
    case 974:
      // Only one option for 16:14:"
      quote = F("Then at * 4.14pm * on March 12 I moved behind zinc-zirconium-not-to-be-revealed-compounds protecting me in this hill...");
      attribution = F("- Denis Johnson, Already Dead");
      return;

    // Codegen for 16:15 for 6 quotations:
    case 975:
      option = random(0, 6);
      // 6 options for 16:15
      switch (option) {
        case 0:
      quote = F("Just the sort of irritating thing you don't expect to hang over your final exit. (Moral: don't try too hard to forget Eniwetok.) Woke 9:40. To sleep * 4:15 *.");
      attribution = F("- The Voices of Time");
      return;
        case 1:
      quote = F("The sun had begun to sink in the west, and the shadow of an oak branch had crept across my knees. My watch said it was * 4.15 *.");
      attribution = F("- The Wind-up Bird Chronicle");
      return;
        case 2:
      quote = F("...like flat feet or diabetes: they're all in danger of ending up alone by their own stubborn choice. The ugly kitchen clock says * four-fifteen *.");
      attribution = F("- Pigs in Heaven");
      return;
        case 3:
      quote = F("I remember the dread with which I at * quarter past four */ Let go with a bang behind me our house front door");
      attribution = F("- False Security");
      return;
        case 4:
      quote = F("It is only a * quarter past four *, (shewing his watch) and you are not now in Bath. No theatre, no rooms to prepare for. Half an hour at Northanger must be enough.");
      attribution = F("- Northanger Abbey");
      return;
        case 5:
      quote = F("On the tenth day of October at * quarter past four * in the afternoon with a dry hot wind blowing through the passed Maria found herself in Baker.");
      attribution = F("- Play it as it Lays");
      return;
      } // end switch (option)

    // Codegen for 16:16 for 1 quotations:
    case 976:
      // Only one option for 16:16:"
      quote = F("* 4.16pm * The terrace outside the bar is packed, and Igor feels proud of his ability to plan things, because even though he's never been to Cannes before...");
      attribution = F("- The Winner Stands Alone");
      return;

    // Codegen for 16:17 for 2 quotations:
    case 977:
      option = random(0, 2);
      // 2 options for 16:17
      switch (option) {
        case 0:
      quote = F("Apparently the great Percy has no sense of humour, for at * four-seventeen * he got tired of it, and hit Skinner crisply in the right eyeball, blacking the same as per illustration.");
      attribution = F("- A Prefect's Uncle");
      return;
        case 1:
      quote = F("...seeing only the police cars, knowing as she glanced down at her watch and saw that it was * seventeen minutes after four *, that for her time had stopped.");
      attribution = F("- Joy Fielding, Life Penalty");
      return;
      } // end switch (option)

    // Codegen for 16:18 for 1 quotations:
    case 978:
      // Only one option for 16:18:"
      quote = F("* 4.18 p.m. * Put Toby into his cage");
      attribution = F("- Mark Haddon");
      return;

    // Codegen for 16:19 for 1 quotations:
    case 979:
      // Only one option for 16:19:"
      quote = F("Jessica [* 4:19 PM *] Don't tease me like that. I haven't been to a play in years. Charles [4:19 PM] Then it'll be my treat. You and the hubby can have big fun on me.");
      attribution = F("- The Other Woman");
      return;

    // Codegen for 16:20 for 2 quotations:
    case 980:
      option = random(0, 2);
      // 2 options for 16:20
      switch (option) {
        case 0:
      quote = F("* 4.20 p.m. * Watch television or a video");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("At * twenty minutes past four * - or, to put it another, blunter way, an hour and twenty minutes past what seemed to be all reasonable hope...");
      attribution = F("- J. D. Salinger");
      return;
      } // end switch (option)

    // Codegen for 16:21 for 1 quotations:
    case 981:
      // Only one option for 16:21:"
      quote = F("* 4.21pm * As they started on, Doug picked up a twig and after rubbing it off, started to move one end of it inside his mouth. \"What are you doing?\" Bob asked.");
      attribution = F("- Hunted Past Reason");
      return;

    // Codegen for 16:22 for 2 quotations:
    case 982:
      option = random(0, 2);
      // 2 options for 16:22
      switch (option) {
        case 0:
      quote = F("He hurt me to the point where I wanted to tell him something. My watch said * 4.22 * now. It had stopped. It was smashed.");
      attribution = F("- The Ipcress File");
      return;
        case 1:
      quote = F("Monday, * 4.22pm * Washington, D.C. Paul Hood took his daily late-afternoon look at the list of names on his computer monitor.");
      attribution = F("- Tom Clancy, Steve Pieczenik, and Jeff Rovin");
      return;
      } // end switch (option)

    // Codegen for 16:23 for 1 quotations:
    case 983:
      // Only one option for 16:23:"
      quote = F("The warming-related 'adjustments' to Earth's orbit had shortened the winter days, so that now, in January, sunset was taking place at * 4.23 *.");
      attribution = F("- A Visit from the Goon Squad");
      return;

    // Codegen for 16:24 for 1 quotations:
    case 984:
      // Only one option for 16:24:"
      quote = F("Mike winked at Ashley and continued with the remaining greetings and hugs and handshakes. The time was * 4:24 *. Six hours to go. The minutes seemed to just melt away.");
      attribution = F("- Travis Thrasher, Teardrop");
      return;

    // Codegen for 16:25 for 2 quotations:
    case 985:
      option = random(0, 2);
      // 2 options for 16:25
      switch (option) {
        case 0:
      quote = F("As I dressed I glanced at my watch. It was no wonder that no one was stirring. It was * twenty-five minutes past four *.");
      attribution = F("- Sir Arthur Conan Doyle");
      return;
        case 1:
      quote = F("As I dressed I glanced at my watch. It was no wonder that no one was stirring. It was * twenty-five minutes past four *.");
      attribution = F("- Arthur Conan Doyle");
      return;
      } // end switch (option)

    // Codegen for 16:26 for 1 quotations:
    case 986:
      // Only one option for 16:26:"
      quote = F("I don't know him. Nor does he know me. Nor ever shall we. She put her bare hand in the sun, where the wind would weather it. It was * twenty-six minutes after four *.");
      attribution = F("- Catch-As-Catch-Can");
      return;

    // Codegen for 16:27 for 0 quotations:
    case 987:
      // No quotes for 16:27
        quote = "";
        attribution = "";
      return;

    // Codegen for 16:28 for 1 quotations:
    case 988:
      // Only one option for 16:28:"
      quote = F("Same day: * 4.28pm *- Right turn at the second bus stop after the gas station. I stopped the car at the first ward post office and inquired at the corner tobacconists.");
      attribution = F("- The Ruined Map: A Novel");
      return;

    // Codegen for 16:29 for 1 quotations:
    case 989:
      // Only one option for 16:29:"
      quote = F("October 21, 2007, * 4:29 pm. * The phone was red. And what William hated most about it, besides the fact that it was inconveniently mounted on a wall...");
      attribution = F("- Believing Cedric");
      return;

    // Codegen for 16:30 for 4 quotations:
    case 990:
      option = random(0, 4);
      // 4 options for 16:30
      switch (option) {
        case 0:
      quote = F("Hester Thrale undulates in in a false fox jacket at 2330 as usual even though she has to be up at like 0* 430 * for the breakfast shift at the Provident Nursing Home...");
      attribution = F("- Infinite Jest");
      return;
        case 1:
      quote = F("At * four-thirty * that afternoon in late January when I stepped into the parlour with Boo, my dog, Hutch was in his favourite armchair, scowling at the television.");
      attribution = F("- Dean Koontz, Odd Hours");
      return;
        case 2:
      quote = F("But after apologies, insults, laughter, and tears, Romeo and Juliet were to be seen together later that afternoon, mushily holding hands in the dark at a * four-thirty * screening...");
      attribution = F("- Essays in Love");
      return;
        case 3:
      quote = F("I leave the office at * four thirty *, head up to Xclusive where I work out on free weights for an hour, then taxi across the park to Gio's in the Pierre Room for a facial...");
      attribution = F("- American Psycho");
      return;
      } // end switch (option)

    // Codegen for 16:31 for 1 quotations:
    case 991:
      // Only one option for 16:31:"
      quote = F("From: Renee Greene - August 5, 2011 - * 4:31 PM * To: Shelley Manning Subject: Re: All Access What should I be worried about, then? JUST KIDDING.");
      attribution = F("- Click: An Online Love Story");
      return;

    // Codegen for 16:32 for 1 quotations:
    case 992:
      // Only one option for 16:32:"
      quote = F("* 4.32pm. * Now the eight Marines next to us leave their emplacement and file quickly past, the last saying, \"Go! Go! Go!\" They break into a run.");
      attribution = F("- Denis Johnson, Seek");
      return;

    // Codegen for 16:33 for 1 quotations:
    case 993:
      // Only one option for 16:33:"
      quote = F("At * 4.33pm *, a short bald man puffing on a cigar arrived at the library. He approached a huge cabinet storing thousands of alphabetically arranged cards and slid a drawer out.");
      attribution = F("- Havana World Series");
      return;

    // Codegen for 16:34 for 1 quotations:
    case 994:
      // Only one option for 16:34:"
      quote = F("There was a wardrobe in the corner. A bedside table with a collection of water glasses of varying ages and an alarm clock with red digital numbers- * 4.34 p.m. *");
      attribution = F("- The Raw Shark Texts");
      return;

    // Codegen for 16:35 for 1 quotations:
    case 995:
      // Only one option for 16:35:"
      quote = F("The Voice shut itself off with a click, and then reopened conversation by announcing the arrival at Platform 9 of the * 4.35 * from Birmingham and Wolverhampton.");
      attribution = F("- 4.50 from Paddington");
      return;

    // Codegen for 16:36 for 0 quotations:
    case 996:
      // No quotes for 16:36
        quote = "";
        attribution = "";
      return;

    // Codegen for 16:37 for 1 quotations:
    case 997:
      // Only one option for 16:37:"
      quote = F("She should have been home by now. * 1637. * Yes. It's as if I had the date of a year on my arm. Every day is a piece of world history.");
      attribution = F("- 101 Reykjavik");
      return;

    // Codegen for 16:38 for 0 quotations:
    case 998:
      // No quotes for 16:38
        quote = "";
        attribution = "";
      return;

    // Codegen for 16:39 for 1 quotations:
    case 999:
      // Only one option for 16:39:"
      quote = F("Harlem enjoys lazy Sabbath mornings, although the pace picks up again in the afternoon, after church. My watch read * 4:39 p.m. *, and I realized that I hadn't eaten all day.");
      attribution = F("- Blood Red Blues");
      return;

    // Codegen for 16:40 for 1 quotations:
    case 1000:
      // Only one option for 16:40:"
      quote = F("* Four forty * P.M. Besta sang another hymn. Everyone knew something was wrong. How long did they wait?");
      attribution = F("- Tip 'T.I.' Harris");
      return;

    // Codegen for 16:41 for 1 quotations:
    case 1001:
      // Only one option for 16:41:"
      quote = F("At * 4:41 * Crane's voice crackled through the walkie-talkie as if he'd read their thoughts of mutiny. \"Everyone into the elevator. Now!\"");
      attribution = F("- Damaged Goods: A Novel");
      return;

    // Codegen for 16:42 for 1 quotations:
    case 1002:
      // Only one option for 16:42:"
      quote = F("I pumped my right fist into the air. The time was * 4:42pm. * Eleven hours and forty-two minutes since the start of the race.");
      attribution = F("- Haruki Murakami");
      return;

    // Codegen for 16:43 for 0 quotations:
    case 1003:
      // No quotes for 16:43
        quote = "";
        attribution = "";
      return;

    // Codegen for 16:44 for 0 quotations:
    case 1004:
      // No quotes for 16:44
        quote = "";
        attribution = "";
      return;

    // Codegen for 16:45 for 2 quotations:
    case 1005:
      option = random(0, 2);
      // 2 options for 16:45
      switch (option) {
        case 0:
      quote = F("The next day Bill took only ten minutes of the twenty-minute break allotted for the afternoon and left at * fifteen minutes before five *.");
      attribution = F("- Where I'm Calling From");
      return;
        case 1:
      quote = F("At * four-forty-five * Miss Haddon went to tea with the Principal, who explained why she desired all the pupils to learn the same duet. It was part of her new co-ordinative system.");
      attribution = F("- EM Forster, Co-ordination");
      return;
      } // end switch (option)

    // Codegen for 16:46 for 1 quotations:
    case 1006:
      // Only one option for 16:46:"
      quote = F("At * 4:46 * an obese, middle-aged man shuffled in. Wearing a starched guayabera and dark green pants, Urena asked for a book on confectionery...");
      attribution = F("- The Havana World Series");
      return;

    // Codegen for 16:47 for 1 quotations:
    case 1007:
      // Only one option for 16:47:"
      quote = F("But maybe it was more than that, maybe Affenlight had erred badly somehow, because here it was 4:49 by his watch, * 4:47 * by the wall clock, and Owen had not yet come.");
      attribution = F("- The Art of Fielding");
      return;

    // Codegen for 16:48 for 1 quotations:
    case 1008:
      // Only one option for 16:48:"
      quote = F("What am I doing? It's * 4:48 a.m. *, and I'm parked outside their darkened house.");
      attribution = F("- What is the What");
      return;

    // Codegen for 16:49 for 1 quotations:
    case 1009:
      // Only one option for 16:49:"
      quote = F("* 4:49 p.m. *, a bald-headed man wearing khakis and ankle-high deck shoes came out through the front door of the purple house on 21st Avenue East.");
      attribution = F("- Beyond Recognition");
      return;

    // Codegen for 16:50 for 3 quotations:
    case 1010:
      option = random(0, 3);
      // 3 options for 16:50
      switch (option) {
        case 0:
      quote = F("The train standing at Platform 3, the Voice told her, \"is the * 4.50 * for Brackhampton, Milchester, Waverton, Carvil Junction, Roxeter and stations to Chadmouth.\"");
      attribution = F("- 4.50 from Paddington");
      return;
        case 1:
      quote = F("They had all frozen at the same time, on a snowy night, seven years before, and after that it was always * ten minutes to five * in the castle.");
      attribution = F("- The 13 Clocks");
      return;
        case 2:
      quote = F("When the clock said * ten minutes to five *, she began to listen, and a few moments later, punctually as always, she heard the tires on the gravel outside...");
      attribution = F("- Lamb to the Slaughter");
      return;
      } // end switch (option)

    // Codegen for 16:51 for 1 quotations:
    case 1011:
      // Only one option for 16:51:"
      quote = F("* Nine minutes to five. * If this wasn't some new ordeal, intended to fray her nerves to shreds, if this important person really did exist...");
      attribution = F("- Compulsory Happiness");
      return;

    // Codegen for 16:52 for 1 quotations:
    case 1012:
      // Only one option for 16:52:"
      quote = F("The five-footed beasts make a point of arriving at the latest at eight or seven minutes to: ritual again. At * eight minutes to five *, there they were.");
      attribution = F("- Chaos and Night");
      return;

    // Codegen for 16:53 for 1 quotations:
    case 1013:
      // Only one option for 16:53:"
      quote = F("It was so quiet in the post office that Trinidad could hear the soft tick of the clock's second hand every time it moved. It was now * seven minutes before five *.");
      attribution = F("- Rita Leganski");
      return;

    // Codegen for 16:54 for 3 quotations:
    case 1014:
      option = random(0, 3);
      // 3 options for 16:54
      switch (option) {
        case 0:
      quote = F("It was * 1654 * local time when the Red October broke the surface of the Atlantic Ocean for the first time, forty-seven miles southeast of Norfolk. There was no other ship in sight.");
      attribution = F("- The Hunt for Red October");
      return;
        case 1:
      quote = F("At * six minutes before five o'clock *, Daisy Robinson, about to reach her own apartment door, paused to look and to listen. Something was out of order.");
      attribution = F("- Charlotte Armstrong");
      return;
        case 2:
      quote = F("* Six minutes to five *. Six minutes to go. Suddenly I felt quite clearheaded. There was an unexpected light in the cell; the boundaries were drawn, the roles well defined.");
      attribution = F("- Elie Wiesel, Dawn: A Novel");
      return;
      } // end switch (option)

    // Codegen for 16:55 for 2 quotations:
    case 1015:
      option = random(0, 2);
      // 2 options for 16:55
      switch (option) {
        case 0:
      quote = F("* 4:55 * - Mank holding phone. Turns to Caddell - 'Who is this?' Caddell: 'Jim.' (shrugs) 'I think he's our man in Cincinnati.'");
      attribution = F("- Hunter S. Thompson");
      return;
        case 1:
      quote = F("About * five minutes to five *, just as they were all putting their things away for the night, Nimrod suddenly appeared in the house.");
      attribution = F("- Robert Tressell");
      return;
      } // end switch (option)

    // Codegen for 16:56 for 1 quotations:
    case 1016:
      // Only one option for 16:56:"
      quote = F("And when that final Friday came, when my packing was mostly done, she sat with my dad and me on the living-room couch at * 4:56 P.M. * and patiently awaited...");
      attribution = F("- Looking for Alaska");
      return;

    // Codegen for 16:57 for 2 quotations:
    case 1017:
      option = random(0, 2);
      // 2 options for 16:57
      switch (option) {
        case 0:
      quote = F("It was * nearly five * in the evening when the cook came aboard. He did not have the cabbages.");
      attribution = F("- A Single Pebble");
      return;
        case 1:
      quote = F("Then at * three minutes to five * - Pendel had somehow never doubted that Osnard would be punctual - along comes a brown Ford hatchback with an Avis sticker on the back window...");
      attribution = F("- The Tailor of Panama");
      return;
      } // end switch (option)

    // Codegen for 16:58 for 2 quotations:
    case 1018:
      option = random(0, 2);
      // 2 options for 16:58
      switch (option) {
        case 0:
      quote = F("I was told that in his vest pocket he kept a chronometer instead of a watch. If someone asked him what time it was, he would say, \"* A minute and twenty-one seconds to five *.\"");
      attribution = F("- The Collected Stories");
      return;
        case 1:
      quote = F("He wants to look death in the face. * Two minutes to five *. I took a handkerchief out of my pocket, but John Dawson ordered me to put it back.");
      attribution = F("- Elie Wiesel, Dawn: A Novel");
      return;
      } // end switch (option)

    // Codegen for 16:59 for 2 quotations:
    case 1019:
      option = random(0, 2);
      // 2 options for 16:59
      switch (option) {
        case 0:
      quote = F("The whole place smells like death no matter what the fuck you do. Gately gets to the shelter at 0* 4:59 * and just shuts his head off as if his head had a control switch.");
      attribution = F("- Infinite Jest");
      return;
        case 1:
      quote = F("The rain stopped * around 5 p.m. * and a few of those people who were out and about expressed mild surprise when the rainbow failed to fade.");
      attribution = F("- Mortality -- 'The Rainbow'");
      return;
      } // end switch (option)

    // Codegen for 17:00 for 11 quotations:
    case 1020:
      option = random(0, 11);
      // 11 options for 17:00
      switch (option) {
        case 0:
      quote = F("* 5.00 p.m. * Read a book");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("About * five *, the Abbot, a young Manchester terrier, began chirruping. He stood on the body of his owner, Flora, with his forepaws on the sill of the balcony, stared...");
      attribution = F("- The People With the Dogs");
      return;
        case 2:
      quote = F("At * five o'clock * adieux were waved, and the ponderous liner edged away from the long pier, slowly turned its nose seaward, discarded its tug, and headed for the widening water...");
      attribution = F("- The Horror at Red Hook");
      return;
        case 3:
      quote = F("At * five o'clock * that afternoon, while Barbara waited in a taxi, Harold went into the convent in Auteuil and explained ...");
      attribution = F("- The Chateau");
      return;
        case 4:
      quote = F("But I took the mixture at * five o'clock * in the afternoon. I run my tongue over my dry mouth. I feel dizzy. I know this dizziness: it's because I haven't had a cigarette for hours.");
      attribution = F("- The End of Mr Y");
      return;
        case 5:
      quote = F("ERE THE HALF-HOUR ended, * five o'clock * struck; school was dismissed, and all were gone into the refectory to tea. I now ventured to descend; it was deep dusk.");
      attribution = F("- Charlotte Bronte, Jane Eyre");
      return;
        case 6:
      quote = F("From * five o'clock * to eight is on certain occasions a little eternity; but on such an occasion as this the interval could be only an eternity of pleasure.");
      attribution = F("- The Portrait of a Lady");
      return;
        case 7:
      quote = F("He found it harder to concentrate on drills that afternoon and when he left the building at * five o'clock *, he was still so worried that he walked straight into someone...");
      attribution = F("- JK Rowling");
      return;
        case 8:
      quote = F("She drove at * five o'clock * to a high floor in a narrow street in the quarter of the Piazza Navona, and was admitted by the portress of the convent, a genial and obsequious person.");
      attribution = F("- The Portrait of a Lady");
      return;
        case 9:
      quote = F("Until * five o'clock * there was no sign of life from the room. Then he rang for his servant and ordered a cold bath.");
      attribution = F("- Sandor Marai, Embers");
      return;
        case 10:
      quote = F("We motored, I remember, leaving London in the morning in a heavy shower of rain, coming to Manderley about * five o'clock *, in time for tea.");
      attribution = F("- Daphne du Maurier, Rebecca");
      return;
      } // end switch (option)

    // Codegen for 17:01 for 2 quotations:
    case 1021:
      option = random(0, 2);
      // 2 options for 17:01
      switch (option) {
        case 0:
      quote = F("* One minute after five. * The seated guests were told that the ceremony would begin shortly. A little more patience was required.");
      attribution = F("- Tip 'T.I.' Harris");
      return;
        case 1:
      quote = F("Oh yes. His clocks were set at * one minute past five *, four minutes past five and seven minutes past five. That was the combination number of a safe, 515457.");
      attribution = F("- Agatha Christie, The Clocks");
      return;
      } // end switch (option)

    // Codegen for 17:02 for 1 quotations:
    case 1022:
      // Only one option for 17:02:"
      quote = F("She stood up, shook her hair into place, smoothed her skirt and turned on the light. It was * two minutes past five *. She would have thought it midnight or five in the morning.");
      attribution = F("- Jane Smiley, Duplicate Keys");
      return;

    // Codegen for 17:03 for 1 quotations:
    case 1023:
      // Only one option for 17:03:"
      quote = F("I sure will. Thanks. Bye, he said, waving to Elliot, who returned the goodbye. It was * 5:03 * when Elliot rested the handset in its cradle.");
      attribution = F("- Comrades in Miami: A Novel");
      return;

    // Codegen for 17:04 for 2 quotations:
    case 1024:
      option = random(0, 2);
      // 2 options for 17:04
      switch (option) {
        case 0:
      quote = F("Just ahead, he saw Marvin and his two friends. Suddenly the whole bridge convulsed. The time was * 5:04 P.M. * Steel screamed.");
      attribution = F("- The Mothman Prophecies");
      return;
        case 1:
      quote = F("Oh yes. His clocks were set at one minute past five, * four minutes past five * and seven minutes past five. That was the combination number of a safe, 515457.");
      attribution = F("- Agatha Christie, The clocks");
      return;
      } // end switch (option)

    // Codegen for 17:05 for 1 quotations:
    case 1025:
      // Only one option for 17:05:"
      quote = F("At approximately * 5:05 p.m. * Joe became aware of a man standing close to the table, about two metres away, talking in Mandarin into a mobile phone.");
      attribution = F("- Charles Cumming, Typhoon");
      return;

    // Codegen for 17:06 for 1 quotations:
    case 1026:
      // Only one option for 17:06:"
      quote = F("The rain stopped * around 5 p.m. * and a few of those people who were out and about expressed mild surprise when the rainbow failed to fade.");
      attribution = F("- Mortality -- 'The Rainbow'");
      return;

    // Codegen for 17:07 for 1 quotations:
    case 1027:
      // Only one option for 17:07:"
      quote = F("Oh yes. His clocks were set at one minute past five, four minutes past five and * seven minutes past five *. That was the combination number of a safe, 515457.");
      attribution = F("- Agatha Christie, The clocks");
      return;

    // Codegen for 17:08 for 0 quotations:
    case 1028:
      // No quotes for 17:08
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:09 for 1 quotations:
    case 1029:
      // Only one option for 17:09:"
      quote = F("...when inquiry of the station attendants educed the information that the two earliest trains to be obtained were the * 5:09 * to Dunkerque and the 5:37 for Ostend.");
      attribution = F("- The Black Bag");
      return;

    // Codegen for 17:10 for 3 quotations:
    case 1030:
      option = random(0, 3);
      // 3 options for 17:10
      switch (option) {
        case 0:
      quote = F("* Five ten P.M. * A ground-to-ground cruise missile, launched from a tractor installed in the backyard of Leonard Sudavico's former home by Rashan ...");
      attribution = F("- Tip 'T.I.' Harris");
      return;
        case 1:
      quote = F("Hours later, at * ten minutes past five *, Saturday afternoon, Nora and Travis and Jim Keene crowded in front of the mattress on which Einstein lay.");
      attribution = F("- Dean Koontz, Watchers");
      return;
        case 2:
      quote = F("If this is not enough to kill me, I have more left in the bottle. * Ten minutes past five *. \"You have just gone, after giving me my composing draught.\"");
      attribution = F("- The Law and the Lady");
      return;
      } // end switch (option)

    // Codegen for 17:11 for 0 quotations:
    case 1031:
      // No quotes for 17:11
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:12 for 1 quotations:
    case 1032:
      // Only one option for 17:12:"
      quote = F("Well, here we are, said Colonel Julyan, \"and it's exactly * twelve minutes past five *. We shall catch them in the middle of their tea. Better wait for a bit\"");
      attribution = F("- Daphne du Maurier, Rebecca");
      return;

    // Codegen for 17:13 for 0 quotations:
    case 1033:
      // No quotes for 17:13
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:14 for 1 quotations:
    case 1034:
      // Only one option for 17:14:"
      quote = F("Do you know what time it is, Atticus? she said. \"Exactly * fourteen minutes past five *. The alarm clock's set for five-thirty. I want you to know that.\"");
      attribution = F("- To Kill a Mockingbird");
      return;

    // Codegen for 17:15 for 1 quotations:
    case 1035:
      // Only one option for 17:15:"
      quote = F("When August Bach emerged from the gloomy chill of the air-conditioned Divisional Fighter Control bunker it was * 17:15 hrs * CET.");
      attribution = F("- Len Deighton, Bomber");
      return;

    // Codegen for 17:16 for 0 quotations:
    case 1036:
      // No quotes for 17:16
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:17 for 0 quotations:
    case 1037:
      // No quotes for 17:17
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:18 for 1 quotations:
    case 1038:
      // Only one option for 17:18:"
      quote = F("I remembered soon after that, at this moment, I happened to look at the clock. It was * eighteen minutes past five *.");
      attribution = F("- Maurice LeBlanc");
      return;

    // Codegen for 17:19 for 1 quotations:
    case 1039:
      // Only one option for 17:19:"
      quote = F("The call came at * 5.19 p.m. * The line was surprisingly clear. A man introduced himself as Major Liepa from the Riga police.");
      attribution = F("- The Dogs of Riga");
      return;

    // Codegen for 17:20 for 1 quotations:
    case 1040:
      // Only one option for 17:20:"
      quote = F("The Meeting was listed as starting at 1730, and it was only around * 1720 *, and Hal thought the voices might signify some sort of pre-Meeting orientation...");
      attribution = F("- Infinite Jest");
      return;

    // Codegen for 17:21 for 1 quotations:
    case 1041:
      // Only one option for 17:21:"
      quote = F("The Meeting was listed as starting at 1730, and it was only * around 1720 *, and Hal thought the voices might signify some sort of pre-Meeting orientation...");
      attribution = F("- Infinite Jest");
      return;

    // Codegen for 17:22 for 0 quotations:
    case 1042:
      // No quotes for 17:22
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:23 for 1 quotations:
    case 1043:
      // Only one option for 17:23:"
      quote = F("What for? \"Just for a chat. Do you know the Royal batsman, near Central Station? We could meet tomorrow at five?\" \"* Five twenty-three *,\" I said");
      attribution = F("- A Fraction of the Whole");
      return;

    // Codegen for 17:24 for 0 quotations:
    case 1044:
      // No quotes for 17:24
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:25 for 3 quotations:
    case 1045:
      option = random(0, 3);
      // 3 options for 17:25
      switch (option) {
        case 0:
      quote = F("George's train home from New Street leaves at * 5.25 *. On the return journey, there are rarely schoolboys.");
      attribution = F("- Arthur and George");
      return;
        case 1:
      quote = F("It was * five-twenty-five * when I pulled up in front of the library. Still early for our date, so I got out of the car and took a stroll down the misty streets.");
      attribution = F("- Haruki Murakami");
      return;
        case 2:
      quote = F("Now said Handsley, when Angela had poured out the last cup, \"it's * twenty-five minutes past five *, At half-past the Murder game is on\"");
      attribution = F("- Ngaio Marsh, A Man Lay Dead");
      return;
      } // end switch (option)

    // Codegen for 17:26 for 0 quotations:
    case 1046:
      // No quotes for 17:26
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:27 for 0 quotations:
    case 1047:
      // No quotes for 17:27
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:28 for 0 quotations:
    case 1048:
      // No quotes for 17:28
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:29 for 0 quotations:
    case 1049:
      // No quotes for 17:29
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:30 for 2 quotations:
    case 1050:
      option = random(0, 2);
      // 2 options for 17:30
      switch (option) {
        case 0:
      quote = F("It was * half-past five * before Holmes returned. He was bright, eager, and in excellent spirits, a mood which in his case alternated with fits of the blackest depression.");
      attribution = F("- The Sign Of Four");
      return;
        case 1:
      quote = F("It was only after driving nearly five miles that he had sufficiently recovered himself to look at his watch, and realise that it was * half-past five *, and he was late.");
      attribution = F("- Leo Tolstoy, Anna Karenina");
      return;
      } // end switch (option)

    // Codegen for 17:31 for 0 quotations:
    case 1051:
      // No quotes for 17:31
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:32 for 0 quotations:
    case 1052:
      // No quotes for 17:32
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:33 for 1 quotations:
    case 1053:
      // Only one option for 17:33:"
      quote = F("At * 5:33 p.m. * there is a blast of two deep, resonant notes a major third apart. On another day there is the same blast at 12:54 p.m. On another, exactly 8:00 a.m.");
      attribution = F("- Varieties of Disturbance");
      return;

    // Codegen for 17:34 for 0 quotations:
    case 1054:
      // No quotes for 17:34
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:35 for 2 quotations:
    case 1055:
      option = random(0, 2);
      // 2 options for 17:35
      switch (option) {
        case 0:
      quote = F("* 5:35 * - All phones ringing now, the swing shift has shot the gap - now the others are waking up.");
      attribution = F("- Hunter S Thompson");
      return;
        case 1:
      quote = F("I squinted at the clock. 'It says * twenty-five before six *,' I said and rolled away from him.");
      attribution = F("- the dice man");
      return;
      } // end switch (option)

    // Codegen for 17:36 for 0 quotations:
    case 1056:
      // No quotes for 17:36
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:37 for 1 quotations:
    case 1057:
      // Only one option for 17:37:"
      quote = F("Look, Lucille, said Joe when Lucille strolled into the office at * 5:37 *. \"I don't know what you said to this gal...\"");
      attribution = F("- Lightning Rods");
      return;

    // Codegen for 17:38 for 0 quotations:
    case 1058:
      // No quotes for 17:38
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:39 for 0 quotations:
    case 1059:
      // No quotes for 17:39
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:40 for 3 quotations:
    case 1060:
      option = random(0, 3);
      // 3 options for 17:40
      switch (option) {
        case 0:
      quote = F("Hey, young man, what time is it? 'What?' I said, is it 5:30 yet? 'Er, * 5:40 *.' Heavens, they'll be starving. But then that's a good thing. Let them.'");
      attribution = F("- Zhu Wen, I Love Dollars");
      return;
        case 1:
      quote = F("It's * five-forty * now. The party's at six. By about ten past, the eleventh floor should be clearing. Arnold is a very popular partner...");
      attribution = F("- The Undomestic Goddess");
      return;
        case 2:
      quote = F("* Twenty minutes to six *. 'Rob's boys were already on the platform, barrows ready.");
      attribution = F("- Bruce Robinson");
      return;
      } // end switch (option)

    // Codegen for 17:41 for 0 quotations:
    case 1061:
      // No quotes for 17:41
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:42 for 0 quotations:
    case 1062:
      // No quotes for 17:42
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:43 for 1 quotations:
    case 1063:
      // Only one option for 17:43:"
      quote = F("* 5.43 * - Mank on phone to 'Mary' in Washington; 'It now appears quite clear that we'll lead the state - without the 21st.'");
      attribution = F("- Hunter S. Thompson");
      return;

    // Codegen for 17:44 for 0 quotations:
    case 1064:
      // No quotes for 17:44
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:45 for 2 quotations:
    case 1065:
      option = random(0, 2);
      // 2 options for 17:45
      switch (option) {
        case 0:
      quote = F("At * 5:45 * a power-transformer on a pole beside the abandoned Tracker Brothers' Truck Depot exploded in a flash of purple light, spraying twisted chunks of metal...");
      attribution = F("- Stephen King, IT");
      return;
        case 1:
      quote = F("Janice is not waiting for him in the lounge or beside the pool when at last * around 5.45 * they come home from playing the par-5 eighteenth.");
      attribution = F("- John Updike, Rabbit Is Rich");
      return;
      } // end switch (option)

    // Codegen for 17:46 for 1 quotations:
    case 1066:
      // Only one option for 17:46:"
      quote = F("...those of the carriage clock on the bookshelf to * fourteen minutes to six *. In other words, it was exactly eight; and Mrs. Hignett acknowledged the fact...");
      attribution = F("- Three Men and a Maid");
      return;

    // Codegen for 17:47 for 0 quotations:
    case 1067:
      // No quotes for 17:47
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:48 for 1 quotations:
    case 1068:
      // Only one option for 17:48:"
      quote = F("Father came home at * 5:48 p.m. * I heard him come through the front door. Then he came into the living room.");
      attribution = F("- Mark Haddon");
      return;

    // Codegen for 17:49 for 0 quotations:
    case 1069:
      // No quotes for 17:49
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:50 for 1 quotations:
    case 1070:
      // Only one option for 17:50:"
      quote = F("What time is it Jack? \"* Ten to six *\" \"Ten more minutes then.\" I shuffle the cards. \"Time for a quick game of rummy?\"");
      attribution = F("- Noughts and Crosses");
      return;

    // Codegen for 17:51 for 0 quotations:
    case 1071:
      // No quotes for 17:51
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:52 for 0 quotations:
    case 1072:
      // No quotes for 17:52
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:53 for 1 quotations:
    case 1073:
      // Only one option for 17:53:"
      quote = F("I should think I have! exclaimed I, in indignant recollection of my education. \"All right; keep your temper. What time are you?\" \"* Seven minutes to six *.\"");
      attribution = F("- Talbot Baines Reed");
      return;

    // Codegen for 17:54 for 1 quotations:
    case 1074:
      // Only one option for 17:54:"
      quote = F("It was * 5:54 pm * when Father came back into the living room. He said, \"What is this?\" but he said it very quietly and I didn't realise that he was angry...");
      attribution = F("- Mark Haddon");
      return;

    // Codegen for 17:55 for 1 quotations:
    case 1075:
      // Only one option for 17:55:"
      quote = F("...as he stood outside and put the shutters up with this own cold hands in despair of further trade. It was * five minutes to six *.");
      attribution = F("- The Deferred Appointment");
      return;

    // Codegen for 17:56 for 0 quotations:
    case 1076:
      // No quotes for 17:56
        quote = "";
        attribution = "";
      return;

    // Codegen for 17:57 for 1 quotations:
    case 1077:
      // Only one option for 17:57:"
      quote = F("When he arrived it was * nearly six o'clock *, and the sun was setting full and warm, and the red light streamed in through the window and gave more colour to the pale cheeks.");
      attribution = F("- Bram Stoker, Dracula");
      return;

    // Codegen for 17:58 for 1 quotations:
    case 1078:
      // Only one option for 17:58:"
      quote = F("It was * nearly six o'clock * in the evening, and the absurd bell in the six-foot tin steeple of the church went clank-clank, clank- clank! as old Mattu pulled the rope within.'");
      attribution = F("- George Orwell, Burmese Days");
      return;

    // Codegen for 17:59 for 1 quotations:
    case 1079:
      // Only one option for 17:59:"
      quote = F("When he arrived it was * nearly six o'clock *, and the sun was setting full and warm, and the red light streamed in through the window and gave more colour to the pale cheeks.");
      attribution = F("- Bram Stoker, Dracula");
      return;

    // Codegen for 18:00 for 9 quotations:
    case 1080:
      option = random(0, 9);
      // 9 options for 18:00
      switch (option) {
        case 0:
      quote = F("* 6.00 p.m. * Have tea");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("Although it was only * six o'clock *, the night was already dark. The fog, made thicker by its proximity to the Seine, blurred every detail with its ragged veils...");
      attribution = F("- Le Club des Hachichins");
      return;
        case 2:
      quote = F("'He said today was no good as he was going to Norwich. But Friday at * six o'clock *, same as usual.' 'Are you sure he said six o'clock?' she asked, puzzled. 'Quite sure.'");
      attribution = F("- The Go-Between");
      return;
        case 3:
      quote = F("King Richard: What is o'clock? Catesby: It is * six o'clock *, full supper time. King Richard: I will not sup tonight. Give me some ink and paper.");
      attribution = F("- Richard III");
      return;
        case 4:
      quote = F("Leon waited all day for * six o'clock * to arrive; when he got to the inn, he found no one there but Monsieur Binet, already at the table.");
      attribution = F("- Madame Bovary");
      return;
        case 5:
      quote = F("Oh oh oh. * Six o'clock * and the master not home yet.");
      attribution = F("- The skin of our teeth");
      return;
        case 6:
      quote = F("The newspaper snaked through the door and there was suddenly a * six o'clock * feeling in the house");
      attribution = F("- Muriel Spark");
      return;
        case 7:
      quote = F("The winter evening settles down With smell of steaks in passageways. * Six o'clock *.");
      attribution = F("- T S Eliot, Preludes");
      return;
        case 8:
      quote = F("When the bells of Calvary Church struck * six *, she saw Mr and Mrs Biggs hurrying down the front stoop, rushing off to the shops before they closed.");
      attribution = F("- Jed Rubenfeld");
      return;
      } // end switch (option)

    // Codegen for 18:01 for 0 quotations:
    case 1081:
      // No quotes for 18:01
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:02 for 1 quotations:
    case 1082:
      // Only one option for 18:02:"
      quote = F("This train does not stop at Birchills, for reasons George has never been able to ascertain. Then it is Bloxwich * 6.02 *, Wyrley & Churchbridge 6.09.");
      attribution = F("- Arthur and George");
      return;

    // Codegen for 18:03 for 1 quotations:
    case 1083:
      // Only one option for 18:03:"
      quote = F("Above it all rose the Houses of Parliament, with the hands of the clock stopped at * three minutes past six *. It was difficult to believe that all that meant nothing...");
      attribution = F("- The Day of the Triffids");
      return;

    // Codegen for 18:04 for 1 quotations:
    case 1084:
      // Only one option for 18:04:"
      quote = F("...we will celebrate it with thanksgivings, all the years of our life. \"We will, we will, Alonzo!\" \"* Four minutes after six *, in the evening, my Rosannah...\"");
      attribution = F("- Mark Twain");
      return;

    // Codegen for 18:05 for 2 quotations:
    case 1085:
      option = random(0, 2);
      // 2 options for 18:05
      switch (option) {
        case 0:
      quote = F("A second man went in and found the shop empty, as he thought, at * five minutes past six *. That puts the time at between 5:30 and 6:05.");
      attribution = F("- The ABC Murders");
      return;
        case 1:
      quote = F("At about * five past six * Piers came in carrying an evening paper and a few books.");
      attribution = F("- A Glass of Blessings");
      return;
      } // end switch (option)

    // Codegen for 18:06 for 1 quotations:
    case 1086:
      // Only one option for 18:06:"
      quote = F("At * 6:06 *, every toilet on Merit Street suddenly exploded in a geyser of shit and raw sewage as some unimaginable reversal took place...");
      attribution = F("- Stephen King, IT");
      return;

    // Codegen for 18:07 for 0 quotations:
    case 1087:
      // No quotes for 18:07
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:08 for 1 quotations:
    case 1088:
      // Only one option for 18:08:"
      quote = F("* 6:08 p.m. * The code-word \"Valkyrie\" reached von Seydlitz Gabler's headquarters");
      attribution = F("- The Night of the Generals");
      return;

    // Codegen for 18:09 for 0 quotations:
    case 1089:
      // No quotes for 18:09
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:10 for 2 quotations:
    case 1090:
      option = random(0, 2);
      // 2 options for 18:10
      switch (option) {
        case 0:
      quote = F("'Let me see now. You had a drink at the Continental at * six ten *.' 'Yes.' 'And at six forty-five you were talking to another journalist at the door of the Majestic?' 'Yes, Wilkins.'");
      attribution = F("- The Quiet American");
      return;
        case 1:
      quote = F("The bus left the station at * ten past six * - and she sat proud, like an accustomed traveller, apart from her father, John Henry, and Berenice.");
      attribution = F("- The Member of the Wedding");
      return;
      } // end switch (option)

    // Codegen for 18:11 for 0 quotations:
    case 1091:
      // No quotes for 18:11
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:12 for 0 quotations:
    case 1092:
      // No quotes for 18:12
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:13 for 0 quotations:
    case 1093:
      // No quotes for 18:13
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:14 for 0 quotations:
    case 1094:
      // No quotes for 18:14
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:15 for 5 quotations:
    case 1095:
      option = random(0, 5);
      // 5 options for 18:15
      switch (option) {
        case 0:
      quote = F("Dumbbell exercise and wall-scaling ..... . * 6.15 *-6.30");
      attribution = F("- The Great Gatsby");
      return;
        case 1:
      quote = F("I checked the time on the corner of my screen. * 6.15 pm. * I was never going to finisah my essay in forty-five minutes");
      attribution = F("- Girl Missing");
      return;
        case 2:
      quote = F("At a * quarter past six * he was through with them.");
      attribution = F("- The Photograph");
      return;
        case 3:
      quote = F("Father expected his shaving-water to be ready at a * quarter past six *. Just seven minutes late, Dorothy took the can upstairs and knocked at her father's door.");
      attribution = F("- A Clergyman's Daughter");
      return;
        case 4:
      quote = F("'* Quarter past six *,' said Tony. 'He's bound to have told her by now.'");
      attribution = F("- A Handful of Dust");
      return;
      } // end switch (option)

    // Codegen for 18:16 for 0 quotations:
    case 1096:
      // No quotes for 18:16
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:17 for 1 quotations:
    case 1097:
      // Only one option for 18:17:"
      quote = F("Dizzy, come on.' He turned slowly, coaxing the animal down on to the pillow. The clock read * six-seventeen *. A second cat, Miles, purred on contentedly...");
      attribution = F("- John Harvey, Lonely Hearts");
      return;

    // Codegen for 18:18 for 0 quotations:
    case 1098:
      // No quotes for 18:18
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:19 for 0 quotations:
    case 1099:
      // No quotes for 18:19
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:20 for 1 quotations:
    case 1100:
      // Only one option for 18:20:"
      quote = F("By the time Elliot's mother arrived at * twenty past six *, Mrs Sen always made sure all evidence of her chopping was disposed of.");
      attribution = F("- Interpreter of Maladies");
      return;

    // Codegen for 18:21 for 1 quotations:
    case 1101:
      // Only one option for 18:21:"
      quote = F("5.20pm - * 6.21pm *: Miss Pettigrew found herself wafted into the passage. She was past remonstrance now, past bewilderment, surprise, expostulation. Her eyes shone. Her face glowed.");
      attribution = F("- Winifred Watson");
      return;

    // Codegen for 18:22 for 1 quotations:
    case 1102:
      // Only one option for 18:22:"
      quote = F("Clock overturned when he fell forward. That'll give us the time of the crime. * Twenty-two minutes past six *.");
      attribution = F("- The Murder at the Vicarage");
      return;

    // Codegen for 18:23 for 0 quotations:
    case 1103:
      // No quotes for 18:23
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:24 for 0 quotations:
    case 1104:
      // No quotes for 18:24
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:25 for 2 quotations:
    case 1105:
      option = random(0, 2);
      // 2 options for 18:25
      switch (option) {
        case 0:
      quote = F("I have this moment, while writing, had a wire from Jonathan saying that he leaves by the * 6.25 * tonight from Launceston and will be here at 10.18...");
      attribution = F("- Bram Stoker, Dracula");
      return;
        case 1:
      quote = F("At * twenty-five past six * I go into the bathroom and have a wash, then while the Old Lady's busy in the kitchen helping Chris with the washing up...");
      attribution = F("- A Kind of Loving");
      return;
      } // end switch (option)

    // Codegen for 18:26 for 1 quotations:
    case 1106:
      // Only one option for 18:26:"
      quote = F("It is * around half past six * in the evening. Dusk is gathering in the living room, an early dusk due to the fog which has rolled in from the Sound...");
      attribution = F("- Eugene O'Neill");
      return;

    // Codegen for 18:27 for 0 quotations:
    case 1107:
      // No quotes for 18:27
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:28 for 0 quotations:
    case 1108:
      // No quotes for 18:28
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:29 for 0 quotations:
    case 1109:
      // No quotes for 18:29
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:30 for 8 quotations:
    case 1110:
      option = random(0, 8);
      // 8 options for 18:30
      switch (option) {
        case 0:
      quote = F("* 6.30 p.m. * Watch television or a video");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("At five o'clock the two ladies retired to dress, and at * half past six * Elizabeth was summoned to dinner.");
      attribution = F("- Pride and Prejudice");
      return;
        case 2:
      quote = F("But two of the three gentlemen assembled in the Saloon at * half-past six * on a wintry evening of late February were in no danger of falling into this error.");
      attribution = F("- Georgette Heyer, Cotillion");
      return;
        case 3:
      quote = F("'Well,' he said, once more across the gap, 'say tomorrow's no good, I'm going to Norwich, but Friday at * half-past six *, same as usual.'");
      attribution = F("- The Go-Between");
      return;
        case 4:
      quote = F("At * six-thirty * I left the bar and walked outside. It was getting dark and the big Avenida looked cool and graceful. On the other side were homes that once looked out on the beach.");
      attribution = F("- The Rum Diary");
      return;
        case 5:
      quote = F("He was ready to close the door the second he was told to. \"What time is it?\" said his father in a drugged voice. \"Oh my God, it's * six-thirty *,\" his mother said.");
      attribution = F("- The Book of Daniel");
      return;
        case 6:
      quote = F("It is * six thirty *. Now the dark night and the deafening racket of the crickets again engulf the garden and the veranda, all around the house");
      attribution = F("- Jealousy");
      return;
        case 7:
      quote = F("Then he got back in the truck. The bar clock said * six-thirty *...By eight-thirty he'd decided that it that was it would take to make the cab arrive...");
      attribution = F("- Cities of the Plain");
      return;
      } // end switch (option)

    // Codegen for 18:31 for 1 quotations:
    case 1111:
      // Only one option for 18:31:"
      quote = F("I had been delayed at a case and it was * a little after half past six * when I found myself at Baker Street once more");
      attribution = F("- Arthur Conan Doyle");
      return;

    // Codegen for 18:32 for 0 quotations:
    case 1112:
      // No quotes for 18:32
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:33 for 1 quotations:
    case 1113:
      // Only one option for 18:33:"
      quote = F("Every evening, Michel took the train home, changed at Esbly and usually arrived in Crecy on the * 6.33pm * train where Annabelle would be waiting at the station.");
      attribution = F("- Atomised");
      return;

    // Codegen for 18:34 for 0 quotations:
    case 1114:
      // No quotes for 18:34
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:35 for 2 quotations:
    case 1115:
      option = random(0, 2);
      // 2 options for 18:35
      switch (option) {
        case 0:
      quote = F("And then it was * 6.35 pm * and I heard Father come home in his van and I moved the bed up against the door so he couldn't get in and he came into the house...");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("My watch lay on the dressing-table close by; glancing at it, I saw that the time was * twenty-five minutes to seven *.");
      attribution = F("- Ravensdene Court");
      return;
      } // end switch (option)

    // Codegen for 18:36 for 1 quotations:
    case 1116:
      // Only one option for 18:36:"
      quote = F("Kaldren pursues me like luminescent shadow. He has chalked up on the gateway '96,688,365,498,702'. Should confuse the mail man. Woke 9:05. To sleep * 6:36 *.");
      attribution = F("- The Voices of Time");
      return;

    // Codegen for 18:37 for 0 quotations:
    case 1117:
      // No quotes for 18:37
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:38 for 0 quotations:
    case 1118:
      // No quotes for 18:38
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:39 for 0 quotations:
    case 1119:
      // No quotes for 18:39
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:40 for 2 quotations:
    case 1120:
      option = random(0, 2);
      // 2 options for 18:40
      switch (option) {
        case 0:
      quote = F("I allowed plenty of time - in fact, too much; for we arrived at * twenty minutes to seven *, and Franching, so the servant said, had only just gone up to dress.");
      attribution = F("- Diary of a Nobody");
      return;
        case 1:
      quote = F("Amy: What's that? I thought I saw someone pass the window. What time is it? Charles: Nearly * twenty to seven *.");
      attribution = F("- The Family Reunion");
      return;
      } // end switch (option)

    // Codegen for 18:41 for 1 quotations:
    case 1121:
      // Only one option for 18:41:"
      quote = F("Stillman's train was not due to arrive until * six forty-one *, but Quinn wanted time to study the geography of the place, to make sure that Stillman would not be able to slip away...");
      attribution = F("- The New York Trilogy");
      return;

    // Codegen for 18:42 for 0 quotations:
    case 1122:
      // No quotes for 18:42
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:43 for 0 quotations:
    case 1123:
      // No quotes for 18:43
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:44 for 0 quotations:
    case 1124:
      // No quotes for 18:44
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:45 for 3 quotations:
    case 1125:
      option = random(0, 3);
      // 3 options for 18:45
      switch (option) {
        case 0:
      quote = F("It was a * quarter to seven * when I let myself into the office and clicked the light on and picked a piece of paper off the floor.");
      attribution = F("- The High Window");
      return;
        case 1:
      quote = F("'Let me see now. You had a drink at the Continental at six ten.' 'Yes.' 'And at * six forty-five * you were talking to another journalist at the door of the Majestic?'");
      attribution = F("- The Quiet American");
      return;
        case 2:
      quote = F("* Six forty-five *, called Louie. \"Did you hear, Ming,\" he asked, \"did you hear?\" \"Yes, Taddy, I heard.\" \"What is it?\" asked Tommy. \"The new baby, listen, the new baby.\"");
      attribution = F("- The Man Who Loved Children");
      return;
      } // end switch (option)

    // Codegen for 18:46 for 2 quotations:
    case 1126:
      option = random(0, 2);
      // 2 options for 18:46
      switch (option) {
        case 0:
      quote = F("At * one minute after the quarter to seven * I heard the rattle of the cans outside. I opened the front door, and there was my man...");
      attribution = F("- The Thirty-Nine Steps");
      return;
        case 1:
      quote = F("...but still he did not come. The fool had chosen this day of all days to be late. At * one minute after the quarter to seven * I heard the rattle of the cans outside.");
      attribution = F("- The Thirty-Nine Steps");
      return;
      } // end switch (option)

    // Codegen for 18:47 for 0 quotations:
    case 1127:
      // No quotes for 18:47
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:48 for 0 quotations:
    case 1128:
      // No quotes for 18:48
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:49 for 1 quotations:
    case 1129:
      // Only one option for 18:49:"
      quote = F("* 6:49 p.m. * Lieutenant-General Tanz escorted by a motorized unit, drove to Corps headquarters");
      attribution = F("- The Night of the Generals");
      return;

    // Codegen for 18:50 for 2 quotations:
    case 1130:
      option = random(0, 2);
      // 2 options for 18:50
      switch (option) {
        case 0:
      quote = F("It was time to go see the Lady. When we arrived at her house at * ten minutes before seven * o'clock, Damaronde answered the door.");
      attribution = F("- Boy's Life");
      return;
        case 1:
      quote = F("At * ten minutes to seven * Dulcie was ready. She looked at herself in the wrinkly mirror. The reflection was satisfactory.");
      attribution = F("- O. Henry, The Four Million");
      return;
      } // end switch (option)

    // Codegen for 18:51 for 1 quotations:
    case 1131:
      // Only one option for 18:51:"
      quote = F("The square of light in the kitchen doorway had faded to thin purple; his watch said * 6:51 *.");
      attribution = F("- Stephen King, Salem's Lot");
      return;

    // Codegen for 18:52 for 0 quotations:
    case 1132:
      // No quotes for 18:52
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:53 for 1 quotations:
    case 1133:
      // Only one option for 18:53:"
      quote = F("It was * near on seven o'clock * when I got to Mr. and Mrs. Fleming's house on 6th Street, where I was renting a room.");
      attribution = F("- All Aunt Hagar's Children");
      return;

    // Codegen for 18:54 for 0 quotations:
    case 1134:
      // No quotes for 18:54
        quote = "";
        attribution = "";
      return;

    // Codegen for 18:55 for 2 quotations:
    case 1135:
      option = random(0, 2);
      // 2 options for 18:55
      switch (option) {
        case 0:
      quote = F("The play was set to begin at seven o'clock and finish before sunset. It was * 6:55 *. Beyond the flats we could hear the hockey field filling up.");
      attribution = F("- Middlesex");
      return;
        case 1:
      quote = F("Indeed how suspicious it would be if you had been completely accurate. Haven't I been? \"Not quite. It was * five to seven * that you talked to Wilkins.\" \"Another ten minutes.\"");
      attribution = F("- The Quiet American");
      return;
      } // end switch (option)

    // Codegen for 18:56 for 1 quotations:
    case 1136:
      // Only one option for 18:56:"
      quote = F("Then it was * 6.56 *. A black Rover - a Rover 90, registration PYX 520 - turned into the street that ran down the left-hand side of The Bunker. It parked.");
      attribution = F("- Dreams of leaving");
      return;

    // Codegen for 18:57 for 2 quotations:
    case 1137:
      option = random(0, 2);
      // 2 options for 18:57
      switch (option) {
        case 0:
      quote = F("asking a man to do these errands ... but that's my problem, not yours. Just bring the supplies and try to be at the church meeting room * a few minutes before seven *.");
      attribution = F("- Max Apple, Bridging");
      return;
        case 1:
      quote = F("...while on what seemed to be a wall, was hung a great clock face; the hands pointing to * three minutes to the hour; which was seven *.");
      attribution = F("- Between the Acts");
      return;
      } // end switch (option)

    // Codegen for 18:58 for 1 quotations:
    case 1138:
      // Only one option for 18:58:"
      quote = F("Walk fast, says Perry, \"it's * two minutes to seven *, and I got to be home by-\" \"Oh, shut up,\" says I.");
      attribution = F("- O. Henry, Roads of Destiny");
      return;

    // Codegen for 18:59 for 1 quotations:
    case 1139:
      // Only one option for 18:59:"
      quote = F("* About seven o'clock * in the evening she had died, and her frantic husband had made a frightful scene in his efforts to kill West...");
      attribution = F("- Herbert West - Reanimator");
      return;

    // Codegen for 19:00 for 9 quotations:
    case 1140:
      option = random(0, 9);
      // 9 options for 19:00
      switch (option) {
        case 0:
      quote = F("* 7.00 p.m. * Do maths practice");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("I had left directions that I was to be called * at seven *; for it was plain that I must see Wemmick before seeing any one else");
      attribution = F("- Great Expectations");
      return;
        case 2:
      quote = F("Edward had been allowed to see me only from * seven * till nine-thirty pm, always inside the confines of my home and under the supervision of my dad's unfailingly crabby glare.");
      attribution = F("- Stephenie Meyer, New Moon");
      return;
        case 3:
      quote = F("... as though all Combray had consisted of but two floors joined by a slender staircase, and as though there had been no time there but * seven o'clock * at night.");
      attribution = F("- Marcel Proust");
      return;
        case 4:
      quote = F("By * seven o'clock * the orchestra has arrived - no thin five-piece affair but a whole pitful of oboes and trombones and saxophones ...");
      attribution = F("- The Great Gatsby");
      return;
        case 5:
      quote = F("It was * seven o'clock * and by this time she was not very far from Raveloe, but she was not familiar enough with those monotonous lanes to know how near she was to her journey's end.");
      attribution = F("- George Eliot, Silas Marner");
      return;
        case 6:
      quote = F("It was * seven o'clock * when we got into the coupe with him and started for Long Island. [...] So we drove on toward death through the cooling twilight.");
      attribution = F("- The Great Gatsby");
      return;
        case 7:
      quote = F("Seven o'clock, already, he said to himself when the clock struck again, \"* seven o'clock *, and there's still a fog like this.");
      attribution = F("- Franz Kafka, Metamorphosis");
      return;
        case 8:
      quote = F("The town clock struck * seven *. The echoes of the great chime wandered in the unlit halls of the library. An autumn leaf, very crisp, fell somewhere in the dark.");
      attribution = F("- Ray Bradbury");
      return;
      } // end switch (option)

    // Codegen for 19:01 for 1 quotations:
    case 1141:
      // Only one option for 19:01:"
      quote = F("He waited until nearly eight, because * around seven * there were always more people coming in and out of the house than at other times.");
      attribution = F("- The Talented Mr Ripley");
      return;

    // Codegen for 19:02 for 1 quotations:
    case 1142:
      // Only one option for 19:02:"
      quote = F("Twas * about seven o'clock * at night, And the wind it blew with all its might, And the rain came pouring down, And the dark clouds seem'd to frown,");
      attribution = F("- The Tay Bridge Disaster");
      return;

    // Codegen for 19:03 for 0 quotations:
    case 1143:
      // No quotes for 19:03
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:04 for 0 quotations:
    case 1144:
      // No quotes for 19:04
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:05 for 2 quotations:
    case 1145:
      option = random(0, 2);
      // 2 options for 19:05
      switch (option) {
        case 0:
      quote = F("Outside my window the sky hung low and gray. It looked like snow, which added to my malaise. The clock read * five after seven *. I punched the remote control...");
      attribution = F("- Dance Dance Dance");
      return;
        case 1:
      quote = F("He decided he'd wait until * five minutes after seven o'clock *, and then he'd call in, take the day off, and make every effort in the book to locate someone reliable.");
      attribution = F("- Where I'm Calling From");
      return;
      } // end switch (option)

    // Codegen for 19:06 for 0 quotations:
    case 1146:
      // No quotes for 19:06
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:07 for 0 quotations:
    case 1147:
      // No quotes for 19:07
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:08 for 2 quotations:
    case 1148:
      option = random(0, 2);
      // 2 options for 19:08
      switch (option) {
        case 0:
      quote = F("Reacher had no watch but he figured when he saw Gregory it must have been * between eight and nine minutes after seven o'clock *.");
      attribution = F("- Lee Child, The Hard Way");
      return;
        case 1:
      quote = F("It was * eight minutes past seven * and still no girl. I waited impatiently. I watched another crowd surge through the barriers and move quickly down the steps.");
      attribution = F("- The Girl from East Berlin");
      return;
      } // end switch (option)

    // Codegen for 19:09 for 0 quotations:
    case 1149:
      // No quotes for 19:09
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:10 for 2 quotations:
    case 1150:
      option = random(0, 2);
      // 2 options for 19:10
      switch (option) {
        case 0:
      quote = F("...he maintained his equilibrium with difficulty, and very soon he would finally have to make a final decision, for * in five minutes it would be a quarter past seven *.");
      attribution = F("- Franz Kafka, Metamorphosis");
      return;
        case 1:
      quote = F("The invitations gave the hour as six-thirty because the family knew everyone would come a little late, so as not to be the first to arrive. At * seven-ten * not a soul had come...");
      attribution = F("- The Elderly Lady");
      return;
      } // end switch (option)

    // Codegen for 19:11 for 1 quotations:
    case 1151:
      // Only one option for 19:11:"
      quote = F("See you soon. Your voice was reassuring. * 19:11:00 *, the clock said. I put the phone back on its hook and I ran. The seat I got, almost the last one in the carriage...");
      attribution = F("- Ali Smith");
      return;

    // Codegen for 19:12 for 2 quotations:
    case 1152:
      option = random(0, 2);
      // 2 options for 19:12
      switch (option) {
        case 0:
      quote = F("...arbitrarily choosing a time based on fifteen-minute intervals. \"Never meet people at 7:45 or 6:30, Jasper, but pick times like * 7:12 * and 8:03!\"");
      attribution = F("- A Fraction of the Whole");
      return;
        case 1:
      quote = F("...the 'stupid human habit' of arbitrarily choosing a time based on fifteen-minute intervals. 'Never meet people at 7:45 or 6:30, Jasper, but pick times like * 7:12 * and 8:03!'");
      attribution = F("- A Fraction of the Whole");
      return;
      } // end switch (option)

    // Codegen for 19:13 for 0 quotations:
    case 1153:
      // No quotes for 19:13
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:14 for 2 quotations:
    case 1154:
      option = random(0, 2);
      // 2 options for 19:14
      switch (option) {
        case 0:
      quote = F("At * 7.14 * Harry knew he was alive. He knew that because the pain could be felt in every nerve fibre.");
      attribution = F("- Jo Nesbo, The Redeemer");
      return;
        case 1:
      quote = F("...he might have thrived as the typical, over-martini'd, cheating husband, leaving every night on the * 7:14 * to White Plains, a smudge of lipstick high on his neck...");
      attribution = F("- Andrew O'Hagan");
      return;
      } // end switch (option)

    // Codegen for 19:15 for 3 quotations:
    case 1155:
      option = random(0, 3);
      // 3 options for 19:15
      switch (option) {
        case 0:
      quote = F("Cell count down to 400,000. Woke 8:10. To sleep * 7:15 *. (Appear to have lost my watch without realising it, had to drive into town to buy another.)");
      attribution = F("- The Voices of Time");
      return;
        case 1:
      quote = F("... but for now he let Leo take charge: they were going to go back to Notting Hill and catch the * seven fifteen * screening of Scarface at the Gate.");
      attribution = F("- The Line of Beauty");
      return;
        case 2:
      quote = F("... everyone would come a little late, so as not to be the first to arrive. .. By * seven-fifteen * not another soul could squeeze into the house.");
      attribution = F("- The elderly lady");
      return;
      } // end switch (option)

    // Codegen for 19:16 for 1 quotations:
    case 1156:
      // Only one option for 19:16:"
      quote = F("* Sixteen past seven PM *? That's when he came into the store or when he left after the fact?");
      attribution = F("- The Last Precinct");
      return;

    // Codegen for 19:17 for 1 quotations:
    case 1157:
      // Only one option for 19:17:"
      quote = F("Colonel Putnis knocked on his door at * 7.17 p.m. * The car was waiting in front of the hotel, and they drove through the dark streets to police headquarters.");
      attribution = F("- The Dogs of Riga");
      return;

    // Codegen for 19:18 for 0 quotations:
    case 1158:
      // No quotes for 19:18
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:19 for 1 quotations:
    case 1159:
      // Only one option for 19:19:"
      quote = F("In the end we won the point- but I can't feel my usual triumph. All I know is, it's * seven-nineteen *, and in eleven minutes I'm supposed to be halfway across town...");
      attribution = F("- The Undomestic Goddess");
      return;

    // Codegen for 19:20 for 2 quotations:
    case 1160:
      option = random(0, 2);
      // 2 options for 19:20
      switch (option) {
        case 0:
      quote = F("The pause, we finally concluded, was to allow the other important people to catch up, those who had arrived at 7:10 waiting for those who had arrived at * 7:20 *.");
      attribution = F("- C Northcote Parkinson");
      return;
        case 1:
      quote = F("The clock read * seven-twenty *, but I felt no hunger. You'd think I might have wanted to eat something after the day I'd had, but I cringed at the very thought of food.");
      attribution = F("- Haruki Murakami");
      return;
      } // end switch (option)

    // Codegen for 19:21 for 1 quotations:
    case 1161:
      // Only one option for 19:21:"
      quote = F("Gripping her gym bag in her right hand, Aomame, like Buzzcut, was waiting for something to happen. The clock display changed to * 7:21 *, then 7:22, then 7:23.");
      attribution = F("- Haruki Murakami, 1Q84");
      return;

    // Codegen for 19:22 for 1 quotations:
    case 1162:
      // Only one option for 19:22:"
      quote = F("Gripping her gym bag in her right hand, Aomame, like Buzzcut, was waiting for something to happen. The clock display changed to 7:21, then * 7:22 *, then 7:23.");
      attribution = F("- Haruki Murakami, 1Q84");
      return;

    // Codegen for 19:23 for 1 quotations:
    case 1163:
      // Only one option for 19:23:"
      quote = F("Gripping her gym bag in her right hand, Aomame, like Buzzcut, was waiting for something to happen. The clock display changed to 7:21, then 7:22, then * 7:23 *.");
      attribution = F("- Haruki Murakami, 1Q84");
      return;

    // Codegen for 19:24 for 1 quotations:
    case 1164:
      // Only one option for 19:24:"
      quote = F("He picked up his hat and coat and Clarice said hello to him and he said hello and looked at the clock and it was * almost twenty-five after seven *.");
      attribution = F("- The Evening's at Seven");
      return;

    // Codegen for 19:25 for 1 quotations:
    case 1165:
      // Only one option for 19:25:"
      quote = F("He picked up his hat and coat and Clarice said hello to him and he said hello and looked at the clock and it was almost * twenty-five after seven *.");
      attribution = F("- The Evening's at Seven");
      return;

    // Codegen for 19:26 for 0 quotations:
    case 1166:
      // No quotes for 19:26
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:27 for 1 quotations:
    case 1167:
      // Only one option for 19:27:"
      quote = F("His appointment with the doctor was for 8.45. It was * 7.27 *.");
      attribution = F("- Henning Mankell");
      return;

    // Codegen for 19:28 for 0 quotations:
    case 1168:
      // No quotes for 19:28
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:29 for 0 quotations:
    case 1169:
      // No quotes for 19:29
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:30 for 4 quotations:
    case 1170:
      option = random(0, 4);
      // 4 options for 19:30
      switch (option) {
        case 0:
      quote = F("On July 25th, 8:30 a.m. the bitch Novaya dies whelping. At 10 o'clock she is lowered into her cool grave, at * 7:30 * that same evening we see our first floes...");
      attribution = F("- Christoph Ransmayr");
      return;
        case 1:
      quote = F("The telephone call came at * 7.30 * on the evening of March 18th, a Saturday, the eve of the noisy, colourful festival that the town held in honour of Saint Joseph the carpenter -");
      attribution = F("- A Simple Story");
      return;
        case 2:
      quote = F("But now he was close - here was the house, here were the gates. Somewhere a clock beat a single chime. 'What, is it really * half-past seven *? That's impossible, it must be fast!'");
      attribution = F("- Crime and Punishment");
      return;
        case 3:
      quote = F("The clock showed * half-past seven *. This was the twilight time. He would be there now. I pictured him in his old navy-blue sweater and peaked cap...");
      attribution = F("- Roald Dahl");
      return;
      } // end switch (option)

    // Codegen for 19:31 for 0 quotations:
    case 1171:
      // No quotes for 19:31
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:32 for 1 quotations:
    case 1172:
      // Only one option for 19:32:"
      quote = F("At * 7:32 *, he suffered a fatal stroke.");
      attribution = F("- Stephen King, IT");
      return;

    // Codegen for 19:33 for 0 quotations:
    case 1173:
      // No quotes for 19:33
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:34 for 0 quotations:
    case 1174:
      // No quotes for 19:34
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:35 for 2 quotations:
    case 1175:
      option = random(0, 2);
      // 2 options for 19:35
      switch (option) {
        case 0:
      quote = F("* 7.35 *-40. Yseut arrives at 'M. and S.', puts through phone call.");
      attribution = F("- The Case of the Gilded Fly");
      return;
        case 1:
      quote = F("I looked at my watch. * Seven thirty-five *.");
      attribution = F("- Kathy Reichs, Bare Bones");
      return;
      } // end switch (option)

    // Codegen for 19:36 for 0 quotations:
    case 1176:
      // No quotes for 19:36
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:37 for 0 quotations:
    case 1177:
      // No quotes for 19:37
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:38 for 0 quotations:
    case 1178:
      // No quotes for 19:38
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:39 for 1 quotations:
    case 1179:
      // Only one option for 19:39:"
      quote = F("Now, at the station, do you recall speaking to Mr Joseph Markew?' 'Yes, indeed. I was standing on the platform waiting for my usual train - the * 7.39 * - when he accosted me.'");
      attribution = F("- Arthur & George");
      return;

    // Codegen for 19:40 for 1 quotations:
    case 1180:
      // Only one option for 19:40:"
      quote = F("She arrives at * 7.40 *, ten minutes late, but the children, Jimmy and Bitsy, are still eating supper and their parents are not ready to go yet.");
      attribution = F("- The Babysitter");
      return;

    // Codegen for 19:41 for 0 quotations:
    case 1181:
      // No quotes for 19:41
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:42 for 1 quotations:
    case 1182:
      // Only one option for 19:42:"
      quote = F("I glance at my watch as we speed along the Strand. * Seven forty-two *. I'm starting to feel quite excited. The street outside is still bright and warm...");
      attribution = F("- The Undomestic Goddess");
      return;

    // Codegen for 19:43 for 0 quotations:
    case 1183:
      // No quotes for 19:43
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:44 for 1 quotations:
    case 1184:
      // Only one option for 19:44:"
      quote = F("It's not exactly how you'd choose to go, is it? You'd rather die flying a kite with your grandchildren, or at a great party or something. Not on the * seven forty-four *.");
      attribution = F("- One Moment, One Morning");
      return;

    // Codegen for 19:45 for 2 quotations:
    case 1185:
      option = random(0, 2);
      // 2 options for 19:45
      switch (option) {
        case 0:
      quote = F("He tells his old friend the train times and they settle on the * 19.45 * arriving at 23.27. I'll book us into the ultra-luxurious Francis Drake Lodge. Running water in several rooms.");
      attribution = F("- Other People's Money");
      return;
        case 1:
      quote = F("... the 'stupid human habit' of arbitrarily choosing a time based on fifteen-minute intervals. \"Never meet people at * 7:45 * or 6:30, Jasper, but pick times like 7:12 and 8:03!\"");
      attribution = F("- A Fraction of the Whole");
      return;
      } // end switch (option)

    // Codegen for 19:46 for 0 quotations:
    case 1186:
      // No quotes for 19:46
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:47 for 0 quotations:
    case 1187:
      // No quotes for 19:47
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:48 for 0 quotations:
    case 1188:
      // No quotes for 19:48
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:49 for 1 quotations:
    case 1189:
      // Only one option for 19:49:"
      quote = F("I noticed that our clock was a couple of minutes slow, and I put it right. \"When did you next see Dr. Wellesley?\" \"At just * eleven minutes to eight *.\"");
      attribution = F("- In the Mayor's Parlour");
      return;

    // Codegen for 19:50 for 2 quotations:
    case 1190:
      option = random(0, 2);
      // 2 options for 19:50
      switch (option) {
        case 0:
      quote = F("At about * ten minutes to eight *, Jim had squared the part of the work he had been doing - the window - so he decided not to start on the door or the skirting until after breakfast.");
      attribution = F("- Robert Tressell");
      return;
        case 1:
      quote = F("At * ten to eight *, he strolled downstairs, to make sure that Signora Buffi was not pottering around in the hall and that her door was not open...");
      attribution = F("- The Talented Mr Ripley");
      return;
      } // end switch (option)

    // Codegen for 19:51 for 0 quotations:
    case 1191:
      // No quotes for 19:51
        quote = "";
        attribution = "";
      return;

    // Codegen for 19:52 for 1 quotations:
    case 1192:
      // Only one option for 19:52:"
      quote = F("He waited until * nearly eight *, because around seven there were always more people coming in and out of the house than at other times.");
      attribution = F("- The Talented Mr Ripley");
      return;

    // Codegen for 19:53 for 2 quotations:
    case 1193:
      option = random(0, 2);
      // 2 options for 19:53
      switch (option) {
        case 0:
      quote = F("Wednesday, 11 th December 1963. * 7.53 p.m. * \"Help me. You've got to help me.\" The woman's voice quavered on the edge of tears.");
      attribution = F("- A Place of Execution");
      return;
        case 1:
      quote = F("What time is it? \"* Seven to eight *. Won't be long now ...\"");
      attribution = F("- Never go back");
      return;
      } // end switch (option)

    // Codegen for 19:54 for 1 quotations:
    case 1194:
      // Only one option for 19:54:"
      quote = F("The body was found at * six minutes to eight *. Doctor Young arrived some thirty minutes later. Just let me get that clear - I've a filthy memory.");
      attribution = F("- Ngaio Marsh, A Man Lay Dead");
      return;

    // Codegen for 19:55 for 1 quotations:
    case 1195:
      // Only one option for 19:55:"
      quote = F("Flora drew her coat round her, and looked up into the darkening vault of the sky. Then she glanced at her watch. It was * five to eight *.");
      attribution = F("- Cold Comfort Farm");
      return;

    // Codegen for 19:56 for 2 quotations:
    case 1196:
      option = random(0, 2);
      // 2 options for 19:56
      switch (option) {
        case 0:
      quote = F("The Castle Gate - only the Castle Gate - and it was * four minutes to eight *.");
      attribution = F("- Thomas Mann, Buddenbrooks");
      return;
        case 1:
      quote = F("'What time is it, Ireno?' Without consulting the sky, without stopping, he replied: 'It's * four minutes to eight *, young Bernardo Juan Franciso.'");
      attribution = F("- Jorge Luis Borges");
      return;
      } // end switch (option)

    // Codegen for 19:57 for 1 quotations:
    case 1197:
      // Only one option for 19:57:"
      quote = F("At * three minutes till eight *, Laszlo and His Yankee Hussars set up onstage. While the band played their Sousa medley, Carter thoroughly checked his kit...");
      attribution = F("- Carter Beats The Devil");
      return;

    // Codegen for 19:58 for 1 quotations:
    case 1198:
      // Only one option for 19:58:"
      quote = F("Robert Langdon stole an anxious glance at his wristwatch: * 7.58pm. * The smiling face of Mickey Mouse did little to cheer him up.");
      attribution = F("- Dan Brown, The Lost Symbol");
      return;

    // Codegen for 19:59 for 2 quotations:
    case 1199:
      option = random(0, 2);
      // 2 options for 19:59
      switch (option) {
        case 0:
      quote = F("Quickly, quickly. * A minute to eight. * My hot water bottle was ready, and I filled a glass with water from the tap. Time was of the essence.");
      attribution = F("- The Thirteenth Tale");
      return;
        case 1:
      quote = F("Kuniang made her appearance in my study * just before eight o' clock *, arrayed in what had once been a \"party frock\".");
      attribution = F("- Daniel Vare");
      return;
      } // end switch (option)

    // Codegen for 20:00 for 15 quotations:
    case 1200:
      option = random(0, 15);
      // 15 options for 20:00
      switch (option) {
        case 0:
      quote = F("* 8.00 p.m. * Have a bath");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("I hear noise at the ward door, off up the hall out of my sight. That ward door starts opening * at eight * and opens and closes a thousand times a day, kashash, click.");
      attribution = F("- Ken Kesey");
      return;
        case 2:
      quote = F("I trace the words, I'll arrive to collect you for drinks * at eight * on Saturday.");
      attribution = F("- Chuck Palahniuk, Tell-All");
      return;
        case 3:
      quote = F("That day he forgot to go to dinner; he noticed the fact * at eight in the evening *, and as it was too late to go to the Rue St Jaques, he ate a lump of bread.");
      attribution = F("- Victor Hugo, Les Miserables");
      return;
        case 4:
      quote = F("Too late. He must go on wanting what he had wanted yesterday. He dressed and rode down to the ground floor * at eight * for breakfast.");
      attribution = F("- Death in Venice");
      return;
        case 5:
      quote = F("Freud had me knock on Jung's door, to no avail. They waited until * eight *, then set off for Brill's without him.");
      attribution = F("- Jed Rubenfeld");
      return;
        case 6:
      quote = F("Arthur thought he could even bear to listen to the album of bagpipe music he had won. It was * eight o'clock * and he decided he would make himself, force himself, to listen...");
      attribution = F("- Douglas Adams");
      return;
        case 7:
      quote = F("At * eight o'clock * that evening, a Saturday, Pamela Chamcha stood with Jumpy Joshi - who had refused to let her go unaccompanied - next to the Photo-Me machine...");
      attribution = F("- Satanic Verses");
      return;
        case 8:
      quote = F("But for now it was still * eight o'clock *, and as I walked along the avenue under that brilliant blue sky, I was happy, my friends, as happy as any man who had ever lived.");
      attribution = F("- Brooklyn Follies");
      return;
        case 9:
      quote = F("By * eight o'clock * Stillman would come out, always in his long brown overcoat, carrying a large, old-fashioned carpet bag. For two weeks this routine did not vary.");
      attribution = F("- Paul Auster, City of Glass");
      return;
        case 10:
      quote = F("It's the twenty-third of June nineteen seventy-five, and it is * eight o'clock * in the evening, seated at his jigsaw puzzle, Bartlebooth has just died.");
      attribution = F("- Life: A User's Manual");
      return;
        case 11:
      quote = F("She looked at her watch- it was * eight o'clock *");
      attribution = F("- The Beautiful and Damned");
      return;
        case 12:
      quote = F("... so everything that happened has a dim, hazy cast over it. Although until after * eight o'clock * the apartment was full of cheerful sun.");
      attribution = F("- The Great Gatsby");
      return;
        case 13:
      quote = F("'TIS * eight o'clock *,--a clear March night, The moon is up,--the sky is blue, The owlet, in the moonlight air, Shouts from nobody knows where.");
      attribution = F("- The Idiot Boy");
      return;
        case 14:
      quote = F("The clock struck * eight *. Had it been ten, Elinor would have been convinced that at that moment she heard a carriage driving up to the house.");
      attribution = F("- Sense and Sensibility");
      return;
      } // end switch (option)

    // Codegen for 20:01 for 2 quotations:
    case 1201:
      option = random(0, 2);
      // 2 options for 20:01
      switch (option) {
        case 0:
      quote = F("It was only * a little after eight o'clock *, so all the shows were about silliness or murder.");
      attribution = F("- Slaughterhouse 5");
      return;
        case 1:
      quote = F("Eight-one, tick-tock, * eight-one * o'clock, off to school, off to work, run, run, eight-one!");
      attribution = F("- There Will Come Soft Rains");
      return;
      } // end switch (option)

    // Codegen for 20:02 for 2 quotations:
    case 1202:
      option = random(0, 2);
      // 2 options for 20:02
      switch (option) {
        case 0:
      quote = F("... bingeley ... * Eight oh two * eh em, Death of Corporal Littlebottombottom ... Eight oh three eh em ... Death of Sergeant Detritus ...");
      attribution = F("- Terry Pratchett, Jingo");
      return;
        case 1:
      quote = F("Anna looked at the railway timetable in the newspapers. An evening train went at * two minutes past eight *. \"Yes, I shall be in time.\"");
      attribution = F("- Leo Tolstoy, Anna Karenina");
      return;
      } // end switch (option)

    // Codegen for 20:03 for 2 quotations:
    case 1203:
      option = random(0, 2);
      // 2 options for 20:03
      switch (option) {
        case 0:
      quote = F("...the 'stupid human habit' of arbitrarily choosing a time based on fifteen-minute intervals. 'Never meet people at 7:45 or 6:30, Jasper, but pick times like 7:12 and * 8:03 *!'");
      attribution = F("- A Fraction of the Whole");
      return;
        case 1:
      quote = F("... bingeley ... Eight oh two eh em, Death of Corporal Littlebottombottom ... * Eight oh three * eh em ... Death of Sergeant Detritus ...");
      attribution = F("- Terry Pratchett, Jingo");
      return;
      } // end switch (option)

    // Codegen for 20:04 for 1 quotations:
    case 1204:
      // Only one option for 20:04:"
      quote = F("The farm country is somber at night. He is grateful when the lights of Lankaster merge with his dim beams. He stops at a diner who's clock says * 8.04 *.");
      attribution = F("- John Updike, Rabbit, Run");
      return;

    // Codegen for 20:05 for 2 quotations:
    case 1205:
      option = random(0, 2);
      // 2 options for 20:05
      switch (option) {
        case 0:
      quote = F("December 23rd At * 8.05 pm * Prof. Preobrazhensky commenced the first operation of its kind to be performed in Europe: removal under anaesthesia of a dog's testicles ...");
      attribution = F("- The Heart of a Dog");
      return;
        case 1:
      quote = F("... Boston time, and saw that the minutes had sped with increasing velocity during this interview, and that it now marked * five minutes past eight *.");
      attribution = F("- Henry James, The Bostonians");
      return;
      } // end switch (option)

    // Codegen for 20:06 for 1 quotations:
    case 1206:
      // Only one option for 20:06:"
      quote = F("...so everything that happened has a dim, hazy cast over it, although until * after eight o'clock * the apartment was full of cheerful sun.");
      attribution = F("- The Great Gatsby");
      return;

    // Codegen for 20:07 for 2 quotations:
    case 1207:
      option = random(0, 2);
      // 2 options for 20:07
      switch (option) {
        case 0:
      quote = F("Bennie pulled the transcripts for that night. The first call had come in at * 8:07 *, with a positive ID.");
      attribution = F("- Mistaken Identity");
      return;
        case 1:
      quote = F("...I opened my eyes and I looked at my watch and it said * 8:07 pm * and I had been sitting on the bench for approximately 5 hours but it hadn't seemed like approximately 5 hours...");
      attribution = F("- Mark Haddon");
      return;
      } // end switch (option)

    // Codegen for 20:08 for 0 quotations:
    case 1208:
      // No quotes for 20:08
        quote = "";
        attribution = "";
      return;

    // Codegen for 20:09 for 1 quotations:
    case 1209:
      // Only one option for 20:09:"
      quote = F("He followed the squeals down a hallway. A wall clock read * 8:09 * - 10:09 Dallas time.");
      attribution = F("- American Tabloid");
      return;

    // Codegen for 20:10 for 2 quotations:
    case 1210:
      option = random(0, 2);
      // 2 options for 20:10
      switch (option) {
        case 0:
      quote = F("At * 2010h. * on 1 April Y.D.A.U., the medical attache is still watching the unlabelled entertainment cartridge.");
      attribution = F("- Infinite Jest");
      return;
        case 1:
      quote = F("Sloane, with his shoulder punctured, was on another lounge. He was half delirious, and kept calling something about a chemistry lecture at * 8:10 *.");
      attribution = F("- This Side of Paradise");
      return;
      } // end switch (option)

    // Codegen for 20:11 for 1 quotations:
    case 1211:
      // Only one option for 20:11:"
      quote = F("For some reason they both wore white rosettes pinned to their dresses. I looked again at the clock: * eight-eleven *.");
      attribution = F("- The Blackpool Highflyer");
      return;

    // Codegen for 20:12 for 0 quotations:
    case 1212:
      // No quotes for 20:12
        quote = "";
        attribution = "";
      return;

    // Codegen for 20:13 for 0 quotations:
    case 1213:
      // No quotes for 20:13
        quote = "";
        attribution = "";
      return;

    // Codegen for 20:14 for 1 quotations:
    case 1214:
      // Only one option for 20:14:"
      quote = F("When a call came through to Dilworth's home number at * fourteen minutes past eight * o'clock, Olbier and Jones reacted with far more excitement...");
      attribution = F("- Dean Koontz, Watchers");
      return;

    // Codegen for 20:15 for 4 quotations:
    case 1215:
      option = random(0, 4);
      // 4 options for 20:15
      switch (option) {
        case 0:
      quote = F("* 8:15 p.m. * Cannot locate operating instructions (for video)");
      attribution = F("- Bridget Jones's Diary");
      return;
        case 1:
      quote = F("* 8.15 p.m. * Get changed into pyjamas");
      attribution = F("- Mark Haddon");
      return;
        case 2:
      quote = F("You scrutinized your wrist: \"It's * eight fifteen *. (And here time forked.) I'll turn it on.\" The screen In its blank broth evolved a lifelike blur, And music welled.");
      attribution = F("- Pale Fire");
      return;
        case 3:
      quote = F("[Puts down her candle] What's the time? Andrey: [Looks at his watch] A * quarter past eight *. Natasha: And Olga and Irina aren't in yet. The poor things are still at work.");
      attribution = F("- The Three Sisters");
      return;
      } // end switch (option)

    // Codegen for 20:16 for 2 quotations:
    case 1216:
      option = random(0, 2);
      // 2 options for 20:16
      switch (option) {
        case 0:
      quote = F("I feel I should have something to read. Or someone to call. My fingers are itching to move. I look at my watch. Still only * eight sixteen *. Oh God.");
      attribution = F("- The Undomestic Goddess");
      return;
        case 1:
      quote = F("He kissed her hand and after a while went to get two more drinks. When he got back, it was * sixteen minutes past eight *, and Lois was humming softly along with the jukebox");
      attribution = F("- The Rotters' Club");
      return;
      } // end switch (option)

    // Codegen for 20:17 for 2 quotations:
    case 1217:
      option = random(0, 2);
      // 2 options for 20:17
      switch (option) {
        case 0:
      quote = F("* 20.17 * A red warning light failed to go on in the Drive Room, beginning a chain of events which would lead, in a further twenty-three minutes, to the total annihilation...");
      attribution = F("- Grant Naylor, Red Dwarf");
      return;
        case 1:
      quote = F("I lean back and watch a little speckled bird pecking the ground nearby for a while. Then I look at my watch again: * eight seventeen *. I can't do this.");
      attribution = F("- The Undomestic Goddess");
      return;
      } // end switch (option)

    // Codegen for 20:18 for 1 quotations:
    case 1218:
      // Only one option for 20:18:"
      quote = F("* 2018 hrs * Katya has arrived at the Odessa Hotel. Barley and Katya are talking in the canteen. Wicklow and one irregular observing. More.");
      attribution = F("- The Russia House");
      return;

    // Codegen for 20:19 for 1 quotations:
    case 1219:
      // Only one option for 20:19:"
      quote = F("I had arranged to meet the Occupational Health Officer at 10:30. I took the train from Watford Junction at * 8.19 * and arrived at London Euston seven minutes late, at 8.49.");
      attribution = F("- Jonathan Coe");
      return;

    // Codegen for 20:20 for 3 quotations:
    case 1220:
      option = random(0, 3);
      // 3 options for 20:20
      switch (option) {
        case 0:
      quote = F("At * 20.20 * all ships had completed oiling. Hove to, they had had the utmost difficulty in keeping position in that great wind; but they were infinitely safer than in the open sea");
      attribution = F("- H.M.S. Ulysses");
      return;
        case 1:
      quote = F("* 8.20 p.m. * Play computer games");
      attribution = F("- Mark Haddon");
      return;
        case 2:
      quote = F("...we never dreamed it would be a full dress affair. I had no appetite. It was quite * twenty minutes past eight * before we sat down to dinner.");
      attribution = F("- Diary of a Nobody");
      return;
      } // end switch (option)

    // Codegen for 20:21 for 1 quotations:
    case 1221:
      // Only one option for 20:21:"
      quote = F("At * 8.21 *, after a knock at the door, a constable said a military police vehicle had just driven into the courtyard, the driver asking for 'Mr.' Murray.");
      attribution = F("- The Ipcress File");
      return;

    // Codegen for 20:22 for 0 quotations:
    case 1222:
      // No quotes for 20:22
        quote = "";
        attribution = "";
      return;

    // Codegen for 20:23 for 2 quotations:
    case 1223:
      option = random(0, 2);
      // 2 options for 20:23
      switch (option) {
        case 0:
      quote = F("* 20.23. * In a few minutes she would go down. She could have borrowed some mascara from her daughter Sally, but it was too late.");
      attribution = F("- The Radiant Way");
      return;
        case 1:
      quote = F("At * 8:23 * there seemed every chance of a lasting alliance starting between Florin and Guilder. At 8:24 the two nations were very close to war.");
      attribution = F("- The Princess Bride");
      return;
      } // end switch (option)

    // Codegen for 20:24 for 2 quotations:
    case 1224:
      option = random(0, 2);
      // 2 options for 20:24
      switch (option) {
        case 0:
      quote = F("At 8:23 there seemed every chance of a lasting alliance starting between Florin and Guilder. At * 8:24 * the two nations were very close to war.");
      attribution = F("- The Princess Bride");
      return;
        case 1:
      quote = F("Peach checked his watch. * 8.24. * If he wasn't in a taxi in twenty minutes he'd be done for.");
      attribution = F("- Dreams of Leaving");
      return;
      } // end switch (option)

    // Codegen for 20:25 for 1 quotations:
    case 1225:
      // Only one option for 20:25:"
      quote = F("She sat down in her usual seat and smiled at her husband as he sank into his own chair opposite her. She was saved. It was only * five and twenty past eight *.");
      attribution = F("- The Listerdale Mystery");
      return;

    // Codegen for 20:26 for 0 quotations:
    case 1226:
      // No quotes for 20:26
        quote = "";
        attribution = "";
      return;

    // Codegen for 20:27 for 1 quotations:
    case 1227:
      // Only one option for 20:27:"
      quote = F("At * seven-and-twenty minutes past eight * Mrs Lofthouse was seated at Aurora's piano, in the first agonies of a prelude in six flats...");
      attribution = F("- Aurora Floyd");
      return;

    // Codegen for 20:28 for 0 quotations:
    case 1228:
      // No quotes for 20:28
        quote = "";
        attribution = "";
      return;

    // Codegen for 20:29 for 2 quotations:
    case 1229:
      option = random(0, 2);
      // 2 options for 20:29
      switch (option) {
        case 0:
      quote = F("At * 8.29 * I punched the front doorbell in Elgin Crescent. It was opened by a small oriental woman in a white apron.");
      attribution = F("- Sebastian Faulks, Engleby");
      return;
        case 1:
      quote = F("* Twenty-nine and a half minutes past eight *, sir. And then, from habit, he glanced at the clock in the tower, and made further oration.");
      attribution = F("- O. Henry, The Four Million");
      return;
      } // end switch (option)

    // Codegen for 20:30 for 3 quotations:
    case 1230:
      option = random(0, 3);
      // 3 options for 20:30
      switch (option) {
        case 0:
      quote = F("...and there's the half-talk code of mysteries and the wink-and-elbow language of delight. * Half-past eight * and there is not a spot upon a mile of road...");
      attribution = F("- Patrick Kavanagh");
      return;
        case 1:
      quote = F("At * half past eight *, Mr. Dursley picked up his briefcase, pecked Mrs. Dursley on the cheek, and tried to kiss Dudley good-bye but missed...");
      attribution = F("- JK Rowling");
      return;
        case 2:
      quote = F("Gerald read a few pages of his book. Then he glanced up at the clock and tossed the book away. \"* Half-past eight *. Time to go down to the cellar and start work.\"");
      attribution = F("- The Listerdale Mystery");
      return;
      } // end switch (option)

    // Codegen for 20:31 for 0 quotations:
    case 1231:
      // No quotes for 20:31
        quote = "";
        attribution = "";
      return;

    // Codegen for 20:32 for 1 quotations:
    case 1232:
      // Only one option for 20:32:"
      quote = F("At the station he captured Miss Lantry out of the gadding mob at * eight thirty-two *. \"We mustn't keep mamma and the others waiting,\" said she.");
      attribution = F("- O. Henry, The Four Million");
      return;

    // Codegen for 20:33 for 1 quotations:
    case 1233:
      // Only one option for 20:33:"
      quote = F("* 20.33 * Navigation officer Henri DuBois knocked his black cona coffee with four sugars over his computer console keyboard.");
      attribution = F("- Grant Naylor, Red Dwarf");
      return;

    // Codegen for 20:34 for 0 quotations:
    case 1234:
      // No quotes for 20:34
        quote = "";
        attribution = "";
      return;

    // Codegen for 20:35 for 4 quotations:
    case 1235:
      option = random(0, 4);
      // 4 options for 20:35
      switch (option) {
        case 0:
      quote = F("* 8:35pm. * Found operating instructions under Hello.");
      attribution = F("- Bridget Jones's Diary");
      return;
        case 1:
      quote = F("Left Munich at * 8.35 p.m. * on 1st May, arriving at Vienna early the next morning");
      attribution = F("- Bram Stoker, Dracula");
      return;
        case 2:
      quote = F("The murderer is bound to have an interest in murder. She had gambled on that, and succeeded. She stole a glance at the clock. It was * five and twenty to nine *.");
      attribution = F("- The Listerdale mystery");
      return;
        case 3:
      quote = F("It was * thirty-five minutes past eight * by the big clock of the central building when Mathieu crossed the yard towards the office which he occupied as chief designer.");
      attribution = F("- Emile Zola, Fruitfulness");
      return;
      } // end switch (option)

    // Codegen for 20:36 for 1 quotations:
    case 1236:
      // Only one option for 20:36:"
      quote = F("* 20.36 * Rimmer stood in the main wash-room on the stasis deck and combed his hair.");
      attribution = F("- Grant Naylor, Red Dwarf");
      return;

    // Codegen for 20:37 for 0 quotations:
    case 1237:
      // No quotes for 20:37
        quote = "";
        attribution = "";
      return;

    // Codegen for 20:38 for 0 quotations:
    case 1238:
      // No quotes for 20:38
        quote = "";
        attribution = "";
      return;

    // Codegen for 20:39 for 0 quotations:
    case 1239:
      // No quotes for 20:39
        quote = "";
        attribution = "";
      return;

    // Codegen for 20:40 for 4 quotations:
    case 1240:
      option = random(0, 4);
      // 4 options for 20:40
      switch (option) {
        case 0:
      quote = F("At this moment the clock indicated * 8.40 *. 'Five minutes more,' said Andrew Stuart. The five friends looked at each other.");
      attribution = F("- Jules Verne");
      return;
        case 1:
      quote = F("I took note of the surrounding objects in detail, and saw that her watch had stopped at * twenty minutes to nine *, and that a clock in the room had stopped at twenty minutes to nine.");
      attribution = F("- Great Expectations");
      return;
        case 2:
      quote = F("It was when I stood before her, avoiding her eyes, that I took note of the surrounding objects in detail, and saw that her watch had stopped at * twenty minutes to nine *...");
      attribution = F("- Great Expectations");
      return;
        case 3:
      quote = F("The letter had been brought in at * twenty minutes to nine *.");
      attribution = F("- The Murder of Roger Ackroyd");
      return;
      } // end switch (option)

    // Codegen for 20:41 for 1 quotations:
    case 1241:
      // Only one option for 20:41:"
      quote = F("By * forty-one minutes past eight * we are five hundred yards from the water's edge, and between our road and the foot of the mountain we descry...");
      attribution = F("- Felicien de Saulcy");
      return;

    // Codegen for 20:42 for 1 quotations:
    case 1242:
      // Only one option for 20:42:"
      quote = F("The hand at this moment pointed to * 8.42 *. The players took up their cards, but their eyes were constantly on the clock.");
      attribution = F("- Jules Verne");
      return;

    // Codegen for 20:43 for 1 quotations:
    case 1243:
      // Only one option for 20:43:"
      quote = F("'* 8.43 *,' said Thomas Flanagan, as he cut the cards placed before him by Gauthier Ralph. There was a moment's pause, during which the spacious room was perfectly silent.");
      attribution = F("- Jules Verne");
      return;

    // Codegen for 20:44 for 1 quotations:
    case 1244:
      // Only one option for 20:44:"
      quote = F("The clock's pendulum beat every second with mathematical regularity, and each player could count every sixtieth of a minute as it struck his ear. \"* 8.44! *\" said John Sullivan.");
      attribution = F("- Jules Verne");
      return;

    // Codegen for 20:45 for 2 quotations:
    case 1245:
      option = random(0, 2);
      // 2 options for 20:45
      switch (option) {
        case 0:
      quote = F("It's not impossible, Phileas said quietly. \"I bet you 20,000 pounds I could do it. If I leave this evening on the * 8.45 * train to Dover, I can be back here at the Reform Club...\"");
      attribution = F("- Jules Verne");
      return;
        case 1:
      quote = F("Beaver arrived at * quarter to nine * in a state of high self-approval; he had refused two invitations for dinner while dressing that evening...");
      attribution = F("- A Handful of Dust");
      return;
      } // end switch (option)

    // Codegen for 20:46 for 1 quotations:
    case 1246:
      // Only one option for 20:46:"
      quote = F("At the tone, the time will be * eight forty six *, exactly. One cubic mile of seawater contains about 50 pounds of gold.");
      attribution = F("- Tom Lichtenberg, Macedonia");
      return;

    // Codegen for 20:47 for 2 quotations:
    case 1247:
      option = random(0, 2);
      // 2 options for 20:47
      switch (option) {
        case 0:
      quote = F("* 8.47 *. Bit misty on the roads");
      attribution = F("- Douglas Adams");
      return;
        case 1:
      quote = F("Just on my way to the cottage. It's, er, ..* 8.47 *. Bit misty on the roads.....");
      attribution = F("- Douglas Adams");
      return;
      } // end switch (option)

    // Codegen for 20:48 for 0 quotations:
    case 1248:
      // No quotes for 20:48
        quote = "";
        attribution = "";
      return;

    // Codegen for 20:49 for 2 quotations:
    case 1249:
      option = random(0, 2);
      // 2 options for 20:49
      switch (option) {
        case 0:
      quote = F("* 8.49 *. I took the phone, cleared my throat, and dialled the keep, the packs stronghold on the outskirts of Atlanta. Just keep it professional. Less pathetic that way.");
      attribution = F("- Ilona Andrews, Magic Bleeds");
      return;
        case 1:
      quote = F("I had arranged to meet the Occupational Health Officer at 10:30. I took the train from Watford Junction at 8.19 and arrived at London Euston seven minutes late, at * 8.49 *.");
      attribution = F("- Jonathan Coe");
      return;
      } // end switch (option)

    // Codegen for 20:50 for 5 quotations:
    case 1250:
      option = random(0, 5);
      // 5 options for 20:50
      switch (option) {
        case 0:
      quote = F("He was, yes, always home from work by * 2050 * on Thursdays.");
      attribution = F("- Infinite Jest");
      return;
        case 1:
      quote = F("* 8.50pm. * Ah Diagram \"Buttons for IMC functions\". But what are IMC functions?");
      attribution = F("- Bridget Jones's Diary");
      return;
        case 2:
      quote = F("all the clocks in London were striking * ten minutes before nine *.");
      attribution = F("- Jules Verne");
      return;
        case 3:
      quote = F("He glanced at the bracket-clock on the mantelpiece, but as this had stopped, drew out his watch. \"It is already too late,\" he said. \"It wants only * ten minutes to nine *.\"");
      attribution = F("- The Reluctant Widow");
      return;
        case 4:
      quote = F("What did it mean by beginning to tick so loudly all of a sudden? Its face indicated * ten minutes to nine *. Mrs Verloc cared nothing for time, and the ticking went on.");
      attribution = F("- The Secret Agent");
      return;
      } // end switch (option)

    // Codegen for 20:51 for 0 quotations:
    case 1251:
      // No quotes for 20:51
        quote = "";
        attribution = "";
      return;

    // Codegen for 20:52 for 0 quotations:
    case 1252:
      // No quotes for 20:52
        quote = "";
        attribution = "";
      return;

    // Codegen for 20:53 for 1 quotations:
    case 1253:
      // Only one option for 20:53:"
      quote = F("Only * eight fifty-three *. The partners' decision meeting starts in seven minutes. I'm not sure I can bear this.");
      attribution = F("- The Undomestic Goddess");
      return;

    // Codegen for 20:54 for 0 quotations:
    case 1254:
      // No quotes for 20:54
        quote = "";
        attribution = "";
      return;

    // Codegen for 20:55 for 2 quotations:
    case 1255:
      option = random(0, 2);
      // 2 options for 20:55
      switch (option) {
        case 0:
      quote = F("And the past. The clock on the dash said * 8:55pm. * And the last pink shard of the sun was reaching up into the night sky, desperately trying to hold on for just one more minute.");
      attribution = F("- Such Great Heights");
      return;
        case 1:
      quote = F("George pulled out his watch and looked at it: it was * five minutes to nine *!");
      attribution = F("- Three Men in a Boat");
      return;
      } // end switch (option)

    // Codegen for 20:56 for 1 quotations:
    case 1256:
      // Only one option for 20:56:"
      quote = F("No. 7 berth-a second-class. The gentleman has not yet come, and it is * four minutes to nine *.");
      attribution = F("- Agatha Christie");
      return;

    // Codegen for 20:57 for 1 quotations:
    case 1257:
      // Only one option for 20:57:"
      quote = F("they're a nuisance, anyway--always ticking and striking and getting you late for dinner. He glanced again at the clock in the tower. The hands stood at * three minutes to nine *.");
      attribution = F("- O. Henry, The Four Million");
      return;

    // Codegen for 20:58 for 2 quotations:
    case 1258:
      option = random(0, 2);
      // 2 options for 20:58
      switch (option) {
        case 0:
      quote = F("It was * two minutes of nine * now - two minutes before the bombs were set to explode - and three or four people were gathered in front of the bank waiting for it to open.");
      attribution = F("- Jim Thompson, The Getaway");
      return;
        case 1:
      quote = F("What time is it? she asked, quiet, definite, hopeless. \"* Two minutes to nine *,\" he replied, telling the truth with a struggle.");
      attribution = F("- Sons and Lovers");
      return;
      } // end switch (option)

    // Codegen for 20:59 for 0 quotations:
    case 1259:
      // No quotes for 20:59
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:00 for 18 quotations:
    case 1260:
      option = random(0, 18);
      // 18 options for 21:00
      switch (option) {
        case 0:
      quote = F("At * 2100 * at night it's cold out.");
      attribution = F("- Infinite Jest");
      return;
        case 1:
      quote = F("* 9.00 p.m. * Watch television or a video");
      attribution = F("- Mark Haddon");
      return;
        case 2:
      quote = F("He was at breakfast * at nine *, and for the twentieth time consulted his \"Bradshaw,\" to see at what earliest hour Dr. Grantly could arrive from Barchester.");
      attribution = F("- The Warden");
      return;
        case 3:
      quote = F("That night * at nine * the President addressed the nation.");
      attribution = F("- All the President's Men");
      return;
        case 4:
      quote = F("To be on the safe side, he did not unpack everything. * At nine * he went to breakfast in the specially designated buffet between the lobby and the dining room.");
      attribution = F("- Death in Venice");
      return;
        case 5:
      quote = F("A fly buzzed, the wall clock began to strike. After the * nine * golden strokes faded, the district captain began. \"How is Herr Colonel Marek?\" \"Thank you, Papa, he's fine.\"");
      attribution = F("- The Radetzky March");
      return;
        case 6:
      quote = F("As * nine o' clock * was left behind, the preposterousness of the delay overwhelmed me, and I went in a kind of temper to the owner and said...");
      attribution = F("- A Single Pebble");
      return;
        case 7:
      quote = F("'I could never get all the way down there before * nine o'clock *.'");
      attribution = F("- A Confederacy of Dunces");
      return;
        case 8:
      quote = F("It was around * nine o'clock * that I crossed the border into Cornwall. This was at least three hours before the rain began and the clouds were still all of a brilliant white.");
      attribution = F("- The Remains of the Day");
      return;
        case 9:
      quote = F("It was * nine o'clock * at night upon the second of August-the most terrible August in the history of the world.");
      attribution = F("- Arthur Conan Doyle");
      return;
        case 10:
      quote = F("On the evening before K.'s thirty-first birthday - it was about * nine o'clock *, when there is a lull in the streets - two gentlemen came to his apartment.");
      attribution = F("- Franz Kafka, The Trial");
      return;
        case 11:
      quote = F("On the stroke of * nine o'clock * Mr. and Mrs. De Voted took their places on either side of the drawing-room fire, in attitudes of gracefully combined hospitality and unconcern...");
      attribution = F("- A Marriage of Passion");
      return;
        case 12:
      quote = F("Shortly after * nine o'clock * that evening, Weyrother drove with his plans to Kutuzov's quarters where the council of war was to be held.");
      attribution = F("- Leo Tolstoy, War and Peace");
      return;
        case 13:
      quote = F("Standing in the chrome-and-tile desolation of the Polar-Shtern Kafeteria at * nine o'clock * on a Friday night, in a snowstorm, he's the loneliest Jew in the Sitka District.");
      attribution = F("- Michael Chabon");
      return;
        case 14:
      quote = F("Then he put on a grey jacket and left the flat to make his way to Praca da Alegria. It was already * nine o'clock *, Pereira maintains.");
      attribution = F("- Pereira Maintains");
      return;
        case 15:
      quote = F("This time, the putting on of her best hat at * nine o'clock * at night with the idea of sallying forth from the castle...");
      attribution = F("- Mervyn Peake, Titus Groan");
      return;
        case 16:
      quote = F("The clock struck * nine * when I did send the nurse; In half an hour she promised to return. Perchance she cannot meet him: that's not so.");
      attribution = F("- Romeo and Juliet");
      return;
        case 17:
      quote = F("To where Saint Mary Woolnoth kept the hours With a dead sound on the final stroke of * nine *.");
      attribution = F("- T S Eliot, The Waste Land");
      return;
      } // end switch (option)

    // Codegen for 21:01 for 1 quotations:
    case 1261:
      // Only one option for 21:01:"
      quote = F("On the evening before K.'s thirty-first birthday - it was * about nine o'clock *, when there is a lull in the streets - two gentlemen came to his apartment.");
      attribution = F("- Franz Kafka, The Trial");
      return;

    // Codegen for 21:02 for 1 quotations:
    case 1262:
      // Only one option for 21:02:"
      quote = F("The good Brants did not allow the boys to play out * after nine * in summer evenings; they were sent to bed at that hour; Eddie honorably remained...");
      attribution = F("- Mark Twain");
      return;

    // Codegen for 21:03 for 2 quotations:
    case 1263:
      option = random(0, 2);
      // 2 options for 21:03
      switch (option) {
        case 0:
      quote = F("...with a change at Swindon on the way, and by the time he got to Bath it was * about nine o'clock * in the evening and the moon was coming up...");
      attribution = F("- Roald Dahl, The Landlady");
      return;
        case 1:
      quote = F("...bell had rung as I was crossing the playground, but you always get five minutes to get to your classroom. \"It's * three minutes past nine *,\" Miss Beckworth announced.");
      attribution = F("- The Lottie Project");
      return;
      } // end switch (option)

    // Codegen for 21:04 for 1 quotations:
    case 1264:
      // Only one option for 21:04:"
      quote = F("At * 9.04pm * trilobites swim onto the scene, followed more or less immediately by the shapely creatures of the Burgess Shale.");
      attribution = F("- Bill Bryson");
      return;

    // Codegen for 21:05 for 1 quotations:
    case 1265:
      // Only one option for 21:05:"
      quote = F("* Nine-five *. A voice spoke from the study ceiling: \"Mrs. McClellan, which poem would you like this evening?\". The house was silent.");
      attribution = F("- There will come soft rains");
      return;

    // Codegen for 21:06 for 0 quotations:
    case 1266:
      // No quotes for 21:06
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:07 for 0 quotations:
    case 1267:
      // No quotes for 21:07
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:08 for 0 quotations:
    case 1268:
      // No quotes for 21:08
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:09 for 1 quotations:
    case 1269:
      // Only one option for 21:09:"
      quote = F("* 9.09 *. Too late to turn around and go back. Too late, too dangerous.");
      attribution = F("- Dreams of Leaving");
      return;

    // Codegen for 21:10 for 1 quotations:
    case 1270:
      // Only one option for 21:10:"
      quote = F("* 10.10pm. * When you turn your recorder on you must adjust clock and the calendar.......Press red and nothing happens. Press numbers and nothing happens.");
      attribution = F("- Bridget Jones's Diary");
      return;

    // Codegen for 21:11 for 1 quotations:
    case 1271:
      // Only one option for 21:11:"
      quote = F("...at one time menacing and sinister, and again the innocent abode of law-abiding citizens about to be attacked by my private army. The luminous watch said * 9.11 *.");
      attribution = F("- The Ipcress File");
      return;

    // Codegen for 21:12 for 1 quotations:
    case 1272:
      // Only one option for 21:12:"
      quote = F("The crime was reported to us (with almost indecent alacrity, Rog) at * 21.12 *, by Susan Trott - of Black Grouse Cottage - who had been, I quote...");
      attribution = F("- Burley Cross Postbox Theft");
      return;

    // Codegen for 21:13 for 0 quotations:
    case 1273:
      // No quotes for 21:13
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:14 for 0 quotations:
    case 1274:
      // No quotes for 21:14
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:15 for 4 quotations:
    case 1275:
      option = random(0, 4);
      // 4 options for 21:15
      switch (option) {
        case 0:
      quote = F("* 9.15 *. Did Roberts pay you yet?");
      attribution = F("- James Joyce, Ulysses");
      return;
        case 1:
      quote = F("That's all right. Okay. Jones pulled a handful of pencils from his back pocket. \"Now, I got the contact first at 0* 915 * or so, and the bearing was about two-six-nine.\"");
      attribution = F("- The Hunt for Red October");
      return;
        case 2:
      quote = F("But it's bloody miles away. I glance at my watch and am shocked to see that it's * nine-fifteen *. We've spent over an hour faffing about and we haven't even had a drink.");
      attribution = F("- Can You Keep a Secret?");
      return;
        case 3:
      quote = F("Miss Pettigrew pushed open the door of the employment agency and went in as the clock struck a * quarter past nine *.");
      attribution = F("- Winifred Watson");
      return;
      } // end switch (option)

    // Codegen for 21:16 for 0 quotations:
    case 1276:
      // No quotes for 21:16
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:17 for 1 quotations:
    case 1277:
      // Only one option for 21:17:"
      quote = F("* 21:17 *, Sunday Evening, Angbyplan. A man is observed outside the hair salon. He presses his face and hands against the glass, and appears extremely intoxicated.");
      attribution = F("- Let The Right One In");
      return;

    // Codegen for 21:18 for 1 quotations:
    case 1278:
      // Only one option for 21:18:"
      quote = F("...recognized her charming house, and thought to drop in. It was * eighteen minutes after nine * when Mr. Martin turned into Twelfth Street.");
      attribution = F("- The Catbird Seat");
      return;

    // Codegen for 21:19 for 0 quotations:
    case 1279:
      // No quotes for 21:19
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:20 for 3 quotations:
    case 1280:
      option = random(0, 3);
      // 3 options for 21:20
      switch (option) {
        case 0:
      quote = F("* 9.20 p.m. * Have juice and a snack");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("I'll compromise by saying that I left home at eight and spent an hour travelling to a nine o'clock appointment. Twenty minutes later is * nine-twenty *.");
      attribution = F("- Girl, Interrupted");
      return;
        case 2:
      quote = F("At * twenty minutes past nine *, the Duke of Dunstable, who had dined off a tray in his room, was still there, waiting for his coffee and liqueur.");
      attribution = F("- P.G. Wodehouse");
      return;
      } // end switch (option)

    // Codegen for 21:21 for 1 quotations:
    case 1281:
      // Only one option for 21:21:"
      quote = F("It was * nine twenty-one *. With one minute to go, there was no sign of Herbert's mother.");
      attribution = F("- Dan Rhodes, This is Life");
      return;

    // Codegen for 21:22 for 2 quotations:
    case 1282:
      option = random(0, 2);
      // 2 options for 21:22
      switch (option) {
        case 0:
      quote = F("Fifteen minutes later (* 21.22 hrs *), Miss Squire arrives in Skipton where she is booked into a local B&B. This B&B is located directly across the road...");
      attribution = F("- Burley Cross Postbox Theft");
      return;
        case 1:
      quote = F("No more throwing stones at him, and I'll see you back here exactly one week from now. She looked at her watch. 'At * nine twenty-two * next Wednesday.'");
      attribution = F("- Dan Rhodes, This is Life");
      return;
      } // end switch (option)

    // Codegen for 21:23 for 2 quotations:
    case 1283:
      option = random(0, 2);
      // 2 options for 21:23
      switch (option) {
        case 0:
      quote = F("* 9.23 *. What possessed me to buy this comb?");
      attribution = F("- James Joyce, Ulysses");
      return;
        case 1:
      quote = F("My father met me at the station, the dog jumped up to meet me, missed, and nearly fell in front of the * 9.23pm * Birmingham express.");
      attribution = F("- Sue Townsend");
      return;
      } // end switch (option)

    // Codegen for 21:24 for 1 quotations:
    case 1284:
      // Only one option for 21:24:"
      quote = F("* 9.24 * I'm swelled after that cabbage. A speck of dust on the patent leather of her boot.");
      attribution = F("- James Joyce, Ulysses");
      return;

    // Codegen for 21:25 for 1 quotations:
    case 1285:
      // Only one option for 21:25:"
      quote = F("* 9:25 p.m. * Aargh. Suddenly main menu is on TV saying Press 6. Realize was using telly remote control by mistake. Now News has come on");
      attribution = F("- Bridget Jones's Diary");
      return;

    // Codegen for 21:26 for 0 quotations:
    case 1286:
      // No quotes for 21:26
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:27 for 0 quotations:
    case 1287:
      // No quotes for 21:27
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:28 for 2 quotations:
    case 1288:
      option = random(0, 2);
      // 2 options for 21:28
      switch (option) {
        case 0:
      quote = F("From that moment on--* 9:28 * in the evening, June 18, 1941--everything was different.");
      attribution = F("- Everything is Illuminated");
      return;
        case 1:
      quote = F("This clock right? he asked the butler in the hall. \"Yes, sir.\" The clock showed * twenty-eight minutes past nine *. \"The clocks here have to be right, sir.\"");
      attribution = F("- Arnold Bennett, Lord Raingo");
      return;
      } // end switch (option)

    // Codegen for 21:29 for 0 quotations:
    case 1289:
      // No quotes for 21:29
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:30 for 6 quotations:
    case 1290:
      option = random(0, 6);
      // 6 options for 21:30
      switch (option) {
        case 0:
      quote = F("* 9.30 p.m. * Go to bed");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("It's * 9:30 p.m. * already. I've gotta head uptown for my appointment with Pavel. Pavel is my shrink. He sees patients at night. He's a Czech Jew, a survivor of Terezin and Auswitz.");
      attribution = F("- Art Spiegelman, Maus");
      return;
        case 2:
      quote = F("he looked at his watch; it was * half-past nine *");
      attribution = F("- A watcher by the dead");
      return;
        case 3:
      quote = F("Forty-eight years old, profoundly asleep at * nine thirty * on a Friday night - this is modern professional life.");
      attribution = F("- Ian McEwan, Saturday");
      return;
        case 4:
      quote = F("It was * nine-thirty *. In another ten minutes she would turn off the heat; then it would take a while for the water to cool. In the meantime there was nothing to do but wait.");
      attribution = F("- Revolutionary Road");
      return;
        case 5:
      quote = F("The light in Mr. Green's kitchen snapped off at * nine-thirty *, followed by the light in his bedroom at his usual ten o'clock. His house was the first on the street to go dark.");
      attribution = F("- A crime in the neighborhood");
      return;
      } // end switch (option)

    // Codegen for 21:31 for 1 quotations:
    case 1291:
      // Only one option for 21:31:"
      quote = F("On the table was a note from my girlfriend: \"Gone out to eat. Back by 9:30.\" The digital clock on the table read 9:30. I watched it flip over to * 9:31 *, then to 9:32.");
      attribution = F("- A Wild Sheep Chase");
      return;

    // Codegen for 21:32 for 2 quotations:
    case 1292:
      option = random(0, 2);
      // 2 options for 21:32
      switch (option) {
        case 0:
      quote = F("On the table was a note from my girlfriend: \"Gone out to eat. Back by 9:30.\" The digital clock on the table read 9:30. I watched it flip over to 9:31, then to * 9:32 *.");
      attribution = F("- A Wild Sheep Chase");
      return;
        case 1:
      quote = F("Sandy barely made the * nine-thirty-two * and found a seat in no-smoking. She'd been looking forward to this visit with Lisbeth.");
      attribution = F("- Judy Blume, Wifey");
      return;
      } // end switch (option)

    // Codegen for 21:33 for 1 quotations:
    case 1293:
      // Only one option for 21:33:"
      quote = F("...the half-ebb at a quarter past seven, low water at * thirty-three minutes past nine *, and half flood at thirty-nine minutes past twelve.");
      attribution = F("- The Toilers of the Sea");
      return;

    // Codegen for 21:34 for 1 quotations:
    case 1294:
      // Only one option for 21:34:"
      quote = F("Thanks; expect me * 9.34 p.m. * 26th'; which produced, three hours later, a reply: 'Delighted; please bring a No. 3 Rippingille stove' - a perplexing and ominous direction...");
      attribution = F("- The Riddle of the Sands");
      return;

    // Codegen for 21:35 for 2 quotations:
    case 1295:
      option = random(0, 2);
      // 2 options for 21:35
      switch (option) {
        case 0:
      quote = F("The Sergeant jotted it down on a piece of paper. 'That checks up with his own story: * 9.35 p.m. * Budd leaves; the North dame arrives.'");
      attribution = F("- A Blunt Instrument");
      return;
        case 1:
      quote = F("* Nine-thirty-five *. He really must be gone. The bird is no longer feeding but sitting at the apex of a curl of razor wire.");
      attribution = F("- The Memory of Love");
      return;
      } // end switch (option)

    // Codegen for 21:36 for 1 quotations:
    case 1296:
      // Only one option for 21:36:"
      quote = F("...like the altimeter and the granola bars and the Swiss army knife I'd dug up in Central Park, so there was nothing else to do. Mom tucked me in at * 9:36 *.");
      attribution = F("- Jonathan Safran Foer");
      return;

    // Codegen for 21:37 for 1 quotations:
    case 1297:
      // Only one option for 21:37:"
      quote = F("...all the details of service, the tea and toast at twenty-three minutes past eight, the shaving-water at * thirty-seven minutes past nine *...");
      attribution = F("- Around the World in 80 days");
      return;

    // Codegen for 21:38 for 1 quotations:
    case 1298:
      // Only one option for 21:38:"
      quote = F("At * nine thirty-eight * the waiter came back and offered us a second helping of cheese, salami and sardines, and Mr Yoshogi who had been converting sterling...");
      attribution = F("- Pig and Pepper");
      return;

    // Codegen for 21:39 for 0 quotations:
    case 1299:
      // No quotes for 21:39
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:40 for 1 quotations:
    case 1300:
      // Only one option for 21:40:"
      quote = F("... the tea and toast at twenty-three minutes past eight, the shaving-water at thirty-seven minutes past nine, and the toilet at * twenty minutes before ten *.");
      attribution = F("- Around the World in 80 days");
      return;

    // Codegen for 21:41 for 0 quotations:
    case 1301:
      // No quotes for 21:41
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:42 for 1 quotations:
    case 1302:
      // Only one option for 21:42:"
      quote = F("Langdon looked at his Mickey Mouse watch. * 9:42 P.M. *");
      attribution = F("- Dan Brown, The Lost Symbol");
      return;

    // Codegen for 21:43 for 0 quotations:
    case 1303:
      // No quotes for 21:43
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:44 for 0 quotations:
    case 1304:
      // No quotes for 21:44
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:45 for 2 quotations:
    case 1305:
      option = random(0, 2);
      // 2 options for 21:45
      switch (option) {
        case 0:
      quote = F("9.15, 9.30, * 9.45 *, 10! Bond felt the excitement ball up inside him like cat's fur.");
      attribution = F("- Ian Fleming");
      return;
        case 1:
      quote = F("But for some unfathomable reason-birth, death, the end of the universe and all things available to man-Cody Menhoff's was closed at * 9:45 PM * on a Thursday...");
      attribution = F("- T. C. Boyle, Riven Rock");
      return;
      } // end switch (option)

    // Codegen for 21:46 for 0 quotations:
    case 1306:
      // No quotes for 21:46
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:47 for 1 quotations:
    case 1307:
      // Only one option for 21:47:"
      quote = F("For Hunter, who was trained to note times exactly, the final emergency started at * thirteen minutes to ten *.");
      attribution = F("- Elegy for a Revolutionary");
      return;

    // Codegen for 21:48 for 0 quotations:
    case 1308:
      // No quotes for 21:48
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:49 for 0 quotations:
    case 1309:
      // No quotes for 21:49
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:50 for 1 quotations:
    case 1310:
      // Only one option for 21:50:"
      quote = F("I passed out on to the road and saw by the lighted dial of a clock that it was * ten minutes to ten *. In front of me was a large building which displayed the magical name.");
      attribution = F("- James Joyce, Dubliners");
      return;

    // Codegen for 21:51 for 0 quotations:
    case 1311:
      // No quotes for 21:51
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:52 for 0 quotations:
    case 1312:
      // No quotes for 21:52
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:53 for 1 quotations:
    case 1313:
      // Only one option for 21:53:"
      quote = F("The bus deposited her about five minutes' walk from Onslow Mansions, an at * seven minutes to ten * precisely she was outside her destination.");
      attribution = F("- Winifred Watson");
      return;

    // Codegen for 21:54 for 0 quotations:
    case 1314:
      // No quotes for 21:54
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:55 for 1 quotations:
    case 1315:
      // Only one option for 21:55:"
      quote = F("At * five to ten * I'm ready in the hall. Nathaniel's mother's house is nearby but apparently tricky to find, so the plan is to meet here and he'll walk me over.");
      attribution = F("- The Undomestic Goddess");
      return;

    // Codegen for 21:56 for 0 quotations:
    case 1316:
      // No quotes for 21:56
        quote = "";
        attribution = "";
      return;

    // Codegen for 21:57 for 2 quotations:
    case 1317:
      option = random(0, 2);
      // 2 options for 21:57
      switch (option) {
        case 0:
      quote = F("Second to last, the inset clock blinks from * 21:57 * to 21:58. Napier's eyes sink, newborn sunshine slants through ancient oaks and on a lost river. Look, Joe, herons");
      attribution = F("- David Mitchell, Cloud Atlas");
      return;
        case 1:
      quote = F("The waiting man pulled out a handsome watch, the lids of it set with small diamonds. \"* Three minutes to ten *,\" he announced.");
      attribution = F("- O. Henry, The Four Million");
      return;
      } // end switch (option)

    // Codegen for 21:58 for 1 quotations:
    case 1318:
      // Only one option for 21:58:"
      quote = F("Second to last, the inset clock blinks from 21:57 to * 21:58 *. Napier's eyes sink, newborn sunshine slants through ancient oaks and on a lost river. Look, Joe, herons");
      attribution = F("- David Mitchell, Cloud Atlas");
      return;

    // Codegen for 21:59 for 2 quotations:
    case 1319:
      option = random(0, 2);
      // 2 options for 21:59
      switch (option) {
        case 0:
      quote = F("The first night, as soon as the corporal had conducted my uncle Toby upstairs, which was * about 10 * - Mrs. Wadman threw herself into her arm chair");
      attribution = F("- Laurence Sterne");
      return;
        case 1:
      quote = F("* One minute to ten. * With a heavy heart Bert watched the clock. His legs were still aching very badly. He could not see the hands of the clock moving...");
      attribution = F("- Robert Tressell");
      return;
      } // end switch (option)

    // Codegen for 22:00 for 11 quotations:
    case 1320:
      option = random(0, 11);
      // 11 options for 22:00
      switch (option) {
        case 0:
      quote = F("Monday 30 March 1668 Up betimes, and so to the office, there to do business till about * 10 o'clock *");
      attribution = F("- The Diary of Samuel Pepys");
      return;
        case 1:
      quote = F("'Tis but * an hour ago since it was nine *, And after one hour more 'twill be eleven.");
      attribution = F("- As You Like It");
      return;
        case 2:
      quote = F("By * ten *, Quoyle was drunk. The crowd was enormous, crushed together so densely that Nutbeem could not force his way down the hall...");
      attribution = F("- The Shipping News");
      return;
        case 3:
      quote = F("I could not doubt that this was the BLACK SPOT; and taking it up, I found writ* ten * on the other side, in a very good, clear hand, this short message: \"You have till ten tonight.\"");
      attribution = F("- Treasure Island");
      return;
        case 4:
      quote = F("Florence set down her knife and reached for Edward's hand and squeezed. From downstairs they heard the wireless, the chimes of Big Ben at the start of the * ten o'clock * news.");
      attribution = F("- Ian McEwan, On Chesil Beach");
      return;
        case 5:
      quote = F("Her body asserted itself with a restless movement of the knee, and she stood up. \"* Ten o'clock *,\" she remarked, apparently finding the time on the ceiling.");
      attribution = F("- The Great Gatsby");
      return;
        case 6:
      quote = F("No one wanted to go to bed when at * ten o'clock * Mrs. March put by the last finished job, and said, \"Come girls.\" Beth went to the piano and played the father's favorite hymn.");
      attribution = F("- Little Women");
      return;
        case 7:
      quote = F("Presently it stopped, as though cut suddenly in exasperation. The clock on the mantelpiece struck * ten o'clock *. Maxim put his arms round me and lifted me against him.");
      attribution = F("- Daphne du Maurier, Rebecca");
      return;
        case 8:
      quote = F("The light in Mr. Green's kitchen snapped off at nine-thirty, followed by the light in his bedroom at his usual * ten o'clock *. His house was the first on the street to go dark.");
      attribution = F("- A crime in the neighborhood");
      return;
        case 9:
      quote = F("...who appeared to have nothing to do on this particular evening; we simply could not get rid of him without being rude; he hung about until * ten o'clock *.");
      attribution = F("- Anne Frank");
      return;
        case 10:
      quote = F("The grandfather clock in the State Room strikes * ten * times.");
      attribution = F("- David Mitchell");
      return;
      } // end switch (option)

    // Codegen for 22:01 for 0 quotations:
    case 1321:
      // No quotes for 22:01
        quote = "";
        attribution = "";
      return;

    // Codegen for 22:02 for 2 quotations:
    case 1322:
      option = random(0, 2);
      // 2 options for 22:02
      switch (option) {
        case 0:
      quote = F("It was now * 10.02pm. * He has less than two hours.");
      attribution = F("- Dan Brown, The Lost Symbol");
      return;
        case 1:
      quote = F("It was * two minutes after ten *; she was not satisfied with her clothes, her face, her apartment. She heated the coffee again and sat down in the chair by the window.");
      attribution = F("- The Daemon Lover");
      return;
      } // end switch (option)

    // Codegen for 22:03 for 1 quotations:
    case 1323:
      // Only one option for 22:03:"
      quote = F("...the time was twenty three minutes past ten, so he tapped in the numbers 10.23. At least, that's what he meant to do. In fact he typed in the numbers * 10.03 *.");
      attribution = F("- Andrew Norriss, Ctrl-Z");
      return;

    // Codegen for 22:04 for 0 quotations:
    case 1324:
      // No quotes for 22:04
        quote = "";
        attribution = "";
      return;

    // Codegen for 22:05 for 2 quotations:
    case 1325:
      option = random(0, 2);
      // 2 options for 22:05
      switch (option) {
        case 0:
      quote = F("...the elevator on which the paramedics came up at 9:20 p.m., the elevator on which they took John (and me) downstairs to the ambulance at * 10:05 p.m. *");
      attribution = F("- Joan Didion");
      return;
        case 1:
      quote = F("Then I glance at my watch. It's already * five past ten *. \"We should get going,\" I say with a little start. \"Your mother will be waiting.\"");
      attribution = F("- The Undomestic Goddess");
      return;
      } // end switch (option)

    // Codegen for 22:06 for 1 quotations:
    case 1326:
      // Only one option for 22:06:"
      quote = F("Of course, they had good reason to be fussy on such a night. And then it was long * after ten o'clock * and yet there was no sign of Gabriel and his wife.");
      attribution = F("- James Joyce, The Dead");
      return;

    // Codegen for 22:07 for 0 quotations:
    case 1327:
      // No quotes for 22:07
        quote = "";
        attribution = "";
      return;

    // Codegen for 22:08 for 1 quotations:
    case 1328:
      // Only one option for 22:08:"
      quote = F("My watch is always a little fast, I said. \"What time do you make it now?\" \"* Ten eight *.\" \"Ten eighteen by mine. You see.\"");
      attribution = F("- The Quiet American");
      return;

    // Codegen for 22:09 for 1 quotations:
    case 1329:
      // Only one option for 22:09:"
      quote = F("He followed the squeals down a hallway. A wall clock read 8:09-* 10:09 * Dallas time.");
      attribution = F("- American Tabloid");
      return;

    // Codegen for 22:10 for 2 quotations:
    case 1330:
      option = random(0, 2);
      // 2 options for 22:10
      switch (option) {
        case 0:
      quote = F("* 10:10 * Shot is fired.");
      attribution = F("- The Hollow Man");
      return;
        case 1:
      quote = F("On the left there was a chest of drawers, on the right a mantelpiece surmounted by a damaged clock without a pendulum, the hands of which marked * ten minutes past ten *.");
      attribution = F("- Emile Zola");
      return;
      } // end switch (option)

    // Codegen for 22:11 for 2 quotations:
    case 1331:
      option = random(0, 2);
      // 2 options for 22:11
      switch (option) {
        case 0:
      quote = F("Therefore a sergeant called Trifonov had been on post all day or all week and then he had left at * eleven minutes past ten * in the evening.");
      attribution = F("- Lee Child, The Enemy");
      return;
        case 1:
      quote = F("...those of the ormolu clock in the sitting-room to * eleven minutes past ten *; those of the carriage clock on the bookshelf to fourteen minutes to six.");
      attribution = F("- Three Men and a Maid");
      return;
      } // end switch (option)

    // Codegen for 22:12 for 2 quotations:
    case 1332:
      option = random(0, 2);
      // 2 options for 22:12
      switch (option) {
        case 0:
      quote = F("The Chinese women scuttled at an amazing rate, given their size and the bags' size. It was c. * 2212 *:30-40h., smack in the middle of the former Interval of Issues Resolution.");
      attribution = F("- Infinite Jest");
      return;
        case 1:
      quote = F("I'll take the coffee tray out, I suggest humbly. As I pick it up I glance again at my watch. * Ten twelve *. I wonder if they've started the meeting.");
      attribution = F("- The Undomestic Goddess");
      return;
      } // end switch (option)

    // Codegen for 22:13 for 1 quotations:
    case 1333:
      // Only one option for 22:13:"
      quote = F("And I sent it to the Daily Telegraph at * thirteen minutes past ten *. \"Bravo, Mr. Blount!\" \"Very good, M. Jolivet.\"");
      attribution = F("- Michel Strogoff");
      return;

    // Codegen for 22:14 for 2 quotations:
    case 1334:
      option = random(0, 2);
      // 2 options for 22:14
      switch (option) {
        case 0:
      quote = F("The shopping bags looked heavy and impressive, their weight making the Chinese women lean in slightly towards each other. Call it * 2214 *:10h.");
      attribution = F("- Infinite Jest");
      return;
        case 1:
      quote = F("Okay. * Ten fourteen *: Mrs. Narada reports that her cat has been attacked by a large dog. Now I send all the boys out looking, but they don't find anything until eleven.");
      attribution = F("- Coyote Blue");
      return;
      } // end switch (option)

    // Codegen for 22:15 for 1 quotations:
    case 1335:
      // Only one option for 22:15:"
      quote = F("* 10:15 p.m. * Aargh Newsnight on in 15 minutes");
      attribution = F("- Bridget Jones's Diary");
      return;

    // Codegen for 22:16 for 0 quotations:
    case 1336:
      // No quotes for 22:16
        quote = "";
        attribution = "";
      return;

    // Codegen for 22:17 for 1 quotations:
    case 1337:
      // Only one option for 22:17:"
      quote = F("* 10:17 p. m. * Casette will not go in");
      attribution = F("- Bridget Jones's Diary");
      return;

    // Codegen for 22:18 for 3 quotations:
    case 1338:
      option = random(0, 3);
      // 3 options for 22:18
      switch (option) {
        case 0:
      quote = F("I know that it was * 10:18 * when I got home because I look at my watch a lot.");
      attribution = F("- Jonathan Safran Foer");
      return;
        case 1:
      quote = F("* 10:18pm. * Ah. Thelma and Louise is in there");
      attribution = F("- Bridget Jones's Diary");
      return;
        case 2:
      quote = F("My watch is always a little fast, I said. \"What time do you make it now?\" \"Ten eight.\" \"* Ten eighteen * by mine. You see.\"");
      attribution = F("- The Quiet American");
      return;
      } // end switch (option)

    // Codegen for 22:19 for 0 quotations:
    case 1339:
      // No quotes for 22:19
        quote = "";
        attribution = "";
      return;

    // Codegen for 22:20 for 2 quotations:
    case 1340:
      option = random(0, 2);
      // 2 options for 22:20
      switch (option) {
        case 0:
      quote = F("At * 10:20 * she returned with a shopping bag from the supermarket. In the bag were three scrub brushes, one box of paperclips and a well-chilled six-pack of canned beer.");
      attribution = F("- A Wild Sheep Chase");
      return;
        case 1:
      quote = F("An hour and twenty-five minutes, she said. The right time now is * twenty past ten *.");
      attribution = F("- James Joyce");
      return;
      } // end switch (option)

    // Codegen for 22:21 for 4 quotations:
    case 1341:
      option = random(0, 4);
      // 4 options for 22:21
      switch (option) {
        case 0:
      quote = F("Liz Headleand stares into the mirror, as though entranced. She does not see herself or the objects on her dressing-table. The clock abruptly jerks to * 10.21 *.");
      attribution = F("- The Radiant Way");
      return;
        case 1:
      quote = F("* 10:21pm. * Frenziedly press all buttons. Cassette comes out and goes back in again");
      attribution = F("- Bridget Jones's Diary");
      return;
        case 2:
      quote = F("* 10:21pm. * Thelma and Louise will not come out");
      attribution = F("- Bridget Jones's Diary");
      return;
        case 3:
      quote = F("On a Saturday c. * 2221h *., Lenz found a miniature bird that had fallen out of some nest and was sitting bald and pencil-necked on the lawn of Unit #3 flapping ineffectually...");
      attribution = F("- Infinite Jest");
      return;
      } // end switch (option)

    // Codegen for 22:22 for 1 quotations:
    case 1342:
      // Only one option for 22:22:"
      quote = F("...then before I had time to figure out what to do, or even what to think or feel, the phone started ringing. It was * 10:22:27 *. I looked at the caller ID and saw that it was him.");
      attribution = F("- Jonathan Safran Foer");
      return;

    // Codegen for 22:23 for 1 quotations:
    case 1343:
      // Only one option for 22:23:"
      quote = F("The date was the 14th of May and the clock on his desk said the time was * twenty three minutes past ten *, so he tapped in the numbers 10.23.");
      attribution = F("- Andrew Norriss, Ctrl-Z");
      return;

    // Codegen for 22:24 for 1 quotations:
    case 1344:
      // Only one option for 22:24:"
      quote = F("Thanks to ten minutes or so of balmy weather, by * 10:24 * the Earth is covered in the great carboniferous forests whose residues give us all our coal...");
      attribution = F("- Bill Bryson");
      return;

    // Codegen for 22:25 for 2 quotations:
    case 1345:
      option = random(0, 2);
      // 2 options for 22:25
      switch (option) {
        case 0:
      quote = F("* 10:25 *: Phone call from Luding, very worked up, urging me to return at once and get in touch with Alois, who was equally worked up.");
      attribution = F("- Heinrich Boll");
      return;
        case 1:
      quote = F("* 10:25pm. * Got new cassette in now. Right. Turn to \"Recording.................. Aargh Newsnight is starting\"");
      attribution = F("- Bridget Jones's Diary");
      return;
      } // end switch (option)

    // Codegen for 22:26 for 2 quotations:
    case 1346:
      option = random(0, 2);
      // 2 options for 22:26
      switch (option) {
        case 0:
      quote = F("* 10:26 * No. Please, no. I can hardly bear to look. It's a total disaster. Everything in the washing machine has gone pink. Every single thing. What happened?");
      attribution = F("- The Undomestic Goddess");
      return;
        case 1:
      quote = F("In the exact center of my visual field was the alarm clock, hands pointing to * ten-twenty-six *.");
      attribution = F("- Haruki Murakami");
      return;
      } // end switch (option)

    // Codegen for 22:27 for 1 quotations:
    case 1347:
      // Only one option for 22:27:"
      quote = F("Mr. Harcourt woke up with mysterious suddenness at * twenty-seven minutes past 10 *, and, by a curious coincidence...");
      attribution = F("- England, Their England");
      return;

    // Codegen for 22:28 for 0 quotations:
    case 1348:
      // No quotes for 22:28
        quote = "";
        attribution = "";
      return;

    // Codegen for 22:29 for 0 quotations:
    case 1349:
      // No quotes for 22:29
        quote = "";
        attribution = "";
      return;

    // Codegen for 22:30 for 3 quotations:
    case 1350:
      option = random(0, 3);
      // 3 options for 22:30
      switch (option) {
        case 0:
      quote = F("according to the clock on the wall, it is barely * ten thirty *.");
      attribution = F("- The Hours");
      return;
        case 1:
      quote = F("She looked at the clock; it was * ten thirty *. If she could get there quickly on the subway, then she could be at his house in less than an hour, maybe a bit longer...");
      attribution = F("- Colm Toibin, Brooklyn");
      return;
        case 2:
      quote = F("The time was * ten-thirty * but it could have been three in the morning, because along its borders, West Berlin goes to bed with the dark");
      attribution = F("- Smiley's People");
      return;
      } // end switch (option)

    // Codegen for 22:31 for 2 quotations:
    case 1351:
      option = random(0, 2);
      // 2 options for 22:31
      switch (option) {
        case 0:
      quote = F("* 10.31pm. * Ok OK. Calm. Penny Husbands-Bosworth, so asbestos leukaemia item is not on yet.");
      attribution = F("- Bridget Jones's Diary");
      return;
        case 1:
      quote = F("And, later on, at * 10.31 pm *, I went out onto the balcony to find out whether I could see any stars, but there weren't any because of all the clouds...");
      attribution = F("- Mark Haddon");
      return;
      } // end switch (option)

    // Codegen for 22:32 for 0 quotations:
    case 1352:
      // No quotes for 22:32
        quote = "";
        attribution = "";
      return;

    // Codegen for 22:33 for 1 quotations:
    case 1353:
      // Only one option for 22:33:"
      quote = F("* 10:33 p.m. * Yessss, yessss. RECORDING CURRENT PROGRAMME. Have done it. Aargh. All going mad. Cassette has started rewinding and now stopped and ejected. Why? Shit. Shit.");
      attribution = F("- Bridget Jones's Diary");
      return;

    // Codegen for 22:34 for 0 quotations:
    case 1354:
      // No quotes for 22:34
        quote = "";
        attribution = "";
      return;

    // Codegen for 22:35 for 1 quotations:
    case 1355:
      // Only one option for 22:35:"
      quote = F("* 10:35 p.m. * Frantic now. Have rung Sahzzer, Rebecca, Simon, Magda. Nobody knows how to programme their videos. Only person I know who knows how to do it is Daniel.");
      attribution = F("- Bridget Jones's Diary");
      return;

    // Codegen for 22:36 for 1 quotations:
    case 1356:
      // Only one option for 22:36:"
      quote = F("Strand post mark and dispatched * ten thirty-six * said Holmes reading it over and over.");
      attribution = F("- Arthur Conan Doyle");
      return;

    // Codegen for 22:37 for 0 quotations:
    case 1357:
      // No quotes for 22:37
        quote = "";
        attribution = "";
      return;

    // Codegen for 22:38 for 1 quotations:
    case 1358:
      // Only one option for 22:38:"
      quote = F("Frantically I scan the cans of products stacked on the shelves. Stain Away. Vanish. There has to be a remedy. ... I just need to think. ... * 10:38 * OK, I have the answer.");
      attribution = F("- The Undomestic Goddess");
      return;

    // Codegen for 22:39 for 0 quotations:
    case 1359:
      // No quotes for 22:39
        quote = "";
        attribution = "";
      return;

    // Codegen for 22:40 for 2 quotations:
    case 1360:
      option = random(0, 2);
      // 2 options for 22:40
      switch (option) {
        case 0:
      quote = F("* 10:40 *: Call from Katharina asking me whether I had really said what was in the News.");
      attribution = F("- Heinrich Boll");
      return;
        case 1:
      quote = F("The station clock told him the time: * twenty to eleven *. He went to the booking office and asked the clerk in a polite tone when was the next train to Paris. 'In twelve minutes.'");
      attribution = F("- Georges Simenon");
      return;
      } // end switch (option)

    // Codegen for 22:41 for 1 quotations:
    case 1361:
      // Only one option for 22:41:"
      quote = F("There, the bastards, said Julian, and smashed the clock with the bottom of the bottle, to give them an approximate time. It was * 10:41 *");
      attribution = F("- Appointment in Samarra");
      return;

    // Codegen for 22:42 for 0 quotations:
    case 1362:
      // No quotes for 22:42
        quote = "";
        attribution = "";
      return;

    // Codegen for 22:43 for 0 quotations:
    case 1363:
      // No quotes for 22:43
        quote = "";
        attribution = "";
      return;

    // Codegen for 22:44 for 1 quotations:
    case 1364:
      // Only one option for 22:44:"
      quote = F("Alec pricked up his ears. \"When was that?\" \"Oh, yesterday evening.\" \"What time?\" \"* About a quarter to eleven. * I was playing bridge.\"");
      attribution = F("- Dead in the Water");
      return;

    // Codegen for 22:45 for 4 quotations:
    case 1365:
      option = random(0, 4);
      // 4 options for 22:45
      switch (option) {
        case 0:
      quote = F("* 10.45pm. * Oh God Daniel fell about laughing when I said I could not programme video. Said he would do it for me. Still at least I have done best for Mum.");
      attribution = F("- Bridget Jones's Diary");
      return;
        case 1:
      quote = F("...so many things to be grateful for are yours as a matter of course. Why it is now five seconds until * fifteen minutes before eleven * o'clock and you are just an old Trojan Horse.");
      attribution = F("- Ogden Nash, Good Intentions");
      return;
        case 2:
      quote = F("If this is so, we have now to determine what Barker and Mrs. Douglas, presuming they are not the actual murderers, would have been doing from * quarter to eleven *, when...");
      attribution = F("- The Valley of Fear");
      return;
        case 3:
      quote = F("They reached King's Cross at a * quarter to eleven *. Mr Weasley dashed across the road to get trolleys for their trunks and they all hurried into the station.");
      attribution = F("- J.K.Rowling");
      return;
      } // end switch (option)

    // Codegen for 22:46 for 1 quotations:
    case 1366:
      // Only one option for 22:46:"
      quote = F("...between 10.30 and 11.00 p.m.on the night of the 25th; and the time-table showed that the only night train arriving at Norden was one from the south at * 10.46 p.m. *");
      attribution = F("- The Riddle of the Sands");
      return;

    // Codegen for 22:47 for 1 quotations:
    case 1367:
      // Only one option for 22:47:"
      quote = F("He whistles in the shower. It is * 10.47 * and he is ready for the off.");
      attribution = F("- Jackie Kay, Trumpet");
      return;

    // Codegen for 22:48 for 1 quotations:
    case 1368:
      // Only one option for 22:48:"
      quote = F("I was down there for a bit last summer. I found it quite convenient for town. Of course the trains only go every hour. 48 minutes past the hour from Waterloo-up to * 10.48 *.");
      attribution = F("- The Sign in the Sky");
      return;

    // Codegen for 22:49 for 2 quotations:
    case 1369:
      option = random(0, 2);
      // 2 options for 22:49
      switch (option) {
        case 0:
      quote = F("By * forty-nine minutes past ten *, we fall in again with a fine portion of the ancient road, which the modern track constantly follows, and descend by some steep windings");
      attribution = F("- Felicien de Saulcy");
      return;
        case 1:
      quote = F("It's * well after 2245h *. The dog's leash slides hissing to the end of the Day-Glo line and stops the dog a couple of paces from the inside of the gate...");
      attribution = F("- Infinite Jest");
      return;
      } // end switch (option)

    // Codegen for 22:50 for 3 quotations:
    case 1370:
      option = random(0, 3);
      // 3 options for 22:50
      switch (option) {
        case 0:
      quote = F("* 10.50 P. M. * This diary-keeping of mine is, I fancy, the outcome of that scientific habit of mind about which I wrote this morning.");
      attribution = F("- The Parasite");
      return;
        case 1:
      quote = F("So think yourself lucky while you're awake and remember a happy crew. Think of Hamburg on the Magic Night. * 22.50 * and they went out neatly, just as they should...");
      attribution = F("- A. L. Kennedy, Day");
      return;
        case 2:
      quote = F("It's a hundred this year, ain't anybody noticed? Jack said, \"What's a hundred?\" I said, \"Pub is. Coach is. Look at the clock.\" Jack said, \"It's * ten to eleven *\"");
      attribution = F("- Graham Swift, Last Orders");
      return;
      } // end switch (option)

    // Codegen for 22:51 for 0 quotations:
    case 1371:
      // No quotes for 22:51
        quote = "";
        attribution = "";
      return;

    // Codegen for 22:52 for 0 quotations:
    case 1372:
      // No quotes for 22:52
        quote = "";
        attribution = "";
      return;

    // Codegen for 22:53 for 2 quotations:
    case 1373:
      option = random(0, 2);
      // 2 options for 22:53
      switch (option) {
        case 0:
      quote = F("He begins to make a record of our observations.'* 10.53 hrs *,' he writes, as we crouch at the top of the stairs, listening to his mother in the hall below.");
      attribution = F("- Michael Frayn, Spies");
      return;
        case 1:
      quote = F("... left-hand corner of the screen, where the figures covering part of her forehead show the minutes and seconds, from * 10:53 * to 10:57, while the hundredths of a second flash by...");
      attribution = F("- W.G. Sebald, Austerlitz");
      return;
      } // end switch (option)

    // Codegen for 22:54 for 0 quotations:
    case 1374:
      // No quotes for 22:54
        quote = "";
        attribution = "";
      return;

    // Codegen for 22:55 for 2 quotations:
    case 1375:
      option = random(0, 2);
      // 2 options for 22:55
      switch (option) {
        case 0:
      quote = F("It is eleven o'clock! * Eleven o'clock, all but five minutes! * \"But which eleven o'clock?\" \"The eleven o'clock that is to decide life or death!\"");
      attribution = F("- The Phantom of the Opera");
      return;
        case 1:
      quote = F("The clock was still saying * five minutes to eleven * when Pooh and Piglet set out on their way half an hour later.");
      attribution = F("- The House at Pooh Corner");
      return;
      } // end switch (option)

    // Codegen for 22:56 for 0 quotations:
    case 1376:
      // No quotes for 22:56
        quote = "";
        attribution = "";
      return;

    // Codegen for 22:57 for 1 quotations:
    case 1377:
      // Only one option for 22:57:"
      quote = F("...looking at the time indicator in the top left-hand corner of the screen, where the figures covering part of her forehead show the minutes and seconds, from 10.53 to * 10.57 *.");
      attribution = F("- W. G. Sebald, Austerlitz");
      return;

    // Codegen for 22:58 for 1 quotations:
    case 1378:
      // Only one option for 22:58:"
      quote = F("Then it grew dark; she would have had them to bed, but they begged sadly to be allowed to stay up; and, * just about eleven o'clock *, the door-latch was raised quietly...");
      attribution = F("- Wuthering Heights");
      return;

    // Codegen for 22:59 for 1 quotations:
    case 1379:
      // Only one option for 22:59:"
      quote = F("They parked the car outside Lowther's at precisely * one minute to eleven *. People were leaving, not all of them happy at having their evening curtailed.");
      attribution = F("- Ian Rankin, The Complaints");
      return;

    // Codegen for 23:00 for 14 quotations:
    case 1380:
      option = random(0, 14);
      // 14 options for 23:00
      switch (option) {
        case 0:
      quote = F("They didn't even sit down to eat until * 2300h *.");
      attribution = F("- Infinite Jest");
      return;
        case 1:
      quote = F("He will be here * at eleven exactly *, sir. At the bar, naked couples had begun dancing.");
      attribution = F("- Smiley's People");
      return;
        case 2:
      quote = F("The train arrived in New York * at eleven * that night.");
      attribution = F("- The Recognitions");
      return;
        case 3:
      quote = F("We got to Waterloo * at eleven *, and asked where the eleven-five started from. Of course nobody knew.");
      attribution = F("- Three Men in a Boat");
      return;
        case 4:
      quote = F("At * eleven o'clock *, I rang the bell for Betteredge, and told Mr. Blake that he might at last prepare himself for bed.");
      attribution = F("- The Moonstone");
      return;
        case 5:
      quote = F("At * eleven o'clock * that night, having secured a bed at one of the hotels and telegraphed his address to his father immediately on his arrival...");
      attribution = F("- Tess of the d'Urbervilles");
      return;
        case 6:
      quote = F("At * eleven o'clock * the phone rang, and still the figure did not respond, any more than it had responded when the phone had rung at twenty-five to seven...");
      attribution = F("- Douglas Adams");
      return;
        case 7:
      quote = F("Big Ben was striking as she stepped out into the street. It was * eleven o'clock * and the unused hour was fresh as if issued to children on a beach.");
      attribution = F("- Mrs Dalloway");
      return;
        case 8:
      quote = F("He says, \"They've killed Jan. Clear out.\" \"The suitcase?\" I ask. \"Take it away again. We want nothing to do with it now. Catch the * eleven o'clock * express.\"");
      attribution = F("- Italo Calvino");
      return;
        case 9:
      quote = F("I keep telling myself that once in the car I will be fine, I can listen to Radio Four Woman's Hour and that will take me till * eleven o'clock * when the class starts.");
      attribution = F("- Patsy Hickman, The Saints");
      return;
        case 10:
      quote = F("It was about * eleven o'clock *; and the Enchantress was attired in a delicious deshabille. With her little feet upon an ottoman near the fender...");
      attribution = F("- The Mysteries of London");
      return;
        case 11:
      quote = F("When they reached the top of the Astronomy Tower at * eleven o'clock *, they found a perfect night for stargazing, cloudless and still.");
      attribution = F("- JK Rowling");
      return;
        case 12:
      quote = F("The clock struck * eleven *. I looked at Adele, whose head leant against my shoulder; her eyes were waxing heavy, so I took her up in my arms and carried her off to bed.");
      attribution = F("- Charlotte Bronte, Jane Eyre");
      return;
        case 13:
      quote = F("Though perhaps' - but here the bracket clock whirred and then hectically struck * eleven *, its weights spooling downwards at the sudden expense of energy.");
      attribution = F("- The Stranger's Child");
      return;
      } // end switch (option)

    // Codegen for 23:01 for 1 quotations:
    case 1381:
      // Only one option for 23:01:"
      quote = F("The clock above the stove says that it is * just past eleven *, and guests will be arriving at the house now.");
      attribution = F("- Mary and O'Neil");
      return;

    // Codegen for 23:02 for 0 quotations:
    case 1382:
      // No quotes for 23:02
        quote = "";
        attribution = "";
      return;

    // Codegen for 23:03 for 2 quotations:
    case 1383:
      option = random(0, 2);
      // 2 options for 23:03
      switch (option) {
        case 0:
      quote = F("Good question. Guess I'll find out. She went back into the kitchen. \"What time are you meeting him?\" she called out. \"* Eleven oh-three *,\" he said.\"");
      attribution = F("- Little Green Men");
      return;
        case 1:
      quote = F("What time are you meeting him? she called out. \"* Eleven oh-three *,\" he said. \"That made me think he's for real. Military and intelligence types set precise appointment times...\"");
      attribution = F("- Little Green Men");
      return;
      } // end switch (option)

    // Codegen for 23:04 for 0 quotations:
    case 1384:
      // No quotes for 23:04
        quote = "";
        attribution = "";
      return;

    // Codegen for 23:05 for 3 quotations:
    case 1385:
      option = random(0, 3);
      // 3 options for 23:05
      switch (option) {
        case 0:
      quote = F("It was * 11.05 *, five minutes later than my habitual bedtime. I felt. I felt guilty at being still up, but the past kept pricking at me...");
      attribution = F("- L.P.Hartley, The Go-Between");
      return;
        case 1:
      quote = F("My watch says * 11:05 *. But whether AM or PM I don't know.");
      attribution = F("- Household Worms");
      return;
        case 2:
      quote = F("It was * five minutes past eleven * when I made my last entry. I remember winding up my watch and noting the time.");
      attribution = F("- The Poison Belt");
      return;
      } // end switch (option)

    // Codegen for 23:06 for 0 quotations:
    case 1386:
      // No quotes for 23:06
        quote = "";
        attribution = "";
      return;

    // Codegen for 23:07 for 2 quotations:
    case 1387:
      option = random(0, 2);
      // 2 options for 23:07
      switch (option) {
        case 0:
      quote = F("At * 11.07 pm *, Samuel \"Gunner\" Wilson was moving at 645 miles per hour over the Mojave Desert. Up ahead in the moonlight, he saw the twin lead jets, their afterburners glowing...");
      attribution = F("- The Andromeda Strain");
      return;
        case 1:
      quote = F("At exactly * seven minutes past eleven * by the ship's clock the Adventurer gave a prolonged screech and, moorings cast off...");
      attribution = F("- The Adventure Club Afloat");
      return;
      } // end switch (option)

    // Codegen for 23:08 for 0 quotations:
    case 1388:
      // No quotes for 23:08
        quote = "";
        attribution = "";
      return;

    // Codegen for 23:09 for 0 quotations:
    case 1389:
      // No quotes for 23:09
        quote = "";
        attribution = "";
      return;

    // Codegen for 23:10 for 3 quotations:
    case 1390:
      option = random(0, 3);
      // 3 options for 23:10
      switch (option) {
        case 0:
      quote = F("* Ten minutes after eleven * in Archie McCue's room on the third floor of the extension to the Robert Matthews' soaring sixties' tower...");
      attribution = F("- Emotionally Weird");
      return;
        case 1:
      quote = F("He had not the strength to help himself, and at * ten minutes past eleven * no one could have helped him, no one in the world");
      attribution = F("- Appointment in Samarra");
      return;
        case 2:
      quote = F("Another Christmas day is nearly over. It's * ten past eleven *. Richard declined with thanks my offer to make up a bed for him here in my study...");
      attribution = F("- David Lodge, Deaf Sentence");
      return;
      } // end switch (option)

    // Codegen for 23:11 for 1 quotations:
    case 1391:
      // Only one option for 23:11:"
      quote = F("Those were the first words I wrote after it happened. The computer dating on the Microsoft Word file (\"Notes on change.doc\") reads \"May 20, 2004, * 11:11 p.m. *,\"");
      attribution = F("- Joan Didion");
      return;

    // Codegen for 23:12 for 2 quotations:
    case 1392:
      option = random(0, 2);
      // 2 options for 23:12
      switch (option) {
        case 0:
      quote = F("I squinted down the street at the bank clock: * 11:12 *, 87 degrees. \"It's only a block and a half and it's not that hot, Daddy. The walk will do you good.\"");
      attribution = F("- A Thousand Acres");
      return;
        case 1:
      quote = F("There was a confirmatory identification done by undercover officer 6475 at * 23:12 * hours at the corner of 147th and Amsterdam.");
      attribution = F("- A Naked Singularity");
      return;
      } // end switch (option)

    // Codegen for 23:13 for 0 quotations:
    case 1393:
      // No quotes for 23:13
        quote = "";
        attribution = "";
      return;

    // Codegen for 23:14 for 0 quotations:
    case 1394:
      // No quotes for 23:14
        quote = "";
        attribution = "";
      return;

    // Codegen for 23:15 for 3 quotations:
    case 1395:
      option = random(0, 3);
      // 3 options for 23:15
      switch (option) {
        case 0:
      quote = F("* 11.15pm. * Humph. Mum just rang \"Sorry, darling. It isn't Newsnigtht, it's Breakfast News tomorrow. Could you set it for seven o'clock tomorrow morning, BBC1?\"");
      attribution = F("- Bridget Jones's Diary");
      return;
        case 1:
      quote = F("Shall be glad if you will come with me. Air and scenery perfect. Leave Paddington by the * 11:15 *.");
      attribution = F("- Sir Arthur Conan Doyle");
      return;
        case 2:
      quote = F("On arriving home at a * quarter-past eleven *, we found a hansom cab, which had been waiting for me for two hours with a letter.");
      attribution = F("- Dairy of a Nobody");
      return;
      } // end switch (option)

    // Codegen for 23:16 for 1 quotations:
    case 1396:
      // Only one option for 23:16:"
      quote = F("But I couldn't get out of the house straight away because he would see me, so I would have to wait until he was asleep. The time was * 11.16 pm. * I tried doubling 2s again...");
      attribution = F("- Mark Haddon");
      return;

    // Codegen for 23:17 for 1 quotations:
    case 1397:
      // Only one option for 23:17:"
      quote = F("It was * seventeen minutes past eleven *: she would have lots of time to have the matter out with Mr. Doran...");
      attribution = F("- James Joyce, Dubliners");
      return;

    // Codegen for 23:18 for 1 quotations:
    case 1398:
      // Only one option for 23:18:"
      quote = F("It is * 11.18 *. A row of bungalows in a round with a clump of larch tree in the middle.");
      attribution = F("- Jackie Kay, Trumpet");
      return;

    // Codegen for 23:19 for 1 quotations:
    case 1399:
      // Only one option for 23:19:"
      quote = F("Peter got onto his knees to look out the window, and Miss Fuller glared at him. Polly looked down at her watch: * 11:19 *. The train.");
      attribution = F("- Connie Willis, Blackout");
      return;

    // Codegen for 23:20 for 4 quotations:
    case 1400:
      option = random(0, 4);
      // 4 options for 23:20
      switch (option) {
        case 0:
      quote = F("Sweeney pointed to the clock above the bar, held in the massive and indifferent jaws of a stuffed alligator head. The time was * 11.20 *.");
      attribution = F("- Neil Gaiman, American Gods");
      return;
        case 1:
      quote = F("OFFICER'S NOTES Disruption alert logged * 11h20 * from Stones' Pool Hall (Premises ID 33CBD-Long181). Officer and Aito 379 responded. On arrival found subject shouting threats...");
      attribution = F("- Lauren Beukes, Moxyland");
      return;
        case 2:
      quote = F("From Balboa Island, he drove south to Laguna Beach. At * eleven-twenty *, he parked his van across the street from the Hudston house.");
      attribution = F("- Dean Koontz, Watchers");
      return;
        case 3:
      quote = F("Harvey looked at the clock, which marked * twenty past eleven *. \"Then I'll sleep here till three and catch the four o'clock freight...\"");
      attribution = F("- Captains Courageous");
      return;
      } // end switch (option)

    // Codegen for 23:21 for 0 quotations:
    case 1401:
      // No quotes for 23:21
        quote = "";
        attribution = "";
      return;

    // Codegen for 23:22 for 1 quotations:
    case 1402:
      // Only one option for 23:22:"
      quote = F("At * 11.22 * he handed his ticket to a yawning guard and walked down a long flight of wooden steps to the car-park. A breeze lifted and dropped the leaves of a tree...");
      attribution = F("- Dreams of leaving");
      return;

    // Codegen for 23:23 for 0 quotations:
    case 1403:
      // No quotes for 23:23
        quote = "";
        attribution = "";
      return;

    // Codegen for 23:24 for 0 quotations:
    case 1404:
      // No quotes for 23:24
        quote = "";
        attribution = "";
      return;

    // Codegen for 23:25 for 3 quotations:
    case 1405:
      option = random(0, 3);
      // 3 options for 23:25
      switch (option) {
        case 0:
      quote = F("OK, Estelle, I willl be at Nice Airport at * 11.25 p.m. * on Saturday, BA: Could you send the driver?");
      attribution = F("- Other People's Money");
      return;
        case 1:
      quote = F("...see if he could make out the figures on the dial. They were plainly visible, and the hands indicated the hour of * eleven o'clock and twenty-five minutes *.");
      attribution = F("- A Wireless Message");
      return;
        case 2:
      quote = F("Keeping her own watch concealed she asked Linda at * twenty-five past eleven * what time it was. Linda looked at her watch and replied that it was a quarter to twelve.");
      attribution = F("- Evil under the Sun");
      return;
      } // end switch (option)

    // Codegen for 23:26 for 1 quotations:
    case 1406:
      // Only one option for 23:26:"
      quote = F("Los Angeles. * 11:26 p.m. * In a dark red room- the color of the walls is close to that of raw liver- is a tall woman dressed cartoonishly in too-tight silk shorts...");
      attribution = F("- Neil Gaiman, American Gods");
      return;

    // Codegen for 23:27 for 1 quotations:
    case 1407:
      // Only one option for 23:27:"
      quote = F("He tells his old friend the train times and they settle on the 19.45 arriving at * 23.27 *. \"I'll book us into the ultra-luxurious Francis Drake Lodge.\"");
      attribution = F("- Other People's Money");
      return;

    // Codegen for 23:28 for 1 quotations:
    case 1408:
      // Only one option for 23:28:"
      quote = F("...from twenty-five minutes past eleven until * twenty-eight minutes past eleven *, from ten minutes to three until two minutes to three...");
      attribution = F("- Sinister Street");
      return;

    // Codegen for 23:29 for 0 quotations:
    case 1409:
      // No quotes for 23:29
        quote = "";
        attribution = "";
      return;

    // Codegen for 23:30 for 5 quotations:
    case 1410:
      option = random(0, 5);
      // 5 options for 23:30
      switch (option) {
        case 0:
      quote = F("This time it was Kumiko. The wall clock said * 11.30 *.");
      attribution = F("- The Wind-up Bird Chronicle");
      return;
        case 1:
      quote = F("He loaded the player and turned on the viewer, his knees popping again as he squatted to set the cue to * 2330 *.");
      attribution = F("- Infinite Jest");
      return;
        case 2:
      quote = F("He would catch the night bus for Casablanca, the one that left the beach at * half past eleven *.");
      attribution = F("- Paul Bowles");
      return;
        case 3:
      quote = F("The Picton boat was due to leave at * half-past eleven *. It was a beautiful night, mild, starry...");
      attribution = F("- The Voyage");
      return;
        case 4:
      quote = F("The ship's clock in the bar says * half past eleven *. Half past eleven is opening time. The hands of the clock have stayed still at half past eleven for fifty years.");
      attribution = F("- Under Milk Wood");
      return;
      } // end switch (option)

    // Codegen for 23:31 for 1 quotations:
    case 1411:
      // Only one option for 23:31:"
      quote = F("It is * twenty-nine minutes to midnight *. Dr Narlikar's Nursing Home is running on a skeleton staff; there are many absentees...");
      attribution = F("- Midnight's Children");
      return;

    // Codegen for 23:32 for 2 quotations:
    case 1412:
      option = random(0, 2);
      // 2 options for 23:32
      switch (option) {
        case 0:
      quote = F("And then it was * 11.32 pm * and I heard voices of people walking along the street. And a voice said, 'I don't care whether you thought it was funny or not,'");
      attribution = F("- Mark Haddon");
      return;
        case 1:
      quote = F("This is the evening. This is the night. It is New Year's Eve. * In about twenty-eight minutes it will be midnight. * I still have twenty-eight minutes left.");
      attribution = F("- Gerard Reve, The Evenings");
      return;
      } // end switch (option)

    // Codegen for 23:33 for 1 quotations:
    case 1413:
      // Only one option for 23:33:"
      quote = F("We are on Colaba Causeway now, just for a moment, to reveal that at * twenty-seven minutes to midnight *, the police are hunting for a dangerous criminal.");
      attribution = F("- Midnight's Children");
      return;

    // Codegen for 23:34 for 2 quotations:
    case 1414:
      option = random(0, 2);
      // 2 options for 23:34
      switch (option) {
        case 0:
      quote = F("* Eleven thirty-four *. We stand on the sidewalk in front of Jean's apartment on the Upper East Side.");
      attribution = F("- American Psycho");
      return;
        case 1:
      quote = F("Reacher retrieved his G36 from under the saloon bar window at * eleven thirty-four * precisely and set out to walk back on the road...");
      attribution = F("- Lee Child, The Hard Way");
      return;
      } // end switch (option)

    // Codegen for 23:35 for 1 quotations:
    case 1415:
      // Only one option for 23:35:"
      quote = F("Then at * eleven thirty-five * the door at the rear of the hall opened and a police sergeant and three constables entered, ushered by Bagot.");
      attribution = F("- Hamlet, Revenge!");
      return;

    // Codegen for 23:36 for 1 quotations:
    case 1416:
      // Only one option for 23:36:"
      quote = F("Then Green knocks at the front door at * 2336 * - Gately has to Log the exact time and then it's his call whether to unlock the door.");
      attribution = F("- Infinite Jest");
      return;

    // Codegen for 23:37 for 0 quotations:
    case 1417:
      // No quotes for 23:37
        quote = "";
        attribution = "";
      return;

    // Codegen for 23:38 for 0 quotations:
    case 1418:
      // No quotes for 23:38
        quote = "";
        attribution = "";
      return;

    // Codegen for 23:39 for 1 quotations:
    case 1419:
      // Only one option for 23:39:"
      quote = F("There's a whisper down the line at * 11.39 * When the Night Mail's ready to depart, Saying \"Skimble where is Skimble has he gone to hunt the thimble?\"");
      attribution = F("- T S Eliot");
      return;

    // Codegen for 23:40 for 1 quotations:
    case 1420:
      // Only one option for 23:40:"
      quote = F("We all have the maps and appliances of various kinds that can be had. Professor Van Helsing and I are to leave by the * 11:40 * train tonight for Veresti...");
      attribution = F("- Bram Stoker, Dracula");
      return;

    // Codegen for 23:41 for 1 quotations:
    case 1421:
      // Only one option for 23:41:"
      quote = F("The phosphorescent figures on his watch attracted his attention. He peered at them. The time was * 11:41 *. I remember...what do I remember?");
      attribution = F("- James Clavell, Noble House");
      return;

    // Codegen for 23:42 for 2 quotations:
    case 1422:
      option = random(0, 2);
      // 2 options for 23:42
      switch (option) {
        case 0:
      quote = F("* 11:42 * I'm doing fine. I'm doing well. I've got the Hoover on, I'm cruising along nicely- What was that? What just went up the Hoover?");
      attribution = F("- The Undomestic Goddess");
      return;
        case 1:
      quote = F("At * 11.42 * then the signal's nearly due And the passengers are frantic to a man- Then Skimble will appear and he'll saunter to the rear:");
      attribution = F("- TS Eliot");
      return;
      } // end switch (option)

    // Codegen for 23:43 for 1 quotations:
    case 1423:
      // Only one option for 23:43:"
      quote = F("The clock told him it was * eleven forty-three * and in that moment, in a flash of illumination, Mungo understood what the numbers at the end of Moscow Centre's messages were");
      attribution = F("- Talking to Strange Men");
      return;

    // Codegen for 23:44 for 1 quotations:
    case 1424:
      // Only one option for 23:44:"
      quote = F("At * eleven forty-four * last night somebody stabbed this girl in the neck with a kitchen knife and immediately thereafter plunged the same knife through her skull...");
      attribution = F("- Ben Elton, Dead Famous");
      return;

    // Codegen for 23:45 for 6 quotations:
    case 1425:
      option = random(0, 6);
      // 6 options for 23:45
      switch (option) {
        case 0:
      quote = F("As he sat smiling, the quarter struck - the * quarter to twelve *.");
      attribution = F("- Mrs Dalloway");
      return;
        case 1:
      quote = F("...I waited till a * quarter to twelve *, and found then that I was in All Souls'. But I wasn't much frightened, for I thought it could be tomorrow as well.");
      attribution = F("- Far from the madding crowd");
      return;
        case 2:
      quote = F("Keeping her own watch concealed she asked Linda at twenty-five past eleven what time it was. Linda looked at her watch and replied that it was a * quarter to twelve *.");
      attribution = F("- Evil under the Sun");
      return;
        case 3:
      quote = F("Said Septimus, very slowly, very drowsily, smiling mysteriously. As he sat smiling at the dead man in the grey suit the quarter struck, the * quarter to twelve *.");
      attribution = F("- Mrs. Dalloway");
      return;
        case 4:
      quote = F("We passed Shiplake as the clock was striking the * quarter to twelve * and then George said thoughtfully: 'You don't happen to remember which of the islands it was, do you?'");
      attribution = F("- Three Men in a Boat");
      return;
        case 5:
      quote = F("The church clocks chimed * three quarters past eleven *, as two figures emerged on London Bridge.");
      attribution = F("- Oliver Twist");
      return;
      } // end switch (option)

    // Codegen for 23:46 for 1 quotations:
    case 1426:
      // Only one option for 23:46:"
      quote = F("In the Kismet Lounge, Mr. Early sees suddenly to his horror it's * 11:46 p.m. * He's been in this place far longer than he'd planned, and he's had more to drink than he'd planned.");
      attribution = F("- Joyce Carol Oates");
      return;

    // Codegen for 23:47 for 1 quotations:
    case 1427:
      // Only one option for 23:47:"
      quote = F("If he had glanced at his watch, he would have seen that it was * thirteen minutes to midnight *. And if he had been interested in what was going on...");
      attribution = F("- Dead Soldiers Don't Sing");
      return;

    // Codegen for 23:48 for 1 quotations:
    case 1428:
      // Only one option for 23:48:"
      quote = F("The little two-seater rattled and shook-Kemper couldn't believe it. It was * 11.48pm. * They were thirty-six hours short of GO.");
      attribution = F("- American Tabloid");
      return;

    // Codegen for 23:49 for 1 quotations:
    case 1429:
      // Only one option for 23:49:"
      quote = F("Tom shrugged. He pushed his pinkish ruffled sleeve back, and saw that it was * eleven minutes to midnight *. Tom finished his coffee.");
      attribution = F("- The Boy Who Followed Ripley");
      return;

    // Codegen for 23:50 for 2 quotations:
    case 1430:
      option = random(0, 2);
      // 2 options for 23:50
      switch (option) {
        case 0:
      quote = F("At * 11.50pm *, I got up extremely quietly, took my things from under the bed, and opened the door one millimeter at a time, so it wouldn't make any noise.");
      attribution = F("- Jonathan Safran Foer");
      return;
        case 1:
      quote = F("The man who gave them to him handed him a ten-shilling note and promised him another if it were delivered at exactly * ten minutes to twelve *.");
      attribution = F("- Agatha Christie");
      return;
      } // end switch (option)

    // Codegen for 23:51 for 1 quotations:
    case 1431:
      // Only one option for 23:51:"
      quote = F("Due at Waterloo at * eleven-fifty-one *, panted Smith. \"That gives us thirty-nine minutes to get to the other side of the river and reach his hotel.\"");
      attribution = F("- The Mystery of Dr Fu Manchu");
      return;

    // Codegen for 23:52 for 2 quotations:
    case 1432:
      option = random(0, 2);
      // 2 options for 23:52
      switch (option) {
        case 0:
      quote = F("It was * eight minutes to midnight *. Just nice time, I said to myself. Indoors, everything was quiet and in darkness. Splendid.");
      attribution = F("- The Green Man");
      return;
        case 1:
      quote = F("At any rate, we whirled into the station with many more, just as the great clock pointed to * eight minutes to twelve * o'clock. \"Thank God! We are in time\"");
      attribution = F("- Anna Sewell, Black Beauty");
      return;
      } // end switch (option)

    // Codegen for 23:53 for 1 quotations:
    case 1433:
      // Only one option for 23:53:"
      quote = F("It was * 7 minutes to midnight *. The dog was lying on the grass in the middle of the lawn in front of Mrs. Shears' house.");
      attribution = F("- Mark Haddon");
      return;

    // Codegen for 23:54 for 2 quotations:
    case 1434:
      option = random(0, 2);
      // 2 options for 23:54
      switch (option) {
        case 0:
      quote = F("His watch read * 11.54pm * Eastern Standard Time. Already it was nearly 6.00am in Rome. He had left a city frozen by a harsh January storm, after a bleak, wet Christmas season.");
      attribution = F("- Greg Tobin, Conclave");
      return;
        case 1:
      quote = F("He swilled off the remains of [his beer] and looked at the clock. It was * six minutes to twelve *.");
      attribution = F("- Hangover Square");
      return;
      } // end switch (option)

    // Codegen for 23:55 for 3 quotations:
    case 1435:
      option = random(0, 3);
      // 3 options for 23:55
      switch (option) {
        case 0:
      quote = F("What time did you arrive at the site? It was * 11:55 *. I remember since I happened to glance at my watch when we got there. We rode our bicycles to the bottom of the hill...");
      attribution = F("- Kafka on the Shore");
      return;
        case 1:
      quote = F("I am going to lock you in. It is- he consulted his watch, \"* five minutes to midnight *. Miss Granger, three turns should do it. Good luck.\"");
      attribution = F("- J. K. Rowling");
      return;
        case 2:
      quote = F("He was tearing off on his bicycle to one of the jobs about * five minutes to twelve * to see if he could catch anyone leaving off for dinner before the proper time.");
      attribution = F("- Robert Tressell");
      return;
      } // end switch (option)

    // Codegen for 23:56 for 2 quotations:
    case 1436:
      option = random(0, 2);
      // 2 options for 23:56
      switch (option) {
        case 0:
      quote = F("I wondered what the time is?' said the latter after a pause'. 'I don't know exactly', replied Easton, 'but it * can't be far-off twelve *.'");
      attribution = F("- Robert Tressell");
      return;
        case 1:
      quote = F("The human race is at the end of the line, the doomsday clock ticks on. It's stopped for a decade at * four minutes to midnight *, but there the hands still stand.");
      attribution = F("- Fay Weldon, Wicked Women");
      return;
      } // end switch (option)

    // Codegen for 23:57 for 2 quotations:
    case 1437:
      option = random(0, 2);
      // 2 options for 23:57
      switch (option) {
        case 0:
      quote = F("I wondered what the time is?' said the latter after a pause'. 'I don't know exactly', replied Easton, 'but it * can't be far-off twelve *.'");
      attribution = F("- Robert Tressell");
      return;
        case 1:
      quote = F("Wells looked out at the street. \"What time is it?\" he said. Chigurh raised his wrist and looked at his watch. \"* Eleven fifty-seven *\" he said. Wells nodded.");
      attribution = F("- No Country for Old Men");
      return;
      } // end switch (option)

    // Codegen for 23:58 for 1 quotations:
    case 1438:
      // Only one option for 23:58:"
      quote = F("Humans emerge * one minute and seventeen seconds before midnight *. The whole of our recorded history, on this scale, would be no more than a few seconds...");
      attribution = F("- Bill Bryson");
      return;

    // Codegen for 23:59 for 3 quotations:
    case 1439:
      option = random(0, 3);
      // 3 options for 23:59
      switch (option) {
        case 0:
      quote = F("At * a minute to midnight *, Roquenton was holding his wife's hand and giving her some last words of advice.");
      attribution = F("- Marcel Ayme");
      return;
        case 1:
      quote = F("There is a big grandfather clock there, and as the hands drew * near to twelve * I don't mind confessing I was as nervous as a cat.");
      attribution = F("- Agatha Christie");
      return;
        case 2:
      quote = F("Chigurgh rose and picked up the empty casing off the rug and blew into it and put it in his pocket and looked at his watch. The * new day was still a minute away *.");
      attribution = F("- No Country for Old Men");
      return;
      } // end switch (option)

  } // end switch (minute)

} // end print_minute()
